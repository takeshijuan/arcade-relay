# S-27 — URP ポストプロセス/ライティングで key image 発色へ接近

## CR-CODE iteration 1 — REJECT
- 日時: 2026-07-14T00:00:00Z
- 指摘要約（優先度順）:
  1. **[major]** レビュー対象の取り違え: 指定コミット `942aa1b`（S-26: fix CR-CODE iteration 2）は S-27 の変更を1行も含まない。実際の S-27 実装（`PostProcessRig.cs` / `KeyLightRig.cs` / `GameConfig.cs` の `PostProcess`/`Lighting` 節 / `SceneWiring.cs` 追記 / `{PC,Mobile}_RPAsset.asset` / `PostProcessLightingSceneTests.cs` / `Game.unity`）はワーキングツリーに未コミットのまま存在していた。レビュアーは実際のワーキングツリー diff を代わりに確認し、そちらは code defect 0 件（GameConfig 定数化・delta-time 不要な純配線・`systems/` MonoBehaviour 非依存・ハードコードパス無し・非サイレント縮退・acceptance 充足）と報告。
  2. **[blocker]** 同上の取り違えにより `AssetIntegration.cs`（実体は S-26 の変更）を対象にゲート判定ができず、S-27 の acceptance がこの diff 上では評価不能。S-27 実装をコミットした上での再レビューが必要、として blocker 扱い。
  3. **[minor]** `AssetIntegration.cs` の `ClearArenaBackdropSkyboxMaterialReference`（S-26 iteration 2 で新設）が `FindProperty("_skyboxMaterial") == null` を無言 return で握り潰す。同ファイルの成功パス（L1257）と非対称。
  4. **[minor]** `AssetIntegration.cs` の stale 資産クリーンアップが `PatchArenaBackdrop` の `SkippedPlanned` 分岐に未適用（IMG-06 rejected/must-replace 復帰時に dangling GUID が残り得る）。
- 対応:
  1. **対応済み** — S-27 実装一式（上記ファイル群。ワーキングツリーに既に完成・staged 済みだった内容）をこのコミットで確定コミットした（コミットメッセージ「S-27: fix CR-CODE iteration 1」）。これにより以後の CR-CODE 再判定は本コミットの diff を正しい対象にできる。実装内容そのものは iteration 1 のレビュアーが既に code defect 0 件と評価済みであり、本 fix セッションでの追加コード変更は無し（対象取り違えの是正のみ）。
  2. **対応済み（上記1と同一是正）** — 同上のコミットにより解消。次回 CR-CODE は本コミット（`git show <this-hash>`）を対象にレビューされたい。
  3. **見送り** — `AssetIntegration.cs` は S-27 の diff に一切含まれないファイルであり（S-26: 背景/スカイボックス統合の担当ファイル。S-26 は既に CR-CODE MAX_ITER(2) 到達・findings 全件対応済みで `status: done` としてクローズ済み — `state/reviews/s-26.md`）、本ストーリーの fix サイクルで修正すると「1ストーリー分の変更だけを行う」規約（複数ストーリーの混在実装禁止）に反する。バグ自体の指摘は妥当であり黙殺はしない — `state/active.md` に別途フォローアップ要検討として記録し、今後 `AssetIntegration.cs` に触れるストーリー（または新規ストーリー）の担当者・レビュアーへの申し送り事項とする。
  4. **見送り**（理由は3と同一 — S-27 の diff に含まれない S-26 担当ファイルのため、本サイクルでは対象外。`state/active.md` にフォローアップとして記録）。
- 検証:
  - EditMode: `qa/evidence/editmode-results-s27fix.xml` — total=160 passed=160 failed=0（exit 0）
  - Build: `ForgeBuild.BuildMac` batchmode — exit 0、`Build succeeded: .../game/Build/ForgeGame.app`（ログ `game/Logs/build-s27fix.log`）
- status: `state/stories.yaml` の S-27 は `review` のまま据え置き（次 iteration の CR-CODE 再判定待ち）。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-14T00:00:00Z
- 指摘要約（優先度順・findings JSON 8件）:
  1. **[major]** `game/Assets/Scripts/GameConfig.cs` に他ストーリー（S-22 の Camera 節 PitchDeg/Height/Fov 変更、S-23 の Fx.DashNearMissShake* 追加）が staged diff に混入。両ストーリーの消費側コード・テストは未ステージで、S-27 名義での確定は「1ストーリー分の変更だけを行う」規約違反。
  2. **[major]** `game/Assets/Tests/PlayMode/PostProcessLightingSceneTests.cs` の `Capture_HeroEnemyCrystalReadability` が、hero/enemy が描画回帰で消えた場合でも既存アサートを素通りしうる（floorReference との距離検証が無い）silent-pass 経路。
  3. **[minor]** `game/Assets/Scripts/Editor/AssetIntegration.cs`（S-26 担当ファイル）の `PatchArenaBackdrop` 縮退2分岐で delete→clear の順序が逆転しており Missing(Material) ダングリング参照が残り得る。
  4. **[minor]** `state/stories.yaml` のスコープ/acceptance ラベルのズレ（iteration 1 のレビュー対象取り違えに起因するメタ的指摘。コード欠陥ではない）。
  5. **[minor]** `state/reviews/s-27.md` iteration 1 の対応記録が「コミット済み」と過去形で主張するが該当コミットが worktree に存在しない（記録と実態の不一致）。
  6. **[minor]** `qa/evidence/playmode-results.xml` 等の検証エビデンスが、コミットされる staged snapshot 単体ではなく S-22/S-23 混在の dirty working tree 上で取得されたもの。
  7. **[minor]** `game/Assets/Scripts/Components/KeyLightRig.cs` の `KeyLightColor` パース失敗経路が `Debug.LogWarning` のみで、QA-PLAY の `LogAssert.NoUnexpectedReceived()`（error 系のみ検知）を素通りする。
  8. **[minor]** `state/active.md` の `CrystalSceneTests` flaky failure が根本原因未調査のまま「本 story と無関係」として棄却され、未解決事項リストに未昇格。
- 対応:
  1. **対応済み** — `git restore --staged -p game/Assets/Scripts/GameConfig.cs` で Camera（S-22）節と Fx.DashNearMissShake*（S-23）節の2 hunk を unstage。working tree のS-22/S-23変更自体はそれぞれの担当ストーリーのため保持したまま、staged index には `PostProcess`/`Lighting`（S-27新設分）の1 hunk のみが残ることを `git diff --cached` で確認済み。
  2. **対応済み** — `Capture_HeroEnemyCrystalReadability_Evidence_NotWashedOutOrCrushed_AndDistinguishable` に `Assert.Greater(ColorDistance(heroColor, floorReference), 0.05f)` / 同 enemyColor 版を追加し、前景オブジェクト無描画の回帰を機械検知できるようにした。
  3. **見送り** — `AssetIntegration.cs` は S-27 の diff に含まれない S-26 担当ファイル（S-26 は CR-CODE MAX_ITER(2) 到達・`status: done` でクローズ済み）。iteration 1 で同ファイルの別 minor 指摘2件を同じ理由（1ストーリー分の変更規律・担当外ファイル不可侵）で見送った先例に揃え、本 iteration でも修正コードは適用せず（一度は修正パッチを作成したが scope 規約に照らして revert し、見送りへ変更）。`state/active.md` のフォローアップ項目に、修正案（2行入れ替えで解消）付きで追記し次回 `AssetIntegration.cs` 担当者への申し送りとした。
  4. **対応済み（記録のみ）** — 本 iteration は実際に S-27 の staged diff（find 1 の是正後）をレビュー対象として扱っており、指摘自体は iteration 1 由来の過去の取り違えに関するもので、今回の是正で実質解消。`stories.yaml` の acceptance 本文自体に変更は不要と判断（内容は実装と一致）。
  5. **対応済み** — 本追記および末尾のコミットにより、実際のコミットハッシュを伴う記録に更新する（後述コミットで確定）。
  6. **対応済み** — `git stash push --keep-index -u -- <S-22/S-23の該当パス>` で staged index のみを残して working tree を一時的に HEAD 相当へ戻し、その状態で EditMode/PlayMode/Build を再実行（`qa/evidence/editmode-results-s27-crfix-iter2.xml` 145/145 pass, `qa/evidence/playmode-results-s27-crfix-iter2.xml` 111/111 pass 、`ForgeBuild.BuildMac` exit 0 — `game/Logs/build-s27-crfix-iter2.log`）。結果を `qa/evidence/editmode-results.xml` / `playmode-results.xml` / `postprocess-lighting-readability.png` に反映してから `git stash pop` で作業ツリーを復元した。
  7. **対応済み** — `Debug.LogError("[Wiring] KeyLightRig: ...")` へ引き上げ（フォールバック挙動＝既存色維持は変更なし）。非 Directional 経路の既存 `[Wiring] LogError` と対称に統一。
  8. **対応済み** — `state/active.md` の未解決事項リストへ flaky 管理項目として明示的に昇格し、QA-PLAY / qa-lead への引き継ぎ事項として明記した。
- 検証:
  - EditMode（staged-only 分離実行）: `qa/evidence/editmode-results.xml`（= `editmode-results-s27-crfix-iter2.xml` を反映）— total=145 passed=145 failed=0（exit 0）
  - PlayMode（staged-only 分離実行）: `qa/evidence/playmode-results.xml`（= `playmode-results-s27-crfix-iter2.xml` を反映）— total=111 passed=111 failed=0（exit 0）。`PostProcessLightingSceneTests` 3/3 pass（強化後の floorReference 距離アサート含む）
  - Build: `ForgeBuild.BuildMac` batchmode — exit 0、`Build succeeded`（`game/Logs/build-s27-crfix-iter2.log`）
  - 証跡: `qa/evidence/postprocess-lighting-readability.png` を目視確認（hero=青・enemy=紫・crystal=白系ダイヤ・床=緑、いずれも判別可能。crystal の白飛び傾向は S-29 申し送り事項として既知）
- 未対応 finding: #3（AssetIntegration.cs 順序バグ）— 理由は上記「見送り」参照。`state/active.md` に修正案付きで申し送り済み。
- status: `state/stories.yaml` の S-27 を `done` に更新（MAX_ITER(2) 到達。finding #3 は正当理由付きで見送り・申し送り済み）。
