# S-03 レビュー履歴 — Menu シーン（必須4要素ハブ）

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-21T18:56:03+09:00
- 対象: commit 591c3500f0ea362503645f4e2fb567d6e27cc3f4「S-03: Menu シーン（必須4要素ハブ）」。差分 = game/Assets/Scripts/Ui/MenuScreen.cs(新規)・Components/GameFlow.cs・Components/GameBootstrap.cs・Scenes/Menu.unity・Tests/PlayMode/MenuScreenPlayModeTests.cs(新規)・ForgeGame.PlayMode.Tests.asmdef。engine=unity。読み取り専用レビュー（Unity/ビルド未起動 — 並走レーン単一インスタンスロック）。
- 指摘要約（優先度順）:
  1. [レイアウト不成立・機能欠陥] MenuScreen.cs:123 `OutgamePanel` / :164 `SettingsPanel` — 両パネルが `new GameObject(..., typeof(RectTransform))` のみでアンカー/サイズ未設定。AddComponent 生成の RectTransform 既定値はゼロサイズの点アンカー（sizeDelta=(0,0)）であり、配下の子（StatsText/HighScoreText/EssenceUpgText/AchievementsHeader/Achievement_ACH-01〜05／SettingsHeader/BgmVolumeSlider/SfxVolumeSlider/OperationInstructionsText）が `anchorMin=anchorMax=(0.5, NextY())` + `anchoredPosition=0` で配置されるため、ゼロサイズ親に対するフラクショナルアンカーが全て親原点（キャンバス隅）へ収束し、要素が1点に重なって表示される。→ acceptance の要素(2)アウトゲーム表示・(3)設定が視覚的に判読不能。PlayMode テストは `Find(name)!=null` の実在のみ検証しレイアウトを見ないため素通りする（QA-PLAY 視覚証跡で顕在化）。傍証: 同ファイルの `CreateFullScreenPanel`(:188) は明示的に anchorMin=zero/anchorMax=one/offset=0 で全画面化しており「既定は stretch でない」ことを示す。PlayButton/BackToTitleButton/MenuTitleText は全画面 Canvas 直下のため正しく配置され遷移テストは通るが、パネルにグルーピングした子だけが崩れる。修正: 両パネルを Background 同様に全画面ストレッチ（anchorMin=(0,0), anchorMax=(1,1), offsetMin/Max=0）にする、または表示/設定の子を `canvasGo.transform` 直下に置く。
  2. [マジックナンバー（レイアウト定数）] MenuScreen.cs:309-310 `CreateVolumeSlider` — ラベルの `anchoredPosition = new Vector2(-16f, 0f)`・`sizeDelta = new Vector2(160f, GameConfig.Ui.MenuSliderHeight * 1.6f)` に `-16f`(オフセットX)・`160f`(ラベル幅)・`1.6f`(行高係数) が本文直書き。rules/unity-code.md 規約1（UI のサイズ/位置は GameConfig.cs に集約）違反。UI 寸法は既に GameConfig.Ui（MenuSliderWidth/Height/MenuButtonWidth 等）に集約済みで、これらだけ取り残されている。特に `1.6f` は既存の `GameConfig.Ui.TextLineHeightFactor`(=1.6f) と同値の重複であり、S-02 iter1 で同種指摘（行高係数を TextLineHeightFactor へ集約）を既に確定済み。修正: `GameConfig.Ui` に `MenuSliderLabelOffsetX`(-16f)・`MenuSliderLabelWidth`(160f) を追加し、高さは `TextLineHeightFactor` を再利用する。（pivot/anchor の 0/0.5/1 は中央・端の慣例値のため対象外）
- 良好点（合格判断の根拠）:
  - acceptance 4要素の実在: (1)PlayButton→`GameFlow.GoToGame()` (2)OutgamePanel に統計/ハイスコア/essence+UPG Lv/実績 ACH-01〜05 (3)SettingsPanel に BGM/SFX スライダー + 操作説明 (4)BackToTitleButton→`GameFlow.GoToTitle()`。遷移はクリック擬似発行（InputTestFixture）で検証。シーン名文字列の直書き無し（GameFlow/GameConfig.Scenes 経由）。
  - 表示専任・状態非複製: essence/実績/統計/UPG Lv は `GameFlow.CurrentSaveData ?? SaveData.CreateDefault()` から読むのみ（UI に状態を持たせない）。SaveData の全参照フィールド（totalRunsPlayed/totalWins/totalKills/highScore/bestClearTimeSec/essence/upgStartingGoldLv/upgTowerDiscountLv/upgEssenceRateLv/ach*5）は MetaTypes.cs に実在し名称一致。
  - ScreenSpaceCamera 規約14: `UiCanvasHelper.ConfigureScreenSpaceCamera` 使用（Overlay 不使用）。Menu.unity に MainCamera タグ有り→`Camera.main` 解決。PlayMode で renderMode==ScreenSpaceCamera・worldCamera!=null を検証。
  - 入力抽象化: クリック判定は InputReader（Input System・旧 Input API 不使用）+ RectTransformUtility 矩形ヒットテスト。EventSystem 未導入プロジェクト規約に沿い TitleScreen と同一パターン。ヒットテストのカメラ引数は Canvas 描画カメラ(uiCamera)で正しい。
  - delta-time: MenuScreen の Update はクリック読取のみで毎フレーム移動/タイマー加算なし → フレームレート依存なし（規約違反なし）。
  - silent-failure: 本 diff に save/load・LogError 降格・batchmode 握り潰しいずれも該当なし。`CurrentSaveData ?? CreateDefault()` は「未ロード時の表示既定」であり黙示初期化ではない（コメントで明示・破損処理は Persistence 層の責務）。GameBootstrap の `SetCurrentSaveData(result.Data)` 追記は既存ロード結果の伝播のみ。
  - 並走レーン: 参照する GameFlow(S-01)/GameConfig/UiCanvasHelper/InputReader は実在。遷移先 Game/Title シーンの Build Settings 登録・実体は他レーン合流時のバッチ検証が保証（実体未実装は blocker としない）。asmdef に UnityEngine.UI 追加は Slider/Text/Image テスト参照のため妥当。
- 対応:
  1. 対応済み。`MenuScreen.cs` に `StretchFullScreen()` ヘルパを追加し、`OutgamePanel`/`SettingsPanel` を全画面ストレッチ（anchorMin=(0,0)/anchorMax=(1,1)/offset=0）にした。`CreateFullScreenPanel` も同ヘルパへ委譲するよう統一。
  2. 対応済み。`GameConfig.cs` の `Ui` に `MenuSliderLabelOffsetX`(-16f)・`MenuSliderLabelWidth`(160f) を追加し、`CreateVolumeSlider` のラベル `anchoredPosition`/`sizeDelta` から本文直書きを排除。行高係数は既存 `TextLineHeightFactor` を再利用（1.6f の重複を解消）。
  3. 対応済み（silent-failure-hunter指摘）。`GameFlow.CurrentSaveData` が null の場合に `Debug.LogWarning("[MenuScreen] GameFlow.CurrentSaveData is null; ...")` を1回出すよう変更し、直前コメントで Warning 採用（Error 昇格は QA-PLAY のエラー0検査を汚すため不採用）の正当性を明記した。
  4. 対応済み（silent-failure-hunter指摘）。`GameFlow.cs` の `CurrentSaveData`/`SetCurrentSaveData` のドキュメントコメントを、更新契機が「Boot ロード直後」に限らず「Persistence.Save 実施側が保存直後に必ず呼ぶ」契約であることを明記する内容に書き換えた（Result 保存ストーリー未割当の TODO も明記）。ui-engineer 担当外領域（Components/）への触れ込みのため doc コメントのみのピンポイント Edit に限定し、ロジック変更は行っていない。
  5. 対応済み（任意扱いだが低コストのため実施・silent-failure-hunter指摘）。`UiCanvasHelper.ConfigureScreenSpaceCamera` に `worldCamera == null` 時の `Debug.LogWarning` を追加。MenuScreen・TitleScreen 双方が共通ヘルパ経由で恩恵を受ける最小コスト修正。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-21T19:10:00+09:00
- 対象: commit 381942a60739c5acc04279f0f7ff2359a9ae3584「S-03: fix CR-CODE iter 1」。差分 = game/Assets/Scripts/Ui/MenuScreen.cs・GameConfig.cs・Components/GameFlow.cs・Ui/UiCanvasHelper.cs・state/reviews/s-03.md。engine=unity。読み取り専用レビュー（Unity/ビルド未起動 — 並走レーン単一インスタンスロック）。
- 判定根拠（iter1 指摘の解消確認）:
  1. [解消] レイアウト不成立 — `StretchFullScreen()` ヘルパを追加し `OutgamePanel`/`SettingsPanel` を全画面ストレッチ（anchorMin=(0,0)/anchorMax=(1,1)/offset=0）へ。`CreateFullScreenPanel` も同ヘルパへ委譲統一。子は共有 `row` カウンタ（NextY()）で全要素一意の Y を持つため、全画面パネル配下でもキャンバス直下と同一絶対位置に配置され重なりが解消。acceptance 要素(2)(3)が判読可能になる。
  2. [解消] マジックナンバー — `GameConfig.Ui` に `MenuSliderLabelOffsetX`(-16f)・`MenuSliderLabelWidth`(160f) を追加、`CreateVolumeSlider` のラベル `anchoredPosition`/`sizeDelta` から本文直書きを排除。行高は既存 `TextLineHeightFactor`(=1.6f) を再利用し重複を解消（数値挙動は 20*1.6=32 で従前と一致）。本コミットで新規のマジックナンバー混入なし。
  3/5. [妥当] `MenuScreen` の `CurrentSaveData==null` 時 `Debug.LogWarning` / `UiCanvasHelper` の `worldCamera==null` 時 `Debug.LogWarning` — いずれも回復不能条件ではなく（前者=Boot 未経由の正当状態→表示既定へ、後者=配線退行検知）、直前ヘッダコメントで Warning 採用（Error 昇格は QA-PLAY のエラー0検査/LogAssert を汚す）の正当性を文書化しており CR-CODE の silent-failure 例外条件を満たす。`LogAssert.NoUnexpectedReceived()` は Warning を失敗にしないため PlayMode 検査を汚さない。
  4. [妥当] `GameFlow.cs` は doc コメントのみの変更（ロジック非変更）。Result→Menu は Boot を通らないため保存側が `SetCurrentSaveData` を呼ぶ契約と未割当 TODO を明記 — 並走レーン跨ぎの正当な契約記述で本 story の blocker ではない。
- delta-time: MenuScreen の Update はクリック読取のみで毎フレーム移動/タイマー加算なし（規約違反なし）。
- acceptance: 4要素の実在・操作性（PlayButton→GoToGame / OutgamePanel の統計/ハイスコア/essence+UPG Lv/実績 ACH-01〜05 / SettingsPanel の BGM/SFX スライダー+操作説明 / BackToTitleButton→GoToTitle）が本 diff で判読可能な配置となり満たせる。全 Canvas ScreenSpaceCamera（UiCanvasHelper 経由）。
- findings: 0件（APPROVE）。
- 非ブロッキング観察（今回 diff 対象外・参考）: BackToTitleButton は anchorY≈0.022（row16）で下端寄りのため約8px下端クリップの可能性があるが、ボタン位置は本 fix コミットで不変（S-03 原コミット由来）かつ矩形ヒットテストは画面外でも成立するため操作性・acceptance に影響なし。build phase の S-15/S-16 レイアウト精緻化で吸収可能。
- 対応（後続 CR-CODE 追加指摘 #1 への対応・iter2 判定根拠3/5 の一部撤回）:
  1. 対応済み。`UiCanvasHelper.ConfigureScreenSpaceCamera` の `worldCamera == null` 分岐を `Debug.LogWarning` → `Debug.LogError` に昇格した（`game/Assets/Scripts/Ui/UiCanvasHelper.cs`）。iter2 で「Warning 採用は正当（配線退行検知・Error 化は QA-PLAY のエラー0検査を汚す）」と判定した根拠のうち `UiCanvasHelper` 側を撤回する。理由: 本ヘルパの全呼び出し元（`MenuScreen.cs:107` / `TitleScreen.cs:87`）は常に `Camera.main` を渡す設計で、`Menu.unity` に MainCamera が実在し、`MenuScreenPlayModeTests` は `worldCamera != null` を前提に assert している以上、`worldCamera == null` は正当な縮退経路ではなく純粋な配線バグである。Warning は `LogAssert.NoUnexpectedReceived()` を失敗させないため、UI が RenderTexture 証跡から全消えする配線破損があっても QA-PLAY の自動エラー0検査が緑のまま通過してしまう（gates.md CR-CODE の「新規コードが回復不能条件を最初から Warning 以下で記録」パターンに該当）。rules/unity-code.md 規約12 の既定（配線破損は LogError 1回）に合わせ Error へ昇格し、ヘッダコメントも「1回警告する」→「1回エラー記録する」へ更新した。健全なビルド・テストではこの分岐に到達しないため Error 化による既存挙動への副作用は無い。`MenuScreen` の `CurrentSaveData==null` 時の Warning（iter2 判定根拠3/5 前段）は今回の指摘対象外のため現状維持。

## CR-CODE iteration 3 — CONCERNS
- 日時: 2026-07-22T11:17:52+09:00
- 対象: commit 0c9674411aa9226267593cd4978e3066b9be44bc「S-03: status done」。差分 = state/stories.yaml のみ（S-03 の `status: review` → `done` への変更＋acceptance 折り畳みブロック末尾への注記1行追加）。engine=unity。読み取り専用レビュー（Unity/ビルド未起動 — 並走レーン単一インスタンスロック）。**本コミットに game/Assets/Scripts/** のコード変更は含まれない**（CR-CODE のコード対象パス外 = 純粋なブックキーピングコミット）。
- 指摘要約（優先度順）:
  1. [状態不整合・要修正] state/stories.yaml S-03 — `status: review` → `done` に更新すると同時に「CR-CODE unresolved — state/reviews/s-03.md 参照」の注記を付している。review-loops.md / contract §2（全成果物は produce→review→revise ループ通過が done の前提）に照らし「done かつ CR-CODE 未解決」は自己矛盾。かつ本コミット時点の s-03.md は iter2 APPROVE（findings 0）＋追加指摘#1 対応済みを既に記録しており、この注記は参照先の事実と食い違う（stale/誤り）。修正: CR-CODE 解決済みなら stale 注記を削除、真に未解決なら status を review に戻す（どちらか一方へ整合させる）。
  2. [YAML 衛生・要修正] 上記注記は `acceptance: >` 折り畳みスカラー本文と同一インデント（6 スペース）に置かれているため YAML コメントにならず、acceptance 文字列値へ「… # note: CR-CODE unresolved …」として畳み込まれ、acceptance フィールドを汚染する。同ファイル S-04 は注記を item レベル（4 スペース）の実コメントとして正しく置いており対照的。修正: 注記を item レベルの `#` 実コメントへ移すか削除する。
  3. [スコープ注記・欠陥ではない] 本 diff に game/Assets/Scripts/** の変更が無いため、CR-CODE のコード観点（マジックナンバー / delta-time / silent-failure / エンジン非依存コア / 永続化層集約）を当てる対象がこの diff には存在しない。acceptance 4要素の実装は本 diff では満たされない（実装は S-03 実装コミット 591c3500 / 381942a に在り、iter1 CONCERNS → iter2 APPROVE で解消済み）。コード面は上流 iter2 APPROVE を踏襲し、本 iteration の CONCERNS は状態ファイル（stories.yaml）の上記2点に限定する。
- 対応:
  1. 対応済み。`state/stories.yaml` S-03 の stale 注記「# note: CR-CODE unresolved — state/reviews/s-03.md 参照」（acceptance 折り畳みブロック内・6スペースインデント）を削除し、`status: done` 直後・item レベル（4スペース）の実コメントとして「CR-CODE iter2 APPROVE（findings 0）+ 追加指摘#1対応済み(d33ab0f) — state/reviews/s-03.md 参照。iter3 CONCERNS（stories.yaml の状態不整合/YAML衛生）は本コミットで解消。」へ置き換えた。S-04 の既存注記スタイル（status 直後・4スペース `#` コメント）に合わせた。
  2. 対応済み（1と同一修正）。注記を `acceptance: >` 折り畳みスカラー本文の外（status 直後の item レベル）へ移動したことで YAML パース時に acceptance 文字列へ混入しなくなったことを `python3 -c "import yaml; ..."` で実測確認（acceptance が注記無しの本来テキストのみになった）。
  3. 対応不要（正当な注記のまま）。本 iteration（stories.yaml のみの状態ファイル修正）にも game/Assets/Scripts/** の変更は無く、CR-CODE のコード観点を当てる対象は引き続き存在しない。コード面は iter2 APPROVE + 追加指摘#1対応(d33ab0f) を踏襲する。
  4. 対応済み（1と同一修正）。
  5. 対応済み。CR-CODE の観点（`UiCanvasHelper.ConfigureScreenSpaceCamera` の `worldCamera==null` 分岐 LogWarning→LogError 昇格、コミット d33ab0f）は本 iteration3 レビュー内で妥当性確認済みと明記されている。本エントリでその再判定を「d33ab0f 再判定＝妥当・クローズ」として記録することで、s-03.md 単体から「何が未解決か」を追跡可能にした。真の未解決コード指摘は無し。stories.yaml の注記文言は実状（iter2 APPROVE + d33ab0f 対応済み）に一致させた。

## CR-CODE iteration 4 — APPROVE
- 日時: 2026-07-22T11:23:37+09:00
- 注記: workflow はこのパスを「iteration 2」として起動したが、本 s-03.md には既に iter1/2/3 が記録済みのため、履歴の時系列整合と重複ヘッダ回避のため iteration 4 として追記する（review-loops.md「レビュー履歴は必ず追記」＝重複しない連番が追跡性の要件）。
- 対象: commit 1be49a27f64fb188b656e7bfa6c0fd967166b472「S-03: fix CR-CODE iter 1」に固定。差分 = state/reviews/s-03.md（iter3 ブロック追記）・state/stories.yaml（S-03 の `# note:` を `acceptance: >` 折り畳みスカラー本文外の item レベル実コメントへ移設）の2ファイルのみ。engine=unity。読み取り専用レビュー（Unity/ビルド未起動 — 並走レーン単一インスタンスロック）。
- 判定根拠:
  1. [コード対象外] 本コミットに `game/Assets/Scripts/**`（contract §11 の CR-CODE コード対象パス）の変更は0件（`git show <sha> --stat -- 'game/Assets/Scripts/**'` が空で機械確認）。純粋な状態ファイル（bookkeeping）コミットのため、コード観点（マジックナンバー / delta-time 非依存 / silent-failure 降格 / エンジン非依存コア / 永続化層集約）を当てる対象がこの diff には存在しない → コード findings 0件。
  2. [state 修正の妥当性・確認済み] stories.yaml の変更は正当な修正。親コミット（1be49a27~1）では S-03 の acceptance 文字列末尾に「… # note: CR-CODE unresolved — state/reviews/s-03.md 参照」が混入していた（`yaml.safe_load` で実測: `#` 行が `acceptance: >` 折り畳みスカラー本文の一部＝リテラル text として畳み込まれ acceptance 値を汚染）。本コミットは当該注記を status 直後の item レベル（4スペース）実コメントへ移設し、パース後の acceptance が注記無しの本来テキストのみになることを `yaml.safe_load` で実測確認。回帰・新規欠陥なし。同ファイル S-04 の既存コメントスタイルとも整合。
  3. [acceptance] acceptance 4要素（(1)プレイ開始→Game / (2)アウトゲーム表示=実績・統計・essence+UPG Lv / (3)設定=音量スライダー+操作説明 / (4)タイトルへ→Title、全 Canvas ScreenSpaceCamera）の**実装はこの diff には含まれない**（実装は 591c3500 / 381942a、iter1 CONCERNS→iter2 APPROVE で解消済み。他コミットはレビュー対象外）。本 diff 単体では acceptance を満たすコードは無いが、これはブックキーピングコミットである性質上の当然の帰結であり欠陥ではない。
- findings: 0件（APPROVE）。本コミットはコード対象パス外の状態ファイル修正のみで、含まれる stories.yaml 修正は YAML 汚染を解消する正当な変更。コード品質・バグ・規約違反の指摘なし。
