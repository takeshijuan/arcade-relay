# S-03 レビュー履歴 — Menu シーン（4タブ・必須4要素の枠）

## CR-CODE iteration 1 — APPROVE
- 日時: 2026-07-10T07:11:46Z
- 対象コミット: a2c100cfc4361704d0ac2903e4e2a0303f358c1a
- レビュアー: pr-review-toolkit:code-reviewer（wave3-code-reviewer）
- 判定: APPROVE（findings 0件。重大バグ・設計欠陥・rule 違反なし・acceptance 充足）
- 指摘要約: なし（未解決指摘なし）
- 検証内容（今回 diff = `MenuController.cs` / `MenuScreen.cs` / `MenuNavigation.cs` / `GameConfig.cs` / `Editor/SceneWiring.cs` / `Menu.unity` + 各 .meta / EditMode・PlayMode テスト / state）:
  1. マジックナンバー（rules/unity-code.md Do/Don't「ゲームパラメータは GameConfig.cs へ集約」）非抵触を確認: タブラベル・項目名・フォントサイズ（MenuTabFontSize=40 等）・色（ColorFocusHighlight）・アンカー Y／行間 px／各 sizeDelta・入力しきい値（MenuNavigateAxisThreshold=0.5f）はすべて `GameConfig.Ui` に集約済み。MonoBehaviour 本文に残る数値は `MenuController` の step 方向 `+1/-1`（意味を持つ方向値＝免除範囲の idiom）と `_focusIndex = 0` 初期値、`MenuScreen` のアンカー X `0.5f`／`pivot=(0.5,0.5)`／背景 `anchorMin/Max=0/1`（構造的中央・フルスクリーン指定）のみ。これらは既承認の S-02 `TitleScreen` と同一パターンで、iteration で合意済みの免除範囲どおり。チューニング値の本文直書きは残存なし。
  2. delta-time（同 Do/Don't「移動・タイマーは Time.deltaTime でスケール」）非抵触を確認: `MenuController.Update()` は入力ポーリング（`WasPressedThisFrame`／`ReadValue<Vector2>` のエッジ検出）とシーン遷移／タブ・フォーカス更新のみで、毎フレームの時間加算・フレームレート依存の移動を持たない。対象外＝違反なし。
  3. エンジン非依存コア（同 Do/Don't「Systems で MonoBehaviour 継承・シーン API 禁止」）適合を確認: `Systems/MenuNavigation` は `UnityEngine` を import しない純粋 static クラス（`CycleTab`=ラップ／`MoveFocus`=クランプの index 演算のみ）。配線は `Components/MenuController`（GetComponent・SceneManager・GameInput 配線のみ、判定は MenuNavigation へ委譲＝薄い）、表示は `Ui/MenuScreen`（uGUI 構築と highlight のみ、SaveData の二重真実源を持たず受領値を文字列化）に分離。層違反なし。
  4. 入力抽象化（同 Do/Don't「入力は Input System を1モジュールに集約・Input.GetKey 禁止」）適合を確認: 入力は `InputLayer/GameInput` facade 経由（`EnableUi`/`Submit`/`Cancel`/`TabPrev`/`TabNext`/`Navigate`）。`Input.GetKey`／`Input.GetAxis` 不使用。フォント参照は `Resources.GetBuiltinResource<Font>(GameConfig.Ui.BuiltinFontName)`＝config 定数経由（パス文字列直書きなし）。
  5. Rendering（同 Do/Don't「HUD/メニュー Canvas を ScreenSpaceOverlay にしない」）適合を確認: `MenuScreen.BuildCanvas()` は `RenderMode.ScreenSpaceCamera` + `worldCamera=Camera.main`。Camera.main が null の場合は `Debug.LogError("[Wiring] ...Overlay...invisible to QA RenderTexture capture")` を出してから代入（黙殺なし）。`Menu.unity` に MainCamera タグ付きカメラが実在するため通常経路で worldCamera は非 null に解決。
  6. サイレント失敗ガード確認: `MenuController.Start()` の MenuScreen 欠落（LogError+return、Update も `_screen==null` ガード）、`ResolveSaveForDisplay` の SessionHolder/Save 欠落（LogWarning+default、S-02 TitleController と同一の縮退方針をヘッダに明記）、`RenderStats` の null save（LogError+return）、`ParseColor` の解析失敗（LogError+white）、`SetActiveTab` の範囲外 index（LogError+return）はいずれも `[Wiring] ...` を1回出す。File I/O は本 diff に無く、セーブ破損プロトコルは非該当。LogWarning は `LogAssert.NoUnexpectedReceived()` の対象外（Error/Assert/Exception のみ捕捉）かつ PlayMode テストは `EnsureCreated` で SessionHolder を先に生成するため未発火＝green を落とさない。
  7. 参照整合・コンパイル整合を確認: `GameInput`（EnableUi/Submit/Cancel/TabNext/TabPrev/Navigate）・`SaveData` 全参照フィールド（highScore/bestSurvivalTimeSec/bestWaveReached/totalRunsPlayed/totalKillCount/totalCrystalsEarned/crystalBalance/upgrade*Level/bgm/sfxVolume）・`SessionHolder.Instance/.Save/.EnsureCreated`・`MetaProgression.UpgradeCost(int)`・`GameConfig.Upgrade.*LevelMax`・`GameConfig.Ui.*`・`GameConfig.Scenes.Title/Menu/Game` すべて実在。item 数の整合（Start=1/Stats=0/Upgrade=3/Settings=2）は `MenuItemCount`／`ItemCountForTab`／`ItemsForTab`／各 `*ItemTexts` 配列長で一致。`.meta` GUID（Controller=ed367559.../Screen=766735e9...）は `Menu.unity` の script 参照と一致し、5シーンとも Build Settings 登録済みで遷移テストが成立。
  8. acceptance 充足を確認:
     - (a) プレイ開始＝「はじめる」+決定→Game: `Update()` の `if (Submit.WasPressedThisFrame() && _activeTab==Start) LoadScene(Game)`。他タブでは Submit 無反応（＝「はじめる」限定）。`SubmitOnStartTab_TransitionsToGameScene` が検証。
     - (b) アウトゲーム表示＝「統計」に必須項目: `BuildStatsPanel` の8行＋`RenderStats` がハイスコア/ベスト生存/最高ウェーブ/累計プレイ・撃破・クリスタル/残高/アップグレードLv を表示。`StatsTab_ShowsAllRequiredSaveDataFields` が全項目を検証。統計はフォーカス項目 0＝表示専用（`ItemsForTab(Stats)=null`）。
     - (c) 設定＝音量項目+操作説明: `SettingsItemTexts`（BGM/SFX 音量）＋`InstructionsText`（WASD/Space）。`SettingsTab_ShowsVolumeItemsAndInstructions` が検証。スライダー A/D 調整は S-13 スコープ外（枠のみ）で正当。
     - (d) 終了導線＝Esc→Title: `if (Cancel.WasPressedThisFrame()) LoadScene(Title)`。`Cancel_TransitionsToTitleScene` が検証。
     - Q/E 循環: `MenuNavigation.CycleTab`（両端ラップ）。`TabNext_CyclesForwardAndWrapsPastLastTab`／`TabPrev_FromStartTab_WrapsBackwardToSettingsTab`＋EditMode `CycleTab_Wraps*` が検証。
     - W/S フォーカス（ラップなし）: `MenuNavigation.MoveFocus`（先頭/末尾クランプ）。タブ切替時 `_focusIndex=0` リセット。`FocusMove_ClampsAtLastAndFirstItem_WithinUpgradeTab`＋EditMode `MoveFocus_Clamps*`/`ZeroItemCount`/`SingleItem` が検証。
     - タブバー形式・必須4要素の実在: `TabBar_HasAllFourRequiredLabels`／`StartsOnStartTab_WithOnlyItsPanelActive`／`MenuCanvas_UsesScreenSpaceCameraRenderMode` が検証。
  9. 既承認 S-02（Title）とのパターン整合を確認: Camera.main ハンドリング・CreateText の anchor/pivot/sizeDelta 方式・GameInput facade 利用・層分離・SceneWiring の try/catch+Fail 方式は S-02 で APPROVE 済みの構造を踏襲。新規リグレッションなし。
- 対応: 追加対応不要（APPROVE のため revise 無し）。次工程（QA-PLAY）へ進行可。

## CR-CODE iteration 2 追加指摘（code-reviewer + silent-failure-hunter 合算）への対応
- 日時: 2026-07-10T16:30:00Z
- 指摘要約:
  1. [MEDIUM] `MenuController.cs:42` — `Start()` の LogError メッセージ「Menu is fully inoperative (rendering, input, and Submit/Cancel scene transitions are all disabled)」が同一コミットの `Update()` 変更（Submit/Cancel を `_screen==null` ガードより前に移動）と矛盾。`_input.EnableUi()` は null ガード前（37行）に実行済みで `_activeTab` 既定値 0 = `MenuTabIndex.Start` のため、MenuScreen 配線失敗時でも Submit→Game / Cancel→Title は実際に動作する。メッセージは縮退範囲を過大報告しており、ログを読んだ開発者/QA が「プレイヤー完全閉じ込め」と誤診して実挙動と食い違い、デバッグを誤誘導する。
- 対応（対応済み・黙殺なし）:
  1. 対応済み: `MenuController.Start()` の LogError メッセージを "[Wiring] MenuController requires a sibling MenuScreen component; Menu rendering and tab/focus navigation are disabled (Submit->Game and Cancel->Title remain functional as escape routes)" に修正。実挙動（描画・タブ/フォーカス操作のみ無効化、Submit/Cancel のシーン遷移は `_activeTab` 既定値=Start により機能し続ける）と一致させ、過大報告を解消。メッセージ文言のみの変更でロジック非変更のため他 acceptance への影響なし。
- 検証結果: EditMode `qa/evidence/editmode-results.xml` total=13 passed=13 failed=0（exit 0）。`ForgeBuild.BuildMac` exit 0・`game/Logs/build.log` に `Build succeeded: .../game/Build/ForgeGame.app`。

## 追記: CR-CODE iteration 1 追加指摘（code-reviewer + silent-failure-hunter 合算）への対応
- 日時: 2026-07-10T16:16:00Z
- 指摘要約（優先度順）:
  1. [MEDIUM] `MenuController.cs:67-70`（旧行）— `Update()` の `_screen == null` 早期 return が Submit(→Game)/Cancel(→Title) のシーン遷移まで無効化し、Menu をソフトロックさせる。ログが「Menu will render nothing」と描画影響のみを主張し、実際の入力・遷移全停止という縮退範囲を過小報告。
  2. [MEDIUM] `MenuScreen.cs:241-246, 254-259` — `SetFocusIndex` に無音パスが2つ。(a) 範囲外 tabIndex が `ItemsForTab` の null 経由で無音 return（`SetActiveTab` は同種でLogError済みなのに非対称）。(b) 範囲外 focusIndex が全項目ハイライト消失をログ無しで発生させる。S-12/S-13 が公開APIを呼ぶ予定であり将来のバグ検出可能性に関わる。
  3. [LOW] `MenuController.cs:128` — `ItemCountForTab` の fall-through `return 0;` が未知タブをログなしで吸収する防御的デフォルト。
  4. [LOW] `MenuSceneTests.cs:261-264` — テストヘルパ `ExpectedColor` が `TryParseHtmlString` の戻り値を無視し、パース失敗時に `default(Color)`（透明黒）を無言で返す。typo 時の失敗メッセージが原因不明になる。
- 対応（全4件 対応済み・黙殺なし）:
  1. 対応済み: `MenuController.Update()` を書き換え、Submit/Cancel の判定・シーン遷移を `_screen == null` ガードより前に移動（両者とも表示状態を読まないため screen 無しでも安全に実行可能）。`Start()` の LogError メッセージを「Menu is fully inoperative (rendering, input, and Submit/Cancel scene transitions are all disabled)」に修正し、実際の縮退範囲（描画のみでなく入力・遷移も含む）を正確に報告するよう変更。
  2. 対応済み: `MenuScreen.SetFocusIndex` に `SetActiveTab` と同形の tabIndex 範囲ガード（`LogError` + return）を追加。加えて `ItemsForTab` が null/空を返すケースを `tabIndex == MenuTabIndex.Stats` かどうかで分岐し、Stats 以外での null/空は配線バグとして `LogError`（Stats のみ無音許容＝表示専用タブとして正当）。さらに focusIndex の範囲外チェック（`LogError` + return）を新設し、ハイライト消失が無言で起きないようにした。
  3. 対応済み: `ItemCountForTab` の fall-through 直前に `Debug.LogError($"[Wiring] ItemCountForTab: unknown tabIndex={tabIndex}")` を追加してから `return 0;`。
  4. 対応済み: `MenuSceneTests.ExpectedColor` を `Assert.IsTrue(ColorUtility.TryParseHtmlString(hex, out Color color), $"failed to parse '{hex}'")` に変更し、パース失敗時にテストが即座に明示的な理由で fail するようにした。
- 検証結果: EditMode `qa/evidence/editmode-results.xml` total=13 passed=13 failed=0（exit 0）。PlayMode `qa/evidence/playmode-results.xml` total=27 passed=27 failed=0（exit 0、MenuSceneTests 全件含む）。`ForgeBuild.BuildMac` exit 0・`game/Logs/build.log` に `Build succeeded: .../game/Build/ForgeGame.app`。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-10T07:31:00Z
- 対象コミット: 73d4bfdf6df78021193135648d1318618af66c40
- レビュアー: pr-review-toolkit:code-reviewer（wave3-code-reviewer）
- 判定: APPROVE（findings 0件。iteration 1 の追加指摘4件の revise を検証し、新規リグレッション・rule 違反・設計欠陥なし）
- 指摘要約: なし（未解決指摘なし）
- 検証内容（今回 diff = `MenuController.cs` / `MenuScreen.cs` / `MenuSceneTests.cs` / `state/reviews/s-03.md`）:
  1. ソフトロック修正の正当性を確認: `Update()` で Submit(→Game)/Cancel(→Title) を `_screen == null` ガードより前に移動。両判定は `_input`（Awake で生成・Start の `EnableUi()` は `_screen` チェックより前に実行済み）と `_activeTab` のみを読み、表示状態に非依存。縮退時（MenuScreen 欠落で Start() 早期 return）でも `_activeTab` は int 既定値 0 = `MenuTabIndex.Start`（GameConfig 確認済み）のため `Submit && _activeTab==Start` が成立し Game へ、Cancel が Title へ遷移＝プレイヤーは出口を失わない。ソフトロック解消を確認。
  2. Update() 再順序化の副作用なしを確認: Submit/Cancel 発火時の早期 `return` により同フレームの HandleTabSwitch/HandleFocusMove はスキップされるが、直後にシーン遷移で MonoBehaviour 破棄されるため `_prevNavigate` 未更新は無影響。各 PlayMode テストは1アクション/フレーム駆動（press→yield→release→yield）で複合入力テストが無く、順序変更は既存アサーションに影響しない。
  3. SetFocusIndex ガード追加の正当性を確認: tabIndex 範囲ガード（`SetActiveTab` と同形）＋ null/空 items の Stats 分岐（Stats のみ無音許容＝表示専用として正当、他タブは配線バグとして LogError）＋ focusIndex 範囲ガードを追加。通常フローでは `MoveFocus` が [0,count-1] にクランプ（itemCount≤0 の Stats は 0 を返し、SetFocusIndex は Stats の null items で先に短絡）するため focusIndex ガードは純粋に防御的で正常系に非干渉。無音パス→LogError 化はサイレント失敗ハント方針に整合。
  4. rule 適合を確認: 追加コードはエラーメッセージ文字列と `0`／`GameConfig.Ui.MenuTabIndex.Count`／`items.Length`／`MenuTabIndex.Stats` による範囲比較のみ＝マジックナンバー混入なし（`return 0;` は sentinel count でidiom）。delta-time 対象の時間加算・フレーム依存移動の新規追加なし。`Systems/` 無変更＝エンジン非依存コア不変。入力は `GameInput` facade 経由を維持。
  5. テストヘルパ修正を確認: `ExpectedColor` の `Assert.IsTrue(ColorUtility.TryParseHtmlString(...), $"failed to parse '{hex}'")` 化でパース失敗が明示メッセージで即 fail＝無音の透明黒返却を排除。
  6. acceptance 非退行を確認: (a) Submit@Start→Game / (b) 統計必須項目表示 / (c) 設定=音量+操作説明 / (d) Cancel→Title、および Q/E 循環・W/S クランプは iteration 1 で検証済みの実装を保持し、本 diff は縮退時の出口確保とログ強化のみで acceptance を退行させない。
- 対応: 追加対応不要（APPROVE のため revise 無し）。次工程（QA-PLAY）へ進行可。

## CR-CODE iteration 1 — APPROVE（commit 0b1843401 のログ文言修正レビュー）
- 日時: 2026-07-11T03:02:39Z
- 対象コミット: 0b1843401d2c0df62ef352bb1b154455fb356aa0（"S-03: fix CR-CODE iter 2"）
- レビュアー: pr-review-toolkit:code-reviewer（wave3-code-reviewer）
- 判定: APPROVE（findings 0件）
- 指摘要約: なし（未解決指摘なし）
- 検証内容（コード変更は `MenuController.cs:42` の LogError 文言1行のみ。他は s-03.md への追記）:
  1. 文言の実挙動整合を確認: 新メッセージ「Menu rendering and tab/focus navigation are disabled (Submit->Game and Cancel->Title remain functional as escape routes)」は縮退パス（`_screen==null`）の実挙動と一致。トレース: `Start()` は null チェック前に `_input.EnableUi()`（UI マップ＝Submit/Cancel/Navigate/Tab を有効化）を実行 → `_screen==null` で早期 return し `_activeTab` は int 既定値 0 のまま。`GameConfig.Ui.MenuTabIndex.Start==0`（GameConfig.cs:196 で確認）のため `Update()` の `Submit && _activeTab==Start` が成立し Game へ、`Cancel` が Title へ遷移。両判定は `_screen==null` ガードより前にあり表示状態非依存。ガード後の HandleTabSwitch/HandleFocusMove はスキップ＝タブ/フォーカス操作は無効。旧メッセージ（"Submit/Cancel scene transitions are all disabled"）の過大報告を正しく解消しており、正確性向上。
  2. unity-code rule 適合を確認: 変更は文字列リテラルのみ＝マジックナンバー混入なし・delta-time 対象の時間/フレーム依存追加なし・`Systems/` 無変更＝エンジン非依存コア不変・入力は `GameInput` facade 経由を維持。
  3. acceptance 非退行を確認: (a) Submit@Start→Game / (b) 統計必須項目表示 / (c) 設定=音量+操作説明 / (d) Esc→Title、Q/E 循環・W/S クランプは iteration 1/2 で検証済みの実装を保持。本 diff は縮退パスのログ文言のみでロジック非変更のため acceptance を退行させない。
- 対応: 追加対応不要（APPROVE のため revise 無し）。次工程（QA-PLAY）へ進行可。
