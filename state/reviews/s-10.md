## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-22T08:43:37Z
- 指摘要約（優先度順）:
  1. [major] `TowerCombatPlayModeTests.TryUpgradeTower_Lv1ToLv2ToLv3_IncreasesDamagePerShot_AndDeductsEffectiveCost` が資金不足で必ず失敗する。StartingGold(100) → 設置(-50) → Lv2強化(-40) = 残10 では Lv3強化(70必要)に届かず `toLv3.Success` の Assert が確実に落ちる。
  2. [major] acceptance 前段「設置済みタワー左クリックでアップグレード/売却パネルを開き」を実装する ui-engineer story が state/stories.yaml に存在しないのに、S-10 の note と `BuildSpotController.TryUpgradeTower` の設計コメントは「未着手の ui-engineer story のスコープ」と存在しない story への委譲を主張していた（黙殺と同型）。
  3. [minor] 新規追加の `Systems/TowerUpgradeSystem.cs` と `Tests/EditMode/TowerUpgradeSystemTests.cs` の `.meta` が未コミット（他の全 .cs には `.cs.meta` が存在する）。tech-stack-unity.md の Visible Meta Files 既定に違反し、バッチ検証区間で Unity が乱数 GUID の .meta を自動生成し他レーンの作業ツリーを汚す。
  4. [minor] `TryUpgrade_WithDiscountRate_AppliesSameFormulaAsPlacement` の `const float discountRate = 0.06f;` がハードコード。`GameConfig.Meta.Upg02DiscountPerLevel`（S-14 で調整レンジ内変更され得る）から導出すべき。

- 対応:
  1. **対応済み**。`game/Assets/Tests/PlayMode/TowerCombatPlayModeTests.cs` の Lv3 強化直前に `buildController.Economy.Add(GameConfig.BastionCannon.UpgradeLv3Cost)` を挿入し、`goldBeforeLv3` をその後に取得するよう修正（100-50-40+70=80 → 強化後10。既存アサーション式と整合）。`EconomySystem.Add(int)` は既存 public API のため呼び出しのみで済む。
  2. **対応済み（記録の訂正のみ・story新設は越権のため見送り）**。gameplay-engineer には新規 story の発行・ui-engineer story への割当権限がないため、story 自体は新設しない。代わりに (a) `BuildSpotController.TryUpgradeTower` の設計コメントを「未着手の ui-engineer story のスコープ」という不正確な主張から「対になる story が state/stories.yaml に存在しない」という正確な記述へ訂正し、(b) `state/stories.yaml` の S-10 note に `[BLOCKER]` 付きの未解決事項を追記して tech-director への差し戻し・Checkpoint での人間提示を明示した。story 新設/既存 story へのスコープ追加自体は tech-director の判断に委ねる。
  3. **対応済み**。`game/Assets/Scripts/Systems/TowerUpgradeSystem.cs.meta`（guid: 0c2db1c346fa4644932c2786cb428fea）と `game/Assets/Tests/EditMode/TowerUpgradeSystemTests.cs.meta`（guid: 1698ec1d53bb43f98b35e1e7a426ad58）を既存 .meta と同一フォーマット（`fileFormatVersion: 2` + `guid`）で作成。リポジトリ内の既存 .meta との GUID 重複無しを grep で確認済み。
  4. **対応済み**。`const float discountRate = 2 * GameConfig.Meta.Upg02DiscountPerLevel;` へ変更（コンパイル時定数のまま導出。`Upg02DiscountPerLevel` は const float のため const 式として成立）。

- 検証: 並走レーン規律により Unity は起動していない。Read/Grep による静的確認のみ実施——
  - `EconomySystem.Add(int)` が public であることを確認（`game/Assets/Scripts/Systems/EconomySystem.cs:30`）。
  - `BuildSpotController.Economy` が `EconomySystem` を返す public プロパティであることを確認（`game/Assets/Scripts/Components/BuildSpotController.cs:27`）。
  - `GameConfig.Meta.Upg02DiscountPerLevel` の実在と型（`const float`）を確認（`game/Assets/Scripts/GameConfig.cs:191`）。
  - 新規 `.meta` 2件の GUID が既存 `.meta` 群と重複しないことを grep で確認。
  - 正式な EditMode/PlayMode 実行（`TryUpgradeTower_Lv1ToLv2ToLv3_...` の exit 0 含む）はレーン合流後のバッチ検証区間（tech-stack-unity.md）で行う。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-22T09:00:30Z
- 対象: S-10 実効差分（コミット 05d0005/899bd28/fc6b7b8/0aa6d47/55f3f42 ＋ ステージ済み iter1 修正）。workflow から渡された対象参照が未解決プレースホルダ `COMMIT_FAILED` だったため、reviewer が git log / git status から再構成した。
- iter1 対応の検証結果（全4件を静的確認で裏取り済み）:
  1. major #1（PlayMode 資金不足）: **解消確認**。100-50-40+70=80 → Lv3 強化(70)後 10。`Economy.Add` は public 実在（EconomySystem.cs:30）、アサーション式と整合。
  2. major #2（存在しない story への委譲）: **記録訂正は確認**。設計コメント訂正・stories.yaml S-10 note への `[BLOCKER]` 追記・tech-director 差し戻し明記を確認。ただし acceptance 前段（左クリック→パネル）のプレイヤー到達経路は依然コード上に無い（下記・唯一の残存 major）。
  3. minor #3（.meta 欠落）: **解消確認**。2件とも既存 .meta と同一形式（fileFormatVersion+guid）、GUID 重複無しを grep で確認。
  4. minor #4（discountRate ハードコード）: **解消確認**。`2 * GameConfig.Meta.Upg02DiscountPerLevel` の const 導出へ変更済み（Upg02DiscountPerLevel は const float・GameConfig.cs:191）。
- 指摘要約（優先度順）:
  1. [major・残存/story 内解消不能] acceptance 前段「設置済みタワー左クリックでアップグレード/売却パネルを開き」を実装する ui-engineer story が state/stories.yaml に未発行のまま。`BuildSpotController.TryUpgradeTower` はテスト/将来 story からのみ到達可能で、QA-PLAY 観点3（acceptance の実操作検証）はこのままでは満たせない。正当理由（gameplay-engineer に story 発行権限なし）と `[BLOCKER]` 記録は iter1 対応で明記済みのため、本レビューでは再修正要求ではなく **Checkpoint への未解決事項エスカレーション対象**として維持する（review-loops 合格基準「findings解消 or 正当理由の明記」の後者に該当）。
  2. [minor・harness] workflow が CR-CODE reviewer へ渡した対象コミット参照が `COMMIT_FAILED`（変数解決失敗のプレースホルダ）。今回は reviewer 側で対象を再構成できたが、解決失敗が黙って下流に流れる経路のため workflow 側の修正要（コード diff 自体の指摘ではない）。
- コード品質所見（silent-failure 重点確認）: 追加コードに catch 節・エラー黙殺・LogError 降格は無し。失敗パスは全て typed `TowerUpgradeFailureReason` で呼び出し側へ返却、不変条件違反（Level 範囲外）は `BaseUpgradeCost`/`DamageForLevel` が throw で表面化。マジックナンバー・Systems/ の MonoBehaviour 依存・パス直書き違反無し。acceptance 後段（Lv 進行・ダメージのみ増加・役割不変・Lv3 打止め・実効コスト減算）は EditMode/PlayMode テストで検証構造が成立（実行はバッチ検証区間）。
- 対応:
  1. [major・残存/story 内解消不能・UI 到達経路]: **見送り（正当理由: story スコープ外・既に escalation 済み）**。
     reviewer 自身が本 iteration 2 で「再修正要求ではなく Checkpoint への未解決事項エスカレーション対象として
     維持する」と明記しており、review-loops.md 合格基準「findings解消 or 正当理由の明記」の後者に該当する。
     gameplay-engineer には ui-engineer story の新設・既存 story へのスコープ追加権限が無く、対になる
     パネル UI story が state/stories.yaml に存在しない限り `BuildSpotController.TryUpgradeTower` へのプレイヤー
     入力到達経路をこの story 単独で作ることはできない（越権禁止）。state/stories.yaml の S-10 note に
     `[BLOCKER]` として記録済み（iter1 対応時）であり、内容は iter2 の指摘と齟齬が無いことを本レビューで再確認した。
     CR-CODE MAX_ITER（2）到達のため、本件は解消せずに Checkpoint 提示へ引き継ぐ（下記 status: done 反映）。
  2. [minor・harness・full-build.js の COMMIT_FAILED]: **見送り（正当理由: gameplay-engineer の担当外）**。
     `.claude/workflows/full-build.js` はオーケストレーション層であり、S-10 の実装対象（`game/Assets/Scripts/**`
     の Systems/Components 層）ではない。gameplay-engineer の役割定義（担当外ストーリー・非game/コードの
     ファイルを触らない）の範囲外のため本 story では修正しない。tech-director/workflow 保守者への別途報告が
     必要な事項として、この対応記録内に明記するのみとする（黙殺ではなく理由付き見送り）。
  3. iter1 対応（major #1 資金不足・major #2 記録訂正・minor #3 .meta・minor #4 discountRate 導出）は本
     iteration 2 レビューで reviewer が全4件の解消を静的確認済み（上記「iter1 対応の検証結果」節）。追加の
     コード変更は無し。

- 検証（並走レーン規律により Unity 未起動。Read/Grep による静的再確認のみ）:
  - `EconomySystem.Add(int)` / `EconomySystem.Gold` が public であることを再確認（`game/Assets/Scripts/Systems/EconomySystem.cs:12,30`）。
  - `GameConfig.Meta.Upg02DiscountPerLevel`（`const float`）の実在を再確認（`game/Assets/Scripts/GameConfig.cs:191`）。
  - `TowerUpgradeSystem.cs.meta`（guid: 0c2db1c346fa4644932c2786cb428fea）・`TowerUpgradeSystemTests.cs.meta`
    （guid: 1698ec1d53bb43f98b35e1e7a426ad58）とも既存 `.meta` 群との GUID 重複が無いことを再確認（`grep -rh guid: `）。
  - コード変更が無いため上記シンボル参照に新規のコンパイル破壊リスクは無い。正式な EditMode/PlayMode 実行は
    レーン合流後のバッチ検証区間（tech-stack-unity.md）で行う。
  - 本対応により CR-CODE MAX_ITER（2）に到達。未解決の major（UI 到達経路 blocker）は state/stories.yaml の
    S-10 note と本ファイルに記録済みのまま Checkpoint エスカレーションへ引き継ぎ、status を `done` に更新する。
