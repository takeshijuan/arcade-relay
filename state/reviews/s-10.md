# state/reviews/s-10.md — S-10 "Game HUD（HP/ダッシュCD/ウェーブ/スコア）"

## CR-CODE iteration 1 — APPROVE
- 日時: 2026-07-10
- 対象コミット: 5bfb23e7601960e407d39fc7a7a257b1e2540697
- 指摘要約: findings 0件。
  - unity-code 規約準拠を確認: マジックナンバー無し（フォント/色/サイズ/配置/文言はすべて `GameConfig.Ui` の名前付き定数に集約。本文の `0f`/`1f`/`0.5f` pivot・`Vector2.zero/one` は意味を持たない構造値で除外対象）。
  - delta-time: HUD は毎フレーム権威状態を反映するだけでフレーム依存の累積加算が無く、規約非該当（正しく非依存）。
  - エンジン非依存コア線引き: スコア算出は `Systems/ScoreSystem.ComputeFinalScore`（pure C#）へ委譲、表示は `Ui/GameHud`、配線は `Components/GameHudController` に分離。Components は式を再実装せず Systems を呼ぶだけで層責務を維持。
  - rule 14（HUD Canvas は ScreenSpaceCamera + worldCamera 固定、ScreenSpaceOverlay 禁止）準拠。`Camera.main` 欠落時は LogError ガードあり。
  - スコア整合性: live HUD スコアと Result の finalScore が同一式・同一入力（`_survivalTimeSec` アクセサ / `RunStatsTracker` の kill・crystal 集計）で算出されることを両呼び出し箇所で確認。Game→Result 遷移でのジャンプ無し。
  - 新規アクセサ（`PlayerController.DashCooldownRemaining` / `HealthComponent.SurvivalTimeSec`）は既存 read-only パターンの踏襲でロジック無し。
  - null ガード: 依存 singleton 欠落は `LogMissingOnce` で1回だけ Debug.LogError（黙殺無し）。
  - acceptance 充足: PlayMode 5テストが 被弾でHP減 / ダッシュ後CD残 / 経過でウェーブ増 / 撃破+回収でスコア増 / renderMode==ScreenSpaceCamera を検証（effectiveMaxHp 基準の HP 表示も controller が `_health.EffectiveMaxHp` を渡して満たす）。
- 対応: 不要（APPROVE）。
