## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-22T13:54:47Z
- 指摘要約（優先度順）:
  1. [major] `AssetIntegrationPlayModeTests.cs` — モデル割当の検証がプレースホルダ盲目。`AssertRendererSane` は Renderer の健全性しか見ておらず、`GeneratedModelFactory` が null を返し `PlaceholderFactory` へフォールバックしても同じ assert が green になる。特に MDL-04 は GLTFast インポート確定タイミングの既知リスクがある。
  2. [minor] `BgmController.cs` — PausePanel の音量スライダー操作が Game シーン本体の BGM AudioSource に反映されない「操作が効かない」ユーザー観測可能なギャップが、ヘッダのスコープ外記載のみで Checkpoint 提示への言及が無い。
  3. [minor] `AssetIntegrationPlayModeTests.cs` — スクリーンショット証跡が PNG 書き出しのみで内容検証ゼロ（真っ黒/白飛びでも PASS）。
  4. [minor] `ForgeAssetIntegration.cs` — S-19 で MDL-04/BGM-01 が「取込済みであるべき資産」になったのに MISSING が report 行のみで PLANNED と区別できず、hardFailure にもならない。
  5. [minor] `GameConfig.cs` — Presentation 節の Warbeast 関連コメントが「status=planned のためプレースホルダのまま」のまま陳腐化（本 diff で MDL-04 取込・EnemyView 配線を実施済みなのに矛盾）。
  6. [minor] `AssetIntegrationPlayModeTests.cs` — スクリーンショット証跡のテストフィクスチャに Light が無く、暗転（SUSPECT_BLANK）を誘発しうる（#3 と関連）。
  7. [minor] `AssetIntegrationPlayModeTests.cs` — `NoopSaveStore` が既存 `Persistence.InMemorySaveStore` と機能重複するテストダブルの増殖。
  8. [minor] `AssetIntegrationPlayModeTests.cs` — TearDown の `Object.Destroy(cameraGo)`（AudioListener 保持）が遅延破棄のため、同一フレーム内で連続実行される `[Test]` 間で AudioListener 重複 Warning が発生し `LogAssert.NoUnexpectedReceived()` を無関係に落とす可能性。
  9. [minor・推測/要バッチ検証確認] `AssetIntegrationPlayModeTests.cs` — `AudioSource.isPlaying` 依存の assert が -batchmode のオーディオデバイス初期化状況次第で一斉に落ちるリスク（記録のみ）。

- 対応:
  1. **対応**: `AssertUsesRealModel` ヘルパーを新設し、(a) `Resources.Load<GameObject>(assetKey)` が非null であること (b) `GeneratedModelFactory` の命名規約（`"{type}Model"`/`"CoreModel"`）どおりの子 GameObject が実在すること（`PlaceholderFactory` の `"{type}PlaceholderVisual"` ではないこと）を明示検証。`PlacedTowersAndSpawnedEnemy_...` テスト（Bastion Cannon/Arc Emitter/Core/スポーン敵 全種別）と `EnemyView_Warbeast_Initialize_ProducesSaneRenderer` テストの両方に追加した。
  2. **対応**: `BgmController.cs` ヘッダに `[KNOWN LIMITATION — 要 Checkpoint 開示]` ブロックを追加し、呼び出し元 workflow が Checkpoint 提示時に未解決事項として明示すべきことを明文化した（実装自体の修正はスコープ外のまま — 根本修正には Game シーン内で設定変更を購読する経路の追加設計が必要なため）。
  3. **対応**: `AssertNotBlank` ヘルパーを新設し、`CaptureSceneScreenshot` 内で ReadPixels 済み Texture2D の平均輝度を計算、gates.md の SUSPECT_BLANK 判定基準（0.02〜0.98）と同じ範囲で `Assert.IsTrue` するようにした。
  4. **部分対応**: `CheckModels`/`CheckAudio`（`ForgeAssetIntegration.cs`）で MISSING を `MISSING_INTEGRATED` という別ステータス語にし、PLANNED（design/assets.md status=planned の既知ギャップ）と区別できるようにした。ただし hardFailure（Exit(1)）への昇格は見送った。理由: MDL-04 は stories.yaml S-19 impl note が明記するとおり GLTFast の ScriptedImporter インポート結果が同一 Editor セッション内で確定しない既知のタイミング差異があり、一括検証実行の直後に hardFailure化すると「まだインポート未確定なだけ」を「壊れている」と誤検知しうる（自己解決するはずのタイミング問題を非0終了で止めてしまう）。構造的破損の確定シグナルは既存どおり `NO_RENDERER`（インポート済みだが Renderer が無い）のみとし、`MISSING_INTEGRATED` は report に明示した上で人間判断（Checkpoint）に委ねる方針をヘッダコメントに追記した。finding 自身も「少なくとも…ステータス語で出し分けること」を最低ラインとして許容している。
  5. **対応**: `GameConfig.cs` の Presentation 節（`WarbeastHeightM` 前後のコメント）と `Placeholder` クラスの XML コメントを「MDL-01〜05 全て Integrate 済み・想定外の縮退経路でのみプレースホルダ参照」という事実に合わせて書き換えた。
  6. **対応**: `SetUp` に `GameConfig.Lighting`（S-21 と同一定数）で Directional Light + Flat 環境光を明示追加。TearDown で `Object.Destroy(lightGo)`。
  7. **対応**: `NoopSaveStore` を削除し、既存 `ForgeGame.Persistence.InMemorySaveStore` を再利用するよう変更（`outcome.SetSaveStoreForTest(new InMemorySaveStore())`）。
  8. **対応**: TearDown の `cameraGo`（AudioListener 保持）破棄のみ `Object.DestroyImmediate` に変更（他オブジェクトは既存どおり `Object.Destroy` のまま — 最小差分）。
  9. **見送り**: finding 自身が「推測・要バッチ検証確認」と明記しており、既存 `AudioCuePlayer` の実績から batchmode でも audio システムは稼働していると推定される。事前の投機的な代替実装は過剰対応と判断し、レーン合流後のバッチ検証で実際に失敗した場合に対処する（対処法は finding が既に代替案 — AudioSource 生成数/clip 割当シグナルへの切替 — を記録済み）。

- 検証: 並走レーン規律により Unity は起動していない（単一インスタンスロック）。参照した型・メンバ・アセットキーの実在は Read/Grep で静的確認済み — `ForgeGame.Persistence.InMemorySaveStore`（コンストラクタ `SaveData initial = null` の既定引数）、`GameConfig.Lighting.*`（`DirectionalIntensityLux`/`DirectionalColor`/`DirectionalEulerAngles`/`AmbientColor` — 全て既存 public 定数）、`GameConfig.AssetKeys.Model*`（5件とも既存）、`UnityEngine.Rendering.AmbientMode`（`EnvironmentView.cs` で同一 using 済み）、`EnemyView.Type`（public getter）、`TowerType`/`EnemyType` enum（`Types.cs`）を確認した。EditMode/PlayMode の実行とコンパイル確定はレーン合流後のバッチ検証区間で行う。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-22T14:07:20Z
- 指摘要約（優先度順）:
  1. [minor] `AssetIntegrationPlayModeTests.cs` — `SetUp` が `RenderSettings.ambientMode`/`ambientLight`（シーン共有のグローバル状態）を Flat へ上書きするが `TearDown` が復元しないため、後続 PlayMode テストへ Flat 上書きが漏れテスト分離が壊れる。
  2. [minor] `AssetIntegrationPlayModeTests.cs` — `CaptureSceneScreenshot` 内で `AssertNotBlank` が PNG 書き出しより前に実行されるため、SUSPECT_BLANK 失敗時に診断用証跡が保存されず（stale な旧 PNG が正準ファイル名のまま残存する場合もある）、失敗診断の一次証跡が失われる。加えて `Texture2D` の `Destroy` が `try` 内にあり失敗経路でリークする。
  3. [minor] `ForgeAssetIntegration.cs` — `CheckAudio` の `MISSING_INTEGRATED` を hardFailure へ昇格しない判断が、音声には「GLTFast のような既知の非同期確定タイミング問題が無い」と自認しているにも関わらず根拠が「一貫性」のみで、かつ harness 側に MISSING_INTEGRATED を消費する経路（`prototype.js`/`full-build.js`）が無いため、Integrate 時点の SFX/BGM 欠落が exit 0 で素通りし発見が QA-PLAY 任せに遅延する。
  4. [minor] `BgmController.cs` — `[KNOWN LIMITATION — 要 Checkpoint 開示]` の開示手段がコードコメントのみで担保機構が無く、review-mode=lean では workflow がコードコメントも state/reviews/ も Checkpoint 素材へ自動転記しないため開示が消失しうる（finding自身は追加のコード修正を要求せず、CR-CODE 残findingとして記録することで unresolvedFindings 経由の Checkpoint 転記を担保することが目的）。
  5. [minor] `PlaceholderFactory.cs` — ヘッダコメント「MDL-04（Warbeast）は常にプレースホルダを使う」が iter1 で追加した `AssertUsesRealModel(go, "WarbeastModel", …)`（Warbeast も実モデル必須）と直接矛盾したまま残存（iter1 finding #5 の GameConfig.cs 側修正の横展開漏れ）。

- 対応:
  1. **対応**: `SetUp` 冒頭で `RenderSettings.ambientMode`/`ambientLight` を `prevAmbientMode`/`prevAmbientLight` フィールドへ退避し、`TearDown` で復元するよう変更した。
  2. **対応**: `CaptureSceneScreenshot` を再構成し、PNG を正準ファイル名で先に書き出してから `AssertNotBlank` を呼ぶ順序に変更（失敗時も直近描画の実写証跡が残り、stale な旧 PNG も最新描画で上書きされる）。`Texture2D` の `Destroy` は `finally` 節へ移動し失敗経路のリークを解消した。
  3. **対応**: `CheckResource<T>` の戻り値を `bool`（`integrated=true` かつ欠落時に `true`）に変更し、`CheckAudio` が呼び出し側で `hardFailure` へ反映するようにした（モデルの `MISSING_INTEGRATED` は GLTFast の既知タイミング差異により従来どおり非昇格のまま維持し、音声のみ昇格という finding の推奨案1を採用）。ヘッダコメントに判断根拠を追記した。
  4. **見送り（finding自身の意図どおり）**: 追加のコード修正は行わない。本 finding は CR-CODE MAX_ITER(2) 到達時の残findingとして記録することで、full-build.js の `unresolvedFindings` → `CD-CHECKPOINT` 経路に確実に乗せ、Checkpoint C 提示要約の「既知の課題」への転記を担保することが目的（finding のとおり対応=Checkpoint 要約への転記のみで足りる）。state/stories.yaml S-19 の note にも本件を明記した。
  5. **対応**: `PlaceholderFactory.cs` ヘッダコメントを「MDL-01〜05 全て Integrate 済み・Resources.Load 失敗時（想定外の縮退経路）にのみプレースホルダへフォールバック」へ書き換え、`GameConfig.cs` の既存記述（`Placeholder` クラス XML コメント等）と同旨に揃えた。

- 検証: 並走レーン規律により Unity は起動していない（単一インスタンスロック）。参照した型・メンバは Read/Grep で静的確認済み — `RenderSettings.ambientMode`/`ambientLight`（`UnityEngine.Rendering.AmbientMode` は既存 `using UnityEngine.Rendering;` で解決済み・`EnvironmentView.cs` で同型使用実績あり）、`CheckResource<T>` の全呼び出し元（`CheckTextures`3件・`CheckAudio`7件）が新シグネチャ（戻り値追加のみ・破壊的変更なし）と整合すること、`hardFailure |=` の演算子オペランドが両辺 `bool` であること、`AssetIntegrationPlayModeTests.cs`/`ForgeAssetIntegration.cs`/`PlaceholderFactory.cs` の3ファイルとも中括弧の対応数が一致すること（`grep -o "{"`/`"}"` カウント一致）を確認した。EditMode/PlayMode の実行とコンパイル確定はレーン合流後のバッチ検証区間で行う。
- 未解決事項（Checkpoint 転記対象）: `BgmController.cs` の `[KNOWN LIMITATION]`（PausePanel の音量スライダー操作が Game シーン BGM 本体の AudioSource に反映されない）。CR-CODE MAX_ITER(2) 到達によりエスカレーション。
