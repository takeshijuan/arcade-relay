# S-23 レビュー履歴

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-23T00:00:00Z（レビュー実施時刻はセッションログ参照）
- レビュー対象: **コミット未成立**（1Password SSH signing agent 障害 — stories.yaml S-23 note 参照）のため、
  working tree + index の S-23 帰属分を対象に実施:
  - `game/Assets/Scripts/Ui/TowerActionPanel.cs`（新規・untracked）+ .meta
  - `game/Assets/Tests/PlayMode/TowerActionPanelPlayModeTests.cs`（新規・untracked）+ .meta
  - `game/Assets/Scripts/Ui/HudPanel.cs`（unstaged 編集・全ハンク S-23 帰属）
  - `game/Assets/Scripts/GameConfig.cs`（staged ハンクのみ = Ui 配下 TowerAction* 定数追加）
  - 除外（他レーン帰属を diff コメントで確認）: BuildSpotController.cs（S-14 iter2/S-24/S-25）・
    TowerView.cs（S-24）・staged の EnemyView.cs / TowerCombatPlayModeTests.cs / s-25.md（S-25）
- acceptance 判定: **全項目コード上で充足**（現在Lv/ダメージ表示・次Lv実効コスト＝EconomySystem.ComputeEffectiveCost 経由・
  Lv3「最大強化済み」+非活性・売却返還額＝ComputeSellRefund 経由・資金不足の事前非活性・TryUpgradeTower/TrySellTower への
  到達経路成立＝S-10/S-11 [BLOCKER] 解消・右クリック/パネル外クリック解除・既存 TowerSelect* 定数無変更・
  PlayMode テスト6本で強化/売却両ケースをクリック擬似発行で検証）。
  ※ 静的確認のみ（並走レーン規律により Unity 未起動 — テスト実行はバッチ検証区間で確認すること）
- 指摘要約（優先度順）:
  1. **[major]** HudPanel.HandleActionPanelOpenInput の TrySellTower 失敗ログが Debug.LogWarning。
     TowerSellFailureReason は TowerNotFound のみ＝パネルが保持する OpenTowerId と System 状態の desync
     （回復不能条件）であり、コードベースの確立済み規範（BuildSpotController の desync 3箇所は LogError）に
     反して QA-PLAY のエラー0検査を素通りする。gates.md CR-CODE の明示パターン「新規コードが回復不能条件を
     最初から Warning 以下で記録」に該当し、sell 側には正当化コメントも無い。Upgrade 側の Warning は
     事前非活性化により資金系失敗が到達不能という正当化コメントがあるが、TowerNotFound だけは同種の desync。
     → 推奨: FailureReason==TowerNotFound は LogError、それ以外（到達不能の防御枝）は現状 Warning 可。
  2. **[minor]** パネル表示が Open() 時点の資金スナップショット固定。開帳中に撃破報酬で資金が増えても
     「(資金不足)」非活性のまま（閉じて開き直すまで）。TowerSelectPanel と同型の既知パターンで
     誤許可方向の不整合は無い（開帳中に資金は減らない）ため minor。
  3. **[minor]** ClickUpgradeButton_UpgradesTowerToLv2_AndDeductsEffectiveCost が期待減算額を基礎コスト
     `GameConfig.BastionCannon.UpgradeLv2Cost` 直書きで検証しており、acceptance の「実効コスト」導出
     （ComputeEffectiveCost × TowerDiscountRate）を経ていない。既定セーブ（割引0）では一致するが、
     売却テスト（ComputeSellRefund で期待値導出）と非対称。
  4. **[minor]** TowerActionPanel.DamageForLevel / BaseUpgradeCost が Systems 側 private 実装の式複製
     （ヘッダコメントで文書化済み・定数は GameConfig 単一情報源）。将来 Systems 側の式変更で UI 表示が
     黙ってドリフトするリスク。レーン合流後に public query 化して複製除去のフォローアップ推奨。
  5. **[minor/infra]** レビュー対象コミット不在（COMMIT_FAILED プレースホルダ）。signing agent 回復後の
     commit 成立時に、本レビュー対象と同一内容であることを確認しハッシュを本ファイルへ追記すること。
- 対応: （reviser 記入欄）

## CR-CODE iteration 1 — fix 対応記録
- 日時: 2026-07-22T18:34:15Z
- 対応内容（findings 1〜5 個別）:
  1. **[major] 対応済み** — `HudPanel.HandleActionPanelOpenInput` の Upgrade/Sell 双方の失敗ログを
     `TowerUpgradeFailureReason`/`TowerSellFailureReason` で分岐。`TowerNotFound`（＝パネルの
     `OpenTowerId` と `BuildSpotSystem` 状態の desync）は `Debug.LogError` に変更し、
     `BuildSpotController` の view/state desync ログ群と同じ規範に揃えた。Sell 側は
     `TowerSellFailureReason` が `None/TowerNotFound` の2値のみのため無条件 `LogError` に変更。
     Upgrade 側の `AlreadyMaxLevel`/`InsufficientGold`（事前非活性化でブロック済み・到達不能想定の
     防御枝）は指摘の推奨どおり `Debug.LogWarning` のまま維持し、正当化コメントを補強した。
     ファイル: `game/Assets/Scripts/Ui/HudPanel.cs`。
  2. **[minor] 対応済み** — `TowerActionPanel.Open()` の表示更新ロジックを `Refresh(TowerInstance,
     int currentGold, float discountRate)` として切り出し（`OpenTowerId`/`IsOpen`/`SetActive` は
     `Open()` 側のみが管理・`Refresh()` は表示更新のみ）、`HudPanel.RefreshDisplays()`（毎フレーム
     呼ばれる既存箇所）からパネル開帳中は現在の `BuildSpotController.Economy.Gold` /
     `TowerDiscountRate` / 対象タワーの最新状態で `Refresh()` を呼ぶよう配線。開帳中の撃破報酬による
     資金増加が強化ボタンの非活性表示へ追従するようになった。表示専任の原則は維持（Systems 層の値を
     都度渡すだけで UI 側に複製しない）。ファイル: `game/Assets/Scripts/Ui/TowerActionPanel.cs`,
     `game/Assets/Scripts/Ui/HudPanel.cs`。
  3. **[minor] 対応済み** — `ClickUpgradeButton_UpgradesTowerToLv2_AndDeductsEffectiveCost` の期待減算額を
     `GameConfig.BastionCannon.UpgradeLv2Cost` 直書きから
     `EconomySystem.ComputeEffectiveCost(GameConfig.BastionCannon.UpgradeLv2Cost, build.TowerDiscountRate)`
     経由の導出に変更し、売却テスト（`ComputeSellRefund` 経由）と対称にした。
     ファイル: `game/Assets/Tests/PlayMode/TowerActionPanelPlayModeTests.cs`。
  4. **[minor] 見送り** — `TowerActionPanel.DamageForLevel`/`BaseUpgradeCost` の式複製解消（Systems 側
     private メソッドの public query 化）は `Systems/TowerCombatSystem.cs`・`Systems/TowerUpgradeSystem.cs`
     の編集を要し、並走レーン規律（ui-engineer は gameplay-engineer 領域の Systems ファイルを編集しない）
     に反するため本 iteration では対応しない。指摘どおりレーン合流後のフォローアップ story を推奨事項として
     報告に記載する（コードは無変更 — 値のドリフトは無く、複製コード形の保守リスクのみ）。
  5. **[minor/infra] 見送り（本 iteration では対応不能）** — 本 fix コミット自体が signing agent 経由で
     成立するかは実行時に判明するため、本ファイルへの追記後の commit 実行結果でこの iteration record 自体の
     hash を報告する。以前の COMMIT_FAILED 分（S-23 実装コミット）の事後確認は本 fix の対応範囲外
     （別途 stories.yaml note のとおり signing agent 回復後に確認要）。
- 静的検証: Unity は起動していない（並走レーン規律 — バッチ検証区間に委ねる）。以下を Read/Grep で確認済み:
  - `TowerUpgradeFailureReason`（`None/TowerNotFound/AlreadyMaxLevel/InsufficientGold`）と
    `TowerSellFailureReason`（`None/TowerNotFound`）の実際の enum 値を `Systems/TowerUpgradeSystem.cs`・
    `Systems/BuildSpotSystem.cs` で確認し、ログ分岐の enum 参照が実在することを確認
  - `HudPanel.cs` は既に `using ForgeGame.Systems;` を持ち追加 using 不要
  - `TowerActionPanel.Refresh` の呼び出しシグネチャ（`TowerInstance, int, float`）と定義が一致することを確認
  - `BuildSpotController.TowerDiscountRate`（public float プロパティ）の実在を確認
  - `EconomySystem.ComputeEffectiveCost(int, float)` の実在を確認
  - 新設 LogError 分岐は既存 PlayMode テスト（成功パスのみ検証）到達しないため `LogAssert.NoUnexpectedReceived()`
    を汚さないことを既存テストコードの読解で確認
  - `TowerActionPanel`/`HudPanel` を参照する他ファイルを grep し、他レーンからの直接依存が無いことを確認

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-22T18:48:07Z
- レビュー対象: iteration 1 fix 後のコード。commit は本 iteration でも不成立
  （COMMIT_FAILED プレースホルダのままワークフローから渡された。signing agent 障害の継続）。
  iteration 1 と同一の帰属特定（`Ui/TowerActionPanel.cs`・`Ui/HudPanel.cs`・
  `Tests/PlayMode/TowerActionPanelPlayModeTests.cs`・`GameConfig.cs` の staged Ui ハンクのみ）で実施。
- 判定要旨: blocker/major は0件。iteration 1 の major #1（失敗ログの LogError 化）・
  minor #2（開帳中の資金追従 Refresh）・minor #3（テスト期待値の実効コスト導出）は全て修正適用済みを
  コード上で確認。#4（式複製）はレーン規律による見送り正当理由を確認。acceptance は全項目静的に充足
  （S-10/S-11 [BLOCKER] の入力到達経路も成立）。
- 指摘要約（優先度順）:
  1. **[minor]** `TowerActionPanel.DamageForLevel`/`BaseUpgradeCost` が
     `Systems/TowerCombatSystem.DamageForLevel`・`Systems/TowerUpgradeSystem.BaseUpgradeCost` と同一式の
     継続重複（iter1 #4 の継続）。値ドリフトは無いが将来 Systems 側変更時に無言 desync するリスク。
     post-merge で Systems 側に public query メソッドを公開し複製除去するフォローアップ story を推奨。
  2. **[minor]** `HudPanel.RefreshDisplays()`（113-117行）が開帳中に `TryFindTowerById` が false を
     返す desync を無ログで Refresh スキップするのみで、クリック経路（`HandleActionPanelOpenInput` の
     `TowerNotFound` 分岐）と異なり表面化しない。現状は到達不能な防御条件だが、将来レーンがタワー除去
     経路を追加した場合 silent stale UI になる。クリック経路と同じ LogError+Close 規範に揃えるべき。
  3. **[minor]** `TowerActionPanelPlayModeTests.cs` に acceptance「資金不足時はボタンを事前に非活性表示
     する」の資金不足ケース直接テストが無い（非活性検証は `MaxLevelTower` ケースの Lv3到達のみ）。
     `GameHudPlayModeTests.InsufficientFundsSpot_BothButtonsDisabled_ClickDoesNotPlace` と対称の
     「`IsUpgradeAvailable==false` かつ非活性ボタンのクリックが `TryUpgradeTower` に到達しない」検証を
     追加するのが望ましい。acceptance の必須2ケース（強化/売却）自体は充足済みのため minor。
  4. **[minor/infra]** レビュー対象コミットが iteration 2 でも不成立（COMMIT_FAILED継続）。iteration 1
     指摘#5 の継続。signing agent 回復後に成立するコミットが本レビュー対象と同一内容であることを確認し
     ハッシュを本ファイルへ追記すること。コード欠陥ではない。
- 対応: （reviser 記入欄）

## CR-CODE iteration 2 — fix 対応記録
- 日時: 2026-07-22T18:48:07Z
- 対応内容（findings 1〜4 個別）:
  1. **[minor] 見送り（継続）** — iter1 #4 と同一の見送り理由が継続適用。並走レーン規律
     （ui-engineer は gameplay-engineer 領域の `Systems/TowerCombatSystem.cs`・
     `Systems/TowerUpgradeSystem.cs` を編集しない）により本 iteration でも Systems 側 public query 化は
     行わない。`Ui/TowerActionPanel.cs` クラス冒頭コメントに iter2 時点の見送り理由（値ドリフトが無い
     ことの根拠・post-merge follow-up story を推奨する旨）を追記して明文化した。
     ファイル: `game/Assets/Scripts/Ui/TowerActionPanel.cs`。
  2. **[minor] 対応済み** — `HudPanel.RefreshDisplays()` の desync 分岐（`TryFindTowerById` が false を
     返すケース）を、クリック経路（`HandleActionPanelOpenInput` の `TowerNotFound` 分岐）と同じ
     `Debug.LogError` + `towerActionPanel.Close()` に変更。無言 Refresh スキップから表面化する形に揃えた。
     ファイル: `game/Assets/Scripts/Ui/HudPanel.cs`。
  3. **[minor] 対応済み** — `TowerActionPanelPlayModeTests.cs` に
     `InsufficientFundsTower_UpgradeButtonDisabled_ClickDoesNotUpgrade` を追加。設置直後に
     `EconomySystem.TrySpend` で資金を `UpgradeLv2Cost` 未満まで減らし、`IsUpgradeAvailable==false` と
     ラベルの「資金不足」表記を確認したうえで強化ボタン位置をクリックし、`TryHandleClick` の
     `upgradeAvailable` ガードにより `TryUpgradeTower` に到達しない（Lv/資金とも不変・パネルは開いた
     まま）ことを検証する。`GameHudPlayModeTests.InsufficientFundsSpot_BothButtonsDisabled_ClickDoesNotPlace`
     と対称のパターン。ファイル: `game/Assets/Tests/PlayMode/TowerActionPanelPlayModeTests.cs`。
  4. **[minor/infra] 見送り（本 iteration では対応不能）** — signing agent 障害はコード側で解消できない。
     本 fix コミットの実行結果（成立可否とハッシュ）を本追記後に確認し、成立すればそのハッシュを次の
     報告に含める。
- 静的検証: Unity は起動していない（並走レーン規律 — バッチ検証区間に委ねる）。以下を Read/Grep で確認済み:
  - `HudPanel.cs` の新設 else 分岐が `TowerInstance openTower` のスコープを崩さないこと（`if/else` の
    `TryFindTowerById` 呼び出しはこの分岐内で完結し、以降のメソッドで `openTower` を参照しないことを確認）
  - `towerActionPanel.Close()` のシグネチャ（引数無し・`IsOpen`/`OpenTowerId`/`SetActive(false)` を設定）が
    既存呼び出し（`HandleActionPanelOpenInput`）と同一であることを確認
  - `EconomySystem.TrySpend(int)` の実在（`public bool TrySpend(int amount)`）と
    `BuildSpotController.Economy`（`public EconomySystem Economy`）経由の呼び出し可能性を確認
  - `GameConfig.BastionCannon.Cost`(50)/`UpgradeLv2Cost`(40)/`GameConfig.Economy.StartingGold`(100) の
    実値を確認し、新規テストの資金不足前提（100-50=50 → `TrySpend(25)` で残高25 < 40）が成立することを
    算術確認（テストコード自体は `build.Economy.Gold` の実行時値を参照するのみで、これらの定数を直接
    参照しないためコンパイルへの影響は無い）
  - `hud.TowerAction.UpgradeLabel`（既存 public プロパティ）・`UpgradeButtonRect`（既存 public プロパティ）
    の実在を確認し、新規テストの参照が既存 API のみで完結することを確認
  - `TowerActionPanel`/`HudPanel` を参照する他ファイルを grep し、他レーンからの直接依存が無いことを再確認
