# S-23 レビュー履歴 — ダッシュ紙一重回避のカメラシェイク演出

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-13T11:24:46Z（fix着手時点で記録。判定自体は着手前に受領）
- 指摘要約（優先度順）:
  1. **[minor] HealthComponent.cs L118-125**: `ArenaCameraRig.Instance` 欠落時の縮退が `Debug.LogWarning` のみで、tech-stack-unity.md 規約12「配線破損は Start で1回 LogError」に違反。`Editor/SceneWiring.WireGame`（L210-212）は ArenaCameraRig を Main Camera へ無条件注入するため、欠落は規約12が LogWarning を許す「縮退が正当なケース」ではなく配線破損クラス。検知位置もトリガ時（敵接触ループ内・ダッシュ窓ごとに再発火）で「Start で1回」に不一致。配線が壊れると S-23 演出全体が QA-PLAY のエラー0ゲート（`LogAssert.NoUnexpectedReceived`）にも Unity Test Framework の LogError 自動失敗にも引っかからず無音で消える。
  2. **[minor] CameraShakeSceneTests.cs**: acceptance の「ArenaCameraRig の固定注視方向は変えず」がどのテストでも assert されていない。実装は `localPosition` のみ書込で現状正しいが、rotation 不変を固定する assertion が無いため将来の回転シェイク混入を検知できない。
- 対応:
  1. **対応済み** — `HealthComponent.Start()` に `ArenaCameraRig.Instance` の1回検査を追加し `Debug.LogError("[Wiring] ...")` を出すよう変更。`ProcessContacts` 側の毎トリガー `Debug.LogWarning` は削除し、実行時の null ガード（シェイクをスキップして継続）のみ残した（ヘッダコメントに規約12 適用の理由を明記）。
  2. **対応済み** — `ContactDuringDashInvulnerability_TriggersSingleShake_ThenRevertsExactlyAfterDuration` に `baseRot = rig.transform.rotation` を採取し、(a) シェイク観測直後 (b) 復帰後 の2箇所で `Assert.AreEqual(baseRot, rig.transform.rotation, ...)` を追加。`ArenaCameraRig` はシェイク中も `Start()`/`ApplyFixedPose` でのみ rotation を設定し `Update()` は `localPosition` のみ書き換えるため、既存実装のまま assertion のみ強化で固定できることを確認済み。

## 検証結果（fix後）
- EditMode: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results.xml -logFile -` → exit 0, 160/160 pass
- Build相当: `"$UNITY" -batchmode -projectPath game -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit -logFile game/Logs/build.log` → exit 0, ログに `Build succeeded`, `game/Build/ForgeGame.app` 生成確認
- PlayMode: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform PlayMode -testResults qa/evidence/playmode-results.xml -logFile ...` → 1回目 109件中 CrystalSceneTests 1件failed（フル suite 内でのタイミング起因のflake。S-23/HealthComponent とは無関係な既存テスト = S-09スコープ）。`-testFilter ForgeGame.Tests.PlayMode.CrystalSceneTests` で単体実行すると 3/3 pass、フル suite を再実行すると 109/109 pass, exit 0 で再現性なし。CameraShakeSceneTests は両実行とも 3/3 pass。
- 未解決事項: なし（S-23 スコープ内の finding は全て対応済み）。CrystalSceneTests のフル suite 内flakeは S-23 の変更差分に起因しないため本story の未解決事項としては扱わない（記録のみ）。

## 判断
- status: review → done（CR-CODE findings 2件とも対応済み、再検証 EditMode/Build/PlayMode 全て exit 0 かつ failed 0 を確認）。
