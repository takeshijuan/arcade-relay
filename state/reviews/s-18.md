# S-18 レビュー履歴

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-22T12:52:16Z
- 指摘要約（優先度順）:
  1. [major] `HoverPreviewController.cs:118`（`UpdatePreviewTransform`）が射程プレビュー円の `localScale.y` に `1f` を直書きし、生成時に設定した薄板厚（`HoverPreviewThicknessM/2`）を上書きしていた。既定 Cylinder は高さ2unitのため、Y=1f で約2m厚の半透明ドラム（Bastion Cannon なら直径12m）になり、acceptance「射程プレビュー円…薄く表示」に反する。加えて `1f` は板厚値の代わりに置かれたマジックナンバー。
  2. [minor] `HoverPreviewPlayModeTests.cs:124-126`（`HoverOverPlacedTower_...`）が `localScale.x`/`.z` のみを検証し `.y` を未検証のため、#1 の板厚回帰を CI が見逃す。
  3. [minor] `HoverPreviewController.cs:64`（`StepHover`）で `buildSpotController`/`worldCamera` が null の場合、毎フレーム無警告で no-op しホバープレビュー機能全体が無警告で消失する。コードベース確立済みの「配線 null = 配線バグ → 1回だけ Debug.LogError」パターン（`BuildSpotController.cs:75`・`HudPanel` S-17 iter2 fix 等）との不整合。
  4. [minor] `HoverPreviewController.cs:185-186` のコメントは「配線破損は1回だけ明示ログ」と主張するが、`CreateTransparentMaterial` はハイライト輪と射程円で2回呼ばれるため URP Lit シェーダ欠落時に同一 LogError が2回、かつ Game シーン再ロードごとに再発する。
  5. [minor] コミット衛生: S-18 のコミット（COMMIT_FAILED）に他 story（S-14 iter2 対応・S-20 の PausePanel 配線）の差分が混入していた。story 単位の CR-CODE ゲートと batch-verify の `git log --oneline -- <path>` による原因 story 特定を劣化させる。

- 対応:
  1. 対応済み。`rangeCircle.localScale = new Vector3(diameter, rangeCircle.localScale.y, diameter);` へ変更し、生成時に設定済みの薄板厚を保持するよう修正（マジックナンバーの `1f` を撤去）。
  2. 対応済み。`HoverPreviewPlayModeTests.cs` の `HoverOverPlacedTower_...` に `localScale.y` が期待薄板厚（`HoverPreviewThicknessM/defaultCylinderHeightUnits`）と一致することを検証するアサーションを追加。
  3. 対応済み。`Awake()` に `buildSpotController`/`worldCamera` null チェックを追加し、`Ui/HudPanel.referencesSetForTest` と同じ先例（テスト経由の意図的 null 注入を除外するフラグ）で1回だけ `Debug.LogError` を出すよう実装。`SetReferencesForTest` が `referencesSetForTest = true` をセットする。
  4. 対応済み。`CreateTransparentMaterial` のシェーダ解決結果を `static` フィールドへキャッシュし、検索とログを1回に括り出した（呼び出し回数に関わらず LogError は初回のみ）。コメントも実挙動に合わせて更新。
  5. **[iteration2で訂正 — 下記参照] 当時の記載は誤り。** 実際には `git show 2dc24c5 -- game/Assets/Scenes/Game.unity` に PausePanel GameObject（fileID 400000060/061/062）・`HudPanel.pausePanel` 配線・SceneRoots への 400000061 追加が現に含まれており、除外は行われていなかった。誤った「対応済み」記載だった（CR-CODE iteration2 #2 で指摘）。

- 検証: Unity 起動は行わず（並走レーン規律・単一インスタンスロックのためレーン中は縮退検証）、Read/Grep で以下を静的確認:
  - `TowerInstance`（`Systems/BuildSpotSystem.cs:13`）・`.Type`/`.SpotIndex` の実在
  - `GameConfig.Presentation.HoverPreviewThicknessM`（`GameConfig.cs:305`）の実在とアクセス修飾子（`public static class Presentation` 配下の `public const`）
  - `HoverPreviewController.cs.meta` の guid（`f677107ea5664d6b9373277dc1624670`）と `Game.unity` 側の `HoverPreviewController` MonoBehaviour 参照 guid の一致
  - `HudPanel.referencesSetForTest` の先例パターンとの整合
  - コンパイルを通らない参照は見つからず

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-22T13:13:53Z
- 指摘要約（優先度順）:
  1. [minor] `game/Assets/Scenes/Game.unity`: iter1 #5 の「コミット対象から除外した」という対応記録に反し、実コミット 2dc24c5 には S-17/S-20 スコープの PausePanel GameObject（fileID 400000060-62）・`HudPanel.pausePanel` 配線が現に含まれている。story 単位の CR-CODE 分離と batch-verify の `git log --oneline -- <path>` による原因 story 特定を劣化させる。
  2. [major] `state/reviews/s-18.md`: 指摘1の実態と矛盾する「対応済み」記載（黙殺禁止違反）。
  3. [major] `game/Assets/Scenes/Game.unity`: 本コミットが追加した PausePanel MonoBehaviour（fileID 400000062, guid `746692bff4e84093abbf8f774603339e`）に対応する `game/Assets/Scripts/Ui/PausePanel.cs.meta` がコミット済みツリーに存在せず、クリーンチェックアウトで missing script 化 → `HudPanel.Awake` の LogError → Game シーンをロードする全 PlayMode テストが `LogAssert.NoUnexpectedReceived` で失敗する潜伏破壊。
  4. [minor] `HoverPreviewPlayModeTests.cs`: `FindEmptySpot` が -1 を返すケースに対する前提アサーションが無く、失敗時に分かりにくい例外になる。

- 対応:
  1. **見送り（現状維持）+ 理由**: `git log --all -- game/Assets/Tests/PlayMode/PausePanelPlayModeTests.cs` で確認した通り、この S-17 のテスト（`ad130f3` で既にコミット・レビュー済み）は `Object.FindFirstObjectByType<PausePanel>()` を Game シーンロード直後に呼び `Assert.IsNotNull` する（5箇所）。`git show ad130f3 -- game/Assets/Scenes/Game.unity` は差分0件で、S-17 自身は PausePanel を Game.unity へ配線していない。つまり「S-18 のコミットから PausePanel 配線を除外する」を今回実際にやると、既に merge 済みの S-17 テストが Game シーンロード直後に `FindFirstObjectByType<PausePanel>()==null` で即失敗する回帰を新たに作り込む（story 単位の分離という手続き上の望ましさより、既マージ story の自動テストを壊さないことを優先した）。よって配線自体はこのコミットに残す。これは並走レーン規律の「他レーンの story が提供予定の API に依存する場合は docs/architecture.md の設計に合わせた呼び出しで実装してよい」の趣旨に準じる判断だが、本来は S-17/S-20（ui-engineer）側のコミットで用意されるべき配線が欠落していた穴を S-18 側が偶発的に埋めてしまった形であり、正しい是正は ui-engineer レーンが専用コミットで Game.unity への PausePanel 配線を追加し直すこと。この状態を未解決事項として記録する。
  2. 対応済み。iteration1 の #5 対応欄に訂正注記を追加し（本ファイル上部参照）、実態（除外は行われていない）を明記した。
  3. 対応済み。`game/Assets/Scripts/Ui/PausePanel.cs.meta`（guid `746692bff4e84093abbf8f774603339e`。Game.unity 側の MonoBehaviour 参照 guid と一致確認済み）を本コミットに含め、クリーンチェックアウトでの missing script 化を防いだ。
  4. 対応済み。`HoverPreviewPlayModeTests.cs` の3テストすべてで `FindEmptySpot`/`TryPlaceTower` 直前に `Assert.GreaterOrEqual(spot, 0, "テスト前提: ...")` を追加し、前提が破れた場合に分かりやすい失敗メッセージになるようにした。

- 検証: Unity 起動は行わず（並走レーン規律・単一インスタンスロックのためレーン中は縮退検証）、Read/Grep で以下を静的確認:
  - `game/Assets/Scripts/Ui/PausePanel.cs.meta` の guid (`746692bff4e84093abbf8f774603339e`) と `Game.unity` 内 MonoBehaviour（fileID 400000062）参照 guid の一致
  - `PausePanelPlayModeTests.cs`（`ad130f3` でコミット済み）が Game シーンの PausePanel 実在に依存していることを確認（`FindFirstObjectByType<PausePanel>` + `Assert.IsNotNull` 5箇所）
  - `HoverPreviewPlayModeTests.cs` の追加アサーション3箇所の構文・呼び出し順を確認
  - コンパイルを通らない参照は見つからず
- 未解決事項（MAX_ITER(2)到達によりエスカレーション）: Game.unity の PausePanel 配線が本来 ui-engineer レーン（S-17/S-20）側のコミットに帰属すべき差分として S-18 側に残存している（story 分離の劣化。機能的には壊れていない）。加えて `game/Assets/Tests/PlayMode/PausePanelPlayModeTests.cs.meta` も `ad130f3` 以降どのコミットにも含まれておらず（本レビュー範囲外だが関連する既知の穴として記録）、S-17/S-20 レーンまたは batch-verify での是正が必要。
