## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-13T11:00:00Z
- 指摘要約（優先度順）:
  1. major — `game/Assets/Scripts/Editor/AssetIntegration.cs`: acceptance の失敗パス「Avatar 生成失敗時 Generic 縮退+MANIFEST 注記＋未解決事項として報告」のうち MANIFEST 注記が未実装。`ConfigureHeroModel` の Generic 縮退分岐（L136-144）はログのみで `game/_generated/MANIFEST.jsonl` への追記コードが存在しない。
  2. major — `state/active.md`: S-18 エントリが「AssetIntegrationSceneTestsの4本＝…Generic縮退時MANIFEST注記の分岐、すべてPASS」と虚偽のテストカバレッジを報告していた（実際の4本にその検証は含まれない）。
  3. minor — `game/Assets/Scripts/Editor/AssetIntegration.cs`: 上記major#1と同一観点の重複指摘（latent/untriggered である旨の補足）。
  4. minor — `game/Assets/Scripts/Editor/AssetIntegration.cs`: `BuildHeroController` 内の遷移ブレンド時間（idleToRun/runToIdle duration=0.1f、attackToIdle exitTime=1f/duration=0.1f）がハードコードでGameConfig未集約。
  5. minor — `state/active.md`: `CrystalSceneTests` の timing-flaky が「未解決事項」行にもどの story にも紐付けられていない。
- 対応:
  1. 対応済み — `AssetIntegration.cs` に `BuildManifestDegradationNoteLine`/`AppendManifestDegradationNote`（既存の revision エントリ慣例＝ファイル本体無変更・メタデータのみ追記に倣う）を追加し、`ConfigureHeroModel` の Generic 縮退分岐から `game/_generated/MANIFEST.jsonl` へ注記行（`asset_id":"MDL-01"`, `note":"integration_degradation"`, `revision_of_sha256`, `revision_reason`, `story":"S-18"`, `generated_at`）を追記するよう配線。追加テスト `Tests/EditMode/AssetIntegrationManifestNoteTests.cs`（4本: JSON整形/エスケープ/既存行を破壊せず追記/親ディレクトリ自動作成）。**方式についての開示**: acceptance文言は「PlayMode テストで…検証する」だが、この分岐は Editor 専用（ModelImporter/AssetDatabase）ロジックで `ForgeGame.PlayMode.Tests` asmdef は `ForgeGame.Editor` を参照しないため PlayMode からは到達不能（`Tests/EditMode/AudioMixerSetupTests.cs` に既存の同種の EditMode/PlayMode 分割理由と同じ）。加えて実際の hero Avatar 生成は現状常に成功しており（縮退は未発火のlatentパス）、FBXを壊さず縮退を実PlayModeで強制することもできない。よってビルダー関数を直接検証するEditModeテストで代替した（黙殺ではなく方式変更・理由をここと state/active.md に明記）。
  2. 対応済み — `state/active.md` の S-18 エントリを訂正し、実際の4テスト名（HeroVisual_HasValidHumanoidAnimatorAndNoMaterialErrors / HeroVisual_BoundingBoxWithinArtBibleHeightRange / SfxLibrary_HasAllGeneratedClipsAssigned / SwarmerPrefab_HasNoMaterialErrorsWhenSpawned）を明記。虚偽記載だった旨と本iterationでの追加対応（EditModeテスト4本）への参照を追記。
  3. 対応済み（#1と同一修正で解消）。
  4. 対応済み — `GameConfig.Animation` に `IdleRunBlendDurationS`（0.1f）/`AttackExitTime`（1f）/`AttackToIdleBlendDurationS`（0.1f）を追加し、`BuildHeroController` から参照するよう変更。値は既存ハードコード値と同一のため、既に生成済みのHero.controller資産の挙動に変化なし。
  5. 対応済み — `state/active.md` の「未解決事項」行に `CrystalSceneTests.KillingEnemyWithinPickupRadius_...`（S-09所属）の timing-flaky を追記し、S-09/QA-PLAY再発時のフォローアップ経路を明示。
- 検証: EditMode 94 passed/0 failed（exit0、新規 `AssetIntegrationManifestNoteTests` 4本含む）、PlayMode 82 passed/0 failed（exit0、退行なし）、`ForgeBuild.BuildMac` exit0（`Build succeeded`）。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-13T02:31:00Z
- 指摘要約（優先度順・全4件 minor）:
  1. minor — `game/Assets/Scripts/Editor/AssetIntegration.cs`: 冪等性違反（縮退パス）。`ConfigureHeroModel` の Generic 縮退分岐が、IntegrateAll 再実行のたびに MANIFEST.jsonl へ無条件で `integration_degradation` 行を追記し、timestamp 以外同一の重複注記が append-only 台帳に恒久的に蓄積する。追記前に同一 `asset_id`+`note=integration_degradation`+`revision_of_sha256` の既存行チェック（dedupe）を入れるべき。
  2. minor — `game/Assets/Scripts/Editor/AssetIntegration.cs`: `BuildManifestDegradationNoteLine` の日時整形（`ToString("yyyy-MM-ddTHH:mm:ssZ")`）が `CultureInfo.InvariantCulture` 非明示で非グレゴリオ暦カルチャ環境では `generated_at` の年が誤記録されうる。また `DateTime` 引数の `Kind` を検証せず `Z` を付与するため、将来ローカル時刻が渡されると UTC を偽る。
  3. minor — `game/Assets/Scripts/Editor/AssetIntegration.cs`: `EscapeJson` が CR/LF/引用符/バックスラッシュ以外の制御文字（タブ・0x00-0x1F）をエスケープせず、混入時に厳密な JSON パーサ（jq/python json）が台帳全体の読み込みに失敗しうる。
  4. minor — `game/Assets/Scripts/Editor/AssetIntegration.cs`: 縮退分岐の配線自体（`rawModelRelative` のメソッド内リテラル・sha256計算・`AppendManifestDegradationNote` 呼び出し配線）が EditMode テスト未カバーで、既存テストは `BuildManifestDegradationNoteLine`/`AppendManifestDegradationNote` の2ヘルパーのみを検証している。
- 対応:
  1. 対応済み — `ManifestHasDegradationNote(manifestPath, assetId, fileSha256)` を追加し、`AppendManifestDegradationNote` の冒頭でこれを呼んで同一 `asset_id`+`note=integration_degradation`+`revision_of_sha256` の既存行があれば追記をスキップ（ログのみ）するよう変更。既存ファイルの書き換え・削除は行わない（追記専用の規約を維持）。新規テスト `AppendManifestDegradationNote_SkipsWhenIdenticalNoteAlreadyExists`（同一sha再実行で1行のまま）/ `AppendManifestDegradationNote_AppendsAgainWhenShaDiffers`（sha変化時は新規追記される＝真の再生成は握り潰さない）/ `ManifestHasDegradationNote_FalseWhenManifestFileDoesNotExist` を追加。
  2. 対応済み — `BuildManifestDegradationNoteLine` で `generatedAtUtc.Kind` を判定して正規化（`Utc`→そのまま、`Local`→`ToUniversalTime()`、`Unspecified`→`DateTime.SpecifyKind(..., Utc)` で明示化）した上で `ToString("yyyy-MM-ddTHH:mm:ssZ", CultureInfo.InvariantCulture)` を使用。新規テスト `BuildManifestDegradationNoteLine_FormatsUtcTimestampWithInvariantCulture`（非グレゴリオ暦カルチャ`ar-SA`をスレッドに設定して検証）/ `BuildManifestDegradationNoteLine_NormalizesUnspecifiedAndLocalKindToUtc`。
  3. 対応済み — `EscapeJson` を文字単位ループに書き換え、`\\`/`"`/`\r`（既存どおり除去）/`\n`/`\t` に加え 0x20 未満の全制御文字を `\uXXXX` で一括エスケープするよう変更。新規テスト `BuildManifestDegradationNoteLine_EscapesControlCharactersInReason`（タブ＋0x01制御バイトを含む reason で検証）。
  4. 対応済み — `ConfigureHeroModel` 内のローカル変数直書きだった `rawModelRelative` をファイル先頭 `private const string HeroRawModelRelative` に集約（他パス定数の慣例と統一）し、パス組み立て＋sha256計算＋`AppendManifestDegradationNote` 呼び出しの配線一式を `public static void RecordHeroAvatarDegradation(string manifestPath, string rawModelFullPath)` として抽出。`ConfigureHeroModel` はこれを呼ぶだけに変更。新規テスト `RecordHeroAvatarDegradation_AppendsNoteWithComputedShaWhenRawFileExists` / `RecordHeroAvatarDegradation_UsesSentinelShaWhenRawFileMissing`（sha計算・`unknown-source-file-missing`センチネル分岐を含む配線ごと検証）。なお acceptance の「PlayMode テストで検証」から EditMode への方式変更自体は iter1 で正当理由を明記済み・iter2 レビューでも指摘対象外と確認済み。
- 検証: EditMode 102 passed/0 failed（exit0、新規8テスト含む — dedupe×2/ManifestHasDegradationNote×1/culture・Kind正規化×2/control-char escape×1/RecordHeroAvatarDegradation配線×2、旧94本+8本=102）、PlayMode 82 passed/0 failed（exit0、退行なし）、`ForgeBuild.BuildMac` exit0（`Build succeeded`）。
- 未対応の指摘: なし（4件全て対応済み）。CR-CODE MAX_ITER（2）到達のため review-loops.md に従い state/stories.yaml の S-18 を status: done に更新する（review-loops.md「MAX_ITER 到達かつ非APPROVE: エスカレーション（未解決指摘一覧を付けて次のCheckpointで人間に提示。パイプラインは止めない）」。ただし本iterationで4件は全て解消済みのため、エスカレーションとして持ち越す未対応事項はない）。
