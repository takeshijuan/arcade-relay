# S-11 レビュー履歴

対象コミット（iteration 1）: `0992c9a`（S-11実装本体: BuildSpotController/BuildSpotSystem/EconomySystem/TowerUpgradeSystem + EditMode/PlayMode テスト、261行追加）。
`8876243e` は stories.yaml の status 変更（in-progress→review）1行のみで実装 diff を含まないため、レビューはこちらではなく `0992c9a` を対象に実施した。

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-22T10:29:54Z
- 指摘要約（優先度順）:
  1. [major] `BuildSpotController.TrySellTower` (L180-183): 売却成功 (`result.Success`) なのに `towerViews.TryGetValue` が false、または view が破棄済みの場合、View 破棄を無言でスキップして成功を返していた。同ファイルの撃破経路（`StepSimulation` 内）は同種の view/state desync を `Debug.LogError` で明示的に表面化させており非対称。現状は `TryPlaceTower` が設置時に必ず View を生成するため到達不能だが、将来 `BuildSpotSystem` への直挿入経路が増えると孤児 View/desync が無言化される。
  2. [minor] 上記と同一箇所の重複指摘（silent-failure パターンとしての一般化）。
  3. [minor] `EconomySystem.ComputeSellRefund` (L53-58) の `Mathf.RoundToInt` は banker's rounding（.5 は偶数側へ丸め）で、acceptance の「round(N×0.5)」（慣用的には half-up）と .5 タイで乖離し得る。現行 GameConfig 値ではたまたま half-up と同結果だが、S-14 の UPG-02 割引適用やチューニングレンジ内の変更で偶数床のタイに到達すると 1G 少なく返還される可能性がある。丸め方式の決定をコメントに明記すれば足りる、との指摘。
  4. [minor] レビュー対象コミット参照の記録: 指定 SHA `8876243e` は実装 diff を含まないため、実装本体コミット `0992c9a` を対象にレビューした旨を記録すること。

- 対応:
  1. 対応済み。`BuildSpotController.TrySellTower` に else 分岐を追加し、`towerViews.TryGetValue` 失敗時・view 破棄済み検出時の両方で `Debug.LogError("[BuildSpotController] sold tower {towerId} had no view; view/state desync")`（および破棄済みケース用の専用メッセージ）を出すよう修正。撃破経路（`StepSimulation` 内 `TryRemoveEnemyView` 失敗ログ）と同型のパターンに統一した。
  2. 対応済み（1と同一修正で解消）。
  3. 対応済み（見送りではなく明記で対応）。`EconomySystem.ComputeSellRefund` の doc comment に、採用している丸め方式（`Mathf.RoundToInt` = banker's rounding。.5 は偶数側）を意図的な既定として明記し、half-up が必要になった場合の変更方法（`Math.Round(x, MidpointRounding.AwayFromZero)`）も併記した。現行 GameConfig 値・既存テストとの整合は崩れないため挙動自体は変更していない。
  4. 対応済み。本ファイル冒頭に対象コミットの記録を追記した（`0992c9a`）。

- 検証: Unity は起動していない（単一インスタンスロック — 並走レーン・資産レーンと衝突するため。バッチ検証区間で EditMode/ビルド検証を一括実行）。代わりに Read/Grep で静的確認:
  - `BuildSpotController.cs`: `Debug.LogError` は既存箇所（`StepSimulation` L44, L115）で同名パターン使用済み・`UnityEngine` は既に import 済み。`Destroy(GameObject)` は既存コードで使用済みのため参照エラーなし。修正箇所で新規に参照する型・メンバ（`TowerView.gameObject`）は既存コードと同一の使い方。
  - `EconomySystem.cs`: doc comment のみの変更でロジック・シグネチャ・参照に変更なし。コンパイルへの影響なし。
  - `TrySellTower` を呼ぶ既存テスト（`TowerCombatPlayModeTests.cs` L180, L206 / `BuildSpotCombatSystemTests.cs`）は全て `TryPlaceTower` 経由で View 生成済みのタワーを売却するため、新設した else 分岐（View 未登録/破棄済み検出ログ）には到達せず、既存テストの期待値・挙動に影響しないことを確認した。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-22T10:38:25Z
- 指摘要約: findings 0件。
- 対応: 対応不要（APPROVE）。status を done に更新。
