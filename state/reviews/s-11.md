# state/reviews/s-11.md — S-11 "Result シーン（最終スコア・ハイスコア更新・リスタート/メニュー導線）"

## CR-CODE iteration 1 — APPROVE
- 日時: 2026-07-10T10:58:08Z
- 対象コミット: ec9e7f96579f1070674f1f6eee266141099410d2
- 指摘要約: findings 0件。
  - マジックナンバー: フォントサイズ/色/文言/アンカーY/サイズはすべて `GameConfig.Ui.Result*` の名前付き定数に集約。本文に残る `new Vector2(0.5f, …)`（アンカーX中央）・pivot `(0.5f,0.5f)`・`Vector2.zero/one` は構造的な中央寄せ定数で、既存 `Ui/TitleScreen`・`Ui/MenuScreen` と同一の許容パターン（意味を持たない構造値）。新規違反なし。
  - delta-time: `ResultController.Update` は `_input.Submit/Cancel.WasPressedThisFrame()` のエッジ判定のみでフレーム依存の累積加算が無く規約非該当。`ResultScreen` は `Update` を持たない。
  - 入力抽象化: 旧 `Input.GetKey` 不使用。`InputLayer.GameInput` ファサード（UI マップ Submit=Enter/Space・Cancel=Esc）経由で `TitleController`/`MenuController` と同一の集約パターン。`Awake` 生成→`EnableUi`→`OnDestroy` で `Dispose`。
  - エンジン非依存コア線引き: ハイスコア判定は `Systems/Meta/MetaProgression.IsNewHighScore`（pure C# static、`run.FinalScore > current.highScore`）へ委譲。`Components/ResultController`（配線）・`Ui/ResultScreen`（表示専任）は算術を再実装せず値を転送するだけで層責務を維持。二重計算回避のため `highScoreUpdated` は pre-run `currentSave` 基準で `HealthComponent` が1回だけ計算し、`SessionHolder.SetLastRunResult` で受け渡す設計を確認（`ApplyRunResult` が highScore を破壊する前に評価）。
  - rule 14（Canvas は ScreenSpaceCamera + worldCamera 固定）準拠。`Camera.main` 欠落時は LogError ガードあり。Result.unity には MainCamera タグ付きカメラが存在し `worldCamera` は解決する。
  - 黙殺なし: `SessionHolder`/`LastRunResult` 欠落は zeroed 表示へ縮退しつつ `Debug.LogWarning` 1回、sibling `ResultScreen` 欠落・色パース失敗は `Debug.LogError`。Submit/Cancel の遷移は `_screen` 非依存に保たれ、配線破損時も脱出経路が残る（`MenuController` と同一方針）。
  - コンパイル整合: テストが参照する公開メンバ（`HealthComponent.SurvivalTimeSec`/`CurrentHp`/`SaveDirectoryOverrideForTests`/`SaveInvocationCountForTests`、`RunStatsTracker.CurrentScore`、`WaveSpawner.CurrentWave`、`EnemyAgent.ActiveEnemies`/`Initialize`、`SessionHolder.EnsureCreated`、`SaveData.highScore`/`CreateDefault`、`ResultScreen.*Text`/`HighScoreNoticeObject`/`Canvas`）はすべて実在。
  - acceptance 充足: EditMode 1（`IsNewHighScore` の境界=同点は非更新）+ PlayMode 9（最終スコア/生存時間/到達ウェーブ表示、ハイスコア通知の表示/非表示、Enter・Space→Game、Esc→Menu、renderMode==ScreenSpaceCamera+worldCamera、リスタートで中心/HP全回復/タイマー/ウェーブ/敵/スコアのリセット、Title→Menu→Game→Result→Menu の1周）が実配線シーン `Result.unity` を InputTestFixture で駆動して検証。リスタートは `LoadScene(Game)` によるシーン再構築でリセットを担保し、テストで実測確認。
- 対応: 不要（APPROVE）。
