# S-04 レビュー履歴 — 経路・ウェーブスポーン・敵移動・コア防衛/敗北

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-21T09:46:14Z
- 対象コミット: 7ae19e1df8966703ed356dad29092f01df908cd8
- reviewer: pr-review-toolkit:code-reviewer（wave3-code-reviewer）
- 総評: レイヤ分離（Systems=純粋C#／Components=薄い配線）・delta-time 駆動・マジックナンバー集約（GameConfig）・silent-failure 回避（LogError 1回・降格なし）は良好。acceptance（Marauder 等速前進→ゴール到達→コアHP減算→HP0で敗北発火・NaN無し）は WaveSpawnSystemTests / CoreDefensePlayModeTests で実質カバーされている。以下2件は修正可能な指摘（CONCERNS）。
- 指摘要約（優先度順）:
  1. [中] ランタイム Material リーク — `PlaceholderFactory.CreateMaterial` が敵/コア1体ごとに `new Material(shader)` を生成し `sharedMaterial` に割り当てるが、`WaveSpawnController.StepSimulation` の `Destroy(view.gameObject)`（ゴール到達時）では割り当てた Material は破棄されない。Unity ではランタイム生成 Material は GameObject/Renderer 破棄で自動解放されず明示 `Object.Destroy` が必要。1ラン74体超の Marauder ＋リスタート毎に累積し漏れる。修正: 色ごとに1枚を static dict でキャッシュ再利用する（推奨・確保数を敵数から色数へ削減）か、View 側 `OnDestroy` で Material を明示破棄する。(PlaceholderFactory.cs:46 CreateMaterial / WaveSpawnController.cs 破棄箇所)
  2. [低] `CurrentWaveNumber` のコメントと実装の不一致 — コメントは「全ウェーブ消化後は Waves.Length+1（=9）を返し続ける」とするが、実装 `Math.Min(currentWaveIndex, Waves.Length - 1) + 1` は消化後も Waves.Length（=8）にクランプされ 9 を返さない。HUD/S-06 が「CurrentWaveNumber==Length+1 で完了検知」を期待すると恒久的に成立せず将来レーンのロジック不具合を招く。修正: コメントを実挙動（Length を返し続ける／完了検知は AllWavesSpawned を使う）に合わせる、または意図通り +1 を返すよう実装を直す。(WaveSpawnSystem.cs:55)
- 対応:
  1. [中] Materialリーク — 対応済み。`PlaceholderFactory` に `static Dictionary<Color, Material> materialCache` を追加し、色ごとに1枚だけ生成して再利用する `GetOrCreateMaterial` に差し替えた（推奨案の色数キャッシュを採用）。敵/コアのスポーン数に比例したアロケートが無くなり、リスタート毎の累積リークを解消。
  2. [低] `CurrentWaveNumber` コメント不一致 — 対応済み。コメントを実挙動（`Waves.Length` にクランプされたまま返す。Length+1 にはならない）に修正し、全消化判定には `AllWavesSpawned` を使うことを明記した。実装（クランプ）側は変更していない（既存の HUD 表示意図＝最終ウェーブ番号で止まる、と整合するため）。
  3. [中] （silent-failure-hunter 指摘・上記2と同一箇所）`CurrentWaveNumber`/`AllWavesSpawned` の契約明文化 — 2と同一修正で対応済み。将来レーン（S-06/S-07）が参照すべき全消化判定APIは `AllWavesSpawned` である旨をXMLdocに明記した。
  4. [中低] （silent-failure-hunter 指摘）`CoreView.ResetForNewRun()` の部分リセット不整合 — 対応済み（選択肢a）。`ResetForNewRun()` を削除した。docs/architecture.md のゲームフロー表でリスタートは Result「もう一度」→ Game の `SceneManager.LoadScene`（シーン再生成）と定義されており、HPのみを戻す部分リセットAPIは `WaveSpawnController` 側（defeatEventFired ラッチ・ウェーブ消化状態・enemyViews）の対リセットが伴わない限り危険なため、部分リセットの可能性自体を除去した。呼び出し元・テストからの参照が無いことを確認済み。
  5. [低] （silent-failure-hunter 指摘）`PlaceholderFactory` のRenderer nullガードによるfailure-hiding — 対応済み。`if (renderer != null)` の無ログスキップ分岐を除去し、`go.GetComponent<Renderer>().sharedMaterial = ...` に直書きした（`GameObject.CreatePrimitive` は常にRendererを付与するため、欠落時はNREで明確に失敗する）。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-21T09:52:48Z
- 対象コミット: 69cd180bacc45f88e1457598a3aaecf7d4d4682f（"S-04: fix CR-CODE iter 1"）
- reviewer: pr-review-toolkit:code-reviewer（wave3-code-reviewer）
- 対象差分: CoreView.cs / PlaceholderFactory.cs / WaveSpawnSystem.cs（＋本レビュー履歴）。読み取り専用レビュー（並走レーン単一インスタンスロックのためエンジン未起動）。
- 総評: iteration 1 の指摘5件すべてが妥当かつ副作用なく解消されている。findings 0 件 → APPROVE。
  - Materialリーク（#1/#3）: 色キー `static Dictionary<Color,Material>` で色数（実質2色=EnemyColor/CoreColor）分のみアプリ寿命確保に変更。敵/コアのスポーン数比例アロケートを排除。`&& cached != null` により破棄済み Material（ドメイン再ロード無し PlayMode 等の擬似null）を自己修復再生成する防御も適切。旧 `CreateMaterial` への参照残存なし。
  - CurrentWaveNumber コメント不一致（#2）: 実装（`Math.Min(idx, Length-1)+1` のクランプ）は不変のまま、XMLdoc を実挙動（Length にクランプ・Length+1 は返さない・全消化判定は `AllWavesSpawned` を正とする）へ修正。将来レーン（S-06/S-07）への契約明文化として妥当（正当理由の明記）。
  - ResetForNewRun 部分リセット不整合（#4）: メソッド削除。リスタートはシーン再生成（`SceneManager.LoadScene`）へ一本化する設計と整合。ツリー全体で参照残存なし（Grep 確認済み・唯一の出現はコメント内）。
  - Renderer nullガードの failure-hiding（#5）: 到達不能な無ログスキップ分岐を除去し `GetComponent<Renderer>()` を直書き（`CreatePrimitive` は全 PrimitiveType で MeshRenderer を必ず付与するため欠落時は NRE で明示失敗）。
- 規約確認: マジックナンバー混入なし（差分の数値はコメント内のみ・`materialCache` は初期値のみ）。delta-time 非依存実装の混入なし（移動ロジック未変更）。silent-failure の新規混入なし（`GetOrCreateMaterial` の shader 欠落は既存の LogError 1回を維持）。Systems/Components のレイヤ分離維持。
- acceptance 確認: 等速前進（delta-time 駆動）→ゴール到達→`CoreDefenseSystem.ApplyDamage` でコアHP減算→HP0で `OnCoreDefeated` 1回発火・NaN無し、を `CoreDefensePlayModeTests`（迎撃なし全ウェーブ消化→CurrentHp==0・defeatFired・毎ステップ NaN 検査・`LogAssert.NoUnexpectedReceived`）＋ `WaveSpawnSystemTests` が実質カバー。本差分はこの実装/テストを退行させない。
- 指摘要約: なし（findings 0 件）。

## CR-CODE iteration 1（再ディスパッチ・独立再レビュー） — APPROVE
- 日時: 2026-07-22T02:19:01Z
- 対象コミット（固定）: aeeeeb1e4b807d68339ab461c0574ffdbe462a73（"S-04: status done"）
- 対象スナップショット: 上記コミット時点の S-04 実装一式（WaveSpawnSystem.cs / CoreDefenseSystem.cs / WaveSpawnController.cs / EnemyView.cs / CoreView.cs / PlaceholderFactory.cs / GameConfig.cs〔経路・ウェーブ構成・プレースホルダ〕/ WaveSpawnSystemTests.cs / CoreDefensePlayModeTests.cs）。読み取り専用レビュー（並走レーン単一インスタンスロックのためエンジン未起動・ビルド/テスト未実行）。
- 補足: 固定コミット aeeeeb1e の diff 自体は stories.yaml の status review→done のみ（コード変更なし）。CR-CODE の趣旨に沿い、同スナップショットにおける S-04 コード本体を独立に精査した（作業ツリーの未コミット変更は対象外）。
- 総評: findings 0 件 → APPROVE。iter1/iter2 の指摘（Material リーク・CurrentWaveNumber コメント不一致・ResetForNewRun 部分リセット・Renderer null ガードの failure-hiding）は解消済みで退行なし。独立精査でも新規の ≥80 確度指摘は無し。
- 規約確認（rules/unity-code.md）:
  - レイヤ分離: `Systems/`（WaveSpawnSystem・CoreDefenseSystem）は MonoBehaviour 非継承・シーン API 未使用の純粋 C#。UnityEngine 参照は Vector3/Mathf/Color の値型・数学型のみ（規約許容範囲）。判定/移動/状態遷移は Systems、生成・Transform 反映・破棄配線は Components に分離。EnemyType 等の共有型は `Types.cs`（純粋値型）。
  - マジックナンバー: 速度/HP/経路長/ダメージ/色/サイズは全て `GameConfig` に集約。本文の数値リテラルは 0/1 初期値・index・0 比較のみ。`PlaceholderFactory.DefaultPrimitiveHeightUnits(2f)` は Unity 既定プリミティブのメッシュ不変値として命名定数化＋根拠コメント付き（ゲームパラメータでない旨明記）で妥当。
  - delta-time: 敵移動 `DistanceTraveledM += speedMps * deltaTime`、スポーン/準備タイマーの deltaTime 累積、EnemyView のボブ位相 `bobPhase += Freq * deltaTime` すべて delta-time 駆動。フレーム固定加算（60fps 前提）なし。`Time.deltaTime` は Components(Update) から Systems へ注入。
  - silent-failure: LogError の Warning 降格なし。`WaveSpawnController.Start` の CoreView 未配線／`PlaceholderFactory.GetOrCreateMaterial` の URP Lit 未検出はいずれも回復不能条件を LogError 1回で明示（新規の握り潰し・無ログスキップの混入なし）。
- acceptance 確認（この diff で充足）:
  - 固定一本道・等速前進・進路不変: 経路は `GameConfig.Path.StartPoint→EndPoint`（PathLengthM=40m）の固定直線で外部要因により変化しない。`GetPathPosition` は Clamp01+Lerp で NaN 非発生（EditMode `GetPathPosition_IsLinearAndNaNFree_AcrossFullRange` が全域検証）。等速性・フレームレート非依存は `Tick_MovesEnemy_ByDeltaTimeAndSpeed_NotFrameRateDependent`（Δ距離==SpeedMps×dt）で検証。
  - ゴール到達→コアHP減算（MARAUDER_CORE_DAMAGE）: `AdvanceMovement` が距離>=PathLengthM で goalEvent 発火＋Active=false、`CoreView.ApplyDamage`→`CoreDefenseSystem.ApplyDamage` が種別別減算（0 下限クランプ）。EditMode `ApplyDamage_Marauder_SubtractsMarauderCoreDamage` 他で検証。
  - HP<=0 で敗北発火: `FireDefeatEventOnce`（ラッチで1回）。PlayMode `UnopposedWaves_DepleteCoreHp_ToExactlyZero_WithNoNaNPositions_AndDefeatEventFires` が迎撃なし全ウェーブ消化→CurrentHp==0・defeatFired・毎ステップ全 Transform の NaN 検査・`LogAssert.NoUnexpectedReceived()` を検証。
  - 時間予算の妥当性（再計算）: 全敵の合計コアダメージ = 74×1 + 27×3 = 155 > CORE_HP_MAX(100)。累積は W7 中に 100 到達（W6 まで累積87／W7 で117）。よってテストの `maxSimulatedSeconds=400f` 内で確実に敗北成立し `Assert.Less(simulated, max)` は満たされる。ApplyDamage の 0 クランプにより敗北時 `CurrentHp==0` が保証される。
- 指摘要約: なし（findings 0 件）。
