# S-04 レビュー履歴 — プレイヤー移動 + 固定俯瞰カメラ + アリーナ境界

## CR-CODE iteration 1 — APPROVE
- 日時: 2026-07-10T07:36:23Z
- 対象コミット: 64e6669fcdbbe37fdba53f1052f6901ffa283f42
- レビュアー: pr-review-toolkit:code-reviewer（wave3-code-reviewer）
- 判定: APPROVE（findings 0件。重大バグ・設計欠陥・rule 違反なし・acceptance 充足）
- 指摘要約: なし（未解決指摘なし）
- 検証内容（今回 diff = `Systems/PlayerMovement.cs` / `Systems/ArenaCameraMath.cs` / `Components/PlayerController.cs` / `Components/ArenaCameraRig.cs` / `Editor/SceneWiring.cs` / `Scenes/Game.unity` + 各 .meta / EditMode・PlayMode テスト / state）:
  1. マジックナンバー（rules/unity-code.md「ゲームパラメータは GameConfig.cs へ集約」）非抵触を確認: カメラ姿勢は `GameConfig.Camera.PitchDeg/Height/Fov`、移動速度は `GameConfig.Player.MoveSpeed`（フォールバック時）と `MetaProgression.EffectiveMoveSpeed`、境界は `GameConfig.Player.ArenaRadius`、プレースホルダ色は `GameConfig.Ui.ColorTextSecondary` に集約済み。本文に残る数値は `PlayerMovement` の単位長しきい値 `1f`（sqrMagnitude 比較＝構造的しきい値）・`ArenaCameraMath` の Euler yaw/roll `0f`・XZ 平面固定の `0f`・`ArenaCenter = Vector3.zero`（gdd「origin-centered arena」の定義値でコメント明記）のみ。いずれも意味を持たない/構造的な値で免除範囲。チューニング値の本文直書きなし。
  2. delta-time（同「移動は Time.deltaTime でスケール」）適合を確認: `PlayerController.Update()` は `PlayerMovement.ComputeDisplacement(inputVector, _moveSpeed, Time.deltaTime)` へ `Time.deltaTime` を渡し、変位を `moveSpeed * deltaTime` でスケール。フレーム毎固定加算・60fps 前提なし。移動はキネマティック（transform 直接更新）で物理非使用のため FixedUpdate 不要。`ArenaCameraRig` は Start で1回姿勢設定のみ（毎フレーム時間加算なし）＝対象外。
  3. エンジン非依存コア（同「Systems で MonoBehaviour 継承・シーン API 禁止」）適合を確認: `Systems/PlayerMovement`・`Systems/ArenaCameraMath` はともに `UnityEngine` の値型（Vector2/Vector3/Quaternion/Mathf）のみ import する純粋 static クラス。MonoBehaviour 継承・`GameObject.Find`/`Instantiate`/`GetComponent`・File I/O なし。配線は `Components/PlayerController`（GameInput 生成・transform 反映のみ、演算は PlayerMovement へ委譲＝薄い）と `Components/ArenaCameraRig`（GetComponent<Camera> + ArenaCameraMath 委譲のみ）に分離。層違反なし。
  4. 入力抽象化（同「入力は Input System を1モジュールに集約・Input.GetKey 禁止」）適合を確認: 入力は `InputLayer/GameInput` facade 経由（`new GameInput()` → `EnableGameplay()` → `Move.ReadValue<Vector2>()`）。`Input.GetKey`/`Input.GetAxis` 不使用。`GameInput` は `.inputactions` 資産でなくコードでアクション生成（rule 4 準拠）。`OnDestroy` で `_input?.Dispose()` によりアクションマップを破棄しリーク回避。
  5. Renderer 参照（tech-stack-unity 規約10「RequireComponent(Renderer) 禁止」）非抵触を確認: `ArenaCameraRig` の `[RequireComponent(typeof(Camera))]` は具象型 Camera で規約10（抽象型 Renderer 禁止）に非該当＝正当。`ApplyPlaceholderColor` は `GetComponent<Renderer>()` + null チェックで参照（規約10 の推奨形）。プレースホルダマテリアルは `Shader.Find("Universal Render Pipeline/Lit") ?? Shader.Find("Standard")` で URP 互換シェーダを明示選択し、既定マテリアルの InternalErrorShader（ピンク）フォールバックを回避。
  6. 参照整合・コンパイル整合を確認: `GameConfig.Camera.PitchDeg/Height/Fov`・`GameConfig.Player.MoveSpeed/ArenaRadius`・`GameConfig.Scenes.Game`・`GameConfig.Ui.ColorTextSecondary`・`SessionHolder.Instance/.Save`・`SaveData.upgradeMoveSpeedLevel`・`MetaProgression.EffectiveMoveSpeed(int)`・`ForgeGame.InputLayer.GameInput`（Move/EnableGameplay/Dispose）すべて実在。`SceneWiring.WireGame` は既承認 S-02/S-03 の try/catch + Fail(exit 1 昇格) 方式を踏襲し、Main Camera 欠落は `Fail` で非0終了（rule 11 準拠）、SaveScene 失敗も `Fail`。
  7. ArenaCameraMath 数式の正当性を確認: `rotation = Quaternion.Euler(pitchDeg,0,0)` により forward = (0, -sin(pitch), cos(pitch))、`distance = height / sin(pitch)`、`position = lookTarget - forward*distance` で position.y = lookTarget.y + height（pitch 非依存で常に height 上空）。pitch=90 で真上（EditMode `ComputeFixedPose_PitchZero_LooksHorizontally` が forward.y=-1・position=(0,10,0) を検証）、pitch=65 で position.y=14（`SitsAtConfiguredHeightAboveTarget`）、forward が lookTarget 方向を Dot≈1 で指す（`RotationForwardPointsAtLookTarget` が Dot>0.99、`ChangingLookTarget_TranslatesPoseAccordingly` が並進追従）を検証。中心注視・追従なしの固定姿勢が数値的に成立。
  8. サイレント失敗ガード確認: `PlayerController.ResolveMoveSpeed` は SessionHolder/Save 欠落時に `Debug.LogWarning("[Wiring] ...")` を出したうえで base MoveSpeed(Lv0)＝`MetaProgression.EffectiveMoveSpeed(0)` 等価値へ縮退（ヘッダコメントで正当性明記＝黙殺なし）。`Awake` の重複インスタンスは `Debug.LogError` + Destroy。File I/O は本 diff に無くセーブ破損プロトコルは非該当。LogWarning は `LogAssert.NoUnexpectedReceived()`（Error/Assert/Exception のみ捕捉）の対象外で PlayMode green を落とさない。
  9. acceptance 充足を確認:
     - WASD/矢印を PlayerMovement(pure C#)で XZ 速度化: `ComputeDisplacement` が `inputVector`（x=左右, y=前後）を `new Vector3(x,0,y)*(moveSpeed*deltaTime)` へ変換。Y=0 で XZ 平面固定。EditMode `AxisAlignedInput_MovesAtExactlyMoveSpeed`/`ScalesWithDeltaTime`/`ZeroInput_ProducesNoMovement` が検証。
     - 斜めは正規化し PLAYER_MOVE_SPEED 超えず: `sqrMagnitude > 1f` で `normalized` にクランプ後スケール。GameInput の 2DVector 既定（DigitalNormalized）とあわせ二重防御。EditMode `DiagonalInput_IsNormalizedAndNeverExceedsMoveSpeed`（magnitude=6 を検証）。
     - ARENA_RADIUS 外へ出ない: `ClampToArena` が XZ 距離を `arenaRadius` にクランプ（Y 素通し）。EditMode `ClampToArena_*`（内側不変・外側クランプ・Y 保存・斜め方向保存）。
     - カメラは中心注視の固定(PITCH/HEIGHT/FOV)で追従なし: `ArenaCameraRig.Start` が ArenaCameraMath 姿勢 + `cam.fieldOfView = Fov` を1回設定、Update なし。
     - PlayMode 右入力で右移動: `RightInput_MovesPlayerToThePositiveXSide`（end.x>start.x・z 不変・NaN なし、InputTestFixture 経由＝rule 8 準拠）。
     - PlayMode 長時間でも半径内: `SustainedInput_NeverLeavesTheArenaRadius`（timeScale=50 で ≈25 秒相当連続入力後も flatDistance ≤ ArenaRadius）。毎フレームクランプのため単フレーム変位が幾ら大きくても半径内に収束。
     - PlayMode カメラ forward が中心方向(Dot>0.2): `ArenaCamera_ForwardPointsTowardArenaCenter`（Dot>0.2 + Fov 一致）、`ArenaCamera_DoesNotFollowThePlayer`（入力後もカメラ position/rotation 不変）。
  10. 既承認 S-02/S-03 とのパターン整合を確認: SceneWiring の try/catch+Fail(exit1)・GameInput facade 利用・Systems/Components 層分離・SessionHolder 縮退方針は既 APPROVE 構造を踏襲。新規リグレッションなし。
- 対応: 追加対応不要（APPROVE のため revise 無し）。次工程（QA-PLAY）へ進行可。
- 対応（追記: silent-failure-hunter 合算分・findings 3件。修正コミット "S-04: fix CR-CODE iter 1"）:
  1. [MEDIUM] `Editor/SceneWiring.cs` `ApplyPlaceholderColor()` の3早期returnが無言 → 対応済み。renderer==null / `ColorUtility.TryParseHtmlString` 失敗 / URP・Standard 両シェーダ null の各分岐に `Debug.LogWarning("[SceneWiring] placeholder color skipped: <理由>")` を追加。wiring 自体は継続（Fail() で止めない＝指摘どおり警告のみ）。
  2. [MEDIUM] `Shader.Find("Universal Render Pipeline/Lit") ?? Shader.Find("Standard")` の無言フォールバック → 対応済み。URP/Lit が null だった時点で `Debug.LogWarning("[SceneWiring] URP/Lit shader not found; falling back to Standard (pink InternalErrorShader risk under URP).")` を出してから Standard を試す2段構成に変更。
  3. [LOW] `Systems/ArenaCameraMath.cs` の `height / Mathf.Sin(pitchRad)` がゼロ除算で無言 Infinity → 対応済み。pure Systems 層にログは持ち込まず、指摘どおり呼び出し側 `Components/ArenaCameraRig.Start()` に `Mathf.Abs(Mathf.Sin(pitchRad)) <= Mathf.Epsilon` ガードを追加し `Debug.LogError("[Wiring] invalid CAMERA_PITCH_DEG (...)")` を出すよう変更。現行 `GameConfig.Camera.PitchDeg=65f` では発火しないため PlayMode の `LogAssert.NoUnexpectedReceived()` は green のまま。
  - 検証: EditMode `-runTests -testPlatform EditMode` → exit 0 / `qa/evidence/editmode-results.xml` total=25 passed=25 failed=0。build 相当 `ForgeBuild.BuildMac` → exit 0 / `game/Logs/build.log` に `Build succeeded` + `Build Finished, Result: Success.`（`game/Build/ForgeGame.app`）。
- 備考（≥80 未満のため未指摘・参考）:
  - `ArenaCameraMath.ComputeFixedPose` は pitch=0 で `sin=0` により distance が発散するが、呼出しは常に `GameConfig.Camera.PitchDeg=65` 固定で到達不能。
  - `PlayerController.Awake` の重複破棄経路（`_input` 未初期化のまま Start が走ると NRE の理論的余地）は、WireGame が単一 Player のみ生成するため実パイプラインで到達不能。
  いずれも現行フローで発火せず、修正必須ではない。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-10T07:52:00Z
- 対象コミット: 20c1aeccfcc83c59442889d6ed97b9aeea0920ff（"S-04: fix CR-CODE iter 1"）
- レビュアー: pr-review-toolkit:code-reviewer（wave3-code-reviewer）
- 判定: APPROVE（findings 0件 ≥80。iteration 1 の silent-failure-hunter 指摘3件がこの diff で解消済み・新規リグレッションなし・rule 非抵触・acceptance 非破壊）
- 検証内容（diff = `Components/ArenaCameraRig.cs` +11 / `Editor/SceneWiring.cs` +10-1 / `state/reviews/s-04.md`）:
  1. silent-failure 3件の解消を確認:
     - `SceneWiring.ApplyPlaceholderColor` の renderer==null / hex parse 失敗 / 両シェーダ null の3早期return がすべて `Debug.LogWarning("[SceneWiring] ...")` を出すようになった（プレースホルダ色は skip 可能な処理なので log→return は妥当。wiring 本体は Fail() で止めず継続＝指摘どおり）。
     - `Shader.Find("URP/Lit") ?? Standard` の無言フォールバックを2段化し、URP/Lit が null の時点で pink InternalErrorShader リスクを警告してから Standard を試す構成に変更。変数再代入・null 連鎖ともコンパイル整合。
     - `ArenaCameraMath` のゼロ除算 Infinity は、pure Systems 層を logging-free に保ったまま（rule 3 遵守）呼出し側 `ArenaCameraRig.Start` に `Mathf.Abs(Mathf.Sin(pitchRad)) <= Mathf.Epsilon` ガード＋`Debug.LogError` を追加＝無言でなくなった。
  2. rule 非抵触を確認: マジックナンバー = `Mathf.Epsilon`/`Mathf.Deg2Rad` は名前付き定数、シェーダ名文字列は Unity 組込識別子でチューニング値でない（Editor ツール）。delta-time = Start 1回実行で時間積分なし＝対象外。エンジン非依存コア = Systems には手を入れず Components 側にガードを閉じ込め rule 3 準拠。
  3. acceptance 非破壊を確認: 移動・正規化・境界クランプ・カメラ数式（PlayerMovement/ArenaCameraMath/PlayMode テスト群）は本 diff で未変更。追加はログのみでカメラ姿勢の算出値を変えない（ガードは log のみで pose を書き換えない）。iteration 1 で APPROVE 済みの acceptance 充足は維持。
- 指摘要約: なし（≥80 の findings なし）
- 備考（≥80 未満・参考）: `ArenaCameraRig.Start` のガードは検知時に `Debug.LogError` を出すが early-return せず、そのまま Infinity pose を `SetPositionAndRotation` に渡す。ただし `GameConfig.Camera.PitchDeg=65f`（sin≈0.906）でガードは到達不能・診断目的（非無言化）が主眼のため機能欠陥ではない。将来 PitchDeg を可変にする場合のみ early-return/クランプを検討。
- 対応: 追加対応不要（APPROVE）。次工程（QA-PLAY）へ進行可。
- 対応（追記: iteration 2 の備考=MEDIUM/LOW 指摘1件を修正コミット "S-04: fix CR-CODE iter 2" で解消）:
  1. [MEDIUM/LOW] `Mathf.Abs(Mathf.Sin(pitchRad)) <= Mathf.Epsilon` ガードが `Mathf.Epsilon`（≈1.4e-45＝非正規化数下限）を閾値にしていたため sin が厳密 0（pitch=0 ちょうど）でしか発火せず、コメントが謳う「at/near 0 or 180 degrees」のうち pitch=180 付近（`Mathf.Sin(180*Deg2Rad)≈-8.7e-8`）や 0.5° 等の近傍値では素通りし退化姿勢（distance≈±1e8）が無言適用されていた → 対応済み。`GameConfig.Camera` に意味のある公差の名前付き定数 `MinAbsSinPitch = 1e-3f` を追加し、`ArenaCameraRig.Start()` の判定を `Mathf.Abs(Mathf.Sin(pitchRad)) < GameConfig.Camera.MinAbsSinPitch` に置換（近傍ケースも検出対象に含める）。あわせて検出時に `Debug.LogError` 後も line 33 の `SetPositionAndRotation` で退化姿勢を適用し続けていた点も対応済み: `cam.fieldOfView` を先に設定したうえで、ガード発火時は `return` してシーン既定 Transform を保持し `ComputeFixedPose`/`SetPositionAndRotation` を呼ばないよう変更（安全 pitch へのクランプではなく early-return を選択。現行 `PitchDeg=65f`（sin≈0.906）ではどのみち到達不能ロジックだが、将来 PitchDeg 変更時に退化姿勢がシーンへ適用されるリグレッションを未然に防ぐため）。
  - 検証: EditMode `-runTests -testPlatform EditMode` → exit 0 / `qa/evidence/editmode-results.xml` total=25 passed=25 failed=0。build 相当 `ForgeBuild.BuildMac` → exit 0 / `game/Logs/build.log` に `Build Finished, Result: Success.` + `Build succeeded: .../game/Build/ForgeGame.app`。

## CR-CODE iteration 1（独立再レビュー: 対象コミット 88246e6 = "S-04: fix CR-CODE iter 2"）— APPROVE
- 日時: 2026-07-11T03:12:47Z
- レビュアー: pr-review-toolkit:code-reviewer（S-04 CR-CODE 専任）
- 対象 diff: `Components/ArenaCameraRig.cs`（+11-8）/ `GameConfig.cs`（+8）/ `state/reviews/s-04.md`
- 判定: APPROVE（confidence ≥80 の findings 0件）
- 検証内容:
  1. 退化ガードの実質化を確認: 閾値を `Mathf.Epsilon`（≈1.4e-45＝厳密0のみ検出）から名前付き定数 `GameConfig.Camera.MinAbsSinPitch = 1e-3f` へ置換し `<=` を `<` に変更。`|sin(pitch)| < 1e-3`（pitch が 0/180° の ±0.057° 相当以内）で発火し、pitch=180°（sin≈-8.7e-8）等の near-degenerate も検出対象に入った。現行 `PitchDeg=65f`（sin≈0.906）では非発火＝正常パス。
  2. early-return を確認: 検知時に `Debug.LogError` 後 `return` し、`ComputeFixedPose`/`SetPositionAndRotation` を呼ばずシーン既定 Transform を保持。iteration 2 備考（log 後も退化 pose を適用していた点）が解消。
  3. `cam.fieldOfView = GameConfig.Camera.Fov` を Start 冒頭へ前置＝ガード early-return 時も FOV は設定される。PlayMode `GameSceneTests.ArenaCamera_ForwardPointsTowardArenaCenter` の `cam.fieldOfView == GameConfig.Camera.Fov` 断言を非現行 pitch でも壊さない改善。
  4. unity-code rule 非抵触: マジックナンバー = 新規数値 `1e-3f` は `GameConfig.Camera.MinAbsSinPitch` へ集約済み（rule#12/13 準拠）。delta-time = Start 1回実行で時間積分なし＝対象外。エンジン非依存コア = Systems 層（ArenaCameraMath）は無変更、ガードは Components 層に閉じ込め（rule#3 準拠）。`GetComponent<Camera>()` は `[RequireComponent(typeof(Camera))]` により非 null 保証。
  5. acceptance 非破壊: PlayerMovement / ArenaCameraMath / PlayMode テスト群は本 diff で未変更。pitch=65 では ComputeFixedPose が従来どおり実行され forward が中心方向（Dot≈0.99+ > 0.2）・非追従・FOV=50 を満たす。EditMode `ArenaCameraMathTests`（Dot>0.99）も無影響。
- 指摘要約: なし（confidence ≥80 の findings 0件）
- 備考（<80・参考のみ・対応不要）: `GameConfig.Camera.MinAbsSinPitch` コメントの「0.5deg ... where sin~=-8.7e-8 ... 退化（~1e8-magnitude）」は数値がやや不正確（sin(0.5°)≈0.00873 で distance≈1604＝有限、~1e8 でも 8.7e-8 でもない。8.7e-8 は 180° の値）。コード挙動は正しく、コメント上の例示の混同のみ＝機能欠陥ではない。
- 対応: 追加対応不要（APPROVE）。次工程（QA-PLAY）へ進行可。
