# S-22 レビュー履歴（固定俯瞰カメラの南側可視性改善）

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-13T19:20:00Z
- 指摘要約（優先度順）:
  1. **[minor] `game/Assets/Tests/EditMode/ArenaCameraMathTests.cs:110`** — `const float ringSouthDistance = 8.1f;` が `GameConfig.Enemy.SpawnRadius(13.5m)*0.6` の導出値をリテラル複製している。`ArenaEnvironmentSceneTests` 側は `GameConfig.Enemy.SpawnRadius * 0.6f` で動的算出するため、`SpawnRadius` を将来チューニングすると PlayMode の証跡リング位置だけが移動し、この EditMode 回帰ガードは古い 8.1m を黙って検証し続ける（silent drift）。
  2. **[minor] `game/Assets/Scripts/Systems/ArenaCameraMath.cs:39-44`** — 新規 public 関数 `ComputeSouthVisibilityLimitZ` に有効入力域のガードも XML doc 記載も無い。`pitchDeg<=0` で `tan(P)=0` 除算となり ±Infinity を黙って返し、`pitchDeg+fovDeg/2=90°` ちょうど（旧値テスト 65°+25° が実際に踏む）では float の `tan(90°)` が巨大有限値を返す偶然に依存して第2項≈0 となる。兄弟関数 `ComputeFixedPose` の sin 除算縮退は `ArenaCameraRig` 側で `Debug.LogError("[Wiring]")` + `GameConfig.Camera` の許容値コメントで明示ガードされており、この規約と非対称。
  3. **[minor] `game/Assets/Scripts/GameConfig.cs:514-518`** — S-22 としてステージされた diff に S-19 由来の hunk（`AssetKeys.SfxUpgradePurchase` / `BgmMainLoop` の `""`→実パス化）が混入している（署名ブロックによるコミット未固定が原因）。S-19 側 CR-CODE（`state/reviews/s-19.md`）が本 hunk のレビュー責任を負うべきだが、両レビューが互いに相手の担当と見なして無審査で通過する取りこぼしリスク。

- 対応（fix担当・gameplay-engineer・2026-07-13）:
  1. **対応**: `ringSouthDistance` を `const float 8.1f` から `float GameConfig.Enemy.SpawnRadius * 0.6f`（動的算出）に変更。コメントも「SpawnRadius から動的算出し、SpawnRadius が将来チューニングされてもこの回帰ガードが追随するようにする」旨に書き換えた。`ArenaEnvironmentSceneTests` 側と同一の算出式に統一され、silent drift が解消された。
  2. **対応**: `ComputeSouthVisibilityLimitZ` に (a) `pitchDeg <= 0` の場合の `System.ArgumentOutOfRangeException` ガードを追加し、(b) XML doc に有効域（`pitchDeg > 0` かつ `pitchDeg + fovDeg/2 < 180`）と、`pitchDeg+fovDeg/2==90°` ちょうどのケースで float `tan(90°)` が有限大値を返すため第2項が期待通り ~0 に収束する（数学的極限と一致）ことの明示的な注記を追加した。例外は plain C# の `System.ArgumentOutOfRangeException` であり `Systems/` 層のエンジン非依存規約（rules/unity-code.md #3・MonoBehaviour/シーンAPI 禁止）には抵触しない。既存の全テスト値（65°+25°=90°境界含む）は例外を踏まないレンジ内であることを確認済み（下記再検証で回帰なし）。
  3. **確認のみ・コード変更なし（正当理由）**: `state/reviews/s-19.md`（CR-CODE iteration 1/2）を確認したところ、`AssetKeys.SfxUpgradePurchase`/`BgmMainLoop` の実パス化と、それに付随する `MenuController`/`BgmPlayer` の配線・LogMissingClipIfExpected 対応・provenance ファイルの git add まで、当該 hunk の機能スコープは S-19 側レビューで既に審査・対応済みであることを確認した。よって本 finding は「取りこぼしが実際に発生している」ものではなく、確認の結果セーフであることが判明した。ただし再発防止のため、本 iteration のコミットでは S-22 で実際に編集した3ファイル（`ArenaCameraMathTests.cs` / `ArenaCameraMath.cs` / 本レビューファイル・`state/stories.yaml`）のみを明示 `git add` し、`GameConfig.cs` 等の既存 staged 内容には触れない運用とした。story 単位のパス限定コミット分離の徹底は今後の運用改善として `state/active.md` に申し送る。

- 再検証:
  - EditMode: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results-s22-crfix.xml -logFile qa/evidence/editmode-s22-crfix.log` → exit 0、結果XML `total=149 passed=149 failed=0`
  - Build: `"$UNITY" -batchmode -projectPath game -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit -logFile game/Logs/build-s22-crfix.log` → exit 0、ログに `Build succeeded: .../game/Build/ForgeGame.app`

- status: 全 findings 対応済み（#1/#2 コード修正、#3 確認済み・正当理由付き見送り）。`state/stories.yaml` S-22 を `status: done` に更新。残存する既知の制約（ARENA_RADIUS 南端の完全カバー未達。レンジ拡張には art-director 協議要）は S-22 acceptance に明記済みの限界点として Checkpoint C へ申し送り継続。
