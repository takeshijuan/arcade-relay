# state/reviews/s-09.md — S-09「Result シーン（スコア/勝敗表示・もう一度/メニュー）」レビュー履歴

## CR-CODE iteration 1 — APPROVE
- 日時: 2026-07-21T11:31:21Z
- レビュー対象: コミット 516d05cfafaac4fced32b7ac36b2268aa26dc62f（game/Assets/Scenes/Result.unity, Scripts/Ui/ResultScreen.cs, Tests/PlayMode/ResultScreenPlayModeTests.cs）
- 指摘要約: findings 0件。
  - unity-code.md 規約適合を確認: 全数値/色は GameConfig.Ui へ集約（マジックナンバー無し。0.5f 中心アンカー等は MenuScreen/TitleScreen で既承認のUI構造値と同一パターン）。Update() に時間依存ロジック無く delta-time 非依存実装は該当せず。Ui 層は薄い配線のみ（判定/計算は ScoreSystem・MetaProgression の純粋層へ委譲、状態複製なし）。入力は InputReader に集約。Canvas は UiCanvasHelper で ScreenSpaceCamera 固定（規約14）。
  - 新規実績判定 BuildNewAchievementsLabel は MetaProgression.ApplyRunResult のロジックと整合（累積系 ACH-01/03 は post 値からの逆算で厳密検出、単一ラン系 ACH-02/04/05 は「既解放でも再表示」の既知限界をコメントで明示。false negative は無し）。
  - isNewHighScore 判定は MetaProgression の max 式と整合（新規更新は常に検出、finalScore>0 ガードで初期値0の誤強調を抑止）。タイ時に「更新！」を出す点はコメントで意図的（「更新に並んだ」）と明記された設計判断で実害軽微 — 指摘閾値未満。
  - silent-failure 観点: RunResult 未キャリー / CurrentSaveData==null での LogWarning は「Boot 未経由の単体テスト/直接ロード」という設計上の正当・回復可能状態（GameFlow.CurrentSaveData のコメント契約）に対するもので、回復不能条件の Warning 降格ではない。MenuScreen の同型分岐（CR-CODE iter1 #3 で既承認）と一致。batchmode ツール・セーブ破損の握り潰しは本 diff に該当なし。
  - 並走レーン前提: CurrentSaveData を Persistence.Save 直後値に更新する契機は S-06（todo）の責務。docs/architecture.md（L64-65/99-104）の設計に合致しており「実体未実装」は指摘としない。
  - acceptance 充足: 勝敗・finalScore・ハイスコア強調・新規実績の表示、もう一度→Game（ClearRunResult+GoToGame、Game 再ロードで GameConfig 初期値リセット）、メニューへ→Menu、および Title→Menu→Game→Result→Menu 1周を PlayMode テスト（7 本）がクリック擬似発行で検証。全5シーンは EditorBuildSettings 登録済み・Result.unity は MainCamera 保持で Camera.main 解決可。
- 対応: 不要（APPROVE）。

### 追記 — silent-failure-hunter 追加指摘（同 iteration 1）への対応
- 日時: 2026-07-21T00:00:00Z
- 指摘1 [MEDIUM] ResultScreen.cs:113-127 — RunResult 未キャリー時のフォールバック（runResult = default）を ScoreSystem.ComputeFinalScore に通すと TimeParSec 由来のボーナスのみで非ゼロスコア（例: 600）が算出され、失敗が成功風の非ゼロ表示に化ける（isNewHighScore の誤強調にも波及し得る）。
  - 対応: 対応済み。`int finalScore = hasRunResult ? ScoreSystem.ComputeFinalScore(runResult) : 0;` に変更し、RunResult 未キャリー時は finalScore=0 固定（既存の `finalScore > 0` ガードによりハイスコア強調も自動抑止される）。
- 指摘2 [LOW] ResultScreen.cs:175-176 — BuildNewAchievementsLabel の prevTotalWins/prevTotalKills 逆算が、GameFlow.CurrentSaveData が post-save 値でない場合（S-06 の SetCurrentSaveData 契約退行時）に負値になり得るが無音で素通しし、古い実績なし表示に落ちていた。
  - 対応: 対応済み。`prevTotalWins < 0 || prevTotalKills < 0` を検出したら `Debug.LogWarning("[ResultScreen] carried SaveData appears stale — SetCurrentSaveData contract regression?")` を1回記録するよう追加。検出は部分的（新規解放の誤判定自体は防げない）で契約責務は S-06 側にあるため advisory 扱いのまま、表示ロジック自体は変更していない。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-21T11:36:34Z
- レビュー対象: コミット 1fafdb5591123cbf240e057974967a12c0199597（S-09: fix CR-CODE iter 1 — game/Assets/Scripts/Ui/ResultScreen.cs の iter1 M/L 指摘対応 diff のみ）
- 指摘要約: findings 0件。iter1 の M/L 両指摘対応を検証し、いずれも正確・過不足なしと確認。
  - 指摘1[M] 修正の妥当性を実データで裏取り: RunResult は値型 struct（Systems/Types.cs）で `default` は全ゼロ。ScoreSystem.ComputeFinalScore(default) は timeTerm = max(0, TimeParSec − 0) × TimeWeight = 300 × 2 = **600** の幻影スコアを返す（GameConfig.Score 実測: CoreWeight=500/KillWeight=8/TimeParSec=300f/TimeWeight=2）。`int finalScore = hasRunResult ? ComputeFinalScore(runResult) : 0;`（ResultScreen.cs:131）はこれを 0 に固定し、既存 `finalScore > 0 && finalScore == data.highScore`（:136）ガードで isNewHighScore の誤強調も自動抑止。修正は正確かつ必要。`0` は敗北フォールバックの意味的既定値で unity-code.md 規約13（0/1 初期値の除外）に該当しマジックナンバー違反ではない。
  - 指摘2[L] 修正の妥当性を検証: `prevTotalWins = post.totalWins − (r.IsWin?1:0)` / `prevTotalKills = post.totalKills − r.KillCount`（:179-180）は MetaProgression.ApplyRunResult（`totalWins = prev+1`（IsWin時）/ `totalKills = prev + KillCount`）の厳密逆算であり、負値は「post が post-save 値でない（＝ stale）」ことの証明。検出→`Debug.LogWarning` 1回（:186-189）は表示専任層が解消不能な他ストーリー（S-06）由来の契約退行に対する advisory で、直前ヘッダコメントに縮退の正当性を明記済み。表示は既存の近似ロジックを継続し実データ（単調な achFlags）は不破壊 = 回復可能な表示劣化のため CR-CODE silent-failure 観点の「回復不能条件の Warning 記録」に非該当。LogError 化は分離テスト（CurrentSaveData=null→CreateDefault + 勝利 RunResult で prevTotalWins=−1）で QA-PLAY のエラー0検査を誤って落とすため Warning が正当な水準。
  - unity-code.md: diff の追加行に時間依存ロジック無く delta-time 非依存実装は非該当。追加数値リテラルは 0/1 のみでマジックナンバー無し。Ui 層は薄い配線・判定委譲を維持。
  - acceptance: 本 diff は通常フロー（hasRunResult=true）の勝敗/finalScore/ハイスコア強調/新規実績表示・もう一度→Game・メニューへ→Menu を変更せず、no-run-result フォールバック（分離テスト経路）の表示正確性のみ精緻化。iter1 で検証済みの PlayMode 遷移検証（Title→Menu→Game→Result→Menu・クリック擬似発行）は退行なし。
- 対応: 不要（APPROVE）。

## CR-CODE iteration 1 — APPROVE（d48d829 status-done ゲート再検証）
- 日時: 2026-07-22T00:00:00Z
- レビュー対象: コミット d48d829023b90c20906ee3dceb22893904da6ed6（S-09: status done）。当該コミット自体の diff は state/stories.yaml の status review→done のみだが、本ゲートは S-09 実装コミット群（ab963bb GameConfig 追加 / 516d05c ResultScreen 本体+テスト / 1fafdb5 CR-CODE iter1 修正）が done コミットのツリー状態に合流した最終形を対象に、読み取り専用（Unity 未起動・静的確認のみ）で再検証した。
- 指摘要約: findings 0件（APPROVE）。以下を独立に確認:
  - マジックナンバー（unity-code.md 規約12/13）: ResultScreen.cs・GameConfig 追加分いずれも数値/色は GameConfig.Ui.Result*（ResultTopAnchorY/ResultRowStepY/ResultOutcomeFontSize/ResultWinColor/ResultLossColor/ResultHighScoreHighlightColor）・Meta.*（CenturySlayerKills/AoeSpecialistKills/FrugalMaxSpots）・Core.HpMax へ集約。本文の裸リテラルは `new Vector2(0.5f, anchorY)` / `pivot(0.5f,0.5f)` / row 初期値 0 と no-run-result 時 finalScore=0 のみで、いずれも UI 中心アンカー／構造値または 0/1 初期値（規約13 除外）。同一パターンは既承認 MenuScreen.cs と一致。違反なし。
  - delta-time（規約14相当）: Update() は transitioning ガード＋InputReader.ClickPressedThisFrame のポーリングとヒットテストのみで、時間依存の移動・加算・タイマー・アニメーションを持たない。フレーム依存実装は非該当。
  - エンジン非依存コア/層分離: ResultScreen は Ui（薄い Component）に閉じ、スコアは ScoreSystem.ComputeFinalScore（純粋層）へ委譲、実績逆算のみ表示用に持つが SaveData/RunResult を複製・二重計算しない。File I/O・PlayerPrefs 直呼びなし（Persistence 非侵犯）。入力は InputReader 集約。Canvas は UiCanvasHelper.ConfigureScreenSpaceCamera で ScreenSpaceCamera 固定。
  - ロジック整合（独立再検証）: BuildNewAchievementsLabel の逆算は MetaProgression.ApplyRunResult と厳密整合（totalWins=+1(win時)/totalKills=+KillCount/ach フラグ単調/highScore=max）。ACH-01/03 は閾値クロスの厳密検出、ACH-02/04/05 は単一ラン近似（既解放の再表示のみの既知限界・false negative なし）。isNewHighScore=`finalScore>0 && finalScore==data.highScore` は max 式と整合し 0 初期値の誤強調をガード。
  - silent-failure: 3 箇所の Debug.LogWarning（RunResult 未キャリー / CurrentSaveData==null / stale SaveData 検出）はいずれも直前ヘッダコメントで正当性を明記した回復可能・設計上想定の状態に対する記録で、回復不能条件の LogError 降格ではない。セーブ破損の黙示初期化・batchmode 握り潰しは本 diff に非該当。
  - 並走レーン前提: CurrentSaveData を Persistence.Save 直後値へ更新する契機は S-06/S-07 の SetCurrentSaveData 契約（GameFlow.cs にコメントで明記）に依存。docs/architecture.md の設計と一致するため「実体未実装」は指摘としない。
  - acceptance 充足: 勝敗（勝利/敗北）・finalScore・ハイスコア更新時強調・新規実績表示、もう一度→Game（ClearRunResult+GoToGame・シーン再ロードで CORE_HP_MAX/STARTING_GOLD 等を GameConfig から都度読む＝新規ランリセット）、メニューへ→Menu を実装し、ResultScreenPlayModeTests.cs（7 本）がクリック擬似発行で Result→もう一度→Game / Result→メニューへ→Menu と Title→Menu→Game→Result→Menu 1周を検証。Result.unity は MainCamera(tag=MainCamera) を保持し Camera.main/worldCamera 解決可。
- 対応: 不要（APPROVE）。
