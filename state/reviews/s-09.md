# S-09 レビュー履歴 — クリスタル ドロップ・自動回収 + スコア算出

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-10T00:00:00Z
- 対象コミット: 7309b2c830c539a1058b12977b4dfed8bbed53eb
- 指摘要約（優先度順）:
  1. [マジックナンバー / unity-code.md 規約1・Don't「MonoBehaviour 本文に数値リテラル直書き禁止」] `game/Assets/Scripts/Components/CrystalPickup.cs:35` `go.transform.rotation = Quaternion.Euler(45f, 0f, 45f);` の初期チルト角 45°/45° がインライン直書き。クリスタルの他の視覚パラメータ（`VisualScale` / `RotationSpeedDegPerSec` / `BobAmplitude` / `BobFrequency`）は同コミットで `GameConfig.Crystal` に集約済みであり、初期チルト角だけが例外的にハードコードされて一貫性を欠く。45° は `Mathf.PI * 2f`（同ファイルで構造的全周として文書化・除外）のような数学的必然ではなく、アート調整対象の任意の見た目パラメータ（30°/60° でも成立）であり、規約の除外対象（配列 index・0/1 初期値・意味を持たない値）に該当しない。修正方針: `GameConfig.Crystal` に `PlaceholderTiltDeg = 45f`（または X/Z 個別定数）を追加し `Quaternion.Euler(GameConfig.Crystal.PlaceholderTiltDeg, 0f, GameConfig.Crystal.PlaceholderTiltDeg)` に置換。プレースホルダ視覚のため深刻度は低いが、`GameConfig` 集約の一貫性を回復するか、構造値として据え置く正当理由をヘッダコメントに明記すること（黙殺禁止）。
- 準拠を確認した観点（ブロッキングなし）:
  - delta-time（unity-code #2）: 準拠。`CrystalPickup.ApplyCodedMotion` の自転 `RotationSpeedDegPerSec * deltaTime`・ボブ `BobFrequency * Mathf.PI * 2f * deltaTime`、`CrystalSystem.TickLifetime` の `remaining - deltaTime` はいずれも `Time.deltaTime` スケール。フレーム固定加算なし。
  - エンジン非依存コア（unity-code #3）: 準拠。`Systems/CrystalSystem` は static・MonoBehaviour 非継承・シーン API 不使用（`Vector3`/`Mathf` の値型・数学型のみ）。`Instantiate`/`Destroy`/`RunStatsTracker` 副作用は `Components/CrystalPickup` に隔離。既存 `HealthSystem.IsContacting` の XZ 半径パターンと同型。
  - Components 薄さ（unity-code #3）: `RunStatsTracker` は加算のみ、`CrystalPickup` は配線のみ。半径/寿命判定ロジックは `CrystalSystem` へ委譲。
  - 資産キー集約 / 色（unity-code #1・#5）: クリスタル発光色は `GameConfig.Ui.ColorCrystalCyan` を参照（hex 直書きなし）。`Shader.Find("Universal Render Pipeline/Lit")` は既存 `WaveSpawner`/`SceneWiring` の URP-safe shader lookup を踏襲した既定パターン（`Resources.Load` 直書きとは別種・pre-existing 規約）で非ブロッキング。
  - シングルトン整合: `RunStatsTracker` は `WaveSpawner.Instance`/`PlayerController.Instance` と同型（Awake 設定・OnDestroy クリア・重複時 LogError+Destroy）。`Game.unity` に `SceneWiring.WireGame` が冪等追加、DontDestroyOnLoad 無しでラン毎に 0 リセット（per-run tally として正）。
  - 撃破→ドロップ順序: `AutoAttackDriver` は `ApplyAutoAttackDamage` 前に `target.transform.position` をキャプチャし、`killed==true` 時のみ `RegisterKill`+`SpawnDrop(killPosition, DropPerKillNormal)`。`EnemyAgent.ApplyAutoAttackDamage` は未初期化/既撃破時 `false` を返すため幻のキル/二重ドロップなし。スコア二重計上なし（`CompleteRun` は `RunStatsTracker.CurrentScore` でなく tally から `ScoreSystem.ComputeFinalScore` を再計算）。
  - acceptance: 満たす。撃破位置に `DropPerKillNormal` 個生成、`CRYSTAL_PICKUP_RADIUS` 内で入力なし自動回収（`TryPickup`→`RegisterCrystalPickup`）、`CRYSTAL_LIFETIME` 経過で `Destroy`、`ScoreSystem.ComputeFinalScore` で最終スコア算出（`HealthComponent.CompleteRun` から実 tally を投入）。PlayMode `CrystalSceneTests`（回収でスコア +SCORE_PER_CRYSTAL・未回収の lifetime 消滅・kill→drop→回収の合成フロー）と EditMode `CrystalSystemTests`（XZ 半径・境界・Y無視・寿命 tick/floor/expiry）で網羅。
- 参考（非ブロッキング・信頼度 < 80・非報告）: `SpawnDrop` の `count > 1`（Heavy=3）時は全クリスタルが同一 position・同一 rotation で生成され視覚的に完全重複する（`_bobPhase` のみランダム化で XZ 分散なし）。Heavy 変種は未 wired かつ acceptance 対象外のため現状無害。将来 Heavy ドロップを wire する際に散布オフセットを要検討。
- 対応:
  1. [マジックナンバー / CrystalPickup.cs:35 チルト角] 対応済み。`GameConfig.Crystal` に `PlaceholderTiltDegX`/`PlaceholderTiltDegZ`（各 45f）を追加し、`SpawnDrop` の `Quaternion.Euler(45f, 0f, 45f)` を `Quaternion.Euler(GameConfig.Crystal.PlaceholderTiltDegX, 0f, GameConfig.Crystal.PlaceholderTiltDegZ)` に置換。他の視覚パラメータ（VisualScale/RotationSpeedDegPerSec/BobAmplitude/BobFrequency）と同じ集約に統一した。
  2. [HealthComponent.cs:159/185/193 CompleteRun のセーブ失敗によるソフトロック] 対応済み（silent-failure-hunter 指摘）。`adapter.Save(nextSave)` を try/catch で包み、失敗時は `Debug.LogError("[SaveCorruption] save write failed: " + ex)` を出した上で処理を継続し、`SessionHolder.UpdateSave` と `SceneManager.LoadScene(Result)` を必ず実行するよう変更。`_runCompleted` は依然 Save 呼び出し前に立てるが（多重セーブ防止の意図は維持）、Save が例外を投げても Result 遷移・入力ロック解除まで到達するためソフトロックしない。永続化は当該ランに限り失われ得るが、QA-PLAY の `LogAssert.NoUnexpectedReceived()` でエラーとして検出可能になる。
  3. [HealthComponent.cs:216 ResolveRunTallies の LogWarning/LogError 不一致] 対応済み（silent-failure-hunter 指摘）。`RunStatsTracker.Instance` 欠落時のログを `Debug.LogWarning` から `Debug.LogError` に変更し、同一の配線破損条件を扱う `AutoAttackDriver.cs:84` / `CrystalPickup.cs:125` と統一（tech-stack-unity.md 規約12: 配線破損は LogError）。あわせて `ResolveRunTallies` のヘッダコメントを更新し、`ResolveWaveReached`（正当な縮退＝LogWarning のケース）とは別種の扱いであることを明記した。
- 検証: EditMode `qa/evidence/editmode-results-s09-fix1.xml`（79/79 passed, failed 0, exit 0）、build `ForgeBuild.BuildMac`（`game/Logs/build.log` に `Build succeeded`, exit 0, `game/Build/ForgeGame.app` 生成確認）。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-10T10:30:00Z
- 対象コミット: 95e42c8f10a0eed37a0cfd79a50b04d3425489dc（"S-09: fix CR-CODE iter 1"）
- 指摘要約: なし（findings 0件）。iteration 1 の全指摘が解消され、新規指摘なし。
- 解消確認:
  1. [マジックナンバー / CrystalPickup.cs チルト角] 解消。`Quaternion.Euler(45f, 0f, 45f)` → `Quaternion.Euler(GameConfig.Crystal.PlaceholderTiltDegX, 0f, GameConfig.Crystal.PlaceholderTiltDegZ)`。定数 `PlaceholderTiltDegX`/`PlaceholderTiltDegZ`（各 45f・単位コメント付き）を `GameConfig.Crystal` に定義し、他の視覚パラメータ（VisualScale/RotationSpeedDegPerSec/BobAmplitude/BobFrequency）と同じ集約へ統一。残る `0f`（Euler の Y 成分）は unity-code #13 の除外対象（0/1・意味を持たない値）で非該当。GameConfig.cs 内の `45f` はここが定数定義の正本のため直書きで正。
  2. [CompleteRun セーブ失敗ソフトロック] 解消。`adapter.Save(nextSave)` を try/catch で包み、失敗時 `Debug.LogError` 後に処理継続。`FileSaveAdapter.Save`（`Directory.CreateDirectory`/`File.WriteAllText`/`File.Move` が unguarded・throw し得る — L66-74 で確認）が例外を投げても `SceneManager.LoadScene(Result)` に必ず到達し、`_runCompleted` latched のままの Game ソフトロックを回避。`SaveInvocationCountForTests++` は Save 成功時のみ加算される位置に置かれ、テスト観測性も正。QA-PLAY の `LogAssert.NoUnexpectedReceived()` で LogError として検出可能。
  3. [ResolveRunTallies の LogWarning/LogError 不一致] 解消。`RunStatsTracker.Instance` 欠落時を `Debug.LogWarning` → `Debug.LogError` に変更し、同一の配線破損条件を扱う `AutoAttackDriver`/`CrystalPickup` と統一。ヘッダコメントも `ResolveWaveReached`（正当な縮退＝LogWarning）との差異を明記。
- acceptance: 満たす（iteration 1 の確認を維持）。撃破位置に `DropPerKillNormal` 個生成→`CRYSTAL_PICKUP_RADIUS` 内で入力なし自動回収→カウンタ/スコア加算、`CRYSTAL_LIFETIME` 経過で消滅、`ScoreSystem.ComputeFinalScore` で最終スコア算出。PlayMode `CrystalSceneTests` が (a) 回収でスコア +SCORE_PER_CRYSTAL・(b) 未回収 lifetime 消滅で非加算・(c) kill→drop→回収の合成フローを検証。
- 非ブロッキング（信頼度 < 80・非報告）: `Debug.LogError("[SaveCorruption] save write failed: ...")` の `[SaveCorruption]` タグは unity-code #24 では本来「読込時のセーブ破損＋3点セット復旧」に予約されたタグで、今回の書込失敗とは別シナリオ。LogError の重大度・検出性は正しく、規約 #24（黙って初期化しない）自体には非抵触のため非ブロッキング。将来 `[SaveWriteFailure]` 等へ分離する余地あり。
- delta-time（unity-code #2）: 本コミットの変更に移動・タイマー処理の追加なし（チルトは静的回転で delta-time 不要）。既存のコードモーション（自転/ボブ/lifetime tick）は引き続き `Time.deltaTime` スケールで準拠。
