## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-13T04:00:00Z
- 指摘要約（優先度順）:
  1. [major] `game/Assets/Scripts/Components/EnemyAgent.cs` `ApplyHeavyTint`（Renderer 不在／GameConfig hex パース失敗の2パス）が `Debug.LogWarning` で処理されており、規約12（配線破損は LogError／LogWarning は文書化された正当縮退のみ）に違反。現行スポーン経路ではどちらも発生し得ない＝発生時は配線破損であり、QA-PLAY の `LogAssert.NoUnexpectedReceived()` は Warning では失敗しないため、退行時にヘヴィ変種の見た目差分喪失がゲートを素通りする。
  2. [minor] `ApplyHeavyTint` が生成する per-instance Material が一切解放されず、`Destroy(gameObject)` では instanced material が解放されないため、エンドレスウェーブ中にヘヴィ撃破のたびに Material が1個ずつリークする。
  3. [minor] `game/Assets/Tests/PlayMode/HeavyEnemySceneTests.cs` の混入統合テスト: (a) `Random.InitState` を「reproducible」「astronomically small」と主張するコメントが実態（実時間ベース待機によりロール消費数が可変＝P(ヘヴィ0体)≈0.2%の残留フレークあり）と乖離し誤誘導的。(b) `DisableAutoAttackAndPlayerHealth` が `FindFirstObjectByType` の null を無言 no-op しており、将来のシーン配線変更でこのヘルパーが防いでいる生存バイアスが痕跡ゼロで再発しうる。
- 対応:
  1. 対応した。`ApplyHeavyTint` の両パスを `Debug.LogError("[Wiring] ...")` に昇格し、それぞれに「現行スポーン経路（CreatePrimitive(Cube) placeholder / MDL-02 prefab）では発生し得ない＝配線破損」である旨のコメントを追加（`WaveSpawner.SpawnOne` の既存 LogError ガードと同型）。`HeavyEnemySceneTests.CreateEnemy` を `new GameObject` から `GameObject.CreatePrimitive(PrimitiveType.Cube)` に変更し、テスト側のヘヴィ生成が実プレハブ経路同様に Renderer を保有するようにした（余分な BoxCollider/MeshFilter は接触判定が `HealthSystem` の XZ 半径オーバーラップでありコライダー非依存のため無害）。
  2. 対応した。`EnemyAgent` に `_heavyTintMaterialInstance` フィールドを追加し `ApplyHeavyTint` で生成した Material をそこに保持、新設した `OnDestroy()` で `Destroy(_heavyTintMaterialInstance)` して解放する。通常 Swarmer は `ApplyHeavyTint` が呼ばれずフィールドが null のままなので no-op。
  3. 対応した。(a) `Random.InitState` 直前および Phase 2 の該当コメントを、実時間ベース待機によりロール消費数が可変であること・統計的検証であること・実測の残留フレーク率（0.85^40≈0.2%、約1/660ラン）を明記する内容に書き換えた（挙動自体は変更せず、誤った確度の主張のみ是正）。(b) `DisableAutoAttackAndPlayerHealth` に `Assert.IsNotNull` を追加し、`AutoAttackDriver`/`HealthComponent` の不在を前提崩壊として明示的に失敗させるようにした（`DisableWaveSpawner` の呼び出し側が既に持つ `spawner` の `Assert.IsNotNull` と同型）。
- 検証結果:
  - EditMode（typecheck相当）: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results.xml` → exit 0 / 結果XML `total="120" passed="120" failed="0"`
  - build相当: `"$UNITY" -batchmode -projectPath game -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit -logFile game/Logs/build.log` → exit 0 / ログに `Build succeeded: .../game/Build/ForgeGame.app` を確認

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-13T13:24:00Z
- 指摘要約（優先度順）:
  1. [major] `AfterUnlockWave_HeavyVariantsMixIntoAutomaticSpawns_WithPlausibleStatMultipliers`（`HeavyEnemySceneTests.cs`）は iter1 修正後も統計的フレークが残存。`Random.InitState` はロールの*系列*のみ固定し、実時間ベース待機（`WaitForSecondsRealtime` under `timeScale=100`）下では消費ロール数がフレーム数依存で可変なため、コメント自身が定量化する P(ヘヴィ0体)≈0.85^40≈0.15%（約1/660ラン）の残留フレークで `Assert.IsTrue(sawHeavy)`（旧260行）が正しいビルドに対して偽の QA-PLAY 退行を報告しうる。固定スポーン回数駆動 or 保証ロール数に基づく assert への変更を推奨。
  2. [major] このコミットの記録済み検証（s-14.md 検証結果 + コミットメッセージ）は EditMode と `ForgeBuild.BuildMac` のみで、diff の主変更対象である PlayMode テスト（`HeavyEnemySceneTests`）自体のランタイム挙動（`CreatePrimitive(Cube)` 化・`Assert.IsNotNull` 追加・`EnemyAgent.OnDestroy` の Material 解放・`ApplyHeavyTint` の `LogError` 昇格）を一度も実行せずに「対応した」と記録していた。acceptance が PlayMode テストでの検証を明示している以上、PlayMode 再実行が必要。
  3. [minor] `ApplyHeavyTint` の `LogError` 昇格が不完全: `EnemyAgent.cs` の `CreateFallbackMaterial` 内、`sharedMaterial` null かつ URP/Lit・Standard 両シェーダーとも `Shader.Find` 失敗という第3の失敗パス（197行）が `Debug.LogWarning` のままで、iter1 で昇格した2パスと同一関数内で扱いが食い違っていた。
  4. [minor] `DisableWaveSpawner`（`HeavyEnemySceneTests.cs`）が `FindFirstObjectByType` の null を無言 no-op する同型パターンが、iter1 で修正した `DisableAutoAttackAndPlayerHealth` の直上に残存。加えてその修正コメントが参照する行番号（「line 189 below」）が既にずれていた。
- 対応:
  1. 対応した。`WaveSpawner` に test-observability カウンタ `SpawnCallCountForTests`（`SpawnOne` 呼び出し毎にインクリメント）を追加し、`HeavyEnemySceneTests` の Phase 2 を「固定実時間ウィンドウでサンプリング」から「ヘヴィが観測されるまで毎フレーム能動的に Normal 個体を Destroy して spawn 枠を空け続け、ロール数（`SpawnCallCountForTests`）と寛容な実時間セーフティネット（15秒）の両方で打ち切る」ループへ書き換えた。`requiredRollsBeforeGivingUp=2000` により残留フレーク確率は 0.85^2000（実質ゼロ）まで低下し、実際は期待値 ~6.7 ロールでヘヴィが見つかるため通常は数フレームで完了する。旧 `Assert.IsTrue(sawHeavy)` は `Assert.IsNotNull(heavyFound, ...)`（観測ロール数を含む失敗メッセージ付き）に置換。
  2. 対応した。PlayMode スイートを実行し `qa/evidence/playmode-results.xml`（`total="88" passed="88" failed="0"`）を取得、EditMode（`total="120" passed="120" failed="0"`）・`ForgeBuild.BuildMac`（`Build succeeded`）と合わせて3コマンド全て exit 0 / 合格を確認してからこのコミットを記録する。
  3. 対応した。`CreateFallbackMaterial` の「どちらのシェーダーも無い」パス（旧197行）を `Debug.LogError` に昇格し、URP/Lit 単独欠如（Standard へのフォールバック成功の可能性がある正当な縮退）は `Debug.LogWarning` のまま維持する旨のコメントを追加して2パスの扱いの理由を明記した。
  4. 対応した。`DisableWaveSpawner` に `Assert.IsNotNull(spawner, ...)` を追加（`DisableAutoAttackAndPlayerHealth` と同型）。`DisableAutoAttackAndPlayerHealth` 側のコメントからハードコード行番号参照を除去し、構造的な相互参照（「DisableWaveSpawner now also asserts...」）に書き換えた。
- 検証結果:
  - EditMode（typecheck相当）: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results.xml -logFile -` → exit 0 / 結果XML `total="120" passed="120" failed="0"`
  - PlayMode（QA相当・今回の主変更対象）: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform PlayMode -testResults qa/evidence/playmode-results.xml -logFile -` → exit 0 / 結果XML `total="88" passed="88" failed="0"`
  - build相当: `"$UNITY" -batchmode -projectPath game -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit -logFile game/Logs/build.log` → exit 0 / ログに `Build succeeded: .../game/Build/ForgeGame.app` を確認
- MAX_ITER(2) 到達につき state/stories.yaml の S-14 を status: done に更新。未対応finding: 無し（5件全て対応済み）。
