# S-14 — ラン間アップグレード効果反映と購入ロジック（UPG-01/02/03）

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-22T11:33:00Z
- 指摘要約（優先度順・findings JSON より）:
  1. [major] 正本 reducer（`MetaProgression.TryPurchaseUpgrade`/`GetUpgradeLevel`）+ 公開 `UpgradeKind` を新設したが、実際の Menu 購入経路（`MenuScreen.HandlePurchaseClick` → 私有 `MenuScreen.TryPurchaseUpgrade` + 私有 `UpgradeKind` + `saveStore.Save`）は差し替えられておらず、新 reducer が本番コードから未参照（テスト専用の実質デッドコード）。`MenuScreen.cs` 冒頭コメントが明示的に要請していた置き換えが未解消。挙動自体は一致し MenuScreen は ui-engineer レーンの別ファイルのため非ブロッキング。
  2. [major] UPG-02 割引がプレイヤー実経路で機能不全: `BuildSpotController.TryPlaceTower` は実効コスト課金になったが、唯一の設置 UI 経路 `TowerSelectPanel.Open` の affordable 判定と価格ラベルは `GameConfig` の基礎コスト固定のまま。割引後なら購入可能な資金帯のプレイヤーが誤ブロックされ、表示価格も課金額と乖離する。この不整合が story note・reviews のどこにも記録されておらず黙殺されていた。
  3. [major] 購入トランザクションの二重実装が S-14 完了後も残存（1と表裏）。`MenuScreen` 側の private switch は default 欠落で、将来 enum 追加時に essence 減算しつつ Lv を上げない黙示 essence 消失の失敗モードを持つ（正本側は throw で防御済み）。レーン合流後のバッチ区間での統合を後続作業として記録すべき。
  4. [minor] `TryPurchaseUpgrade` が失敗理由を返さない bool 戻りで、`PlacementResult`/`TowerUpgradeResult`/`TowerSellResult` の `FailureReason` enum 付き結果構造体という既存慣例から逸脱。
  5. [minor] `GameFlow.CurrentSaveData==null` 時の `BuildSpotController.Awake` フォールバックがログ・フラグ無しで観測不能。出荷ビルドや Editor 直接再生で Boot 非経由のまま Game に入ると、購入済みアップグレードが黙って無効なランが開始されても QA のエラー0検査に映らない。
  6. [minor] `ScaffoldSmokeTests.MetaProgression_TryPurchaseUpgrade_InsufficientEssence_Fails_AndDoesNotChangeLevel` の `Assert.AreEqual(prev.essence, next.essence)` が同語反復（失敗パスで `next==prev` の同一参照のため、実装が `prev.essence` を破壊的更新しても検出できない）。

- 対応:
  1. **見送り（正当理由: レーン境界・stories.yaml 既記載）**。`game/Assets/Scripts/Ui/MenuScreen.cs` は ui-engineer 担当領域（Ui 層）のファイルであり、gameplay-engineer は書き換えない（並走レーン規律）。この技術的負債は既に `state/stories.yaml` S-14 ブロックの impl note（「Ui/MenuScreen.cs（S-15・ui-engineer 担当）は購入操作の暫定実装を既に持ち…本 story では ui-engineer レーン境界を尊重し置き換えなかった」）および `state/reviews/s-15.md`（CR-CODE iteration 1 対応3「見送り（次iteration/S-14着手時に対応）」）に相互参照済み。本 iteration で追加対応はしない。Checkpoint での人間提示（未解決の技術的負債）に委ねる。
  2. **見送り（正当理由: レーン境界・未解決事項として本 review に記録）**。`game/Assets/Scripts/Ui/TowerSelectPanel.cs` は ui-engineer 担当の Ui 層ファイルであり、gameplay-engineer のレーン規律（担当領域外のコード書き換え禁止。並走レーン中の縮退検証プロンプトも UI 修正を許可していない）により書き換えない。findings が示す不整合は正当な指摘であり黙殺しない — 下記「未解決事項」節に記録し、Checkpoint での人間提示 / ui-engineer への引き継ぎ対象とする（`TowerSelectPanel.Open` の affordable 判定・価格ラベルを `BuildSpotController.TowerDiscountRate` 経由の実効コストへ揃える対応が必要）。
  3. **見送り（正当理由: 1・2と同型のレーン境界問題・記録済み）**。統合（`MenuScreen` 側実装を正本 reducer 呼び出しへ置き換え）はレーン合流後のバッチ検証区間、または S-14/S-15 双方が完了した後続の統合 story で行うべき事項として下記「未解決事項」節に記録する。
  4. **対応済み**。`Systems/Meta/MetaProgression.cs` に `PurchaseFailureReason`（`AlreadyMaxLevel`/`InsufficientEssence`）enum と `PurchaseUpgradeResult`（`Success`/`FailureReason`/`Data`）構造体を新設し、`TryPurchaseUpgrade` の戻り値を `bool + out SaveData` から `PurchaseUpgradeResult` へ変更（`BuildSpotSystem.PlacementResult`/`TowerUpgradeSystem.TowerUpgradeResult` と同じ命名・構造慣例に統一）。呼び出し元は本 story 内の EditMode/PlayMode テスト（`ScaffoldSmokeTests.cs`・`BuildSpotControllerUpgradePlayModeTests.cs`）のみのため、シグネチャ変更に伴う全呼び出し箇所を追随修正済み（`Ui/MenuScreen.cs` はこの reducer を呼んでいないため無関係 — 対応1参照）。
  5. **対応済み**。`Components/BuildSpotController.cs` の `Awake()` で `GameFlow.CurrentSaveData == null` を検知した場合に `Debug.Log("[BuildSpotController] GameFlow.CurrentSaveData is unset; falling back to SaveData.CreateDefault() ...")` を1回出力するよう変更（`LogType.Log` — `LogAssert.NoUnexpectedReceived()` は Error/Assert/Exception 級のみを対象とするため既存 PlayMode テストへの影響なし。`GameBootstrap` の recovered ログと同じ「情報レベルログ」パターンに統一）。
  6. **対応済み**。`ScaffoldSmokeTests.MetaProgression_TryPurchaseUpgrade_InsufficientEssence_Fails_AndDoesNotChangeLevel` で `int essenceBefore = prev.essence;` を呼び出し前に退避し、`Assert.AreEqual(essenceBefore, result.Data.essence)` へ変更（PlayMode 側 `essenceBeforePurchases` と同じパターン）。`AtLevelCap` テストは元々 literal `9999` 比較のため対象外（finding原文のとおり有効）。

## 未解決事項（Checkpoint 提示用・黙殺しない）
- [BLOCKER相当] `Ui/TowerSelectPanel.cs`（ui-engineer 担当）の affordable 判定・価格ラベルが UPG-02 割引を未反映のまま `GameConfig` の基礎コスト固定値を使用しており、割引後なら購入可能な資金帯のプレイヤーが「資金不足」で誤ブロックされる。`BuildSpotController.TowerDiscountRate`（S-14 で新設・public）経由で実効コストを算出するよう ui-engineer 側の修正が必要。
- `Ui/MenuScreen.cs`（ui-engineer 担当・S-15）の購入経路が本 story の正本 `MetaProgression.TryPurchaseUpgrade` を呼ばず、私有の重複実装（`MenuScreen.TryPurchaseUpgrade` + 私有 `UpgradeKind`）を使い続けている。挙動は現状一致するが、二重実装はドリフト源（`MenuScreen` 側 switch の default 欠落による黙示 essence 消失リスクを含む）。レーン合流後のバッチ検証区間、または統合 story で `MenuScreen` 側を正本 reducer 呼び出しへ置き換えることを推奨（`state/reviews/s-15.md` 既記載の技術的負債と同一事象 — 二重計上しない）。

## 検証（並走レーン規律: Unity をここでは起動しない）
- 参照実在の静的確認（Read/Grep）:
  - `MetaProgression.PurchaseUpgradeResult`/`PurchaseFailureReason`: 新設した型が `Systems/Meta/MetaProgression.cs` 内に定義され、同ファイル内 `TryPurchaseUpgrade` の戻り値として使用されていることを確認。
  - `SaveData.Clone()`・`SaveData.essence`・`upgStartingGoldLv`/`upgTowerDiscountLv`/`upgEssenceRateLv` フィールドは既存 `Types.cs` 定義のまま変更なし（`GetUpgradeLevel`/`SetUpgradeLevel` は無変更）。
  - `ScaffoldSmokeTests.cs`: `using ForgeGame.Systems.Meta;` が既に存在し `PurchaseUpgradeResult`/`PurchaseFailureReason` の名前解決に問題ないことを確認。全 `TryPurchaseUpgrade` 呼び出し（3箇所）を新シグネチャへ更新済みで旧シグネチャ（`out SaveData`）の残存呼び出しが無いことを `grep -n "TryPurchaseUpgrade"` で確認。
  - `BuildSpotControllerUpgradePlayModeTests.cs`: 同様に全3呼び出しを更新済み・旧シグネチャ残存なしを確認。
  - `BuildSpotController.cs`: `Debug.Log` 追加箇所は既存 `using UnityEngine;` 経由で解決済み（同ファイル内の既存 `Debug.LogError` 呼び出しと同一クラス）。`Ui/MenuScreen.cs`・`Ui/TowerSelectPanel.cs` は本 iteration で無変更（Read のみ）。
- 正式な EditMode/PlayMode 実行・コンパイル確認はレーン合流後のバッチ検証区間（tech-stack-unity.md「検証コマンド」）に委ねる。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-22T11:44:34Z
- 指摘要約（優先度順・findings JSON より）:
  1. [major] `BuildSpotController.Awake()` の新設 `Debug.Log`（iter1 #5 対応）が既存 PlayMode テストを破壊する。
     `LogAssert.NoUnexpectedReceived()` は Unity Test Framework の `LogScope.AddLog` 実装上 `LogType.Log` を含む
     全ログを記録し、未 `Expect` のログを LogType 不問で `UnhandledLogMessageException` として throw する
     （iter1 対応時の「LogType.Log は汚さない」という前提が実ソース — `LogScope.cs` — と矛盾）。
     `TowerCombatPlayModeTests`（6箇所）・`PausePanelPlayModeTests`（2箇所）・`GameHudPlayModeTests`（実行順依存）が
     `GameFlow.CurrentSaveData` 未設定のまま `BuildSpotController` を生成するため新ログで false-fail する。
     `LogWarning` への降格でも `NoUnexpectedReceived()` は LogType 不問のため不成立。
  2. [minor] acceptance「UPG を Lv 上限3まで購入できる」の Lv2→Lv3 成功境界が未検証
     （`MetaProgression_TryPurchaseUpgrade_Succeeds_...` は3回分の資金を用意しながら購入を Lv2 で停止）。

- 対応:
  1. **対応済み**。`Debug.Log` を撤回し、ログを出さない観測用プロパティ `public bool UsedDefaultSaveFallback` へ
     置き換えた（`Components/BuildSpotController.cs`）。これにより `TowerCombatPlayModeTests` /
     `PausePanelPlayModeTests` / `GameHudPlayModeTests`（いずれも他 story/ui-engineer 側の既存ファイルのため
     並走レーン規律により本 story では無変更）を一切変更せずに false-fail を解消する（そもそもログを
     出さないため実行順・フィクスチャに依存しない）。フラグの観測は本 story が新設した
     `BuildSpotControllerUpgradePlayModeTests.cs`（gameplay-engineer/S-14 の自ファイル）に
     `UsedDefaultSaveFallback` の true/false 両ケースのアサーションを追加して行う（`NoUpgrades_StartingGold_...`
     に true、`PurchasedUpg01Lv2_RunStart_...` に false）。iter1 #5 が求めていた「フォールバック発生の可視化」
     自体は維持（ログではなくプロパティ経由。将来 Ui 層で表示する余地を残す）。iteration 1 の「既存 PlayMode
     テストへの影響なし」記載は事実誤認だったため本記録で訂正する。
  2. **対応済み**。`ScaffoldSmokeTests.MetaProgression_TryPurchaseUpgrade_Succeeds_DeductsEssence_AndIncrementsLevel`
     に3回目の購入（Lv2→Lv3）を追加し、成功（`r3.Success`）・`afterLv3.upgStartingGoldLv == 3`・essence 減算を断言。
     acceptance 文言・既存の資金設定コメント（「Lv1〜3まで購入できる資金」）と整合させた。

## 検証（並走レーン規律: Unity をここでは起動しない）
- 参照実在の静的確認（Read/Grep）:
  - `BuildSpotController.cs`: `grep -n "Debug.Log"` で新設ログ行が除去され `Debug.LogError` 4箇所のみ残存する
    ことを確認。未使用となった旧ローカル変数 `usedFallback` の残存が無いことを確認。
  - `UsedDefaultSaveFallback` プロパティが `Components/BuildSpotController.cs` で定義され、
    `Assets/Tests/PlayMode/BuildSpotControllerUpgradePlayModeTests.cs` の2箇所（true/false 両ケース）から
    参照されていることを確認。
  - `MetaProgression.PurchaseUpgradeResult`/`.Success`/`.Data`・`GameConfig.Meta.UpgradeMaxLevel`（=3）・
    `GameConfig.Meta.UpgradePurchaseCostPerLevel`（=30）・`UpgradeKind.Upg01StartingGold` は既存定義のまま
    変更なしで `ScaffoldSmokeTests.cs` 追加断言から参照可能なことを確認。
  - `TowerCombatPlayModeTests.cs`・`PausePanelPlayModeTests.cs`・`GameHudPlayModeTests.cs` は本 iteration で
    無変更（Read のみ・並走レーン規律により編集せず）。
- 正式な EditMode/PlayMode 実行はレーン合流後のバッチ検証区間（tech-stack-unity.md「検証コマンド」）に委ねる。

## MAX_ITER(2) 到達
- CR-CODE iteration 2 も CONCERNS だったが review-loops.md の合格基準「findings解消 or 正当理由の明記」を
  iteration 2 対応で満たした（未対応finding無し）ため、MAX_ITER(2) 到達で status: done へ進める。
  未解決事項（Checkpoint 提示用）は本ファイル iteration 1 節の「未解決事項」を参照（`TowerSelectPanel`
  UPG-02 割引未反映・`MenuScreen` 二重実装 — いずれも ui-engineer レーン境界の技術的負債で本 iteration の
  対象外）。
