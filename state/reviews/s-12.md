# S-12 レビュー履歴 — Menu「アップグレード」タブ 購入操作

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-13T00:00:00Z
- 指摘要約（優先度順・severity: minor ×4）:
  1. [`Components/MenuController.cs:133` `UpgradeKindForFocusIndex` default分岐] LogError 後に `Attack` へフォールバックして購入を続行する。不正 `focusIndex` という配線バグ発生時に「誤った項目に通貨を消費する不可逆な誤トランザクション」へ変換される。`MenuNavigation.MoveFocus` のクランプとタブ切替時のフォーカスリセットにより到達不能なため minor だが、安全な縮退は購入中断（no-op）。
  2. [`Components/MenuController.cs` フォーカス行→UpgradeKind の対応] `GameConfig.Ui.MenuUpgradeLabels` の並び順・`MenuScreen.RenderUpgradeRow(0/1/2, …)` の直書き順・`MenuController` の switch の3箇所が暗黙に同期している前提で、単一情報源がない。将来 `MenuUpgradeLabels` を並べ替えると表示と購入対象が食い違う事故がエラーログなしに発生し得る。`GameConfig` に `UpgradeKind[]` の行順配列を定義し両者がそこから導出する形を推奨。
  3. [`Components/MenuController.cs:155-168` `PersistPurchase`] セーブ書込失敗は `Debug.LogError("[SaveCorruption] ...")` のみでプレイヤー可視の通知がない。`HealthComponent.CompleteRun` の既レビュー済み同一パターンの踏襲で、本 diff 単体では正当理由がコード内に明記済み。catch の型を `IOException`/`UnauthorizedAccessException` 等へ絞れるとなお良い、との提案。
  4. [`Assets/Tests/PlayMode/UpgradePurchaseSceneTests.cs`] テストカバレッジの隙間2点: (a) `Purchase_WithInsufficientBalance`/`Purchase_AtMaxLevel` が失敗購入時にセーブファイル未書込であることを検証していない。(b) UPG-02（移動速度、focus index 1）の UI 経由購入経路が未検証（index 0/2 のみ検証済み）。
- 対応:
  1. [default分岐フォールバック] 対応済み。`UpgradeKindForFocusIndex` の戻り値を `MetaProgression.UpgradeKind?` に変更し、範囲外 `focusIndex` は `Debug.LogError` の上 `null` を返す。`HandlePurchase` は `null` の場合 `TryPurchase` を呼ばず即 return（購入を一切実行しない）。誤った項目への通貨消費という不可逆トランザクションへの変換を根絶した。
  2. [単一情報源の欠如] 対応済み（範囲限定）。`GameConfig.Ui.MenuUpgradeRowKinds`（`MetaProgression.UpgradeKind[]`、Attack/MoveSpeed/MaxHp の順）を新設し、`MenuController.UpgradeKindForFocusIndex` はこの配列からの index lookup に置換した（switch 文を削除）。これにより購入経路の「行 index → kind」の情報源は1箇所（GameConfig）に集約された。ただし `Ui/MenuScreen.cs`（`RenderUpgradeRow` 呼び出しの 0/1/2 直書き）は ui-engineer 担当の UI 層ファイル（S-03 所有）であり、gameplay-engineer 担当の本 story のスコープ外（Must NOT Do: 他ストーリーの UI 層ファイル変更禁止）のため未変更。表示側（ラベル文言・並び）と購入側（kind 解決）の間の同期は依然2箇所に存在する — game-designer/ui-engineer への申し送り: `MenuScreen.RenderUpgradeRow` も `GameConfig.Ui.MenuUpgradeRowKinds`（または対応する新配列）から順序を導出するようリファクタする追補 story を検討されたい。
  3. [セーブ書込失敗の catch 型] 見送り。`FileSaveAdapter.Save` の呼び出しチェーン（`Directory.CreateDirectory`/`File.WriteAllText`/`File.Exists`/`File.Delete`/`File.Move`）は `IOException`/`UnauthorizedAccessException` 以外にも `ArgumentException`/`PathTooLongException`/`DirectoryNotFoundException`/`SecurityException` 等を投げ得る。本 catch は「セーブ書込障害で購入処理自体がクラッシュ/黙って落ちることを防ぐ」ための最終防波堤であり、型を絞ると同種の未処理例外リスクを再導入する。既レビュー済みの `HealthComponent.CompleteRun` と同一パターンでの一貫性を優先し、System.Exception のまま維持。理由をコードコメントに追記済み。プレイヤー可視通知の追加は指摘3自体が認める通り将来 story 扱い（見送り）。
  4. [テストカバレッジの隙間] 対応済み。(a) `Purchase_WithInsufficientBalance`/`Purchase_AtMaxLevel` の両テストに `Assert.IsFalse(File.Exists(reader.SavePath), …)` を追加し、失敗購入時にディスクへ一切書き込まれないことを検証。(b) 新規テスト `Purchase_MoveSpeedUpgrade_ViaUpgradeTabFocusIndex1_IncrementsMoveSpeedOnly` を追加し、S キー1回でフォーカスを index 1 に移動→Enter で UPG-02（移動速度）のみが購入され、Attack/MaxHp が変化しないことを検証（finding 2 の回帰=index マッピング崩れも検出できる）。
- 検証: EditMode `qa/evidence/editmode-results.xml`（80/80 passed, failed 0, exit 0）。PlayMode `qa/evidence/playmode-results.xml`（76/76 passed, failed 0, exit 0。`UpgradePurchaseSceneTests` 5件全通過、新規 `Purchase_MoveSpeedUpgrade_ViaUpgradeTabFocusIndex1_IncrementsMoveSpeedOnly` 含む）。build `ForgeBuild.BuildMac`（`game/Logs/build.log` に `Build succeeded`, exit 0, `game/Build/ForgeGame.app` 生成確認）。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-13T00:54:07Z
- 指摘要約: findings 0件
- 対応: 対応不要（承認）
