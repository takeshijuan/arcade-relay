## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-22T08:19:11Z
- 指摘要約:
  1. [minor] `WaveSpawnSystemTests.cs` — acceptance「ウェーブ間に WAVE_PREP_SEC の準備フェーズを挟む」が波間ギャップとして直接 assert されていない（波内出現間隔のみ検証）。
  2. [minor] `WaveSpawnSystemTests.cs` — `Tick_SpawnIntervalsWithinEachWave_MatchConfiguredMultiplier` の `while (!system.AllWavesSpawned)` に反復上限・経過時間ガードが無く、回帰時にハングしうる（診断情報ゼロで失敗）。
  3. [minor] `state/stories.yaml` — S-12 コミット(30b0988)に別レーン story S-16（ui-engineer）の status/note 変更が混入している（コミット衛生の指摘）。
- 対応:
  1. 対応済み。既に記録済みの `spawnTimestamps` を使い、`Tick_SpawnIntervalsWithinEachWave_MatchConfiguredMultiplier` 内で波 `w>0` ごとに「前波最終スポーン→当該波先頭スポーン」の時刻差を `Assert.AreEqual(GameConfig.Wave.WavePrepSec, waveGap, StepSeconds * 1.5f, ...)` で直接検証する assert を追加した。これにより WAVE_PREP_SEC を短縮する回帰（例: 5%短縮）は本テストで検出される。
  2. 対応済み。全ウェーブの理論所要時間 `Σ(WavePrepSec + intervalSec × 体数)` + バッファを `safetyLimitSec` として事前計算し、ループ先頭で `Assert.Less(elapsed, safetyLimitSec, ...)` を追加した。波遷移条件が壊れて `AllWavesSpawned` が立たなくなった場合、ハングせず明示的な失敗メッセージで即座に Fail する。
  3. 見送り。該当の混入は既に main 系列へコミット済み（30b0988）の履歴であり、本 iteration の対象は同コミット以降に加えた差分（テストファイルのみ）。今この時点で履歴を書き換える（rebase/filter で S-16 分をレーン先のコミットへ移動）のは、並走レーン・資産トラックが共有する index/履歴を破壊的に書き換えるリスクがあり（git safety protocol: 破壊的操作は明示指示がある場合のみ）、CR-CODE の対象diff（`WaveSpawnSystemTests.cs`）の品質にも影響しない。指摘は記録として残し、以後の自分のコミットでは `state/stories.yaml` への追記を自 story のブロックのみへの単独コミットとして厳守する（今回のfixコミットでも stories.yaml には触れない）。
- 検証: Unity は起動していない（並走レーン規律）。追加・変更した参照 — `GameConfig.Wave.WavePrepSec`（GameConfig.cs:67）、`GameConfig.Wave.SpawnIntervalBase`、`GameConfig.WaveComposition.Waves` / `WaveDef.MarauderCount` / `WarbeastCount` / `SpawnIntervalMultiplier`（GameConfig.cs:94-102）— は Grep で実在を確認済み。ファイル全体の中括弧バランスも機械確認（open=close=55）。コンパイル整合の最終確認はレーン合流後のバッチ検証区間に委ねる。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-22T08:25:45Z
- 指摘要約: なし（findings 0件）
- 対応: 該当なし
