# S-21 レビュー履歴（環境ビジュアル本仕上げ: 地形タイル取込・URPライト・固定俯瞰カメラ本配置）

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-22T07:35:07Z
- 指摘要約（優先度順）:
  1. **[major]** `EnvironmentView.CreateTileMaterial` のテクスチャ未ロード時フォールバックが完全無言（LogWarning すら無し）で、AssetKeys パス不一致・取込失敗による回復不能な障害が「未生成の想定内縮退」と機械的に区別できない。acceptance 中核の「IMG-01/02 をタイリング表示」を検証する条件付きテストも無い。
  2. **[minor]** `EnvironmentConfigTests.Camera_Composition_CoversFullPathAndBuildSpots` は水平方向の粗い距離チェックのみで、acceptance の「全 NUM_BUILD_SPOTS・コア(EndPoint) を1画面に収める」を実際には検証していない（テスト名が示唆する範囲と実装が不一致）。
  3. **[minor]** `ConfigureLighting()` の `FindFirstObjectByType<Light>()` が `LightType.Directional` でフィルタしていない。現状は Directional 1灯のみで動作するが、後続 story が Light を追加すると誤った Light を再構成しうる潜在的無言誤構成（重複指摘: 同ファイル・同箇所に2件）。
  4. **[minor]** S-21 が唯一の所有者と宣言する新設定数（`GameConfig.Camera`/`Lighting`/`Environment`/`Placeholder.GroundColor`・`PathColor`/`AssetKeys.TileGrass`・`TileDirtPath`）が本レビュー対象 diff に含まれず、別 story 名義のコミット `5b8f490`（S-15）に混入して先行コミットされている。story 単位 CR-CODE のトレーサビリティ欠落。
  5. **[minor]** `CreateTileMaterial` の `new Material(shader)` がどこでも `Destroy` されない。シーンアンロード（Game→Result→Game のリスタートループ、PlayMode テストのシーンロード反復）で無制限にリークする。
  6. **[minor]** `EnvironmentPlayModeTests.CaptureSceneScreenshot` が既存 `QaVisualEvidencePlayModeTests` の RenderTexture 撮影ヘルパをほぼ複製している。撮影方式変更時に片方だけ更新される証跡品質ドリフトのリスク。

## 対応（fix — 2026-07-22T07:35:07Z 実施）

1. **major #1 → 対応済み**: `EnvironmentView.CreateTileMaterial` のフォールバック分岐に1回だけ `Debug.LogWarning` を追加（未取込は想定内だが AssetKeys 不一致/取込失敗との区別ができるよう理由を明記）。ヘッダコメントも「無言フォールバック」から「LogWarning による正当縮退の文書化」へ更新。加えて `EnvironmentPlayModeTests.Game_TileTextures_LoadOrFallbackAreConsistent` を新設し、`Resources.Load` 成否で分岐して (a) 成功時: `mainTexture` 非null・`Texture2D` 型・`wrapMode==Repeat` を検証、(b) 失敗時: `material.color` がフォールバック色と一致することを検証。
2. **minor #2 → 対応済み**: `Camera_Composition_CoversFullPathAndBuildSpots` に実際の `Camera.WorldToViewportPoint` を用いた投影チェックを追加。`GameConfig.Camera` の値で一時 `Camera` を構成し、`GameConfig.Path.EndPoint`（コア）と `GameConfig.Build.SpotPositions` 全6点が viewport `[0,1]×[0,1]`・カメラ前方（`viewport.z>0`）に収まることを検証する `AssertInViewport` ヘルパーを追加。数値を Python で事前検算し全点が範囲内であることを確認済み（EndPoint viewport≈(0.79,0.50)、ビルドスポット viewport x∈[0.26,0.74]・y∈[0.44,0.55]）。
3. **minor #3 → 対応済み**（#3 と実質同一指摘のため一括対応）: `ConfigureLighting` を `FindDirectionalLight()`（`FindObjectsByType<Light>` で `LightType.Directional` を明示的に絞り込み）経由に変更。
4. **minor #4 → 見送り（記録のみ）**: 過去コミットの混入自体は取り消し不能（履歴の書き換えは禁止事項）。本記録により「`GameConfig` 新設ブロックは `5b8f490` 混入分を S-21 iter1 で検証済み」であることを明示し、以後の共有ファイル規律（コミット単位の story 分離）を徹底する注意点として残す。
5. **minor #5 → 対応済み**: `EnvironmentView` に `createdMaterials` リストを追加し、`CreateTiledPlane` で生成した `Material` を登録、`OnDestroy()` で `Destroy` するように変更。
6. **minor #6 → 見送り（意図的）**: `QaVisualEvidencePlayModeTests.cs` は qa-lead 所有のテストファイルであり、並走レーン規律（担当外ファイルへの編集は最小修正の例外時のみ）に基づき本 fix では編集しない。将来的な共有ヘルパー抽出は次回の担当領域整理時の推奨事項として記録する。

### 検証（並走レーン規律 — Unity 未起動）
Unity をここでは起動していない（単一インスタンスロック）。以下を Read/Grep で静的確認した:
- `GameConfig.Path.EndPoint` / `GameConfig.Build.SpotPositions` / `GameConfig.Placeholder.GroundColor` / `PathColor` / `AssetKeys.TileGrass` / `TileDirtPath` の実在（`game/Assets/Scripts/GameConfig.cs` に定義済み）。
- `EnvironmentView.cs` の新規参照（`System.Collections.Generic.List<Material>`、`Object.FindObjectsByType<Light>`、`Object.DestroyImmediate`）に型・オーバーロードの不整合が無いこと。
- `EnvironmentConfigTests.cs` / `EnvironmentPlayModeTests.cs` の新規アサーションが既存 using 宣言（`UnityEngine`、`NUnit.Framework`、`ForgeGame`）の範囲内で解決すること。
- viewport 投影の数値検算（Python 再現計算）で新規 EditMode アサーションが実際に通過する値であることを事前確認済み。
- エンジン起動を要する最終確認（EditMode/PlayMode 実行・build）はレーン合流後のバッチ検証区間に委ねる。

status: review のまま（次 iteration の CR-CODE 再判定待ち）

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-22T07:47:49Z
- 指摘要約（優先度順）:
  1. **[major]** `EnvironmentView.CreateTileMaterial` の未取込フォールバック `Debug.LogWarning`（iter1 major #1 対応分）は、IMG-01/02 が未取込の現状で Game シーンロード毎に2回発火する。ヘッダ/分岐コメントの「Unity Test Framework は Warning でテスト失敗しないため QA エラー0検査と両立」は既定挙動（Error のみ fail）にしか成立せず、tech-stack-unity.md「QA-PLAY の実行方法」観点2が正本手段に指定する `LogAssert.NoUnexpectedReceived()` は LogType 不問で未 Expect の全ログを fail させるため、Game シーンをロードして同 API を呼ぶ ui-engineer 所有の `GameHudPlayModeTests`（136行・188行）が false-fail する可能性がある事実誤認。
  2. **[minor]** `EnvironmentPlayModeTests.Game_TileTextures_LoadOrFallbackAreConsistent` が実装と同一の `Resources.Load`・同一キーで分岐するタウトロジーで、AssetKeys キー不一致/取込失敗クラスの欠陥を機械検出できない。また成功分岐が `mat.mainTexture` とロード済み `tex` の同一性・`mainTextureScale` を検証していない。
  3. **[minor]** `EnvironmentConfigTests.Camera_Composition_CoversFullPathAndBuildSpots` の viewport 投影チェックが `GameConfig.Path.EndPoint` と `SpotPositions` のみで `StartPoint`（経路西端）を含まず、将来のカメラ位置/yaw変更で経路始点の画面外れを検知できない。
  4. **[minor]** `EnvironmentView.cs` のレビュー指摘番号コメント（`iter1 major #3`／`minor #1/#4`／`minor #6`）と `state/reviews/s-21.md` iteration 1 の実際の指摘番号（`#1`／`#3`／`#5`）が不一致で、トレーサビリティを損なう。

## 対応（fix — 2026-07-22T07:47:49Z 実施）

1. **major #1 → 部分対応 + 既知リスク記録**: `EnvironmentView.cs` のヘッダコメントおよび `CreateTileMaterial` 分岐コメントの誤った両立根拠（「Warning はテスト失敗しない」）を削除し、`LogAssert.NoUnexpectedReceived()` が LogType 不問で fail する正しい挙動と、`GameHudPlayModeTests` との潜在衝突を明記した（コメント修正は本 story 担当範囲内のため対応済み）。一方、実際の解消手段——(a) IMG-01/02 の Integrate をバッチ検証より前に完了させる順序付け（workflow 側の責務）、または (b) `GameHudPlayModeTests`（ui-engineer 所有ファイル）への `LogAssert.Expect(LogType.Warning, ...)` 追加——はいずれも並走レーン規律上 S-21（gameplay-engineer・EnvironmentView 単体）の担当範囲外のため、本 fix では実施を見送り、既知リスクとしてこの iteration 2 記録とコード内コメントに残した。**エスカレーション**: レーン合流後のバッチ検証実施者は、PlayMode バッチ実行前に IMG-01/02 の Integrate 完了を確認するか、`GameHudPlayModeTests` に `LogAssert.Expect` を追加すること。LogWarning 自体（想定内縮退の可視化）は iter1 major #1 の趣旨に沿うため維持する（規約12: 正当縮退はヘッダ文書化+LogWarning）。
2. **minor #2 → 対応済み**: `Game_TileTextures_LoadOrFallbackAreConsistent` に (a) `Assert.AreEqual(tex, mat.mainTexture)` によるキー取り違え検知、(b) `CreateTileMaterial` と同一計算式の `mainTextureScale` 期待値アサーション（GroundPlane/PathStrip それぞれの widthX/widthZ を渡す）を追加。タウトロジー（Resources.Load キー不一致自体の検知不能）は機械検証の限界としてテストコメントに明記し、Integrate/QA 側の MANIFEST・`_generated` 実在突合に委ねる旨を記録した。
3. **minor #3 → 対応済み**: `Camera_Composition_CoversFullPathAndBuildSpots` に `AssertInViewport(cam, GameConfig.Path.StartPoint, "経路始点(StartPoint)")` を追加。
4. **minor #4 → 対応済み**: `EnvironmentView.cs` の指摘番号コメントを s-21.md iteration 1 の実際の番号（フォールバック警告=major #1、Light フィルタ=minor #3、マテリアルリーク=minor #5）に統一した。

### 検証（並走レーン規律 — Unity 未起動）
Unity をここでは起動していない（単一インスタンスロック）。以下を Read/Grep で静的確認した:
- `GameConfig.Environment.GroundMarginM` / `GroundWidthZFullM` / `PathStripWidthZM` / `TileWorldSizeM` / `GameConfig.Path.StartPoint` / `EndPoint` / `GameConfig.Wave.PathLengthM` の実在（`game/Assets/Scripts/GameConfig.cs` 245-248行、77-80行）。
- `EnvironmentPlayModeTests.cs` の新規アサーション（`Vector2`・`Assert.AreEqual(float, float, float)` 許容誤差オーバーロード・`Assert.AreEqual(Texture2D, Texture)` 参照比較）が既存 using 宣言（`NUnit.Framework`、`UnityEngine`）の範囲内で解決すること。
- `EnvironmentConfigTests.cs` の追加1行が既存 `AssertInViewport` シグネチャ（`Camera, Vector3, string`）と一致すること。
- `EnvironmentView.cs` のコメント変更はコード実行パスに影響しない（文字列リテラル・ロジックの変更無し）ことを確認。
- `GameHudPlayModeTests.cs` の136/188行 `LogAssert.NoUnexpectedReceived()` の実在と、`LoadGame()`（30行）が `GameConfig.Scenes.Game` をロードすることを確認（major #1 の衝突可能性の根拠）。
- エンジン起動を要する最終確認（EditMode/PlayMode 実行・build）はレーン合流後のバッチ検証区間に委ねる。

status: MAX_ITER(2) 到達のため done へ更新。未解決事項（major #1 の実解消手段（Integrate順序保証 or GameHudPlayModeTests への LogAssert.Expect 追加）は state/stories.yaml S-21 注記および本記録によりエスカレーション。
