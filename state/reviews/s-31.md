# S-31 レビュー履歴（ダッシュ移動のアフターイメージ（トレイル）VFX）

## CR-CODE iteration 1 — REJECT
- 日時: 2026-07-14T12:00:00Z
- 指摘要約（優先度順）:
  1. **[blocker]** `Components/DashTrailSpawner.cs`: `SpawnGhost()` は `ResolveVisual()` が HeroVisual 子未統合時に `_visualRoot = transform`（Player ルート自身）へフォールバックする状態で `Instantiate(_visualRoot.gameObject, ...)` を無条件実行しており、この縮退経路（`Editor/AssetIntegration.PatchPlayerVisual` の `[DEGRADED] Hero prefab not found` 分岐が出荷し得る documented なランタイム状態）では Player ルート全体（CapsuleCollider・PlayerController・AutoAttackDriver・HealthComponent・HeroFxController・DashTrailSpawner 自身）が丸ごと複製される。`PlayerController.Awake()` は `Instantiate()` 内で同期実行され `PlayerController.Instance` シングルトンを無条件に上書きするため、他の全システム（WaveSpawner/EnemyAgent ターゲティング・CrystalPickup・HealthComponent 無敵判定・GameHudController）を破壊し、複製された DashTrailSpawner は再帰的にさらなる複製を生成しうる。acceptance「ゴーストは当たり判定・無敵フラグ等の既存ロジックに一切関与しない純粋な視覚オブジェクト」に正面から違反。既存 PlayMode テスト（統合済み Game.unity をロード）はこの縮退経路を一切踏まないため検出できない。
  2. **[major]**（プロセス指摘）: 当初指定された対象コミット `e046f50` は S-29 の CR-CODE iter2 fix であり S-31 のコードを一切含まない。S-31 実装は git index にステージ済み（未コミット）の変更として存在し、レビューはステージ済みの実体に対して実施された。
  3. **[major]** `Components/DashTrailSpawner.cs`: 指摘1と同一問題の重複詳細化（黙殺フォールバックが acceptance 違反を招く経路の説明）。
  4. **[minor]** `Components/DashTrailSpawner.cs`: `CreateGhostMaterial()` の縮退ログ（L165 LogWarning / L170・L205 LogError 相当）に one-shot ゲートが無く、ゴースト1体×マテリアルスロット毎に毎回ログされうる（シェーダ欠落セッションでは1ダッシュあたり数十件）。既存規約（`WaveSpawner._prefabMissingAgentLogged` / `SfxLibrary._mixerErrorLogged` / `CrystalPickup._vfxLibraryMissingLogged`）と非対称。
  5. **[minor]** `Components/DashTrailSpawner.cs`: `Update()` の `while (DashTrailSystem.ShouldSpawnGhost(...))` ループが非正の `GameConfig.Fx.DashTrailSpawnIntervalS` に対して無防備（`ShouldSpawnGhost` 自体に非正区間ガードが無く、`timer -= interval` が非正 interval では timer を縮小しないため無限ループでフリーズしうる）。`Systems/HeroFxSystem.ComputeProgress` 等の既存 wiring-error guard パターンと非対称。
  6. **[minor]** `Tests/PlayMode/DashTrailSceneTests.cs`: `DashActive_SpawnsGhostsAtConfiguredCadence_AndStopsOutsideDash` は下限（>=3）のみで上限アサートが無く、`DashTrailSpawnIntervalS` を完全に無視して毎フレーム生成する回帰でも合格してしまう。
- 対応:
  1. **対応した**（blocker）。`Components/DashTrailSpawner.cs` の `SpawnGhost()` を分岐: `_visualRoot == transform`（フォールバック = Player ルート自身）の場合は新設した `BuildPlaceholderGhost()` を使う。これは Player ルートを `Instantiate` せず、`MeshFilter.sharedMesh` のみを新規のベア `GameObject` へコピーして `MeshFilter`+`MeshRenderer` を付与する（プレースホルダのカプセルは `MeshFilter`/`MeshRenderer` の1ペアのみで子を持たないため単一ノードコピーで十分）。HeroVisual 統合済みの分岐（`_visualRoot != transform`）は既存の `Instantiate(_visualRoot.gameObject, ...)` のまま変更していない（Hero プレハブは MonoBehaviour を一切持たない純粋なアート資産であり、この分岐は既存テストで検証済み・安全なため）。これにより `PlayerController.Awake()` を含むいかなる gameplay MonoBehaviour/Collider も Instantiate 対象に含まれなくなり、`PlayerController.Instance` が上書きされることはなくなる。新規 PlayMode 回帰テスト `DashActive_PreIntegrationPlaceholder_GhostsCarryNoGameplayComponents`（`Tests/PlayMode/DashTrailSceneTests.cs`）を追加し、`Editor/SceneWiring.WireGame` の pre-integration 配線（`CreatePrimitive(Capsule)` + PlayerController/AutoAttackDriver/HealthComponent/HeroFxController/DashTrailSpawner）を再現した上でダッシュを発動させ、`PlayerController.Instance` が不変であること・`PlayerController`/`DashTrailSpawner`/`CapsuleCollider`/`AutoAttackDriver`/`HealthComponent`/`HeroFxController` のシーン内個数がゴースト生成前後で増加しないことを機械検証する。
  2. **対応した**（プロセス指摘）。本 fix はステージ済み S-31 差分（`Systems/DashTrailSystem.cs` / `Components/DashTrailSpawner.cs` / `Components/DashTrailGhost.cs` / `GameConfig.cs` Fx 節 / `Editor/SceneWiring.cs` / `Tests/EditMode/DashTrailSystemTests.cs` / `Tests/PlayMode/DashTrailSceneTests.cs`）に対して実施し、本コミットで正しい S-31 リビジョンとして記録する。
  3. **対応した**（major）。指摘1と同一の修正で解消（上記参照）。
  4. **対応した**（minor）。`Components/DashTrailSpawner.cs` に静的 one-shot フラグ `_urpShaderMissingWarned` / `_noGhostShaderFoundLogged` / `_unknownShaderPropertiesLogged`（`CrystalPickup._vfxLibraryMissingLogged` と同型: `[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.SubsystemRegistration)]` でセッション毎にリセット）を追加し、`CreateGhostMaterial()` の3箇所のログをそれぞれ1回のみに制限した。
  5. **対応した**（minor）。`Systems/DashTrailSystem.ShouldSpawnGhost` に非正 `spawnIntervalSeconds` の wiring-error guard（`Systems/HeroFxSystem.ComputeProgress` と同型）を追加し、`spawnIntervalSeconds <= 0f` なら常に `false` を返すようにした（`Components/DashTrailSpawner.Update()` 側のループ形状は変更していない — 呼び出し元を直さず発生源の pure 関数で閉じる方針）。`Tests/EditMode/DashTrailSystemTests.cs` に `ShouldSpawnGhost_NonPositiveInterval_AlwaysFalse_NoInfiniteCatchUpLoop` を追加し機械検証した。
  6. **対応した**（minor）。`Tests/PlayMode/DashTrailSceneTests.cs` の `DashActive_SpawnsGhostsAtConfiguredCadence_AndStopsOutsideDash` に `Mathf.CeilToInt(GameConfig.Player.DashDuration / GameConfig.Fx.DashTrailSpawnIntervalS) + 3`（batchmode フレームタイミング分散の余裕込み）を上限とする `Assert.LessOrEqual` を追加した。
- 検証: EditMode `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results-s31-crfix.xml -logFile qa/evidence/editmode-s31-crfix.log` exit 0、166/166 pass, failed 0。PlayMode `"$UNITY" -batchmode -projectPath game -runTests -testPlatform PlayMode -testResults qa/evidence/playmode-results-s31-crfix2.xml -logFile qa/evidence/playmode-s31-crfix2.log` exit 0、134/134 pass, failed 0（新規テスト2本を含む。初回実行は新規テストの `CapsuleCollider` 絶対数アサートが WaveSpawner が並行スポーンした敵のコライダーを誤検出して1件失敗したため、絶対数比較をダッシュ発動前後の差分比較へ修正し再実行して解消）。`ForgeGame.EditorTools.ForgeBuild.BuildMac` exit 0（`Build succeeded: .../game/Build/ForgeGame.app`、`game/Logs/build-s31-crfix.log`）。未対応 finding は無し（全6件 対応済み）。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-14T12:52:12Z
- 指摘要約（優先度順、findings JSON より）:
  1. **[minor]** `Components/DashTrailSpawner.cs`: `BuildPlaceholderGhost()`（`_visualRoot == transform` の pre-integration/degraded フォールバック経路）が `AddComponent<MeshRenderer>()` で作る MeshRenderer はマテリアルスロット0のまま生成される。`ApplyGhostMaterials()` は `slotCount == 0` のレンダラーを `continue` でスキップするため、プレースホルダのゴーストは生成・カウント（`GhostsSpawnedForTests++`）されるが半透明ティントマテリアルが一切適用されず、実質的に不可視のままダッシュトレイル効果が消える。PlayMode テストはゴースト数のみを検証しており可視性は未検証のため検出されていなかった。
  2. **[minor]** `Components/DashTrailSpawner.cs`: `SpawnGhost` の `if (_visualRoot == null) return;`（L114）と `if (ghost == null) return;`（L138、`BuildPlaceholderGhost` がメッシュ欠落時に null を返す経路）が無言でスキップされ続け、この状態に入るとダッシュトレイル機能全体が誰にも気付かれず恒久的に no-op になる。rules/unity-code.md 規約12（配線破損は無言 return で隠さず1回 LogError/LogWarning）と同ファイル内の他縮退路（シェーダ欠落は one-shot 済み）に対して非対称。
  3. **[minor]** `Tests/PlayMode/DashTrailSceneTests.cs`: acceptance「各ゴーストが DashTrailGhostLifetimeS 経過後に消滅すること」の『経過後』（＝それより早く消えない）側が未検証。`Ghost_FadesAndSelfDestructs_AfterLifetime` は最終的に `LiveGhostCount()==0` になることしか主張せず、寿命が誤配線され即消滅する回帰でも現行アサートは全て合格してしまう。
  4. **[minor]**（プロセス指摘）: iteration 1 fix コミット `dfc2c1c`（「S-31: fix CR-CODE iteration 1」）に S-30 の断片（`Editor/SceneWiring.cs` の `WireTitleUiFrameKit`/`WireMenuUiFrameKit`/`WireGameUiFrameKit`/`WireResultUiFrameKit`/`TryBakeSprite`、`GameConfig.cs` の S-30 Ui レイアウト定数・`AssetKeys.UiFrameKit*`）が混入しており、対応する `Ui/*.cs` のシリアライズフィールドと `ui-frame-*.png` 派生スプライトは本コミット時点で未コミットの working tree 変更のまま。story 単位のレビュー/リバート単位を崩す。
- 対応:
  1. **対応した**（minor）。`BuildPlaceholderGhost()` で `_visualRoot.GetComponent<MeshRenderer>()`（プレースホルダのカプセル自身の Renderer）のマテリアルスロット数（無ければ1にフォールバック）を求め、新規 `MeshRenderer.sharedMaterials` をそのスロット数で初期化するよう変更。これにより `ApplyGhostMaterials` が `slotCount > 0` を見てゴーストマテリアルを生成・適用できるようになった。
  2. **対応した**（minor）。`_urpShaderMissingWarned` 等と同型の static one-shot フラグ `_visualRootMissingWarned` / `_placeholderGhostUnavailableWarned`（`[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.SubsystemRegistration)]` でセッション毎リセット）を追加し、`SpawnGhost()` の両 `return;` 経路にそれぞれ1回のみの `Debug.LogWarning` を追加した（`[Wiring] DashTrailSpawner: visual root is null — dash trail ghost spawn skipped.` / `[Wiring] DashTrailSpawner: placeholder visual has no MeshFilter/sharedMesh — dash trail ghosts suppressed.`）。
  3. **対応した**（minor）。`Ghost_FadesAndSelfDestructs_AfterLifetime` に、ダッシュ終了直後（`DashTrailGhostLifetimeS`(0.25s) > `DashDuration`(0.2s) のため最後に生成されたゴーストもまだ生存しているタイミング）に `Assert.Greater(LiveGhostCount(), 0, ...)` を1本追加し、寿命が誤って短い値に配線される回帰（例: `DashTrailSpawnIntervalS` との取り違え）を検出できるようにした。
  4. **見送り（対応しない）**（minor・プロセス指摘）。技術的指摘自体は妥当と認めるが、本 story（S-31・gameplay-engineer 担当）のコード自体に問題は無く、修正には (a) 既にリモート相当として扱われている過去コミット `dfc2c1c` の書き換え（rebase/amend — CLAUDE.md のGit Safety Protocolにより破壊的操作はユーザーの明示要求が無い限り禁止、かつ本セッションの指示は「新規コミットを作成」であり `--amend` は明示不許可）、または (b) 現在 working tree に残存する S-30 の残余（`Ui/*.cs` のシリアライズフィールド・`ui-frame-*.png` 派生スプライト等）を本セッションでコミットすること、のいずれかが必要になる。(b) は ui-engineer 担当の S-30 のスコープであり、gameplay-engineer の Must NOT Do（担当外ストーリーのファイル変更・他 agent の成果物のコミット責任の肩代わり禁止）に抵触するため行わない。是正は S-30 側の担当者が `state/reviews/s-30.md` の申し送り（1Password コミットブロッカー解消後にステージ済み一式をそのままコミット）を実行した時点で自然に解消する（`Ui/*.cs` 側が揃った独立コミットとして記録されるべき）。本 finding は state/active.md の未解決事項として申し送る。
- 検証: EditMode `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results-s31-crfix2.xml -logFile qa/evidence/editmode-s31-crfix2.log` exit 0、結果XML 166/166 pass, failed 0。PlayMode `"$UNITY" -batchmode -projectPath game -runTests -testPlatform PlayMode -testResults qa/evidence/playmode-results-s31-crfix3.xml -logFile qa/evidence/playmode-s31-crfix3.log` exit 0、結果XML 134/134 pass, failed 0（新規アサート1本を含む既存4テスト全て green）。`ForgeGame.EditorTools.ForgeBuild.BuildMac` exit 0（`Build succeeded: .../game/Build/ForgeGame.app`、`game/Logs/build-s31-crfix2.log`）。
- **未対応 finding**: finding 4（minor・commit衛生のプロセス指摘）は上記理由によりスコープ外につき未対応のまま。**MAX_ITER(2) 到達によりエスカレーション** — `state/stories.yaml` の S-31 を `status: done` に更新し、未解決の finding 4（S-30 側のコミット完了待ち）を Checkpoint C で人間へ提示する（`.claude/docs/review-loops.md` の MAX_ITER到達時の扱いに従う）。
- **コミットブロッカー（継続・S-30/S-23と同一根本原因）**: 上記 fix 一式（`Components/DashTrailSpawner.cs`・`Tests/PlayMode/DashTrailSceneTests.cs`・`state/reviews/s-31.md`・`state/stories.yaml`・`qa/evidence/{editmode,playmode}-results-s31-crfix{2,3}.xml`）のみを対象パスとして `git add` 後、`git commit -- <該当パス>` を2回試行（間に2秒待機して1回リトライ、`index.lock` 以外の要因のため一般的なリトライ手順を準用）したが、いずれも `error: 1Password: agent returned an error` / `fatal: failed to write commit object`（exit 128）で失敗した。診断: `op whoami` → `account is not signed in`、`ssh-add -L` は `user.signingkey`（fingerprint 対応鍵）ではなく無関係な `redacted@example.com` 鍵のみを返す — state/active.md 記載の S-30/S-31 iteration 1 で継続報告済みの 1Password SSH エージェント認可未確立と同一の根本原因。CLAUDE.md のグローバル規約（署名バイパスはユーザーの明示的要求が無い限り禁止）に従い `--no-gpg-sign` 等のバイパスは行っていない。`git rev-parse HEAD` は `dfc2c1cef443fa160bb5b890ac3b61a43b22b402`（S-31 iteration 1 fix）のまま変化なし。本 iteration 2 の fix コード・テスト・stories.yaml/s-31.md の更新は全て worktree に適用済み（`git add` 済みだが未コミット）で、次に 1Password のインタラクティブ認可が復旧可能な人間/セッションが、追加のコード変更なしに上記対象パスのみをそのままコミットすること。
