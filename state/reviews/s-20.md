## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-22T11:14:00Z
- 指摘要約（優先度順）:
  1. [major] `MenuScreen.cs` — `GameFlow.SaveFailed` 通知の黙殺。`GameFlow.cs` の前回 CR-CODE 指摘（★TODO(S-20)）は S-20 実装時に Result/Menu UI が `GameFlow.SaveFailed` を購読して保存失敗を表示することを明示要求していたが、本 commit は recovered トーストのみ実装し `SaveFailed` の消費者がゼロのままだった。`Persistence.Save` が例外で失敗してもプレイヤーに通常画面が表示され進行喪失に気付けない（サイレント失敗）。
  2. [minor] `MenuScreen.cs` — acceptance「一度表示」との齟齬。`GameFlow.Recovered` を消費しないため、正準フロー Result→Menu で戻るたびに復旧トーストが再表示され続ける（「また破損した」と誤認させる余地がある）。
  3. [minor] `ResultScreen.cs` — 文言・判定式のコピペ二重化。復旧トースト文言が `TitleScreen.cs`/`MenuScreen.cs` に同一リテラルで二重定義、ベストクリアタイムの判定式+書式も `MenuScreen.cs`/`ResultScreen.cs` に二重化しており drift が機械検知されない。
  4. [minor] `ResultScreenPlayModeTests.cs`（および `MenuScreenPlayModeTests.cs`）— 新規テストが `FindFirstObjectByType`/`Transform.Find` の戻り値を null 確認なしで deref しており、UI 構築退行時に actionable でない `NullReferenceException` で落ちる。
- 対応:
  1. 対応済み。`Ui/MenuScreen.cs` と `Ui/ResultScreen.cs` に `SaveFailedNoticeText` を追加し `GameFlow.SaveFailed` を表示するようにした。Menu は `RecoveryNoticeText` と同じ行にサブ行として重ね（`GameConfig.Ui.MenuSaveFailedNoticeOffsetY` 新規追加。Menu は既に末尾行が anchorY≒0.05 まで詰まっており `NextY()` で1行追加すると画面外へ落ちるため）、`BuildUi()` 初期表示に加え `PersistPurchase()`（UPG購入の保存試行）の都度 `RefreshSaveFailedNotice()` で追随させる。Result は `RunOutcomeController.FinalizeRun` が Result 遷移前に一度だけ保存するため `BuildUi()` の1回読みで足りる（動的更新不要）。`GameFlow.SaveFailed` は「未解決の保存失敗」を表す継続ステータスとして扱い、表示のたびに消費はしない（次の保存成功で `GameFlow.SetSaveFailed(false)` されるまで表示し続ける — Recovered とは異なる性質のため #2 とは別処理とした）。PlayMode テストを追加（`MenuScreenPlayModeTests.SaveFailedFlag_ShowsSaveFailedNotice_OnLoad` / `NotSaveFailed_HidesSaveFailedNotice_OnLoad` / `ClickUpg01BuyButton_WhenSaveThrows_ShowsSaveFailedNotice`、`ResultScreenPlayModeTests.SaveFailedFlag_ShowsSaveFailedNotice` / `NotSaveFailed_HidesSaveFailedNotice`）。
  2. 対応済み。`MenuScreen.BuildUi()` で復旧トースト表示後に `GameFlow.SetRecovered(false)` を呼び消費するようにした。正準フロー `Boot→Title→Menu` は必ず Title を経由してから最初の Menu に到達するため、Menu 側でのみ消費すれば「Title・Menu 双方で最低1回は表示」という契約（ファイル冒頭コメント）は崩れない（Title 側は消費しない設計のまま）。TitleScreenPlayModeTests / MenuScreenPlayModeTests の既存 Recovered 系テストへの副作用は無いことを確認済み（テストは表示直後の状態のみを assert しており、TearDown が別途 `SetRecovered(false)` するため相互汚染も無い）。
  3. 対応済み。新規 `Ui/UiText.cs`（`SaveCorruptionRecoveredMessage` 定数・`BuildSaveFailedMessage(contextLabel)`・`FormatBestClearTime(bestClearTimeSec)`）を追加し、`TitleScreen.cs`/`MenuScreen.cs`/`ResultScreen.cs` の該当箇所をこれの呼び出しへ置き換えた。UiCanvasHelper と同じ「UI 層の薄い共通ヘルパ」の位置づけで、状態を持たない静的クラス。
  4. 対応済み。`ResultScreenPlayModeTests.UnrecordedBestClearTime_ShowsDoubleDash` / `RecordedBestClearTime_ShowsFormattedSeconds`、`MenuScreenPlayModeTests.UnrecordedBestClearTime_ShowsDoubleDash` に `Assert.IsNotNull`（対象コンポーネント・`Transform.Find` の中間結果・`GetComponent<Text>()` 各段）を追加し、失敗時に actionable なメッセージで落ちるようにした。
- 検証: Unity をここでは起動しない（並走レーン規律 — エンジン検証はレーン合流後のバッチ検証区間に委譲）。代わりに Read/Grep で以下を静的確認: `GameFlow.SaveFailed`/`SetSaveFailed`/`SetRecovered`（`GameFlow.cs`）の実在とシグネチャ一致、`GameConfig.Ui.ResultLossColor`/`MenuSaveFailedNoticeOffsetY`（新規・追記のみ）の実在、`ISaveStore`/`LoadResult`（`ForgeGame.Persistence`）と `SaveData.CreateDefault()`（`ForgeGame.Systems.Meta`）のシグネチャ一致（`MenuScreenPlayModeTests.ThrowingSaveStore` の実装が既存 `RunOutcomePlayModeTests.ThrowingSaveStore` と同型であることを比較確認）、`UiText` の呼び出し側（3ファイル）が全て `namespace ForgeGame.Ui` 内にあり追加 `using` 不要であることの確認、`MenuScreen.CreateText`/`ResultScreen.CreateText` ヘルパのシグネチャ変更（`offsetY` オプション引数追加）が既存呼び出し元の位置引数に影響しないこと（既存呼び出しは全て名前付き引数 `offsetX:`/位置引数を変更していない）、新規/変更ファイルの中括弧対応（`grep -o "{"`/`"}"` の個数一致）を機械確認。新規ファイル `Ui/UiText.cs` の `.meta`（`fileFormatVersion: 2` + 新規 GUID）を既存ファイルと同じ最小形式で追加。コンパイル整合はレーン合流後のバッチ検証（`tech-stack-unity.md`）が最終確認する。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-22T11:28:23Z
- 指摘要約（優先度順）:
  1. [major] `Tests/PlayMode/MenuScreenPlayModeTests.cs` — 新規テスト `ClickUpg01BuyButton_WhenSaveThrows_ShowsSaveFailedNotice` に `LogAssert.Expect` が無く、`PersistPurchase` の catch が必ず発火させる `Debug.LogError("[SaveFailure] MenuScreen failed to persist SaveData...")` により、Unity Test Framework の既定（予期しないErrorログ＝テスト失敗）でこの acceptance 検証テスト自体がバッチ検証で確実に失敗する。既存の `RunOutcomePlayModeTests` の同型テストには `LogAssert.Expect` が正しく置かれているのに本テストのみ欠落。
  2. [minor] `Ui/MenuScreen.cs` — `SaveFailedNoticeText`（offsetY=-34px）が `PlayButton` 矩形と静的計算上 ~40px 幅で重なり、保存失敗通知の可読性を損なう恐れ（cosmetic。機能影響は無い）。
  3. [minor] `Tests/PlayMode/MenuScreenPlayModeTests.cs` — 同テスト内 `Find("UPG-01BuyButton").GetComponent<RectTransform>()` を null 確認なしで deref しており、UI 構築退行時に actionable でない NullReferenceException で落ちる（iteration1 #4 で対応した既存テストと同じ非ハードニングパターンの再導入）。
  4. [minor]（#2と同一事象・重複指摘）`MenuSaveFailedNoticeOffsetY` の静的重なり計算の詳細補足。
  5. [minor] `Tests/PlayMode/MenuScreenPlayModeTests.cs` — `GameFlow.Recovered` の Menu 側消費（BuildUi 内 `SetRecovered(false)`）に回帰テストが無く、消費タイミングの将来退行が機械検知されない。
- 対応:
  1. 対応済み。クリック発行（`Move`/`Press`）の直前に `LogAssert.Expect(LogType.Error, new Regex(@"^\[SaveFailure\] MenuScreen failed to persist SaveData"))` を追加した（`RunOutcomePlayModeTests` の同型テストと同一パターン）。`using System.Text.RegularExpressions;` を新規追加。
  2/4. 対応済み。`GameConfig.Ui.MenuSaveFailedNoticeOffsetY` を `-34f` → `-4f` に変更した。この密詰めレイアウトでは行間隔48.6px（=ReferenceHeight1080×MenuRowStepDenseY0.045）に対しテキスト半分22.4px+ボタン半分32px=54.4px>48.6pxのため、RecoveryNoticeText行とPlayButton行の間には元々空きが無く（RecoveryNoticeText自身もPlayButtonと約5.8px重なっており2回のCR-CODEで未指摘=許容基準値と判断）、どんな高さのボックスを追加してもPlayButtonまたはRecoveryNoticeTextの少なくとも一方と数学的に重ならない配置は不可能と判明した（詳細は `GameConfig.cs` の該当コメント）。NextY()で専用行を追加する案（レビュー指摘の代替案）は、Menu末尾行(BackToTitleButton)が既にanchorY≈0.05まで詰まっており、1行追加するとanchorY≈0.005となりボタン矩形が画面下端からはみ出す（元のiteration1コメントの懸念どおり悪化する）ため見送った。offsetY=-4pxはPlayButtonとの重なりを基準値相当の約9.8px（-34pxの約40pxから75%削減）まで縮小する。この定数は `MenuScreen.cs` 内でのみ使用される本story専有の新規定数（他レーン・他ファイルからの参照なしをgrep確認済み）のため値変更は自story範囲内。
  3. 対応済み。`buyButtonTransform = menuScreen.OutgamePanel.Find("UPG-01BuyButton")` と `buyButtonRect = buyButtonTransform.GetComponent<RectTransform>()` の各段に `Assert.IsNotNull` を追加した。
  5. 対応済み。回帰テストを2件追加: `RecoveredFlag_ConsumedAfterFirstMenuLoad_NotShownOnSecondLoad`（recovered=true で Menu を2回ロードし、2回目はトースト非表示・`GameFlow.Recovered`が消費されていることを検証）、`RecoveredFlag_ShownOnceEachInTitleThenMenu_TitleToMenuOrder`（Title→Menu の順で Title は消費せず、両方で最低1回ずつ表示されることを検証）。
- 検証: Unity をここでは起動しない（並走レーン規律 — エンジン検証はレーン合流後のバッチ検証区間に委譲）。代わりに Read/Grep で以下を静的確認: `GameConfig.Ui.MenuSaveFailedNoticeOffsetY` は `MenuScreen.cs` のみが参照（他レーン・他ファイルからの参照なし）。`LogType`/`LogAssert`（`UnityEngine`/`UnityEngine.TestTools`。既存 using で解決）・`Regex`（新規 `using System.Text.RegularExpressions;` 追加）・`ForgeGame.Ui.TitleScreen`（`Ui/TitleScreen.cs`、`namespace ForgeGame.Ui` に既存 `using ForgeGame.Ui;` で解決、追加 using 不要）・`GameConfig.Scenes.Title`（`GameConfig.cs` 既存定数）・`GameFlow.Recovered`/`GameFlow.SetRecovered`（`GameFlow.cs` 既存メンバ）・`TitleScreen.RecoveryNoticeVisible` プロパティの実在とシグネチャ一致を確認。編集3ファイル（`GameConfig.cs`/`MenuScreen.cs`/`MenuScreenPlayModeTests.cs`）の中括弧対応を `grep -o "{"`/`"}"` の個数一致で機械確認（各ファイルで一致）。コンパイル整合はレーン合流後のバッチ検証（`tech-stack-unity.md`）が最終確認する。
- MAX_ITER(2)到達。全5件のfindingに対応済み（黙殺なし）のため state/stories.yaml の S-20 を status: done に更新する。
