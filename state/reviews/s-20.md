# S-20 レビュー履歴（アリーナ環境ビジュアル + クリスタル発光 + 全体調整）

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-13T14:00:00Z
- 指摘要約（優先度順）:
  1. **[major] `game/Assets/Scripts/Components/ArenaEnvironment.cs`** — 配線破損パス（Renderer 欠落 `:74`／hex パース失敗 `:115`／URP・Standard 両シェーダー欠落 `:126`）が `Debug.LogWarning` のままで、rule 12（配線破損は LogError）に反する。EnemyAgent.cs（S-14 iter1 major）で同一3パターンが既に LogError へ昇格済みの再発。
  2. **[major] `qa/evidence/arena-four-direction.png`** — acceptance「四方の敵を同時視認できる可読性」が証跡上未達（南側の敵が画角外）。固定カメラ定数（CAMERA_HEIGHT/PITCH/FOV）は gdd 由来で conventions.md §2 により engineer 単独変更禁止のため見送り＋escalation は正当だが、acceptance 自体は現時点で未充足。Checkpoint C へ継続提示必須。
  3. **[minor] `ArenaEnvironmentSceneTests.cs`** — P-01 の camera frustum 非対称性は本 diff のコード欠陥ではなく、game-designer/creative-director への正当な escalation（対応済み・非defect）。
  4. **[minor] `ArenaEnvironmentSceneTests.cs:184`** — `Capture_FourDirectionReadability_Evidence` のコメント「inside the spawn ring, clearly visible on screen」が南側について事実と異なる（南側は画面外に切れる）。
  5. **[minor] `GameConfig.cs` `SpawnRingColor`** — `#8B12A5`（敵主色）をそのまま流用しており、敵がスポーンリングを横切る瞬間に同色相で輪郭コントラストが低下する懸念。art-bible.json の locked style_block からの逸脱。engineer 単独ではなく art-reviewer/game-designer 確認が望ましい。

- 対応（fix担当・gameplay-engineer・2026-07-13）:
  1. **対応**: `ApplyMatteMaterial` の Renderer 欠落パス（旧`:76`）、`CreateUrpSafeMaterial` の hex パース失敗パス（旧`:117`。hex 文字列自体をメッセージに含めるよう追加修正）、両シェーダー欠落パス（旧`:128`）の3箇所を `Debug.LogError` に昇格。URP→Standard フォールバック**試行**行（旧`:123`）は正当な縮退のため `Debug.LogWarning` のまま維持。ヘッダコメント（旧`:70`）の rule 10/12 引用が終端失敗パスに誤って掛かっていた点も、フォールバック試行のみを指す旨に修正。
  2. **見送り（正当理由）**: カメラ定数（`GameConfig.Camera.Height/PitchDeg/Fov`）は gdd 由来の初期値であり conventions.md §2「初期値は必ず gdd 記載値を写す（技術都合で変えない。変更が必要なら game-designer へ提案）」により gameplay-engineer 単独では変更不可。判断根拠・手計算（南側可視限界 z≈-6.5m 等）・GDD調整レンジ内での試算（HEIGHT→18/PITCH→60/FOV→55で z≈-9.6mまで改善見込み）・実装見送り理由の詳細は `state/active.md`（S-20 セクション「判断根拠（記録）— カメラ調整レンジは今回変更しなかった」）に既に記録済み。本 iteration では新規対応を追加せず、Checkpoint C への継続提示を `state/active.md` の「次: CR-CODE 待ち」行に維持。game-designer/creative-director の裁定（カメラ調整レンジ内の変更 or acceptance 再定義）待ち。
  3. **対応不要（reviewer 自身が非defect と判定済み）**: escalation の適切性を確認するのみ。追加対応なし。
  4. **対応**: `ringRadius` 計算部のコメントを、南側可視限界の非対称性を明示する記述に書き換え（「四方とも画面内に収まる」という主張を撤回し、SpawnRadius をそのまま縮尺再現する意図・既知の非対称性・game-designer/creative-director への申し送り事項である旨を明記）。証跡の配置半径自体は実ゲームプレイの `SpawnRadius` をそのまま反映する現状維持を選択（配置半径を可視域内に縮めて見た目を取り繕うと、実際のゲームプレイ条件を証跡が偽って表現することになり finding #2 の是正意図に反するため）。
  5. **対応**: `SpawnRingColor` を敵主色 `#8B12A5` から `Ui.ColorTextSecondary`（`#3488D1`。ヒーロー青のUI補助テキスト用途であり、3D空間内のキャラクターシルエット役割としては未使用）へ暫定変更。床（緑・約107°差）・敵（紫・約82°差）の双方から十分な色相差を確保。art-bible.json の palette は全13色が役割1対1割当て済みで「空き」の色が無いため、キャラクターシルエット役割と衝突しないUI用途色を暫定採用した旨と、恒久確定は art-reviewer/game-designer 確認待ちである旨を `GameConfig.cs` のコメントに明記。

- 再検証:
  - EditMode: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results.xml -logFile -` → exit 0、結果XML `total=125 passed=125 failed=0 result=Passed`
  - Build: `"$UNITY" -batchmode -projectPath game -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit -logFile game/Logs/build.log` → exit 0、ログに `Build succeeded: .../game/Build/ForgeGame.app`

- status: `state/stories.yaml` S-20 は `review` のまま維持（次 CR-CODE iteration の再判定待ち）。未解決の finding #2（四方可読性の acceptance 未充足）は Checkpoint C まで必ず継続提示すること。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-13T14:10:00Z
- 指摘要約（優先度順）:
  1. **[major] `ArenaEnvironmentSceneTests.cs`** — acceptance「四方の敵を同時視認できる可読性」がコード上未達のまま。固定カメラ（CAMERA_HEIGHT=14/PITCH=65°/FOV=50°）では南側(-Z)可視限界が z≈-6.5m しかなく、南側の敵(z=-8.1m)は画角外に切れる。gameplay-engineer 単独ではカメラ定数を変更できず見送り+escalationは正当だが、acceptance自体は未充足。Checkpoint C で game-designer/creative-director の裁定が必須。
  2. **[minor] `ArenaEnvironmentSceneTests.cs`** — `ringRadius = SpawnRadius*0.6`(=8.1m) の配置なのに、コメントが「SpawnRadius(=13.5m)の実配置をそのまま再現」「スポーンリング上」と記述し実際の配置半径と食い違う。
  3. **[major] `GameConfig.cs` `SpawnRingColor`** — 暫定色 `Ui.ColorTextSecondary`(#3488D1) を「非ロール色」と正当化するコメントが事実誤認。art-bible.md パレット表では #3488D1 は「プレイヤー主色」であり、`SceneWiring.cs` がプレイヤープレースホルダ体色として現に使用中。iter1で是正した「敵ロール色の流用」と同種の欠陥（プレイヤーロール色の流用）が再発。
  4. **[major] `GameConfig.cs`** — 再検証が EditMode+build のみで PlayMode 未実行のため、`qa/evidence/arena-four-direction.png` がコミットより古い旧色（敵主色 #8B12A5）のリングを写したまま。視覚変更の目的（コントラスト改善）を目視検証していない。
  5. **[minor] `GameConfig.cs`** — `SpawnRingColor = Ui.ColorTextSecondary` のエイリアスにより、UIテキスト色・HUDバー色・プレイヤー体色・スポーンリング色という無関係な4用途が1定数に結合され、片方の変更が意図せず他方に波及するリスク。

- 対応（fix担当・gameplay-engineer・2026-07-13）:
  1. **見送り（正当理由）**: iteration 1 と同一の理由（conventions.md §2「初期値は必ずgdd記載値を写す。技術都合で変えない。変更が必要ならgame-designerへ提案」）により、gameplay-engineer 単独ではカメラ定数（`GameConfig.Camera.Height/PitchDeg/Fov`）を変更しない。`state/active.md`「判断根拠（記録）」の南側可視限界の手計算・GDD調整レンジ内での試算・見送り理由3点は変更なく維持。Checkpoint C への継続エスカレーションとする。
  2. **対応**: `ArenaEnvironmentSceneTests.cs` の `ringRadius` 算出直前のコメントを、実際の配置半径（`SpawnRadius*0.6`=8.1m、スポーンリング半径13.5mより内側）を明示する記述に修正。「SpawnRadiusをそのまま縮尺再現」という誤った主張を削除。
  3. **対応**: `SpawnRingColor` を `Ui.ColorTextSecondary`（実際はプレイヤー主色）から独立リテラル `"#F5F7FA"`（art-bible palette[11]「UI/テキスト」役。HSV実測: floor sat≈0.66・enemy sat≈0.89・hero(旧採用値#3488D1) sat≈0.75 に対し sat≈0.02 の近白色で彩度によって明確に区別される、真にキャラクターロールと衝突しない色）へ変更。`GameConfig.cs` のコメントを事実に即して書き直し、旧コメントの「非ロール色」という誤った主張を撤回。`state/active.md` の該当記述にも訂正エントリを追記済み。
  4. **対応**: PlayMode テストスイートを再実行（93 passed/0 failed, exit0）し `qa/evidence/arena-four-direction.png` を新色で再撮影・目視確認。新しいスポーンリング（近白色）は緑床・暗void双方に対して明瞭なコントラストを保ち、hero(青)/敵(紫)いずれのシルエット色とも混同しないことを確認した。既知の南側非対称性（finding #1）は変わらず残存。
  5. **対応**: `SpawnRingColor` を `Ui.ColorTextSecondary` へのエイリアスから独立リテラル `"#F5F7FA"` + 由来コメントへ変更（対応3と同一修正）。これにより UI テキスト色・HUDバー色・プレイヤー体色とスポーンリング色の意図しない連動は解消。

- 再検証:
  - EditMode: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results.xml -logFile -` → exit 0、結果XML `total=125 passed=125 failed=0`
  - PlayMode: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform PlayMode -testResults qa/evidence/playmode-results.xml -logFile -` → exit 0、結果XML `total=93 passed=93 failed=0`（`qa/evidence/arena-four-direction.png` 再生成・目視確認済み）
  - Build: `"$UNITY" -batchmode -projectPath game -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit -logFile game/Logs/build.log` → exit 0、ログに `Build succeeded: .../game/Build/ForgeGame.app`

- status: MAX_ITER(2) 到達につき `state/stories.yaml` S-20 を `status: done` に更新。未対応finding: #1（四方可読性 acceptance 未充足。gameplay-engineer 単独権限外のため正当に見送り）のみ、Checkpoint C へエスカレーション。他4件は全て対応済み。

## S-22 追記 — iteration 2 finding #1（四方可読性 acceptance 未充足）への対応記録
- 日時: 2026-07-13T19:10:00Z
- 対応（gameplay-engineer・S-22「固定俯瞰カメラの南側可視性改善」）:
  finding #1 は「gameplay-engineer 単独ではカメラ定数を変更できない（conventions.md §2）」ため iteration 1/2 とも見送りとしていたが、Phase 3 Polish で game-designer が design/gdd.md「カメラ」節を正式に更新した（既存の調整レンジ 60–70°/12–18m/45–55° の中で南側可視限界が最も深くなる境界値の組み合わせ CAMERA_HEIGHT=18m/CAMERA_PITCH_DEG=60°/CAMERA_FOV=55° へ）ことを受け、S-22 でその更新値を `GameConfig.Camera.PitchDeg/Height/Fov` へ反映した（旧 65f/14f/50f → 60f/18f/55f）。
  `Systems/ArenaCameraMath.ComputeSouthVisibilityLimitZ`（新規 pure 幾何関数）で南側可視限界を機械計算し、旧値 z≈-6.529m → 新値 z≈-9.606m（+約3.08m/+約47%）へ拡大したことを EditMode テスト4本で回帰検証済み（`ArenaCameraMathTests.cs`）。`ArenaEnvironmentSceneTests.Capture_FourDirectionReadability_Evidence` を再実行して `qa/evidence/arena-four-direction.png` を再撮影し、南側リング地点（ringRadius=SpawnRadius*0.6≈8.1m）に配置した敵が新値では画角内に完全に収まることを目視確認した（旧値では画角外に切れていた）。north/east/west 側の可視性の回帰は無いことも同じ証跡画像で確認済み。
  **finding #1 はこれで部分的に解消**（南側可視限界の大幅な拡大により「四方の敵を同時視認できる可読性」への実質的な改善を達成）。ただし **ARENA_RADIUS(12m)南端・SpawnRadius(13.5m)そのものの南端の完全カバーは、既存の調整レンジ内（H≤18/P≥60/F≤55）では引き続き未達**（S-22 acceptance で明記済みの既知の限界点）。四方を完全に均等カバーするにはレンジ自体の拡張（H>18m または F>55°）が必要で、これはカメラ画角がアート構図・key image に影響するため art-director との協議を要し、Phase 3 Polish のスコープ（config値変更のみで完結する調整）を超える。この残存制約は新たな未達としてではなく「改善の限界点」として Checkpoint C へ引き続き申し送りとする。
- 検証: EditMode `total=149 passed=149 failed=0`、PlayMode `total=106 passed=106 failed=0`、`ForgeBuild.BuildMac` exit 0（成果物 `game/Build/ForgeGame.app`）。
- status: `state/stories.yaml` S-20 は既に `done`（変更なし）。S-22 は本対応を経て `status: review`（CR-CODE 判定待ち）。
