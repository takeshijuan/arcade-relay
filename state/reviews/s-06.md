# state/reviews/s-06.md — S-06「勝敗判定・スコア算出・RunResult 確定と Result 遷移」レビュー履歴

## CR-CODE iteration 1 — APPROVE
- 日時: 2026-07-21T11:44:39Z
- 対象コミット: 665f49adb081e37d65b6f66f5e587c6842944c65（HEAD）
- 対象ファイル: game/Assets/Scripts/Components/RunOutcomeController.cs / game/Assets/Tests/PlayMode/RunOutcomePlayModeTests.cs / game/Assets/Scenes/Game.unity
- 指摘要約: findings 0 件。
  - rules/unity-code.md 準拠: 本番コードに数値リテラル直書き無し（遅延=GameConfig.Presentation.WinResultDelaySec、HP/スコア/報酬は全て既存 System/GameConfig 経由）。`0f`/`0` 初期値・比較のみで規約13 例外内。
  - delta-time: Update()→StepSimulation(Time.deltaTime)。演出待機も postFinalizeTimer を deltaTime 駆動の状態機械で消化（coroutine WaitForSeconds 不使用）。規約2 準拠。
  - 層分離: Components 層は判定合成と配線のみ。スコア算出/メタ確定/HP判定は Systems・Systems/Meta の純粋関数へ委譲。永続化は ISaveStore 抽象越し（GameBootstrap と同一パターン。File I/O 直呼び無し）。規約3/9 準拠。
  - silent-failure: 回復不能条件の Warning 降格・握り潰し無し。SetSaveStoreForTest は Start 後/ null 注入を throw で即時失敗（黙示無効化禁止パターンを踏襲）。finalized ガードで1回性を保証。
  - acceptance 充足: 勝利=AllWavesSpawned && ActiveEnemyCount==0（敗北判定を先に評価しコア生存を担保）/ 敗北=coreView.IsDefeated（ポーリング）+ OnCoreDefeated イベントの二重経路、いずれも finalized ガードで FinalizeRun を1回だけ実行。FinalizeRun 内順序は ComputeFinalScore→ApplyRunResult→saveStore.Save→SetCurrentSaveData で contract/architecture §3 と一致。連打・複数フレーム再判定に対し Save/遷移とも1回。PlayMode テストは勝利/敗北両経路で SaveCallCount==1・RunResult 確定・Result シーン遷移を検証。
- 並走レーン注記（REJECT/blocker としない前提）: 本番の saveStore は `??= new InMemorySaveStore()` 既定（GameBootstrap と同一 scaffold）。FileSaveStore への差し替えは S-07 の責務で docs/architecture.md §4 の設計に合致。RunResult のディスク永続は S-07 合流まで未発生だが本ストーリーの欠陥ではない。
- 参考（閾値未満・記録のみ、対応不要）:
  - FinalizeRun 冒頭の ScoreSystem.ComputeFinalScore はログ用で、ApplyRunResult 内で再計算される（純粋関数・同値）。冗長だが害無し。
  - テストは component.enabled=false 運用のため RunOutcomeController.Start() が走らず OnCoreDefeated→HandleCoreDefeated イベント経路は未カバー（敗北はポーリング経路で検証済み・acceptance は充足）。
- 対応: —（APPROVE のため revise 不要）
- 追記（合算 code-reviewer + silent-failure-hunter 指摘への対応・2026-07-21）:
  - [MEDIUM] RunOutcomeController.cs:63 の `saveStore ??= new InMemorySaveStore()` が GameBootstrap.cs と別インスタンスの
    第2の scaffold 構築点で、S-07 が GameBootstrap 側だけ差し替えると本コンポーネントが使い捨てインメモリストアに
    成功を返し続ける懸念 → **対応済み**。RunOutcomeController.cs:63 に GameBootstrap.cs と同一の S-07 差し替えマーカーを
    追加し、GameBootstrap.cs 側にも「RunOutcomeController.cs に第2の構築点あり、同時に差し替えること」の相互参照コメントを
    追加した（両ファイルとも gameplay-engineer 担当 S-01/S-06 の範囲内）。単一ファクトリ化（Persistence/SaveStoreFactory 等）は
    見送り: S-07 未着手の現時点でファクトリを追加すると FileSaveStore 実装時の差し替え対象がさらに1ファイル増えるだけで
    根本解決にならず、S-07 のスコープ（ISaveStore 実装追加＋両呼び出し箇所の差し替え）で一元化する方が変更を最小化できる
    ため、マーカーの相互参照によるコメントベースのガードに留めた。
  - [LOW] FinalizeRun 内で `saveStore.Save(next)` が `GameFlow.SetCurrentSaveData(next)` に先行しており、将来
    FileSaveStore が I/O 例外を投げた場合に CurrentSaveData が未更新のまま Result 遷移が進行しうる懸念 → **対応済み**。
    RunOutcomeController.cs:171-178 で `GameFlow.SetCurrentSaveData(next)` を `saveStore.Save(next)` より先に呼ぶ順序へ
    入れ替えた。Save が例外を投げた場合はその場で Update 呼び出し元へ伝播し（catch して握り潰さない）、GameFlow.cs の
    「保存直後に必ず SetCurrentSaveData」契約をプロセス内の真実の更新を先に確定させる形で満たす。
  - 検証: Unity 未起動（並走レーン規律）。Read/Grep で参照型（ISaveStore/SaveData/GameFlow/InMemorySaveStore）・
    メンバシグネチャ・RunOutcomePlayModeTests.cs の呼び出し順依存の有無を静的確認し、コンパイル不整合が無いことを確認した。
    EditMode/PlayMode 実行はレーン合流後のバッチ検証区間で行う。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-21T11:51:00Z
- 対象コミット: a2fdd7edc25d8ccaa2f2cdd6aff3c10a41ac6f8d「S-06: fix CR-CODE iter 1」（固定・作業ツリー未コミット分は対象外）
- 対象ファイル: game/Assets/Scripts/Components/GameBootstrap.cs / game/Assets/Scripts/Components/RunOutcomeController.cs
- 指摘要約: findings 0 件（≥80 確信の指摘なし）。iter1 の [MEDIUM]/[LOW] 両対応を確認した。
  - [MEDIUM] 第2 scaffold 構築点（RunOutcomeController.cs:67 `saveStore ??= new InMemorySaveStore()`）: 両ファイルに相互参照 S-07 差し替えマーカーを追加（GameBootstrap.cs:16-17 / RunOutcomeController.cs:63-66）。コメントのみで機能差分なし。ファクトリ化見送りの判断も妥当。適切な対応。
  - [LOW] 保存順序: RunOutcomeController.cs:177-178 で `GameFlow.SetCurrentSaveData(next)` を `saveStore.Save(next)` より前へ入れ替え。ComputeFinalScore→ApplyRunResult→Save の acceptance 相対順序は保持（SetCurrentSaveData は acceptance の要求列に含まれない）。現行 InMemorySaveStore.Save は例外を投げないため挙動差分ゼロ。
  - unity-code.md 準拠: 差分はコメント追加＋既存2呼び出しの並べ替えのみ。新規数値リテラル無し（規約12/13）、delta-time ロジック不変（規約2）、層分離不変（規約3/9）。silent-failure: LogError→Warning 降格・try/catch 握り潰しの新規混入なし。
  - acceptance 充足（不変）: finalized ガードで FinalizeRun/Save/遷移とも1回性を維持。PlayMode 両経路テスト（SaveCallCount==1・RunResult 確定・Result 遷移）は順序入れ替えの影響を受けず成立。
- 参考（閾値未満・記録のみ、対応不要）:
  - RunOutcomeController.cs:174-176 のコメントは「例外時に Result 遷移まで到達しない方が正しい」と述べるが、finalized=true 確定後は StepSimulation の postFinalizeTimer 経路が後続フレームで Result へ遷移するため、Save 例外は当該 Update 呼び出しのみ中断し遷移自体は阻止しない。将来 FileSaveStore が例外を投げる設計になった場合、コメントの含意（遷移阻止）と実挙動が乖離しうる — S-07 で例外時に遷移を止めたいなら明示ハンドリングが要る。現行 store は例外を投げず機能影響なし・並走レーン前提のため本 iter では非指摘。
  - 同順序変更は将来 FileSaveStore の Save 例外時に CurrentSaveData=next（新値）だけが確定しディスクは旧値のまま Menu が新値を表示しうる（セッション内の成功見せかけ）。設計上の論点であり現行 store では発生せず、S-07 側の設計判断に委ねる。
- 対応: —（APPROVE のため revise 不要）

## CR-CODE iteration 3（合算 code-reviewer + silent-failure-hunter 指摘への対応・2026-07-21）
- 対象: RunOutcomeController.cs:173-178（コメント誤り）/ RunOutcomeController.cs:63-67・GameBootstrap.cs:15-18（scaffold 二重構築点）
- 指摘要約:
  - [MEDIUM] RunOutcomeController.cs:173-178 のコメント「例外時に Result 遷移まで到達しない」が実挙動と逆。
    FinalizeRun は冒頭で finalized=true/finalizedResult を確定してから Save を呼ぶため、Save が例外を投げても
    StepSimulation の finalized 分岐が後続フレームで通常どおり Result へ遷移し、保存失敗がプレイヤーに
    見分けのつかない「成功した Result 画面」として現れる。state/reviews/s-06.md（iter2 参考欄）は正しく記録済みだが
    コード内コメントだけが誤りで、S-07 実装者への「catch 不要」の誤った根拠になりうる。
  - [LOW] InMemorySaveStore の二重 scaffold 構築点（RunOutcomeController.cs / GameBootstrap.cs）が相互参照コメントのみで、
    S-07 が片側だけ FileSaveStore に差し替えると残った側が使い捨てインメモリストアに成功を返し続けるデータ消失経路が
    構造的に未封鎖。Persistence 層の静的ファクトリへの一元化を推奨（記録のみ・blocker としない）。
- 対応:
  - [MEDIUM] **対応済み**。コメントを実挙動（Save 例外後も Result 遷移は進行しうる）に訂正した上で、
    `saveStore.Save(next)` を try/catch し、失敗時に `Debug.LogError("[SaveFailure] ...")` を1回出力し
    `GameFlow.SetSaveFailed(true)` で GameFlow.Recovered と同型のプロセス内フラグへ伝播するよう実装した
    （RunOutcomeController.cs:187-199、GameFlow.cs に `SaveFailed` プロパティ + `SetSaveFailed` を追加）。
    Result/Menu UI 側でこのフラグを購読して通知表示する配線は ui-engineer 担当領域（Ui/ 層）のため本ストーリーでは
    実装せず、GameFlow.cs のコメントと RunOutcomeController.cs 内に ★TODO(S-07) として明示した（並走レーン規律の
    担当外ファイル不変更に従う）。finalized 確定後は遷移自体を止めても保存は復旧しないため、例外は Save 呼び出しの
    みを局所的に catch し、Result 遷移のブロックはしない設計とした（コメントにその理由を明記）。
  - [LOW] **見送り（記録のみ）**。iter1/iter2 と同様、S-07（FileSaveStore 導入）のスコープで
    Persistence 層の静的ファクトリへ一元化するのが最小変更になるため、本 iter では両ファイルの scaffold コメントに
    「S-07 実装時はファクトリ化を推奨」の一文を追記するに留めた（RunOutcomeController.cs:67-69 / GameBootstrap.cs:15-17）。
    構造変更（実際のファクトリ追加）は S-07 未着手の現時点では差し替え対象を増やすだけで見送る判断を維持。
  - 検証: Unity 未起動（並走レーン規律）。Read/Grep で GameFlow.SaveFailed/SetSaveFailed の新規参照、
    RunOutcomeController.cs の `using System;`（Exception 型解決済み）、既存 PlayMode テスト
    （RunOutcomePlayModeTests.cs / GameFlowPlayModeTests.cs / TitleScreenPlayModeTests.cs / ResultScreenPlayModeTests.cs）に
    `SaveFailed` への依存が無くデフォルト値 false で既存アサーションへ影響しないことを確認した。
    EditMode/PlayMode 実行はレーン合流後のバッチ検証区間で行う。

## ステータス訂正（gameplay-engineer・再点検）
- 日時: 2026-07-22
- state/stories.yaml の S-06 が `status: done` になっていたが、上記 iteration 3 は fix 記録のみで
  reviewer による正式な verdict 行（`CR-CODE: APPROVE|CONCERNS|REJECT`）が本ファイルに存在しない
  （review-loops.md の必須形式未充足）。contract.md「done にするのは CR-CODE 通過（findings解消
  or 正当理由の明記）後のみ」に照らし `status: review` へ訂正した。
- 実装内容自体は静的確認（Read）で再検証し acceptance を充足していることを確認済み:
  RunOutcomeController.FinalizeRun が isWin いずれの経路でも finalized ガード配下で
  ScoreSystem.ComputeFinalScore→MetaProgression.ApplyRunResult→GameFlow.SetCurrentSaveData→
  saveStore.Save（例外は catch し GameFlow.SetSaveFailed(true)＋LogError、遷移は妨げない）の順に
  1回だけ実行し、StepSimulation が GameConfig.Presentation.WinResultDelaySec 経過後に
  GameFlow.GoToResult で Result へ遷移する。RunOutcomePlayModeTests.cs の2テスト
  （AllWavesCleared…SavesOnce…／CoreDepleted…SavesOnce…）が勝利/敗北両経路で
  `saveStore.SaveCallCount==1`・RunResult 各フィールド・Result シーン遷移を検証しており、
  コードの追加変更は不要と判断した。
- 対応: 実装は変更なし。stories.yaml のみ訂正。次の CR-CODE ラウンドで iteration 3 分の正式な
  APPROVE verdict をこのファイルに追記してから `status: done` に進めること。
  検証: Unity 未起動（並走レーン規律）。Read で RunOutcomeController.cs / GameFlow.cs /
  ScoreSystem.cs / RunOutcomePlayModeTests.cs / GameConfig.cs（WaveCount/Presentation/AssetKeys 定数実在）
  を再確認し、コンパイル不整合が無いことを確認した。EditMode/PlayMode 実行はレーン合流後の
  バッチ検証区間で行う。

## CR-CODE iteration 3 — APPROVE
- 日時: 2026-07-22T02:40:01Z
- 対象コミット: 97ce397dd9a457abca4ee1869c0c92ef55b9db9b（固定・作業ツリー未コミット分/他コミットは対象外）。
  本コミット自体は state のみ（s-06.md / stories.yaml）の status 訂正（done→review）で game/ コード差分なし。
  実装本体は祖先コミット 665f49a（S-06 初出）+ fix a2fdd7e（iter1）+ 33d89a4（iter2 相当・iter3 fix）に存在するため、
  97ce397 時点の各実装ファイルを Read で取得して CR-CODE 判定を行った。iteration 3 の fix に対する正式な
  reviewer verdict 行（従前は fix 記録のみ）を本追記で確定する。
- 対象ファイル（97ce397 時点の内容を Read で確認）: Components/RunOutcomeController.cs / Components/GameFlow.cs /
  Systems/ScoreSystem.cs / Systems/Meta/MetaProgression.cs / Types.cs（RunResult）/ Systems/Meta/MetaTypes.cs（SaveData）/
  Tests/PlayMode/RunOutcomePlayModeTests.cs / GameConfig.cs
- 指摘要約: findings 0 件（≥80 確信の指摘なし）。iteration 3 の fix（[MEDIUM] Save 例外の try/catch +
  Debug.LogError("[SaveFailure] …") 1回 + GameFlow.SetSaveFailed 伝播・コメント訂正／[LOW] scaffold ファクトリ化
  見送りの記録）を正式に APPROVE する。
  - silent-failure: Save 失敗は LogError 1回 + GameFlow.SetSaveFailed(true) 伝播で、Warning 降格・握り潰しなし。
    iter2 参考欄で指摘したコメント誤り（「例外時に遷移まで到達しない」）は iter3 で実挙動どおりに訂正済み。
    セーブ破損の黙示初期化は本ストーリー対象外（S-07 FileSaveStore の責務）。
  - unity-code.md 準拠: 本番コードに数値リテラル直書きなし（遅延=GameConfig.Presentation.WinResultDelaySec、
    HP/スコア/報酬/実績閾値は全て GameConfig 経由、`0`/`0f` は初期値・null 既定で規約13 例外内）。delta-time:
    Update()→StepSimulation(Time.deltaTime)、elapsedSec/postFinalizeTimer とも deltaTime 駆動・固定加算なし（規約2）。
    層分離: 判定合成/配線は Components、スコア/メタ/HP は Systems・Systems/Meta の純粋関数、I/O は ISaveStore
    抽象越し（File I/O 直呼びなし・規約3/16/22/23）。
  - API 整合（97ce397 時点で静的確認）: OnCoreDefeated / WaveSystem.AllWavesSpawned・ActiveEnemyCount・Enemies・
    ApplyDamage / CoreView.IsDefeated・CurrentHp / BuildSpotController.EnemyHealth(KillCount/AoeKillCount/RecordKill)・
    BuildSpots.Towers・TryPlaceTower / SaveData.CreateDefault・Clone / SaveStores.CreateDefault / GameConfig 参照定数
    （Core.HpMax・Score.*・Meta.*・Presentation.WinResultDelaySec・Scenes.Result）すべて実在し誤用なし。
  - acceptance 充足: coreView.IsDefeated を先に評価した上で 勝利=AllWavesSpawned && ActiveEnemyCount==0 /
    敗北=IsDefeated ポーリング + OnCoreDefeated イベントの二重経路、いずれも finalized ガードで FinalizeRun を1回だけ実行。
    順序 RunResult 確定→ComputeFinalScore→ApplyRunResult→（SetCurrentSaveData→）Save を1回、StepSimulation が
    WinResultDelaySec 消化後 GameFlow.GoToResult で Result へ遷移。PlayMode 2テストが勝利/敗北両経路で
    SaveCallCount==1・RunResult 各フィールド・Result シーン遷移を検証。連打/複数フレーム再判定に対し二重保存なし。
- 参考（閾値未満・対応不要）:
  - FinalizeRun 冒頭 ComputeFinalScore はログ用で ApplyRunResult 内でも再計算される（純粋関数・同値・害無し）。
  - RunOutcomePlayModeTests.cs の TearDown は GameFlow.SaveFailed/Recovered の静的状態を明示リセットしない。
    本2テストは両フラグを設定・参照しないため影響なし（テスト衛生上の潜在事項）。
- 対応: —（APPROVE のため revise 不要）。本 verdict の追記により review-loops.md の必須形式を充足したため
  status: review → done へ進めてよい。
- 検証: 読み取り専用レビュー（並走レーン単一インスタンスロックのため Unity 未起動・ビルド/テスト未実行）。
  git show 97ce397 で対象コミットを取得、実装本体は同コミット時点の各ファイルを Read で確認し API 整合・
  コンパイル不整合の有無を静的検証した。EditMode/PlayMode 実行はレーン合流後のバッチ検証区間で行う。

## CR-CODE iteration 1（合算 code-reviewer + silent-failure-hunter 指摘への対応・2026-07-22）
- 対象: RunOutcomeController.cs:183-194（GameFlow.SaveFailed リセット漏れ）/
  GameFlow.cs:28（SaveFailed の文書契約）/ Tests/PlayMode/RunOutcomePlayModeTests.cs（Save 失敗経路の未テスト）
- 指摘要約:
  1. RunOutcomeController.FinalizeRun の try 内で `saveStore.Save(next)` が成功しても
     `GameFlow.SetSaveFailed(false)` が呼ばれず、GameFlow.SaveFailed が一方向（失敗後に true のまま）の
     フラグになっていた。GameFlow.cs のコメント契約「直近の Persistence.Save 呼び出しが例外で失敗したか」と
     矛盾し、失敗ラン→成功ランの順でプレイした場合に成功ランでも偽の保存失敗通知が出続ける
     （static フィールドのためシーンリロードでも残存）。ui-engineer 側の S-20 購読配線後に顕在化する。
  2. RunOutcomePlayModeTests.cs は SaveCountingStore（常に成功）のみを使っており、Save が例外を投げる経路
     （[SaveFailure] LogError 1回→GameFlow.SaveFailed=true→Result 遷移は継続）を検証するテストが無かった。
     将来 catch 内の LogError 削除・Warning 降格・遷移停止という退行が黙って通り得る。
- 対応:
  1. **対応済み**。RunOutcomeController.cs の try ブロック内、`saveStore.Save(next)` 成功直後に
     `GameFlow.SetSaveFailed(false)` を追加した（catch 側の `GameFlow.SetSaveFailed(true)` はそのまま）。
     これにより「直近の Save 呼び出しが失敗したか」という GameFlow.cs の文書契約と実挙動が一致し、
     成功ランでは必ずフラグがリセットされる。GameFlow.cs 側の契約コメントは元々正しい記述だったため変更不要。
  2. **対応済み**。RunOutcomePlayModeTests.cs に `ThrowingSaveStore`（Save が必ず `IOException` を投げる
     ISaveStore ダブル）を追加し、新規テスト
     `SaveThrows_LogsSaveFailureOnce_SetsGameFlowSaveFailed_ButStillTransitionsToResult` で
     `LogAssert.Expect(LogType.Error, ^\[SaveFailure\]…)` ＋ `GameFlow.SaveFailed==true` ＋
     `outcome.HasTransitioned==true`（Result 遷移継続）を検証した。`SetSaveStoreForTest` は Start() 前
     しか呼べない契約のため、SetUp が Start 済みの `outcome`/`outcomeGo` を一旦 Destroy し、
     ThrowingSaveStore を注入した新しい RunOutcomeController に差し替えて同フィールドへ再代入すること
     （TearDown の既存 Destroy 処理をそのまま利用できる）で対応した。あわせて TearDown に
     `GameFlow.SetSaveFailed(false)` を追加し、static フラグのテスト間汚染（本テストが true を残したまま
     次テストに漏れる）を防いだ。
  - 並走レーン領域: 本 fix は gameplay-engineer 担当の Components/Persistence 層（RunOutcomeController.cs・
    RunOutcomePlayModeTests.cs）のみを変更。GameFlow.cs は無変更（既存コメント契約が既に正しかったため）。
    共有ファイル（GameConfig.cs 等）・UI 層（Ui/）・stories.yaml の他ブロックには触れていない。
- 検証: Unity 未起動（並走レーン規律・単一インスタンスロック）。Read/Grep で以下を静的確認した:
  `ISaveStore`/`LoadResult`/`SaveData.CreateDefault` のシグネチャが `ThrowingSaveStore`/`SaveCountingStore`
  と一致すること、`GameFlow.SetSaveFailed`/`GameFlow.SaveFailed` の既存シグネチャに変更が無いこと、
  `System.Text.RegularExpressions.Regex` の using 追加が他 using と衝突しないこと、新規テストメソッドが
  既存 `DrainPostFinalizeDelay` ヘルパーと `GameConfig.Scenes.Result`/`GameConfig.Presentation.WinResultDelaySec`
  を正しい型・シグネチャで参照していること。EditMode/PlayMode 実行はレーン合流後のバッチ検証区間で行う。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-22T02:48:14Z
- 対象コミット: f7eadf4f2e86bbca9773dd11a6d6f3da6b04f30a「S-06: fix CR-CODE iter 1」（固定・作業ツリー未コミット分/他コミットは対象外）
- 対象ファイル: game/Assets/Scripts/Components/RunOutcomeController.cs / game/Assets/Tests/PlayMode/RunOutcomePlayModeTests.cs
- 指摘要約: findings 0 件（≥80 確信の指摘なし）。上記「iteration 1 fix（2026-07-22）」の [1]/[2] 両対応と本コミット同梱の演出SFX Integrate を確認し APPROVE する。
  - [fix #1] SaveFailed リセット漏れ: try 内 `saveStore.Save(next)` 成功直後に `GameFlow.SetSaveFailed(false)` を追加（RunOutcomeController.cs:190 付近）。GameFlow.cs の契約「直近の Save が失敗したか」と実挙動が一致し、失敗ラン→成功ランで static フラグが偽陽性のまま残存する経路を封鎖。catch 側の `SetSaveFailed(true)` は不変。適切。
  - [fix #2] Save 失敗経路の未テスト: `ThrowingSaveStore`（Save が必ず IOException を投げる ISaveStore ダブル）＋新規 UnityTest `SaveThrows_LogsSaveFailureOnce_SetsGameFlowSaveFailed_ButStillTransitionsToResult` を追加。`LogAssert.Expect(LogType.Error, ^\[SaveFailure\]…)`＋`GameFlow.SaveFailed==true`＋`HasTransitioned==true`＋Result 遷移を検証。`SetSaveStoreForTest` の Start 前契約に合わせ SetUp の outcome を Destroy→ThrowingStore 注入で再構築する手順も妥当。TearDown に `GameFlow.SetSaveFailed(false)` を追加し static フラグのテスト間汚染も封じた。適切。
  - 演出SFX（本コミット同梱の Integrate・RunOutcomeController.cs:167-173）: 勝敗確定の瞬間に `AudioCuePlayer.PlayOneShot(isWin ? GameConfig.AssetKeys.SfxVictoryJingle : GameConfig.AssetKeys.SfxCoreHit, coreView 位置 or Vector3.zero)` を finalized ガード配下で1回だけ発火。unity-code.md 準拠: 資産参照は AssetKeys 定数経由（規約 Resources.Load 文字列直書き禁止に適合）、数値リテラル直書きなし（規約12/13）、時間ロジック不介在で delta-time 影響なし（規約2）。AudioCuePlayer は他レーン提供（Components 層・working tree に実在。CoreView/BuildSpotController と同一の確立パターン）で AudioListener 不在時は早期 return・未生成クリップは Warning でスキップし throw しないため、Save 順序を阻害せず既存 PlayMode テストにも無影響。並走レーン前提により実体参照は blocker としない。
  - silent-failure: LogError→Warning 降格・try/catch 握り潰しの新規混入なし。Save 失敗は LogError 1回＋プロセス内フラグ伝播で遷移は止めない設計を維持。
  - acceptance 充足（不変）: finalized ガードで FinalizeRun/Save/遷移とも1回性を維持。順序 RunResult 確定→ComputeFinalScore→ApplyRunResult→（SetCurrentSaveData→）Save は本コミットで不変（SFX は RunResult 確定と ComputeFinalScore の間に挿入されるが acceptance 要求列に SFX は含まれず順序を破壊しない）。勝利/敗北の両 PlayMode テスト（SaveCallCount==1・RunResult 各フィールド・Result 遷移）成立。
- 参考（閾値未満・記録のみ、対応不要）:
  - 敗北の致命打で SfxCoreHit が二重再生されうる: CoreView.ApplyDamage は被弾ごと（HP=0 の致命打含む）に SfxCoreHit を鳴らし、RunOutcomeController は敗北 finalize でも SfxCoreHit（コア崩壊音として流用）を鳴らすため、同一ワンショットがほぼ同時に2回発火する。design/assets.md 記載の予算制約による意図的な音の流用であり、コード欠陥・規約違反ではない（確信 <80）。将来 FeedbackCueSystem（S-13）で演出選択を一元化する際に整理余地あり。
  - 本コミットは「iter1 fix」（SaveFailed リセット＋テスト）と演出SFX Integrate を1コミットに同梱している。上部の iteration 1 fix 記録は SFX 分を明記していないが、変更自体は健全（プロセス上の記録粒度の指摘のみ）。
- 対応: —（APPROVE のため revise 不要）。本 verdict の追記により review-loops.md の必須形式を充足。
- 検証: 読み取り専用レビュー（並走レーン単一インスタンスロックのため Unity 未起動・ビルド/テスト未実行）。git show f7eadf4 で対象コミットを取得し、参照 API（AudioCuePlayer.PlayOneShot / GameConfig.AssetKeys.SfxVictoryJingle・SfxCoreHit / GameConfig.Presentation.WinResultDelaySec / GameConfig.Scenes.Result / GameFlow.SetSaveFailed・SaveFailed / ScoreSystem.ComputeFinalScore / MetaProgression.ApplyRunResult / ISaveStore・LoadResult・SaveData.CreateDefault）の実在とシグネチャ整合を Read/Grep で静的確認した。EditMode/PlayMode 実行はレーン合流後のバッチ検証区間で行う。

## CR-CODE iteration 2 fix（合算 code-reviewer + silent-failure-hunter 指摘への対応・2026-07-22）
- 対象: RunOutcomeController.cs:170-173（f7eadf4 時点。演出SFX呼び出しの挿入位置）
- 指摘要約:
  1. [MEDIUM/silent-failure] `AudioCuePlayer.PlayOneShot` 呼び出しが `finalized=true` 確定直後・
     永続化ブロック（ComputeFinalScore→ApplyRunResult→SetCurrentSaveData→Save の try/catch）より前段に
     挿入されていた。try/catch は `saveStore.Save` のみを覆うため、PlayOneShot が例外を投げるとスコア算出・
     メタ進行確定・保存の全てがスキップされる一方 `finalized` は既に true のため StepSimulation は通常どおり
     Result へ遷移し `GameFlow.SaveFailed` も false のまま — 181-193行目のコメントが自ら警告する「保存失敗が
     成功と区別のつかない Result 画面」の失敗モードをプレゼンテーション呼び出しの位置ミスで再導入していた。
  2. 確認済み・問題なし（対応不要）: SaveFailed リセットの適切性、ThrowingSaveStore テストの妥当性、
     SfxCoreHit 流用が design/assets.md 記載の意図的設計であること、coreView null 時の Vector3.zero
     フォールバックが Start() の配線破損検知と文書化済みであること、GameConfig.AssetKeys.SfxVictoryJingle/
     SfxCoreHit・GameFlow.SetSaveFailed のシグネチャ整合。
- 対応:
  1. **対応済み**（指摘の修正方針どおり）。`AudioCuePlayer.PlayOneShot` 呼び出しを、永続化ブロック
     （`GameFlow.SetCurrentSaveData`→`try { saveStore.Save } catch`）の直後、末尾の `Debug.Log`
     （run finalized ログ）の直前へ移動した（RunOutcomeController.cs 203-214行）。これにより SFX 再生が
     例外を投げても、その時点で ComputeFinalScore・ApplyRunResult・SetCurrentSaveData・Save（成功/失敗いずれも
     GameFlow.SaveFailed へ正しく伝播済み）は既に完了しているため、指摘の failure mode（保存スキップ＋
     成功に見える Result 画面）は構造的に発生しなくなった。移動理由・従前の問題点をコード内コメントに明記した
     （同一フレーム内での可聴差はゼロ）。代替案（現位置で独自 try/catch）は採らなかった — 指摘の推奨修正
     （Save の try/catch 直後への移動）の方が根本的で、SFX 専用の catch を追加で持つより変更が単純なため。
  2. 対応不要（レビューで確認済み事項として維持。コード変更なし）。
- 検証: Unity 未起動（並走レーン規律・単一インスタンスロック）。Read で以下を静的確認した:
  移動後も `AudioCuePlayer.PlayOneShot(string, Vector3, float=1f)` のシグネチャ・
  `GameConfig.AssetKeys.SfxVictoryJingle`/`SfxCoreHit`（GameConfig.cs:352,354）の実在に変更なし、
  `outcomeSfxPosition`/`isWin`/`coreView` のスコープが移動先でも有効（メソッド冒頭で定義済み・
  ローカル変数の再定義なし）、`finalScore`/`killCount` を参照する末尾 `Debug.Log` の直前に配置し
  既存の変数依存順序を破壊していないことを確認した。EditMode/PlayMode 実行はレーン合流後のバッチ検証区間で行う。
