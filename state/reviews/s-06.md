# S-06 レビュー履歴 — 自動攻撃（最寄り索敵・瞬間ヒット）+ 敵HP・撃破

## CR-CODE iteration 1 — APPROVE
- 日時: 2026-07-10T08:39:02Z
- 対象コミット: 9ea0fce5dd02b8fb4cbe87c466bc6ddba4202269
- 指摘要約: ブロッキング指摘なし（findings 0件）。
  - マジックナンバー禁止（unity-code #12/#13）: 準拠。`AutoAttackDriver` は `GameConfig.Player.AutoAttackInterval/AutoAttackRange/AutoAttackDamageBase` のみを参照。`AutoAttackSystem` の残る数値リテラルは `NoTarget = -1`（名前付きセンチネル定数）・`float.MaxValue`（初期最小値）・ループ index の `0`/`i++` のみで、いずれもチューニング値ではなく規約の除外対象。`Types.cs` の `Math.Max(0, ...)` の `0` は HP 下限で意味を持たない構造値。
  - delta-time（unity-code #14/#15）: 準拠。`AutoAttackDriver.Update` は `_timer += Time.deltaTime` で累積し、`_timer -= AutoAttackInterval`（0リセットでなく減算）で端数を保持。フレーム固定加算なし。WaveSpawner のタイマー方式と同型。
  - エンジン非依存コア（unity-code #16）: 準拠。`Systems/AutoAttackSystem` は static クラス・MonoBehaviour 非継承・シーンAPI(Find/Instantiate/GetComponent)不使用。`Vector3`/`IReadOnlyList` の値型・コレクションのみ（規約の許容範囲）。ダメージ適用・Destroy・入力の副作用は `Components/AutoAttackDriver` と `Components/EnemyAgent` に隔離。`EntityState.ApplyDamage`（Types.cs）は副作用なしの純粋 reducer で敵HP・将来の player HP 両方で再利用可能。
  - 入力抽象化 / 攻撃ボタン非存在（gdd 操作仕様・P-02）: `AutoAttackDriver` は入力を一切読まず、タイマーのみで発火。`GameInput` に Attack アクションが存在しないことを EditMode 構造テスト（`GameInput_ExposesNoAttackAction`）と PlayMode 挙動テスト（`HeldMovementAndDashInput_DoesNotAffectAttackTiming`）で二重に担保。
  - effectiveAttackDamage 算出: `MetaProgression.EffectiveAttackDamage(upgradeAttackLevel)` を `Mathf.RoundToInt` で int 化して単体適用。式の再実装なし（conventions.md §5）。`SessionHolder` 欠落時（Boot 非経由）は `AutoAttackDamageBase`（Lv0）へフォールバックし `Debug.LogWarning` を出す — `PlayerController.ResolveMoveSpeed` と同型の正当縮退。`_attackDamage` を `Start()` で一度だけ解決してキャッシュするのは、アップグレードがラン間（Menu）でのみ変化する仕様に照らし正しい。
  - 最寄り索敵の正当性: XZ 平面距離（Y無視、`EnemyApproachSystem` / conventions §3 と一致）・二乗距離比較（sqrt回避）・境界 `maxRangeSqr` 以下は範囲内・同距離は最小 index に決定的に解決。`_candidatePositions` は `ActiveEnemies` と同順で構築し `enemies[nearestIndex]` に適用するため index 整合が成立（構築〜適用間で `ActiveEnemies` は非変更、`Destroy` は遅延のため同フレーム内で二重ヒットなし）。
  - 撃破処理: `EnemyAgent.ApplyAutoAttackDamage` が `EntityState.ApplyDamage` で HP を減算し、`IsDead`（Hp<=0）で `Destroy(gameObject)`。`OnDisable` が `ActiveEnemies` から除去するため次tickにスタール参照なし。登録は `OnEnable`（Instantiate 中・同フレーム同期の Initialize 前）だが、AutoAttackDriver.Update がフレーム境界を跨いで未初期化敵を見ることはなく既存の wiring guard 方針と整合。
  - シーン配線: `SceneWiring.WireGame` が Player GameObject へ `AutoAttackDriver` を冪等追加。`Game.unity` に MonoBehaviour が直列化済み（GUID `8ae33775b916240a383ed619f5315347` = `AutoAttackDriver.cs.meta` と一致）で、PlayMode テストがシーンロードのみで駆動可能。攻撃原点 `transform.position` は PlayerController と同一 GameObject のためプレイヤー位置＝正。
  - acceptance: 満たす。入力なし・AUTO_ATTACK_INTERVAL 周期・RANGE 内最寄り1体・瞬間ヒット・effectiveAttackDamage 単体適用・HP0 撃破、いずれも実装済み。PlayMode `EnemyWithinRange_TakesDamageInBaseIncrements_AndDiesAfterTwoHits`（HP が AUTO_ATTACK_DAMAGE_BASE 刻みで減少・ENEMY_HP_BASE=40 を2発で撃破・範囲外敵は非対象）と EditMode `EntityStateTests.ApplyDamage_EnemyHpBase_DiesAfterExactlyTwoAutoAttackHits`・`AutoAttackSystemTests`（範囲外/空/境界/同距離/Y無視）で受け入れ条件を網羅。
- 参考（非ブロッキング・対応不要）:
  1. PlayMode テストのダメージ期待値 `HpBase - AutoAttackDamageBase`(=20) は「SessionHolder 不在 → Lv0 フォールバック(20)」に依存する。当フィクスチャは `TearDownSession` で SessionHolder を破棄し、シーンも Boot 非経由でロードするため成立するが、他フィクスチャが `upgradeAttackLevel>0` の DontDestroyOnLoad SessionHolder をリークした場合のみ理論上ダメージが変わり得る。テスト分離一般の懸念でありレビュー対象コードの欠陥ではない（信頼度<50、非報告）。
- 対応: 追加対応不要（findings 0件・APPROVE）。

## CR-CODE iteration 1 fix — 追加指摘対応（2026-07-10）
- 指摘: `EnemyAgent.cs:55-64` `ApplyAutoAttackDamage` が未初期化状態（`Initialize()` 前）で呼ばれると `_health` が `default(EntityState)`（Hp=0）のため即 `IsDead` → `Destroy(gameObject)` して `true` を返す（幻の即死キルを成功報告）。同クラス `Update()` の rule 12 配線ガード方針と不整合。既に撃破済みの個体への再呼び出しも `true` を返し将来のキル数二重カウントの温床になり得る。現行コールグラフ（`WaveSpawner` が `Instantiate` 直後に同期的に `Initialize` するため `AutoAttackDriver` が未初期化敵を観測することはない）では到達しないため深刻度は中〜低、と評価された。
- 対応: 対応済み。`ApplyAutoAttackDamage` 冒頭に2ガードを追加した。
  1. `!_initialized` の場合: `Debug.LogError("[Wiring] EnemyAgent.ApplyAutoAttackDamage called before Initialize().")` を出し `false` を返す（`Update()` の既存 rule 12 ガードと同じ「配線破損は1回 LogError で表面化」方針に統一。未初期化時の幻の撃破成功報告を防止）。
  2. `_health.IsDead` の場合（既に撃破済みの個体への再呼び出し）: 追加ダメージ適用も `Destroy` の再呼び出しもせず `false` を返す（キル数二重カウント防止）。
  - 既存の正常系（初期化済み・生存中の敵へのダメージ適用→HP0で撃破）の挙動・戻り値は変更なし。既存 PlayMode/EditMode テストへの影響なし（両ガードとも現行コールグラフでは未到達のため既存アサーションは不変）。
  - 検証: EditMode テスト `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results.xml -logFile -` → exit 0、結果XML `total="54" passed="54" failed="0"`。`ForgeGame.EditorTools.ForgeBuild.BuildMac` ビルド → exit 0、ログに `Build succeeded: .../game/Build/ForgeGame.app`。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-10T08:43:54Z
- 対象コミット: fda9c2b37f78dbc3e18d7b058538b3ed4cb5e7c0
- 指摘要約: ブロッキング指摘なし（findings 0件）。iteration 1 fix で追加された2ガードを再レビュー。
  - 正当性: `!_initialized` ガードは `default(EntityState)`（Hp=0）に対する幻の即死キル成功報告を防止し `false` を返す。`Initialize()` が `_health` と `_initialized` を同時設定するため「初期化済みなら `_health` 有効」が保証され、初期化ガード通過後の `_health.IsDead` 判定に穴はない。`_health.IsDead` ガードは撃破済み個体への再呼び出しで追加ダメージ・再 `Destroy`・重複 `true` を防ぐ。正常系（初期化済み・生存中→ApplyDamage→HP0で Destroy+true）の戻り値・挙動は不変で acceptance の2発撃破フロー（hit1: 未死亡→false / hit2: HP0→Destroy→true）に影響なし。
  - マジックナンバー禁止（unity-code #12/#13）: 準拠。diff に数値リテラルの新規混入なし（ガードは `_initialized`/`_health.IsDead` の bool 判定のみ）。
  - delta-time（unity-code #14/#15）: 準拠。タイマー・移動処理に変更なし（`ApplyAutoAttackDamage` は瞬間ヒットで時間非依存）。
  - エンジン非依存コア（unity-code #16）: 準拠。変更は `Components/EnemyAgent`（MonoBehaviour = シーン配線層）に閉じており、`Debug.LogError`/`Destroy` の副作用は同層に正しく隔離。`Systems/` は無変更。
  - 両ガードとも現行コールグラフ（`AutoAttackDriver.Update` は1フレーム1回・同期 `Initialize` 直後にのみ観測）では未到達の純粋防御コードであり、既存 PlayMode/EditMode アサーションは不変（iteration 1 fix の検証で54/54 pass・ForgeBuild 成功を確認済み）。
- 参考（非ブロッキング・対応不要）:
  1. `!_initialized` ガードの `Debug.LogError` は `Update()` の `_wiringErrorLogged` のような1回制御を持たず、配線破損時は攻撃tickごとにログしうる。ただし現行コールグラフでは未到達で、QA の `LogAssert.NoUnexpectedReceived()` は1回でも失敗検出するため実害差は無い（信頼度<50・非報告）。
- 対応: 追加対応不要（findings 0件・APPROVE）。

## CR-CODE iteration 1（資産統合コミット c2089c3 — S-06 差分の再レビュー） — APPROVE
- 日時: 2026-07-11T03:35:53Z
- 対象コミット: c2089c33a0caab5f9377b52e942288c806b2b9c4（phase2: integrate assets）
- スコープ: この commit で S-06 の中核ファイル `AutoAttackDriver.cs` に加わった差分（ANM-01 攻撃アニメ + SFX-01 ヒット音の配線）。依存する `GameConfig.Animation`（新規）・`Components/SfxLibrary.cs`（新規）も併せて確認。S-06 のコアロジック（最寄り索敵・瞬間ヒット・ダメージ適用・撃破）は本 commit では無変更。
- 指摘要約: ブロッキング指摘なし（findings 0件）。
  - 追加差分の内容: `AutoAttackDriver.Start` が `GetComponentInChildren<Animator>()` で animator を1回解決（unity-code #10 準拠 — `RequireComponent` 不使用・null は「プレハブ未統合」の正当状態）。`TryAttackNearest` で `ApplyAutoAttackDamage` 後、着弾ごと（撃破時含む）に `_animator != null` ガード下で `SetTrigger(GameConfig.Animation.AttackTrigger)`、`SfxLibrary.Instance != null` ガード下で `Play(AttackHit)`。いずれもプレイヤー自身の animator/共有 AudioSource への副作用でターゲット破棄フローとは独立。
  - マジックナンバー禁止（unity-code #1/#12・rule 13）: 準拠。差分に数値リテラルの新規直書きなし。animator パラメータ名は `GameConfig.Animation.SpeedParam/AttackTrigger`、閾値 `RunSpeedThreshold`、スケール基準 `ModelScale.*`、資産パスは `AssetKeys.*` へ集約。rule 13 通りコードは `SetTrigger`/`SetFloat` でパラメータを流すのみで、Idle<->Run 閾値遷移・AnyState→Attack トリガー遷移は生成された `Hero.controller` 資産側に存在（`Editor/AssetIntegration.cs` が同名パラメータを生成することを確認）。
  - delta-time（unity-code #2）: 準拠。攻撃周期の `_timer += Time.deltaTime` / `_timer -= AutoAttackInterval` は無変更。追加した SetTrigger/SFX は瞬間ヒットの副作用で時間非依存。フレーム固定加算の新規混入なし。
  - エンジン非依存コア（unity-code #3）: 準拠。追加副作用は `Components/AutoAttackDriver`（シーン配線層）に閉じ、`Systems/AutoAttackSystem`（純粋 C# 索敵）は無変更。`SfxLibrary` も Components 層で薄く（clip 保持＋共有 AudioSource のみ、鳴らす/鳴らさないは呼び出し側判断）。
  - 資産キー集約（unity-code #5）: 準拠。SFX clip は `Editor/AssetIntegration` が SerializeField 直代入（`WaveSpawner.enemyPrefab` と同じインスペクタ直参照パターン）、パス文字列は `GameConfig.AssetKeys` 経由でランタイムは文字列ロードせず。`Resources.Load` 直書きなし。
  - acceptance 影響: 満たしたまま（非退行）。追加された animator/SFX は両方 null ガード付きの純粋な提示副作用で、索敵・ダメージ刻み・2発撃破・範囲外非対象・入力非関与のいずれのアサーションにも介入しない。`SetTrigger("Attack")`/`SetFloat("Speed")` は `Hero.controller` に両パラメータが実在するため「parameter does not exist」警告を出さず、QA-PLAY の `LogAssert.NoUnexpectedReceived()` も汚さない。既存 PlayMode `AutoAttackSceneTests`（範囲内2発撃破・範囲外非対象・入力非干渉）は無変更で成立。
- 参考（非ブロッキング・対応不要・信頼度<80）:
  1. `Editor/AssetIntegration.cs` の AnimatorController 生成部に遷移 `duration = 0.1f` / `exitTime = 1f` の数値直書きがある（`RunSpeedThreshold` は GameConfig 抽出済みだが遷移時間は未抽出で非対称）。ただし Editor 資産オーサリング用の一回性定数であり S-06 ゲームプレイコードの範囲外。本ゲート（S-06）では非報告。
  2. `PlayerController` の `SetFloat(Speed, inputVector.magnitude)` は生入力の大きさを渡すため、ダッシュ中（入力ゼロで移動継続）は idle 表示になり得る。純粋に見た目の枝葉で機能・acceptance に無関係（信頼度<50）。
- 対応: 追加対応不要（findings 0件・APPROVE）。

## CR-CODE iteration 1 fix — 資産統合コミット再レビュー追加指摘対応（2026-07-11）
- 対象: 直前の「資産統合コミット c2089c3 — S-06 差分の再レビュー」に対する code-reviewer + silent-failure-hunter 合算レビューで新たに検出された5件。全件対応済み。
1. [MEDIUM] `Components/SfxLibrary.cs` / `Editor/AssetIntegration.cs:AssignClip` — 生成済み SFX-01〜04 の配線失敗が無音化される。
   - 対応: 対応済み。(a) `AssetIntegrationSceneTests` に `SfxLibrary_HasAllGeneratedClipsAssigned`（PlayMode）を新設し、配線済みシーンで `SfxLibrary.Instance` の `AttackHit`/`Dash`/`PlayerHit`/`CrystalPickup` が非null であることを機械検証。(b) `SfxLibrary.Awake()` に `LogMissingClipIfExpected` を追加し、`GameConfig.AssetKeys.Sfx*` が非空（＝生成済みのはず）にもかかわらず対応する SerializeField が null の場合のみ `Debug.LogError("[Wiring] ...")` を1回出す（WaveSpawner の `_prefabMissingAgentLogged` と同じ「配線破損は1回 LogError」方針・rule 12 準拠）。SFX-05/06 のような `AssetKeys` が空文字＝未生成のケースは従来通り無音（`Play(null)` のドキュメント化済み no-op は維持）。
2. [MEDIUM] `Editor/AssetIntegration.cs` `FixMaterialsForUrp` — `ExtractTextures` の bool 戻り値無視・テクスチャ0枚時の縮退ログ欠落。
   - 対応: 対応済み。`ExtractTextures` の戻り値を `texturesExtracted` に捕捉し、`!texturesExtracted || textureGuids.Length == 0` の場合に `Log("[DEGRADED] no textures extracted from <modelPath> ... — material(s) will have no BaseMap/BumpMap.")` を追加。既存の末尾サマリログ（`baseMap=none` 等）は変更せず併存。
3. [MEDIUM] `Tests/PlayMode/AssetIntegrationSceneTests.cs` `CaptureEvidenceScreenshot` — `Camera.main == null` の無言 return で視覚証跡未生成が不可視化。
   - 対応: 対応済み。`Assert.IsNotNull(cam, ...)` に変更しテスト失敗として表面化させた（配線済み Game シーンには Main Camera が必ず存在する前提のため妥当と判断。指摘の代替案「LogWarning のみ」は不採用 — シーン破損を検知できる方を優先）。
4. [LOW] `Editor/AssetIntegration.cs` `ConfigureHeroModel` — `MeasureHeight` の失敗センチネル `0f` が「補正不要」と区別不能。
   - 対応: 対応済み。`measuredHeight <= 0f` を明示分岐し `Log("[DEGRADED] hero height measurement failed (0m ...) — scale correction skipped.")` を出すように変更。既存の範囲外判定ロジック（`else if`）は変更なし。
5. [LOW] `Editor/AssetIntegration.cs` `BuildHeroController` — 既存 controller の存在チェックが誤パス合成（`Assets/` 二重除去）で常に false。
   - 対応: 対応済み。`File.Exists(...)` によるファイルシステムパス合成を廃止し `AssetDatabase.LoadAssetAtPath<AnimatorController>(path) != null` に置き換え（プロジェクト相対パスをそのまま使い、手動パス演算を排除）。
- 検証: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results.xml -logFile -` → exit 0、結果XML `total="80" passed="80" failed="0"`。`"$UNITY" -batchmode -projectPath game -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit -logFile game/Logs/build.log` → exit 0、ログに `Build succeeded: .../game/Build/ForgeGame.app`。追加で PlayMode（`-testPlatform PlayMode`）も実行し exit 0・`total="71" passed="71" failed="0"`（新設 `SfxLibrary_HasAllGeneratedClipsAssigned` を含む `AssetIntegrationSceneTests` 4件全て Passed、`Debug.LogError` 想定外発生なしを確認）。

## CR-CODE iteration 2 — CONCERNS（レビュー対象コミット不在＝ブロッカー）
- 日時: 2026-07-11T00:00:00Z
- 指定レビュー対象: コミット `COMMIT_BLOCKED_1PASSWORD_SIGNING`（タスクで固定指定・`git show` で取得指示）。
- 結論: このコミットは存在せず（`git show`/`git show-ref`/`git rev-parse` すべて解決不能）、レビューを実施できない。
- 根拠:
  - リポジトリの署名設定は `commit.gpgsign=true` / `gpg.format=ssh` / `gpg.ssh.program=/Applications/1Password.app/Contents/MacOS/op-ssh-sign`。非対話ハーネスでは 1Password の op-ssh-sign 承認プロンプトを通せず `git commit` が失敗するため、対象コミットは生成されなかった（ref 名 `COMMIT_BLOCKED_1PASSWORD_SIGNING` はこの事象を明示している）。
  - 作業ツリーにも S-06 のコアロジック（自動攻撃・敵HP・撃破）の未コミット変更は一切存在しない（`AutoAttackDriver/AutoAttackSystem/EnemyAgent/HealthComponent/HealthSystem` に diff なし）。ステージ済み変更は `SfxLibrary.cs`/`Editor/AssetIntegration.cs`/`AssetIntegrationSceneTests.cs`（=資産統合の再レビュー指摘5件の fix）であり、タスクが明示的にスコープ外とした「作業ツリーの未コミット変更」に該当し、かつ S-06 acceptance（索敵・瞬間ヒット・2発撃破・範囲外非対象・入力非関与）とは別領域のため代替レビュー対象にはならない。
- 判定の扱い: findings 0件ではないため APPROVE 不可。レビュー可能な成果物が存在しない状態で APPROVE を記録することは「証跡なき合格主張」（state/active.md の S-02 監査で silent-failure-hunter が検出した過去のプロセス欠陥）そのものであり、明確に回避する。設計欠陥（REJECT）ではなく修正可能なプロセス/ツールのブロッカーのため CONCERNS とする。
- 指摘要約（要対応・1件）:
  1. [BLOCKER/process] 指定レビュー対象コミットが 1Password 署名ブロックにより未生成でレビュー不能。remediation: 署名ブロックを解消（op-ssh-sign 承認を通す、またはハーネス方針として `git commit --no-gpg-sign` を許可）して当該変更を実コミット化し、生成された実 SHA を対象に CR-CODE を再実行する。実コミットが揃うまで S-06 iteration 2 の APPROVE は保留。
- 補足（非ブロッキング・事実確認）: S-06 のコア実装自体は既に別コミットでレビュー済み・合格（iteration 1 = 9ea0fce APPROVE、iteration 1 fix、iteration 2 = fda9c2b APPROVE。加えて資産統合コミット c2089c3 の S-06 差分再レビューも APPROVE）。本ブロッカーは「今回指定された特定コミット」が取得できない点に限定され、既存の合格記録を覆すものではない。
