# S-29 レビュー履歴（クリスタル発光強化 + パーティクル juice）

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-14T18:20:00Z
- 指摘要約（優先度順、全て minor）:
  1. **[minor]** `Editor/AssetIntegration.cs`: `BuildCrystalGlowVfxPrefab`/`BuildCrystalCollectVfxPrefab`（L955/L1009）が `HitVfxMaterial.mat` のディスク存在を「IMG-04 が今回の実行で健在」の代理指標にしている。IMG-04 テクスチャが消失した縮退実行では `BuildHitVfxPrefab` が `[DEGRADED]` で stale `HitVfx.prefab` を削除する一方、過去実行の `HitVfxMaterial.mat`（`_BaseMap` が宙吊り参照）はディスクに残るため、両ビルダーは stale マテリアルを参照した prefab を生成して `crystal_glow_vfx_built=True`/`crystal_collect_vfx_built=True` と成功報告する。同一レポート内の `hit_vfx_built=False` と矛盾し、テクスチャ無しの加算白矩形パーティクルが無言で出荷される（CR-CODE S-17 iter1 で塞いだ「stale 資産の無言配線」と同型・マテリアル1段下）。
  2. **[minor]** `Components/CrystalPickup.cs`: `SpawnDrop`（L45）/`TryPickup`（L152）の `if (CrystalVfxLibrary.Instance != null)` ガードは、ライブラリがシーンに存在しない/inactive な場合に S-29 の glow/collect juice 全体を一切のログ無しでスキップする。prefab 欠落時は `CrystalVfxLibrary.Awake` の `LogMissingPrefabIfExpected` が LogError で一度表面化するが、`Instance` 自体が null のケースは Awake が走らないため runtime では完全に無音 — 同メソッド内の `RunStatsTracker` null ガードが LogError する扱いと非対称。

- 対応:
  1. **対応した**（minor）。`IntegrateAll` が `BuildHitVfxPrefab` の結果（`hitVfxPrefab != null`）を `hitVfxHealthyThisRun` として `BuildCrystalGlowVfxPrefab`/`BuildCrystalCollectVfxPrefab` に明示的に渡すよう変更。両ビルダーは `hitVfxHealthyThisRun` が false の場合、`HitVfxMaterial.mat` のディスク存在確認自体をスキップして `material = null` 扱いにし、`[DEGRADED]` ログ + `DeleteStalePrefabIfPresent` で縮退する。指摘の代替案（`material.GetTexture("_BaseMap") == null` を検査する案）ではなく、既に `IntegrateAll` が計算済みの「同一実行での IMG-04 健全性」の一次シグナルをそのまま渡すゲート方式を採用した（BuildHitVfxPrefab のテクスチャ欠落分岐がマテリアル自体に一切触れずに early-return するため、`_BaseMap` を検査してもテクスチャ削除直後の実行以外の縮退経路——例: prefab save 失敗——を捕捉できず不完全なため）。これにより `hit_vfx_built=False` の実行では `crystal_glow_vfx_built`/`crystal_collect_vfx_built` も必ず `False` になり、レポート内の矛盾が構造的に解消される。
  2. **対応した**（minor）。`Components/CrystalPickup.cs` に静的 one-shot フラグ `_vfxLibraryMissingLogged`（`WaveSpawner._prefabMissingAgentLogged`/`SfxLibrary._mixerErrorLogged` と同型）を追加し、`SpawnDrop`/`TryPickup` の両方で `CrystalVfxLibrary.Instance == null` 分岐に `Debug.LogError("[Wiring] CrystalVfxLibrary.Instance is null — S-29 crystal glow/collect VFX disabled this session")` を1回だけ出す `LogVfxLibraryMissingOnce()` を追加した。既存 PlayMode テスト（`CrystalSceneTests`/`CrystalVfxSceneTests`）は全て `SceneManager.LoadSceneAsync(GameConfig.Scenes.Game, ...)` でシーンを丸ごとロードし `CrystalVfxLibrary.Instance` が非 null であることを前提に assert しているため、この LogError は通常のテストスイート実行では発火せず `LogAssert.NoUnexpectedReceived()` を壊さない。

- 検証: EditMode 160/160 pass（`qa/evidence/editmode-results.xml`）。PlayMode は初回実行で `CrystalSceneTests.KillingEnemyWithinPickupRadius_...`/`GameHudSceneTests.OverTime_WaveNumber_...` の2件が失敗したが、いずれも本修正と無関係な既知のタイミング依存（frame-poll ベース）テストで、単体フィルタ再実行（8/8 pass）およびフルスイート再実行（122/122 pass）で再現せず flaky と確認した。最終的な `qa/evidence/playmode-results.xml` は 122/122 pass。`ForgeGame.EditorTools.ForgeBuild.BuildMac` exit 0（`Build succeeded: .../game/Build/ForgeGame.app`、`game/Logs/build.log`）。未対応 finding は無し（全2件 対応済み）。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-14T09:30:00Z
- 指摘要約（優先度順、全て minor）:
  1. **[minor]** `Components/CrystalPickup.cs`: L30 の静的一発ログフラグ `_vfxLibraryMissingLogged` は、コメントが「`WaveSpawner._prefabMissingAgentLogged`/`SfxLibrary._mixerErrorLogged` と同型」と主張するが、両者は instance フィールド（シングルトンのコンポーネント寿命で自然リセット）であり同型ではない。static のため (a) Enter Play Mode Options（ドメインリロード無効）のエディタ反復や同一プロセス内の複数 PlayMode テストを跨いで永続し、最初の1回以降は同障害が再発しても無音に戻る、(b) リセット手段が無いためこの分岐自体のテストが実行順依存になりテスト0件だった。SubsystemRegistration でのリセットメソッド追加＋エラーパスの PlayMode テスト1本を推奨。
  2. **[minor]** `Editor/AssetIntegration.cs`: iter1 #1 の消費者側ゲート（`hitVfxHealthyThisRun`）は健全だが、宙吊り `_BaseMap` を持つ stale `HitVfxMaterial.mat` 自体は縮退実行後もディスクに残り続ける（`BuildHitVfxPrefab` のテクスチャ/シェーダ欠落分岐がマテリアルに一切触れず early-return するため）。現行3消費者は全て安全だが、今後 `HitVfxMaterialPath` を直接 `LoadAssetAtPath` する4人目の消費者が追加された瞬間に同型の「stale 資産の無言配線」が再発しうる。`DeleteStalePrefabIfPresent` と同じ論法で `BuildHitVfxPrefab` の縮退分岐で stale マテリアルを削除し発生源で閉じることを推奨。
- 対応:
  1. **対応した**（minor）。`CrystalPickup.cs` に `[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.SubsystemRegistration)] ResetStaticStateForDomainReloadDisabled()` を追加し、Play Mode への各エントリで `_vfxLibraryMissingLogged` をリセットするようにした（Unity 公式のドメインリロード無効時の static リセット手段）。フィールドのコメントも WaveSpawner/SfxLibrary との相違点（instance vs static の理由）を明記するよう修正。あわせて `Tests/PlayMode/CrystalVfxSceneTests.cs` に `SpawnDrop_LogsMissingLibraryExactlyOnce_AndStillSpawnsCrystal_WhenCrystalVfxLibraryInstanceIsNull` を新規追加し、CrystalVfxLibrary シングルトンを破棄した状態で `SpawnDrop` を2回呼び、1回目のみ `LogAssert.Expect` で捕捉・2回目は捕捉なし（再発すれば UTF の未処理エラーログとして自動失敗）で one-shot 挙動を機械検証、かつ VFX 無しでもクリスタル自体は生成される（no-op 縮退）ことも確認した。
  2. **対応した**（minor）。`AssetIntegration.cs` に `DeleteStaleHitVfxMaterialIfPresent(reason)`（`DeleteStaleHitVfxPrefabIfPresent` と同型: 存在すれば `AssetDatabase.DeleteAsset`、失敗時は `Fail()` でエスカレーション）を追加し、`BuildHitVfxPrefab` のテクスチャ欠落・シェーダ未解決の両 early-return 分岐で `DeleteStaleHitVfxPrefabIfPresent` の直前に呼ぶよう配線した（prefab 保存失敗の分岐は対象外 — その時点でマテリアル自体はこの実行で正しいテクスチャで健全に構築済みのため、stale ではない）。これにより発生源（`HitVfxMaterial.mat` 自体）で閉じ、将来の4人目の消費者が `hitVfxHealthyThisRun` ゲートを踏まずに直接ロードしても null を受け取るようになる。新規の自動テストは追加しなかった — 既存コードベースにも `DeleteStaleHitVfxPrefabIfPresent`（S-17 iter1/iter2 由来、iter1 で追加済み）を含め、AssetIntegration の縮退分岐（テクスチャ/シェーダ欠落時の資産削除）を直接検証する EditMode/PlayMode テストは1本も存在しない（`grep` で確認済み）。既存の生成済みシーン資産を破壊的に操作するテストの追加は本 story のスコープを超えると判断し見送った。
- 検証: EditMode `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results.xml` exit 0、160/160 pass, failed 0。PlayMode `"$UNITY" -batchmode -projectPath game -runTests -testPlatform PlayMode -testResults qa/evidence/playmode-results.xml` exit 0、123/123 pass, failed 0（新規テスト1本を含む）。`ForgeGame.EditorTools.ForgeBuild.BuildMac` exit 0（`Build succeeded: .../game/Build/ForgeGame.app`、`game/Logs/build.log`）。未対応 finding は無し（全2件 対応済み）。MAX_ITER(2) 到達のため本 story は status: done とする。
