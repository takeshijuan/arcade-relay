# S-15 — Menu アウトゲームパネル（実績・統計・essence/UPG 購入 UI）

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-22T07:32:38Z
- 指摘要約（優先度順）:
  1. [major] `MenuScreen.cs` の `TryPurchaseUpgrade`/`GetUpgradeLevel`（essence消費+Lv加算のUPG購入 reducer）が、正本であるべき `Systems/Meta/` ではなく Ui 層に実装されている（contract §11 / rules/unity-code.md 逸脱）。ただし純粋関数・数値は GameConfig.Meta 経由で gdd と一致・S-14未着手中の暫定実装であることがファイル冒頭コメントに明記済みのため blocker ではない。
  2. [major] `CreateIcon` の null 無言スキップが、実際には既に generated かつ Assets/Resources へ取込済みの IMG-04(IconEssence) を根拠に「IMG-05/06=planned」という不正確な理由で正当化されていた。ロード失敗時の異常が完全に不可視になり、acceptance の「アイコンにピンク/欠落が無い」が反証不能だった。
  3. [minor] 指摘1と同種（UPG購入ロジックのUi層配置）。stories.yaml S-14 側への逆リンク追加を推奨（ただし S-14 は ui-engineer のレーン境界外）。
  4. [minor] ACH-04「現在値」表示が `GameFlow.LastRunResult`（プロセス内キャリー）に依存し、フレッシュ起動直後の Menu では常に0表示になり誤解可読性がある。恒久対応は SaveData への永続フィールド追加（Systems/Meta/Persistence 変更）が必要でレーン外。
  5. [minor] 購入成功/資金不足の PlayMode テストが、永続化(ISaveStore.Save)の発生/非発生を一切 assert していない。`PersistPurchase` 呼び出しを削除してもテストが通ってしまう。

- 対応:
  1. **見送り（正当理由: レーン境界）**。ui-engineer は `Assets/Scripts/Systems/` を編集できず、S-14（gameplay-engineer・build phase）は本コミット時点で未着手。既にファイル冒頭コメントに「S-14 着手時に Systems/Meta 側 reducer 呼び出しへ置き換える」移行方針を明記済みであることに加え、CR-CODE iter1 の判定結果（techdebt追跡・stories.yaml S-14 側への逆リンク推奨）をヘッダコメントへ追記した。stories.yaml S-14 ブロックの編集は担当外のため本 iteration では行わない（S-14 着手時 or workflow のバッチ検証区間で対応されるべき技術的負債として明記）。
  2. **対応済み**。(a) `CreateIcon` のドキュメンテーションコメントを実態（IMG-04=generated かつ Assets/Resources へ取込済み、IMG-05/06 は generated だが本ファイルから未呼出）に合わせて修正。(b) null 時に `Debug.LogWarning` を1回発行するよう変更（Error 昇格はQA-PLAYのログエラー0検査を汚すため見送り、既存の null-CurrentSaveData パターンと同じ Warning 方針に統一）。(c) `MenuScreenPlayModeTests.OutgameDisplay_ReflectsLoadedSaveData` に `EssenceIcon` の実在 + `Image.sprite` 非null の assert を追加し、退行を検知可能にした。
  3. **見送り（次iteration/S-14着手時に対応）**。理由は対応1と同様（stories.yaml S-14 ブロックはレーン境界外）。MenuScreen.cs 冒頭コメントに techdebt 追跡の明記を追加済み。
  4. **見送り（レーン外・Checkpoint C エスカレーション対象）**。SaveData への永続フィールド追加は Systems/Meta + Persistence 層の変更を要し ui-engineer のレーン境界外。既存コードコメント（GetAchievementProgress のXMLコメント）に妥協点として明記済み。workflow への未解決事項として報告する。
  5. **対応済み**。`ClickUpg01BuyButton_IncreasesLevelAndDecreasesEssence` と `ClickUpg01BuyButton_WithInsufficientEssence_DoesNotChangeLevel` の両テストで `InMemorySaveStore` インスタンスを変数保持し、購入操作後に `saveStore.Load().Data` を assert（成功時: essence=70・UPG-01 Lv=1 に永続化されていること／不足時: essence=5・UPG-01 Lv=0 のまま保存されていないこと）。

## 検証（並走レーン規律: Unity 未起動）
- 静的確認（Read/Grep）で以下を確認: `LoadResult.Data`（`Systems/Meta/MetaTypes.cs`）・`InMemorySaveStore` コンストラクタ引数 `initial`（`Persistence/InMemorySaveStore.cs`）・`SaveData.Clone()` の public 修飾・`SaveData.essence`/`upgStartingGoldLv` フィールドの存在。
- `game/Assets/Resources/Generated/textures/icon-essence.png`（+`.meta`）の実在を確認（IMG-04・generated・取込済み）。
- Unity バッチモード / EditMode・PlayMode 実行はレーン合流後のバッチ検証区間に委ねる（本 iteration では起動しない）。

## 未解決事項（Checkpoint 提示用）
- ACH-04 の「現在値」表示がプロセス内キャリーのみで永続化されていない（フレッシュ起動直後は常に0表示）。恒久対応には SaveData への永続フィールド追加（例: `maxAoeKillsInRun`）とマイグレーションが必要。S-14 または game-designer への提案事項。
- UPG購入トランザクション reducer が暫定的に Ui 層（MenuScreen.TryPurchaseUpgrade）にあり、S-14 着手時に Systems/Meta 正本へ置き換える必要がある技術的負債。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-22T07:41:36Z
- 指摘要約（優先度順）:
  1. [major] `MenuScreen.cs` `CreateIcon`（L481-487）: iter1 で「異常系（キーtypo/.meta退行/取込順序ズレ）のみを指す・正当な未生成状態ではない」と自ら認定した回復不能条件を、なお `Debug.LogWarning` で記録していた。理由付け（「Error 昇格は QA-PLAY のログエラー0検査を汚すため」）自体が gates.md CR-CODE の明示禁止パターン（回復不能条件を最初から Warning 以下で記録）に該当。健全なビルドでは本ログは発火しないため LogError化しても偽陽性は生じない。追加した PlayMode assert も現在の唯一の呼び出し元（EssenceIcon/IMG-04）のみを守り、将来の IMG-05/06 呼び出し元はテスト非対象のまま。
  2. [minor] `MenuScreenPlayModeTests.cs` L125: `essenceIconImage?.sprite` の `?.` が UnityEngine.Object のオーバーロード済み null 判定を迂回し、Image 欠落時に fake-null が素通りして無説明の `MissingComponentException` でテストが落ちる。
- 対応:
  1. **対応済み**。`CreateIcon` の `Debug.LogWarning` を `Debug.LogError` に昇格。ドキュメンテーションコメントを「本条件には正当な到達経路が存在しない（CR-CODE iter1 #3 の null-CurrentSaveData とは事情が異なる）」と明記した上で tech-stack-unity.md 規約12（LogWarning は縮退が正当なケース限定）への準拠を明示。将来 IMG-05/06 の呼び出し元が配線された際も本 LogError が QA-PLAY 観点1（起動ログエラー0検査）の安全網として機能する旨をコメントに追記（PlayMode テストのカバレッジ有無に依存しない設計に変更）。
  2. **対応済み**。`essenceIcon.GetComponent<Image>()` の結果を変数に保持し、`Assert.IsNotNull(essenceIconImage, ...)` → `Assert.IsNotNull(essenceIconImage.sprite, ...)` の2段アサートに分離。UnityEngine.Object の通常の null 判定（`==`）を経由するため fake-null 時も診断メッセージ付きで正しく検知される。

## 検証（並走レーン規律: Unity 未起動・iteration 2）
- `game/Assets/Scripts/Ui/MenuScreen.cs`: `Debug.LogError` は `UnityEngine.Debug` の既存 static メソッド（`LogWarning` と同一クラスの姉妹メソッド）でシグネチャ変更なし。コンパイル影響なし。
- `game/Assets/Tests/PlayMode/MenuScreenPlayModeTests.cs`: `Image` 型は既存 `using UnityEngine.UI;` 経由で解決済み（同ファイル内の他アサートで使用中）。`essenceIcon.GetComponent<Image>()` は既存コードの呼び出しパターンと同一。`Image.sprite` プロパティの実在を `UnityEngine.UI.Image` API（既存利用箇所 L109/113/116 相当の `GetComponent<Text>()` パターンと同型）で確認。
- Unity バッチモード / EditMode・PlayMode 実行はレーン合流後のバッチ検証区間に委ねる（本 iteration では起動しない）。

## MAX_ITER 到達によるエスカレーション
- MAX_ITER(2) に到達。findings は上記 対応済み。status を `done` に更新し、未対応の技術的負債（上記「未解決事項」節: ACH-04 現在値の非永続化・UPG購入reducerのUi層暫定配置）は Checkpoint での人間提示に委ねる。
