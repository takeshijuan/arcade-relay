# S-15 レビュー履歴 — ウェーブ切替フィードバック（SFX + HUD パルス）

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-13T07:00:00Z
- 指摘要約（優先度順・全て severity: minor）:
  1. [死にコード / SfxLibrary.cs:37-44,92] `PlayCallCountForTests` はどのテストからも参照されておらず、doc コメントの「S-15's PlayMode test uses this to assert the wave-start SFX fires exactly once」は虚偽（実際は `GameHudController.WaveStartSfxTriggerCountForTests` を使用）。将来の読者がテスト観測点を誤認する。
  2. [自己完結性 / GameHudController.cs:145] `WaveStartSfxTriggerCountForTests++` が `SfxLibrary.Instance != null` ガードの外にあるため、カウンタは「SFX再生」ではなく「トリガ判断」を数えている。`WavePulseSceneTests` の『SFX 再生が1回発火』assert が単体では自己完結しない（SfxLibrary 不在でも成立してしまう）。
  3. [結合度 / GameHudController.cs:107-117] `UpdateWavePulse()` が `UpdateWave()` 内の `WaveSpawner.Instance` null ガードの後ろにあるため、パルス進行中に WaveSpawner が消えると HUD ウェーブ数値スケールが永久凍結し得る。現行ライフサイクルでは到達不能だが、パルス減衰はトリガ元と独立させるべき。
  4. [テストのフレーク源 / WavePulseSceneTests.cs FastForwardToWave2] `safetyMarginSec=3f`・`fastTimeScale=20f` の組み合わせは `Time.maximumDeltaTime`（既定 1/3s）との相互作用で (a) 早送り中の実効加速が20倍未満になり6秒のポーリング期限に届かない、(b) 遷移フレームの deltaTime が `WavePulseDuration`(0.3s) を超えパルスが1フレームで完了し `localScale>1f` のサンプリングが失敗する、のいずれかの余地がある。
- 対応:
  1. 対応済み。`SfxLibrary.PlayCallCountForTests` プロパティおよびインクリメント処理（L37-44, L92 相当）を削除。死にメンバーと虚偽コメントを除去。テスト観測は既存の `GameHudController.WaveStartSfxTriggerCountForTests` に一本化（元々こちらが実際に使われていた）。
  2. 対応済み。`WavePulseSceneTests.WaveIncrease_TriggersWaveStartSfxExactlyOnce_AndPulsesHudWaveScale` の冒頭（spawner 取得直後）に `Assert.IsNotNull(SfxLibrary.Instance, ...)` と `Assert.IsNotNull(SfxLibrary.Instance.WaveStart, ...)` を追加し、`AssetIntegrationSceneTests.SfxLibrary_HasAllGeneratedClipsAssigned` と同型の非null検査で SFX-05 クリップの実在を本テスト内で保証。これにより後段の trigger-count assert が「実際に Play() が呼ばれた」ことの自己完結した証拠になる。`GameHudController` 側のカウンタ実装自体は変更していない（トリガ判断カウントのままだが、テストがクリップ実在を先に保証するため実害を解消）。
  3. 対応済み。`GameHudController.Update()` で `UpdateWavePulse()` を `UpdateWave()` の外（`WaveSpawner.Instance` null ガードの影響を受けない位置）に移動。`UpdateWave()` からは呼び出しを削除し、`UpdateWavePulse()` の doc コメントに変更理由（減衰はトリガ元データソースと独立させるべき）を追記。
  4. 対応済み。`WavePulseSceneTests` に `_originalMaximumDeltaTime`/`_maximumDeltaTimeOverridden` フィールドを追加し、`FastForwardToWave2` が `Time.timeScale` を通常速度に戻した直後（早送り区間終了後・ポーリング開始前）に `Time.maximumDeltaTime` を `GameConfig.Fx.WavePulseDuration * 0.5f` に一時変更、`TearDownSession` で必ず復元する実装に変更。早送り区間（`WaitForSecondsRealtime` ベースで実時間依存・timeScale/maximumDeltaTime非依存）には手を入れず (a) を悪化させないようにしつつ、(b) の「遷移フレームの deltaTime がパルス全体を1フレームで消費する」経路を構造的に排除した。
- 検証: EditMode `qa/evidence/editmode-results.xml`（145/145 passed, failed 0, exit 0）、PlayMode `qa/evidence/playmode-results.xml`（99/99 passed, failed 0, exit 0 — `WavePulseSceneTests.WaveIncrease_TriggersWaveStartSfxExactlyOnce_AndPulsesHudWaveScale` と `GameHudCanvas_StillScreenSpaceCamera_WithWaveScaleFeature` を含む）、build `ForgeBuild.BuildMac`（`game/Logs/build.log` に `Build succeeded`, exit 0, `game/Build/ForgeGame.app` 生成確認）。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-13T07:40:00Z
- 指摘要約（優先度順・全て severity: minor。対象ファイル: `game/Assets/Tests/PlayMode/WavePulseSceneTests.cs`）:
  1. iteration 1 の Fix 4（`Time.maximumDeltaTime` cap = `WavePulseDuration*0.5f` = 0.15s）はフレークを構造的には排除していない。`GameHudController.Update()` 順序次第でウェーブ切替の遷移フレーム自身が既にパルス開始とその同一フレームでの `UpdateWavePulse` 蓄積を行い、さらにテストの明示的な `yield return null`（L117 相当）がもう1フレーム蓄積するため、2フレーム連続で cap に張り付くと 0.15+0.15==0.30==`WavePulseDuration` に到達し、`UpdateWavePulse` の `>=` 判定でスケールが 1.0 にスナップされてから `Assert.Greater(localScale.x, 1f)` がサンプルし失敗し得る。修正案: cap を `WavePulseDuration/3f` や `/4f` に下げ、2〜3 蓄積フレームでも duration 未満を維持するマージンを持たせる。
  2. 早送りレグ（`WaitForSecondsRealtime` 区間）中は `Time.maximumDeltaTime` の cap が未装着（timeScale=1 復帰後にしか装着されない）ため、プロジェクト既定値 0.3334s のまま `fastTimeScale=20` と掛け合わさり、1フレームで最大 ~6.67s の game time が進み得る。旧 `safetyMarginSec=3f` ではこれを吸収できず、レグ内で Wave1→2 境界を越え、かつその越えた巨大 deltaTime のフレームがパルス全体を1フレームで消費し得る。このオーバーシュート自体はどこにも検出・報告されず、失敗はL122 のmid-pulse assert という無関係に見えるメッセージで表面化する。修正案: `Time.timeScale=1f` 直後・cap装着前に `Assert.AreEqual(1, spawner.CurrentWave, ...)` を追加して明示的な setup failure に変える、または `safetyMarginSec` を `fastTimeScale × 既定maximumDeltaTime`（≈6.67s）超（例 8f 以上）へ引き上げる。
  3. 指摘1と本質的に同一（cap 値 0.15s が2フレーム蓄積でパルス全体を消費し得るという同一現象の重複報告）。指摘1の対応で解消。
- 対応:
  1. 対応済み。`FastForwardToWave2` の `Time.maximumDeltaTime` cap を `GameConfig.Fx.WavePulseDuration * 0.5f`(0.15s) から `GameConfig.Fx.WavePulseDuration / 4f`(0.075s) に変更。トリガフレーム＋テストの追加 `yield return null` の2フレーム分が両方 cap に張り付いても合計 0.15s（duration の半分）に留まり、`>=` スナップバックまで 0.15s のマージンを確保。3〜4フレーム連続でも 0.3s 未満を維持する。
  2. 対応済み。`safetyMarginSec` を `3f` から `10f`（`fastTimeScale(20) × 既定maximumDeltaTime(0.3334s)`≈6.67s の worst-case 単フレームジャンプに対して十分な余裕を持つ値）へ引き上げ、かつ `Time.timeScale = 1f;` 直後・cap 装着前に `Assert.AreEqual(1, spawner.CurrentWave, "fast-forward leg overshot into wave 2 while Time.maximumDeltaTime was still uncapped...")` を追加。オーバーシュートが発生した場合、mid-pulse assert の紛らわしい失敗ではなく、この明示的な assert が原因を特定できるメッセージ付きで先に落ちるようにした。`safetyMarginSec` 引き上げに伴い、その後のポーリング `deadline`（旧: 固定 `6f`）も `safetyMarginSec + 5f`（≈15s）に連動させて拡張（timeScale=1 復帰後はもう早送りされないため、10s のマージンを閉じるには実時間で概ね10s前後必要になるため。旧 6f 固定のままだと逆に新規タイムアウト失敗を生むことを実機テストで確認し修正）。
  3. 指摘1の対応で解消（同一finding）。
- 検証: EditMode `qa/evidence/editmode-results.xml`（145/145 passed, failed 0, exit 0）。PlayMode を連続2回実行し両方 `qa/evidence/playmode-results-s15-fix2b.xml` / `playmode-results-s15-fix2c.xml`（各99/99 passed, failed 0, exit 0 — `WavePulseSceneTests` の両テストを含む）でフレーク再発無しを確認（初回の中間検証 `playmode-results-s15-fix2.xml` は safetyMarginSec 引き上げ直後・deadline 未調整の状態で `WaveIncrease_...` が意図通り新しい原因（poll deadline 不足）で1件失敗し、deadline 修正後に解消したことを記録として残す）。build `ForgeBuild.BuildMac`（`game/Logs/build.log` に `Build succeeded`, exit 0, `game/Build/ForgeGame.app` 生成確認）。
- MAX_ITER(2) 到達につき、本 iteration の対応をもって S-15 のレビューループを終了し status を `done` に更新（review-loops.md: findings解消 or 正当理由の明記が合格基準。全指摘に対応済みのため未解決事項なし）。
