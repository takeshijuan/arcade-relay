## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-23T04:50:00Z（対応記録時点。判定自体の受領日時は呼び出し元記録を参照）
- 指摘要約（優先度順）:
  1. [minor] `CoreView.cs` が被弾時のヒットフラッシュで `Renderer.material` に書き込んでおり、初回アクセスでレンダラごとにマテリアルをインスタンス化するが `OnDestroy` で破棄していない。`SceneManager.LoadScene` によるリスタートのたびにインスタンスマテリアルが解放されず累積リークする（`PlaceholderFactory.materialCache` が明示的に回避しているのと同種のリーク）。
  2. [minor] `Update()` が `hitFlashElapsedSec += Time.deltaTime` を適用前に加算しているため、被弾直後の最初のフレームで「0秒時点で最大フラッシュ」というコメントの意図が描画されない。さらに初回フレームの `deltaTime` が `CoreHitFlashSec` 以上だと `intensity=0` のまま演出全体が無変化で終わりうる（flaky の原因）。
  3. [minor] `CoreHitFlashPlayModeTests` が「変化観測→復帰観測」の2点しか見ておらず、`CoreHitFlashSec` の経過時間そのものを検証していない。1フレームで即復帰する誤実装でも合格してしまう。
  4. [minor] Awake のコメント「実際のインスタンス化はフラッシュ開始時に遅延する」が不正確（`CurrentTintColor` getter も `.material` アクセスでインスタンス化するため実際は被弾前の読み取りでも発生しうる）。上記1と同根。
  5. [minor/process] レビュー対象 ref が `COMMIT_FAILED`（1Password SSH signing agent 障害で S-26 のコミットが未成立）。作業ツリー上の差分に対してレビュー実施。
- 対応:
  1. 対応した。`Renderer.material` への書き込みをやめ、`MaterialPropertyBlock`（`Shader.PropertyToID("_BaseColor")`。プロジェクト内で一貫する `Universal Render Pipeline/Lit` の主色プロパティ）経由の一時色上書きへ変更。マテリアルをインスタンス化しないため `OnDestroy` での破棄も不要になった。`CurrentTintColor` は `currentTintColors` フィールド（`MaterialPropertyBlock` 適用時に同時更新）を返すよう変更し、`.material`/`.sharedMaterial.color` の読み返しをやめた。
  2. 対応した。`Update()` を「t/intensity を計算 → 適用 → (継続時のみ) 時間を加算」の順序へ変更。`hitFlashElapsedSec=0` の最初のフレームで必ず `intensity=1`（最大フラッシュ・最大パルス）が適用されてから次フレーム用に時間を進めるため、フレームレート・deltaTime の大小に関わらず最低1フレームは可視変化が保証される。
  3. 対応した。`CoreHitFlashPlayModeTests` に `CoreHitFlashSec` の半分が経過するまで変化が継続していることを検証する下限アサート（`stillChangedNearHalfDuration`）と、既定値への復帰が `CoreHitFlashSec` の半分以上経過してから起きていることを検証する下限アサート（`elapsedSinceApply >= halfDuration`）を追加。1フレーム即復帰の誤実装は失敗するようになった。
  4. 対応した。不正確だったコメントを削除し、`MaterialPropertyBlock` 方式への変更を反映した新コメントに置き換えた（Awake の該当行、および新設 `ApplyTint` メソッドのXMLコメント）。
  5. 対応なし（プロセス指摘であり本 iteration のコード修正対象外）。ただし本 fix commit も同一の 1Password signing agent 障害（`op whoami` → account is not signed in → `error: 1Password: failed to fill whole buffer` → `fatal: failed to write commit object`）で2回リトライ後も失敗した。詳細は state/stories.yaml の S-26 note(infra-blocker追記) を参照。--no-gpg-sign 等の署名バイパスは規約上使用していない。
- 検証: Unity は起動していない（並走レーン規律）。修正で参照した型・メンバの静的確認:
  - `MaterialPropertyBlock` / `Renderer.GetPropertyBlock` / `Renderer.SetPropertyBlock` / `Shader.PropertyToID` は `UnityEngine` 標準API（既存コードで `Shader.Find` 等の同名前空間APIを多用しているプロジェクトで参照実績あり）。
  - `GameConfig.Presentation.CoreHitFlashSec` / `GameConfig.Presentation.CoreHitScalePulse` の実在を `game/Assets/Scripts/GameConfig.cs` で確認済み（既存story実装から変更なし）。
  - `CoreHitFlashPlayModeTests.cs` から追加参照した `GameConfig`（`ForgeGame` 名前空間、既存 `using ForgeGame;` で解決）・`Time.time`（`UnityEngine`, 既存 `using UnityEngine;` で解決）の実在をファイル内 using 節と `GameConfig.cs` の namespace 宣言（`namespace ForgeGame`）で確認済み。
  - `CoreView.cs` 内の新規参照（`currentTintColors` フィールド・`propertyBlock` フィールド・`ApplyTint` メソッド）はすべて同一ファイル内定義で完結しており未定義参照は無い。
  - コミットは1Password signing agent 障害により未成立（COMMIT_FAILED）。git 側のワークツリー破損（index.lock 等）は無く、修正内容自体はワークツリー上に存在する。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-22T20:07:17Z
- 指摘要約（優先度順）:
  1. [major] `CoreView.cs` の `ApplyTint`（旧124-134行）は MaterialPropertyBlock で `"_BaseColor"` に書き込むが、この前提が成立するのは PlaceholderFactory 自作の URP/Lit マテリアルのみ。実際は `model-core-crystal.glb` が取込済みで `Resources.Load` が成功するため、Renderer 群は glTFast（`com.unity.cloud.gltfast` 6.19.0）取込マテリアル＝glTF/PBRGraph 系シェーダグラフで、主色プロパティ名は glTF 準拠の `baseColorFactor`（`_BaseColor` は存在しない）。存在しないプロパティへの MPB `SetColor` は黙って無視され、ベース色キャッシュの `sharedMaterial.color`（旧70-72行）も実ベース色を返さない。`CurrentTintColor` は「設定した意図値」を追跡する test seam のため、レンダリングが無変化でも PlayMode テストは合格し、被弾ヒットフラッシュが出荷経路で silent failure になる。
  2. [minor] `CoreHitFlashPlayModeTests.cs` の halfDuration 下限アサート（旧66-74行）に2つのフレーク窓: (a) 最初の変化観測までに75ms（`CoreHitFlashSec` 0.15sの半分）超が経過するとループ本体が一度も実行されず `stillChangedNearHalfDuration=false` のまま spurious fail、(b) ループ条件が yield 前判定のため判定通過後のフレームスパイクで復帰後を観測し spurious fail。
  3. [minor/process] レビュー対象 ref `COMMIT_FAILED`（1Password SSH signing agent 障害で S-26 本体・iter1 fix ともコミット未成立）は解決不能。working tree 上の差分に対してレビュー実施。
- 対応:
  1. 対応した。`UrpLitBaseColorPropertyId`（`"_BaseColor"`）と `GltfBaseColorPropertyId`（`"baseColorFactor"`。Library/PackageCache の `com.unity.cloud.gltfast.../Runtime/Scripts/Material/MaterialProperty.cs` で `MaterialProperty.BaseColor = Shader.PropertyToID("baseColorFactor")` と確認済み）の2つを定義し、Awake 時に各 Renderer の `sharedMaterial.HasProperty` で実際に持つ主色プロパティを解決（`colorPropertyIds[]`/`resolvedColorProperty[]`）。ベース色の初期読み取り（`visualBaseColors[i]`）も解決済みプロパティから `GetColor` する形へ変更し、`ApplyTint` の書き込み先も同プロパティを使うよう変更。どの Renderer も解決できない場合は `Debug.LogError("[CoreView] no renderer material exposes a known base color property ...")` を1回出し、無言no-op化を防止（`GeneratedModelFactory` の Renderer欠落ログと同型）。glTFast の URP マテリアル生成経路（`UniversalRPMaterialGenerator : ShaderGraphMaterialGenerator`）を Library/PackageCache 内のソースで確認し、`model-core-crystal.glb.meta` が glTFast の ScriptedImporter（guid一致）で取り込まれていることも確認済み。
  2. 対応した。「half-duration 経過前を毎フレーム即時アサート」する単調な監視ループへ置換（`for (int i = 0; i < maxWaitFrames && Time.time - applyTime < halfDuration; i++) { Assert.IsTrue(stillChanged, ...); yield return null; }`）。どのフレーム間隔でも成立し、最初の観測遅延やフレームスパイクに起因する spurious fail が構造的に発生しない形にした（`maxWaitFrames` 境界は既存パターンに合わせて維持し無限ループを回避）。
  3. 対応なし（プロセス指摘であり本 iteration のコード修正対象外。1Password signing agent 障害は本レーンで解決不能な外部要因）。
- 検証: Unity は起動していない（並走レーン規律）。修正で参照した型・メンバの静的確認:
  - `Material.HasProperty(int)` / `Material.GetColor(int)` / `Shader.PropertyToID(string)` は `UnityEngine` 標準API（既存コードで同名前空間APIを多用しているプロジェクトで参照実績あり）。
  - `"baseColorFactor"` が glTFast の主色プロパティ名であることを `game/Library/PackageCache/com.unity.cloud.gltfast@92828f845157/Runtime/Scripts/Material/MaterialProperty.cs`（`BaseColor = Shader.PropertyToID("baseColorFactor")`）と `UniveralRPMaterialGenerator.cs`（`ShaderGraphMaterialGenerator` 継承・URP経路で使用）の実ソースで直接確認した（読み取り専用レビューではなく、パッケージキャッシュのソースコードを直接参照した一次情報）。
  - `model-core-crystal.glb.meta` の `ScriptedImporter` guid（`715df9372183c47e389bb6e19fbc3b52`）が glTFast の importer であることを確認済み（同ファイルは `game/Assets/Resources/Generated/models/` 配下）。
  - `CoreView.cs` 内の新規参照（`colorPropertyIds`/`resolvedColorProperty` フィールド）はすべて同一ファイル内定義で完結しており未定義参照は無い。`CoreHitFlashPlayModeTests.cs` の変更も既存 using 節で解決済みの型のみ使用。
  - コミットは iter1 と同一の1Password signing agent 障害（`op whoami` → "account is not signed in" → `error: 1Password: failed to fill whole buffer` → `fatal: failed to write commit object`）で2回リトライ後も失敗（COMMIT_FAILED）。対象4ファイルは `git add` 済みで index 上に存在し、破損は無い。詳細は state/stories.yaml の S-26 note(infra-blocker追記・CR-CODE iter2 fix) を参照。
