## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-14T05:30:00Z
- 指摘要約（優先度順）:
  1. [major] `Components/ArenaBackdrop.cs` が `Start()` 毎にランタイムで `Shader.Find("Skybox/6 Sided")` を呼びマテリアルを `new` していたが、このビルトインシェーダを参照する `.mat` 資産が存在せず `GraphicsSettings.asset` の Always Included Shaders にも無いため、スタンドアロンビルド（出荷物）ではシェーダがストリップされ `Shader.Find` が `null` を返し、Skybox がシーン既定のまま=S-26 の機能が丸ごと欠落するリスク。Editor 実行のみの PlayMode テストでは検知不能。
  2. [major]（重複詳細）同上ファイル L40 — 修正案(a) エディタ時に `.mat` 資産として焼き込む / (b) Always Included Shaders への追加、のいずれかを要求。
  3. [major] `game/Assets/Scenes/Game.unity` に配線結果（ArenaBackdrop root + IMG-06 参照）が未コミット（d2ef822 時点で0件、working tree のみに存在）。クリーンチェックアウトでは新規 PlayMode テスト3件が fail する。
  4. [major] `Tests/PlayMode/ArenaBackdropSceneTests.cs` の `Capture_BackdropVsForeground_Evidence...` が「四隅が非白」「前景と1隅が色距離>0.05」のみを検証し、ニアブラック void（旧実装の欠陥そのもの）でも全 assert が通ってしまう＝「背景描画」自体を機械検証できていない。
  5. [minor]（重複詳細）同上 — 「上コーナー群と下コーナー群の色距離」による縦グラデーション実証の追加を提案。単純な「コーナー非黒」は上コーナーが実測ニアブラックのため false-fail する。
  6. [minor] `LastAppliedSkyboxForTests` の doc（"Null when Start() bailed out"）が実挙動と乖離 — 成功時のみ代入・リセット無しのため bail 時に前回成功時の stale material を保持し続ける。
  7. [minor] `Start()` 毎の `new Material` を `Destroy` していないため、Game シーン再ロードの度にネイティブ Material がリークする。
- 対応:
  1&2. **対応済み**。`Components/ArenaBackdrop.cs` からランタイム `Shader.Find`/`new Material` 構築を全廃し、`[SerializeField] Material _skyboxMaterial` を `RenderSettings.skybox` へ代入するだけの薄い配線に変更。`Editor/AssetIntegration.cs` に `BuildArenaBackdropSkyboxMaterial()`（`BuildHitVfxPrefab` の idempotent load-or-create-in-place パターンを踏襲）を追加し、Editor 時に `Assets/Generated/Materials/ArenaBackdropSkybox.mat` を生成・IMG-06 を全6面に焼き込み・保存、`PatchArenaBackdrop()` から `ArenaBackdrop._skyboxMaterial` へ SerializedProperty 経由で配線する。マテリアル資産がビルドに含まれるシーンの SerializeField から参照されることで、Unity の既定シェーダストリッピングがこのビルトインシェーダを自動的にビルドへ含める（Unity 公式の既知の回避策）。`GraphicsSettings.asset` の Always Included Shaders への追加（案(b)）は、案(a) 単独で十分かつ fileID を推測で編集するリスクの方が大きいため見送り（理由: builtin shader の fileID は既知の一覧が無く誤った値を書くとビルド破損の恐れがあるため、公式に推奨される「アセット参照によるシェーダ包含保証」の方を採用）。ビルド後の実機検証として `ForgeBuild.BuildMac` の成果物 `.app` を実際に起動し Player.log を確認、`[Wiring]`/shader-not-found エラーが出ないことを確認済み（qa/evidence/standalone-s26-crfix-player.log）。
  3. **対応済み**。`AssetIntegration.IntegrateAll`（`PatchArenaBackdrop` 含む）を再実行し、配線済み `Game.unity`（新 `_skyboxMaterial` 参照込み）と新規 `Assets/Generated/Materials/ArenaBackdropSkybox.mat`(+`.meta`) を本コミットに含めた。
  4&5. **対応済み**。`ArenaBackdropSceneTests.cs` の Capture テストに、上コーナー平均色と下コーナー平均色の `ColorDistance` が閾値 0.05 を超えることを検証するアサーションを追加（`IMG-06` の縦グラデーションが実際にレンダリングされている証明。void や単色フォールバックでは上下が等しくなり fail する）。既存の非白・前景判別チェックはそのまま維持（指摘通り「コーナー非黒」への置換はしていない — 上コーナーは実測ニアブラックのため false-fail するとの指摘を踏まえた）。
  6. **対応済み**。`Start()` 冒頭で `LastAppliedSkyboxForTests = null;` にリセットしてから配線を試みるよう変更。doc と実挙動が一致。
  7. **対応済み**。ランタイム `new Material()` 呼び出し自体を廃止（Editor で焼き込んだ同一アセットインスタンスを毎回再利用）したため、リークの発生源が構造的に無くなった。

### 検証結果
- EditMode（typecheck相当）: `qa/evidence/editmode-results-s26-crfix.xml` — 160/160 pass, exit 0
- AssetIntegration 再実行（マテリアル焼き込み・シーン再配線）: `qa/evidence/assetintegration-s26-crfix.log` — exit 0, `PatchArenaBackdrop: IMG-06-backed ArenaBackdropSkybox material assigned...`
- PlayMode: `qa/evidence/playmode-results-s26-crfix.xml` は1件 fail（`CrystalSceneTests.KillingEnemyWithinPickupRadius_DropsCrystal...` — S-26と無関係な既存テストの一過性フレーク）。再実行 `qa/evidence/playmode-results-s26-crfix-retry.xml` — 112/112 pass, exit 0（`ArenaBackdrop_RootExists_AndWiresRenderSettingsSkybox` を含む新規/既存 ArenaBackdrop 系3テスト全て pass）
- build相当: `game/Logs/build-s26-crfix.log` — exit 0, `Build succeeded: .../game/Build/ForgeGame.app`
- 追加の実機確認: ビルド済み `.app` を起動し Player.log にシェーダ欠落/Wiring エラーが無いことを確認（`qa/evidence/standalone-s26-crfix-player.log`）

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-14T05:20:00Z
- 指摘要約（優先度順、いずれも minor）:
  1. `BuildArenaBackdropSkyboxMaterial` の最終 `AssetDatabase.LoadAssetAtPath`（L1293相当）が null を返すケース（CreateAsset 失敗）が無言 Failed になり、report からは原因不明。ログを追加すべき。
  2. 縮退時（IMG-06 テクスチャ欠落 / マテリアル構築失敗）に、以前の成功run が焼き込んだ `_skyboxMaterial` 参照と `ArenaBackdropSkybox.mat` がそのまま残る（S-17 iter2 major と同型の stale 資産残置パターン）。
  3. acceptance が「マテリアルキーは GameConfig の AssetKeys 経由」と明記するが、`ArenaBackdropSkyboxMaterialPath` が `Editor/AssetIntegration.cs` の private const のまま。AssetKeys への移設 or 非対応理由の明記が必要。PlayMode テストもテクスチャキーのみ検証しマテリアルキー未検証。
  4. iteration 1 の対応記録が引用する `qa/evidence/*-s26-crfix*`（editmode-results / playmode-results(-retry) / assetintegration.log / standalone-player.log）が untracked のままコミットに含まれていない（iter1 major#3 と同型の検証再現性欠如）。
- 対応:
  1. **対応済み**。`BuildArenaBackdropSkyboxMaterial` の最終 `AssetDatabase.LoadAssetAtPath` が null を返した場合に `Log("[DEGRADED] BuildArenaBackdropSkyboxMaterial: ... could not be saved/reloaded after CreateAsset/SaveAssets ...")` を追加。他の全 Failed 分岐と同じく理由付きログを出すようにした。
  2. **対応済み**。`DeleteStaleArenaBackdropSkyboxMaterialIfPresent(reason)`（`DeleteStaleHitVfxPrefabIfPresent` を踏襲）を新設し、`PatchArenaBackdrop` の2つの縮退分岐（テクスチャ欠落／マテリアル構築失敗）それぞれで呼び出して stale `.mat` を削除。加えて `ClearArenaBackdropSkyboxMaterialReference(backdrop)` を新設し、同じ縮退分岐で `ArenaBackdrop._skyboxMaterial` の SerializedProperty を null にリセットするようにした（`ArenaBackdrop.Start()` は既存の null 分岐で `[Wiring]` エラーを出す設計のため、縮退runでは「スカイボックス無し」で一貫する）。
  3. **対応済み**。`ArenaBackdropSkyboxMaterialPath` private const を廃止し `GameConfig.AssetKeys.ArenaBackdropSkyboxMaterial` へ移設。`BuildArenaBackdropSkyboxMaterial` はこの AssetKeys 経由の値のみを参照する。`Tests/PlayMode/ArenaBackdropSceneTests.cs` の `ArenaBackdrop_TextureIsWiredViaGameConfigAssetKey_NotHardcodedPath` に、`GameConfig.AssetKeys.ArenaBackdropSkyboxMaterial` が非空であること・適用済み Skybox マテリアルの名前がこのキーのファイル名と一致することを検証するアサーションを追加し、マテリアルキーも機械検証対象にした。
  4. **対応済み**。iteration 1 が引用した5ファイル（`editmode-results-s26-crfix.xml` / `playmode-results-s26-crfix.xml` / `playmode-results-s26-crfix-retry.xml` / `assetintegration-s26-crfix.log` / `standalone-s26-crfix-player.log`）を本コミットに含めた。加えて iteration 2 自身の検証証跡（`editmode-results-s26-crfix-iter2.xml` / `playmode-results-s26-crfix-iter2.xml` / 付随ログ）も同梱した。

### 検証結果（iteration 2）
- EditMode（typecheck相当）: `qa/evidence/editmode-results-s26-crfix-iter2.xml` — 160/160 pass, exit 0
- build相当（`ForgeGame.EditorTools.ForgeBuild.BuildMac`）: `game/Logs/build-s26-crfix-iter2.log`（.gitignore対象のためコミット外） — exit 0, `Build succeeded: .../game/Build/ForgeGame.app`
- PlayMode: `qa/evidence/playmode-results-s26-crfix-iter2.xml` — 112/112 pass, exit 0（`ArenaBackdropSceneTests` 3件（Root/Skybox 配線・AssetKeys 経由テクスチャ＋マテリアル検証・背景vs前景可読性キャプチャ）全て pass）

### 未対応事項
なし（4件全て対応済み）。
