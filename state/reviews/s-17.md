## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-13T06:00:00Z
- 指摘要約（優先度順）:
  1. [major] `game/Assets/Scripts/Components/AutoAttackDriver.cs` `Start()`（L67-73）と `game/Assets/Scripts/Editor/AssetIntegration.cs` の `BuildHitVfxPrefab`（L525/531/585）・`PatchAutoAttackDriverVfx`（L768）が、同じ「IMG-04 ヒットVFX欠落」状態を矛盾する契約で扱っていた: Editor 側は `[DEGRADED]`＋続行（exit 0）で扱う一方、Component 側は同じ null 状態を「Editor/AssetIntegration wiring is broken」と断定する `Debug.LogError` で報告していた。S-17 では VFX は acceptance そのものであり「presentation-only だから Fail しない」という正当化だけでは Component 側の "broken" 断定と整合しない、という指摘。
  2. [minor] `BuildHitVfxPrefab` の縮退パス（テクスチャ/シェーダー欠落での early return）が前回実行時の古い `HitVfx.prefab` を削除しないため、直後の `PatchAutoAttackDriverVfx` が stale prefab をロードして「成功」ログを出し、実際には欠落テクスチャ/削除済みマテリアルを参照する壊れた VFX が無言で出荷され得る。また `HitVfxMaterial.mat` を毎回 Delete→CreateAsset するため再実行毎に GUID が churn する。
  3. [minor] `WriteReport`（L1002-1010）に `hit_vfx_built` の構造化キーが無く、VFX 統合の成否がフリーテキストのコメント行にしか出ていない。
  4. [minor] `game/Assets/Tests/PlayMode/AutoAttackSceneTests.cs` の新規テスト（L242-303）: (a) `vfxAfter[vfxAfter.Length - 1]` で生成VFXを特定しており `FindObjectsByType(..., FindObjectsSortMode.None)` の順序未規定に依存、(b) `Assert.IsNotNull(enemy)` が Unity の `== null` 破棄検知を素通しし、失敗時に意図と異なる `MissingReferenceException` になる。
- 対応:
  1. 対応した。設計判断: Editor 側の `[DEGRADED]`-continue 方針（Hero avatar 縮退・Hero/Swarmer prefab 縮退・SFX 縮退など、このファイル全体で確立済みの「生成資産の欠落は個別に握りつぶさずログしつつ IntegrateAll 全体は止めない」方針）と整合させる方向で統一し、`Fail()` への昇格は選ばなかった（VFXテクスチャ欠落1件のために Hero/Swarmer/SFX の統合まで巻き込んで exit 1 にするのは、S-17 の他の acceptance-critical 要素（SFX同期・アニメ再生）を無関係に道連れにしてしまい悪化させるため）。代わりに rule 12（tech-stack-unity.md #12「縮退が正当なケースはヘッダコメントで文書化した上で LogWarning」）に従い、`AutoAttackDriver.Start()` のログを `Debug.LogError`→`Debug.LogWarning` に変更し、「wiring is broken」という断定を外して考えられる原因（IMG-04テクスチャ/シェーダー欠落・prefab保存失敗・IntegrateAll未実行）を列挙する文言に書き換えた。あわせてコメントで Editor 側との契約が一致していることを明記した。
  2. 対応した。`AssetIntegration.cs` に `DeleteStaleHitVfxPrefabIfPresent(reason)` を追加し、`BuildHitVfxPrefab` の3つの縮退パス（テクスチャ欠落・シェーダー欠落・prefab保存失敗）全てで呼び出すようにした。あわせて `HitVfxMaterial.mat` を Delete+CreateAsset ではなく既存アセットがあれば in-place 更新（`shader`/`_BaseMap`/`_Surface`/`_Blend` を設定して `EditorUtility.SetDirty`+`AssetDatabase.SaveAssets`）に変更し、GUID churn を解消した。
  3. 対応した。`WriteReport` に `hitVfxBuilt` 引数を追加し `hit_vfx_built=<bool>` を構造化キーとして出力するようにした。呼び出し元 `IntegrateAll` は既存の `hitVfxPrefab != null` を渡すだけで済んだ。
  4. 対応した。(a) `AutoAttackSceneTests.cs` に攻撃発火前の `ParticleSystem[]` 集合を `HashSet<ParticleSystem>` として保持し、発火後の配列との差分（before集合に無い要素）で新規スポーンVFXを特定するよう変更（配列末尾インデックス依存を排除）。(b) `Assert.IsNotNull(enemy)` を `Assert.IsTrue(enemy != null, ...)` に変更し、Unity の `UnityEngine.Object.==` オーバーロード（破棄検知）を経由するようにした。
- 検証結果:
  - EditMode（typecheck相当）: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results.xml -logFile qa/evidence/editmode-run.log` → exit 0 / 結果XML `total="140" passed="140" failed="0"`
  - build相当: `"$UNITY" -batchmode -projectPath game -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit -logFile game/Logs/build.log` → exit 0 / ログに `Build succeeded: .../game/Build/ForgeGame.app` を確認
  - PlayMode（QA相当だが影響範囲確認のため追加実行）: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform PlayMode -testResults qa/evidence/playmode-results.xml -logFile qa/evidence/playmode-run.log` → `total="97" passed="96" failed="1"`。失敗は `CrystalSceneTests.KillingEnemyWithinPickupRadius_...`（S-09由来・本diffで一切触れていないファイル）で、本 iteration の変更とは無関係。単体フィルタ再実行（`-testFilter "ForgeGame.Tests.PlayMode.CrystalSceneTests"`）で `total="3" passed="3" failed="0"` となり、フルスイート同時実行時のタイミング起因の既存flakinessであることを確認（`qa/evidence/playmode-crystal-retry.xml`）。本diffが変更した `AutoAttackSceneTests` は4件全てPassed（新規VFXテスト含む）。
- state/stories.yaml の S-17 は `status: review` のまま据え置き（次iterationで再レビュー）。未対応finding: 無し（4件全て対応済み）。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-13T15:40:00Z
- 指摘要約（優先度順）:
  1. [major] `game/Assets/Scripts/Editor/AssetIntegration.cs` L531 `DeleteStaleHitVfxPrefabIfPresent` が `AssetDatabase.DeleteAsset(string)` の戻り値（bool）を無視して握り潰していた。削除失敗（ファイルロック/権限/VCS連携等）時でも「deleted a stale HitVfx prefab...」と成功を断定するログを無条件に出し、直後の `PatchAutoAttackDriverVfx`（L804）が生き残った stale prefab をロード・配線して成功ログを出す一方、構造化レポートは `hit_vfx_built=false` — iteration 1 minor #2 で修正したはずの「欠落/削除済みアセット参照の stale VFX が無言で出荷される」バグがレポートと実配線の矛盾という形で復活していた。rule 11（batchmode ツールは壊れた状態でシーン/アセットを保存せず非0終了）違反。
  2. [minor] `PatchAutoAttackDriverVfx`（L805-809）の縮退分岐（HitVfx prefab 未検出）が `AutoAttackDriver._hitVfxPrefab` の serialized 参照をクリアせずに return していたため、前回成功run→今回縮退runの再実行順序で、削除済みprefabアセットへのdangling GUID参照がGameシーンに残り得た。ランタイムはUnityの`==null`判定で安全だが、「縮退runはend to endで正直にVFX無し」という本fix自身の契約と矛盾し、`AutoAttackSceneTests.cs:253`の`Assert.IsNotNull(driver.HitVfxPrefabForTests, ...)`がCLRベースの非null判定であるため、missingスタブ（CLR非null・Unity null）を素通しして失敗がより診断しにくい後段assertにずれ得る指摘も含む。
  3. [minor] `AutoAttackSceneTests.cs` L279-281 の `Assert.Greater(vfxAfter.Length, vfxBefore.Length)` が、同diffで導入したset-diff方式（L264-268のコメントが想定する「将来シーンに追加される無関係な環境ParticleSystem」対策）と矛盾する形で残存し、before スナップショットに含まれる短命ParticleSystem（`stopAction=Destroy`等）が攻撃待機ウィンドウ中にdespawnすると、hit VFXが正しく生成されていてもfalse failureになり得る時限爆弾だった。
- 対応:
  1. 対応した。`DeleteStaleHitVfxPrefabIfPresent` を修正し、`AssetDatabase.DeleteAsset` の戻り値が `false` の場合は成功ログを出さず `Fail("...stale HitVfx prefab could not be deleted — refusing to continue and risk wiring AutoAttackDriver to a stale asset.")` を呼んで非0終了させるようにした（`BuildSwarmerPrefab`/`SaveAsPrefabAsset`の`out bool`確認パターンと同じ「保存/削除の成功シグナルを無視しない」規約に統一）。削除成功時のみ従来通りの`[DEGRADED]`ログを出す。
  2. 対応した。`PatchAutoAttackDriverVfx`を再構成し、`SerializedObject`/`FindProperty`取得を先に行った上で、HitVfx prefabが見つからない縮退分岐で`prop.objectReferenceValue`が非null（＝前回runの残留参照）の場合のみ明示的に`null`へクリアして`ApplyModifiedPropertiesWithoutUndo()`する（既にnullなら冪等でno-op）処理を追加した。あわせてテスト側L253を`Assert.IsNotNull`から`Assert.IsTrue(driver.HitVfxPrefabForTests != null, ...)`（Unity null対応形式。L300の`enemy != null`と同一パターン）に変更した。
  3. 対応した。個数比較assertを、既存のbefore/after `HashSet<ParticleSystem>`差分から求めた新規インスタンスリスト（`List<ParticleSystem> newVfxInstances`）の件数に対する`Assert.GreaterOrEqual(newVfxInstances.Count, 1, ...)`に置換した。これにより「VFXが生成されたか」の判定はset-diffのみを唯一の情報源とし、`spawnedVfx`特定ロジックも同じ`newVfxInstances`リストを再利用する形に統合した（重複ロジック排除）。
- 検証結果:
  - EditMode（typecheck相当）: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results-s17-iter2.xml -logFile game/Logs/editmode-s17-iter2.log` → exit 0 / 結果XML `total="140" passed="140" failed="0"`
  - build相当: `"$UNITY" -batchmode -projectPath game -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit -logFile game/Logs/build-s17-iter2.log` → exit 0 / ログに `Build succeeded: .../game/Build/ForgeGame.app` を確認
  - PlayMode（追加確認）: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform PlayMode -testResults qa/evidence/playmode-results-s17-iter2.xml -logFile game/Logs/playmode-s17-iter2.log` → exit 0 / `total="97" passed="97" failed="0"`。本diffが変更した`AutoAttackSceneTests.AttackLanding_SpawnsHitVfxAtTargetPosition_PlaysSfx_AndTransitionsAnimatorToAttack`もPassed。
- MAX_ITER（2）到達のため、state/stories.yaml の S-17 は `status: done` に更新する。未対応finding: 無し（3件全て対応済み・検証で回帰なしを確認）。
