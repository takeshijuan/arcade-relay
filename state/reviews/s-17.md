# S-17 レビュー履歴

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-22T10:25:05Z
- 指摘要約（優先度順）:
  1. [major] `HudPanel.cs:89` の一時停止ガードが `Time.timeScale` のグローバル値推論のみに依存し、`PausePanel` との `Update` 実行順が未規定（両者とも `DefaultExecutionOrder` 無し）なため、同一フレーム内で `PausePanel.Update()` が先に走り `Resume()` が `timeScale` を戻した直後、同じフレームの `HudPanel.Update()` が同じクリックを再処理し得た（Resume ボタン矩形と空きビルドスポットの画面座標が重なる場合にオーバーレイ越しのタワー選択パネル誤起動）。
  2. [minor] 上記と同一原因の重複指摘（`HudPanel.HandleInput()` の実行順 race）。
  3. [minor] `PausePanelPlayModeTests.TearDown` が `Time.timeScale` はリセットするが `GameFlow.SetCurrentAudioSettings(null)` を行わず、`ClickNearRightEdgeOfBgmVolumeSlider...` テストが書き込んだ BGM 音量 (~0.95) が後続フィクスチャへ順序依存でキャリーする。
  4. [minor] `PauseCanvas` と `HudCanvas` が完全に同一条件（`ScreenSpaceCamera`・同一 `worldCamera`・`planeDistance=1`・`sortingOrder` 未指定=0）のため、一時停止オーバーレイの描画順が未規定になり得る。
  5. [minor] acceptance「Title/Menu/Result 中の Esc は無反応」の自動テストが Title のみで、Menu/Result は構造的保証（PausePanel を配置しない）のみでリグレッション検知テストが無い。
  6. [minor・プロセス] レビュー指示対象コミット `COMMIT_FAILED`（未解決プレースホルダ）— S-17 のコミット自体が git 履歴に存在せず、workflow のコミットステップが失敗していた。S-17 一式が S-10 と混在してステージされていないか要確認。

- 対応:
  1&2. 対応済み。`PausePanel` に `LastResumeFrame`（直近 `Resume()` 呼び出し時の `Time.frameCount`）を追加し、`HudPanel` に `[SerializeField] private PausePanel pausePanel` を新設して参照配線（`Game.unity` の `HudPanel` コンポーネントへ `pausePanel: {fileID: 400000062}` を追記）。`HandleInput()` のガードを `Time.timeScale` チェックに加え `pausePanel.IsOpen || pausePanel.LastResumeFrame == Time.frameCount` の OR 条件にし、`PausePanel`/`HudPanel` の `Update` 実行順のどちらであっても同一フレームでの二重処理を防ぐ（`DefaultExecutionOrder` による順序固定ではなく、順序非依存な状態フラグで解決）。`SetReferencesForTest` に `PausePanel pause = null` を追加（既存呼び出しなし・後方互換）。
  3. 対応済み。`PausePanelPlayModeTests.TearDown` に `GameFlow.SetCurrentAudioSettings(null);` を追加（S-16 `MenuScreenPlayModeTests.TearDown` と同一パターン）。
  4. 対応済み。`GameConfig.Ui.PauseCanvasSortingOrder`（=10）を追加し、`PausePanel.BuildUi()` で `canvas.sortingOrder` に明示設定して一時停止オーバーレイを常に最前面に固定。
  5. 対応済み。`EscInMenuScene_DoesNothing` / `EscInResultScene_DoesNothing` を追加（`EscInTitleScene_DoesNothing` と同型。Result 版は `ResultScreenPlayModeTests` と同じ `GameFlow.GoToResult(...)` 直接呼び出しパターンを踏襲し、`RunOutcomeController` 経由のコア破壊は不要な依存として避けた）。`TearDown` に `GameFlow.ClearRunResult()` を追加してキャリー漏れを防止。
  6. 見送り（本 fix コミット自体が今回のコミットで解消: S-17 分のみをパス指定で単独コミットする。GameConfig.cs/Game.unity は他ストーリーとの共有ファイルだが、`git diff --cached` で確認した限り現在ステージ済みの内容は S-17 分（一時停止関連定数・シーン配線）のみで S-10 の混在は無かった）。

- 検証: Unity 単一インスタンスロックのため本レーンでは起動しない（並走レーン規律）。参照実在は Read/Grep で静的確認済み:
  - `GameConfig.Ui.PauseCanvasSortingOrder` 定義（`GameConfig.cs`）と参照（`PausePanel.cs`）の一致を確認。
  - `PausePanel.LastResumeFrame` の定義（get; private set;・`Resume()` 内で設定）と `HudPanel.cs` からの参照（型・アクセス修飾子）を確認。
  - `HudPanel.pausePanel` の `[SerializeField]` と `Game.unity` の `pausePanel: {fileID: 400000062}`（PausePanel コンポーネントの実 fileID と一致）を確認。
  - `PausePanelPlayModeTests.cs` の新規テスト参照シンボル（`GameFlow.ClearRunResult`, `RunResult`, `GameConfig.Scenes.Menu/Result`）が既存コードに実在することを確認（`ForgeGame.Systems`/`ForgeGame.Components` は既存 using 節でカバー済み）。
  - `SetReferencesForTest` の新シグネチャに既存呼び出し元が無いことを確認（後方互換破壊なし）。

## CR-CODE iteration 2 — REJECT
- 日時: 2026-07-22T10:40:00Z
- 指摘要約（優先度順）:
  1. [blocker] acceptance「Title 中の Esc は無反応」が満たされず、追加テスト `EscInTitleScene_DoesNothing` は失敗する見込み。`InputReader.cs` は `anyKey` を `<Keyboard>/anyKey`（Escape を含む）にバインドしており、`TitleScreen.cs` の `Update()` は `AnyKeyPressedThisFrame` で `GameFlow.GoToMenu()` を呼ぶため、Title で Esc を押すと Menu へ遷移してしまう。
  2. [minor] `PausePanel.GoToTitle()` が `LastResumeFrame` を設定しないため、iter1 が塞いだ同一フレーム二重処理 race が「タイトルへ戻る」ボタンについて再発する（実害はシーン破棄により無い）。
  3. [major] 指摘1と同一原因の重複指摘（`PausePanelPlayModeTests.cs` 側からの詳細）。「PausePanel 不在なら Esc は構造的に無反応」という PausePanel.cs 冒頭コメントの一般化が誤りで、TitleScreen 側の独自 anyKey 購読が Esc に反応してしまうことの指摘。
  4. [minor] 指摘2と同一原因の重複指摘（`GoToTitle()` 側からの詳細）。
  5. [minor] `HudPanel.cs` の `pausePanel != null &&` ガードが、将来のシーン編集で配線欠落した場合に無警告で `Time.timeScale` ガードのみへ縮退する。`SetReferencesForTest` の呼び出し元が現状ゼロのため、Awake で `pausePanel==null` 時に1回 `LogError`（テスト注入経路には抑制フラグ）としても既存テストは壊れない、との指摘。
  6. [minor] iter1 #1(major) の `LastResumeFrame` 二重処理防止自体のリグレッションテストが無い（今回は静的確認のみ）。`ClickResumeButton_ClosesOverlay_AndRestoresTimeScale` に「Resume クリック後のフレームで `HudPanel.TowerSelect.IsOpen == false`」の assert を追加すれば片側の実行順を検知できる、との提案。

- 対応:
  1&3. 対応済み。`TitleScreen.Update()` の遷移判定を `!inputReader.PausePressedThisFrame && (ClickPressedThisFrame || AnyKeyPressedThisFrame)` に変更し、Escape 単独押下では Menu へ遷移しないようにした（クリック・Escape 以外のキーは従来どおり遷移）。`PausePanel.cs` 冒頭コメントに「この保証は PausePanel 自身にしか及ばず、他コンポーネントの独自 Esc 購読は個別対応が必要」という注記を追加。
  2&4. 対応済み。`PausePanel.GoToTitle()` に `LastResumeFrame = Time.frameCount;` を追加し、`Resume()` と同じ後処理にした。
  5. 対応済み。`HudPanel` に `referencesSetForTest` フラグを追加し、`Awake()` で `pausePanel == null && !referencesSetForTest` の場合に `Debug.LogError` を1回発行するようにした。既存テスト（`GameHudPlayModeTests`）は実 `Game.unity` シーンをロードし `pausePanel` はインスペクタ配線済み（`fileID: 400000062`）のため誤検知しない。`HudPanel` を参照するテストが同ファイルの1本のみで `SetReferencesForTest` の呼び出し元も無いことを Grep で確認済み。
  6. 対応済み。`ClickResumeButton_ClosesOverlay_AndRestoresTimeScale` に `hudPanel.TowerSelect.IsOpen` が `Resume` クリック後 `false` のままであることを検証する assert を追加。

- 検証: Unity 単一インスタンスロックのため本レーンでは起動しない（並走レーン規律）。参照実在は Read/Grep で静的確認済み:
  - `InputReader.PausePressedThisFrame` の定義（既存プロパティ）と `TitleScreen.cs` からの参照を確認。
  - `PausePanel.LastResumeFrame` の setter が `private set` で同一クラス内 (`GoToTitle()`) からのみ書き込まれることを確認。
  - `HudPanel.referencesSetForTest` の新規フィールドと `SetReferencesForTest`/`Awake()` 両方からの参照を確認。既存呼び出し元が無いため後方互換への影響なし。
  - `PausePanelPlayModeTests.cs` に追加した `Object.FindFirstObjectByType<HudPanel>()` ・ `hudPanel.TowerSelect.IsOpen` が `HudPanel.cs` の公開型 (`public TowerSelectPanel TowerSelect => towerSelectPanel;`) およびファイル冒頭 `using ForgeGame.Ui;` で解決可能なことを確認。`TowerSelectPanel.IsOpen` が既存の `get; private set;` 公開プロパティであることを確認。
  - `Game.unity` の `HudPanel` コンポーネント (`fileID: 400000032`) の `pausePanel: {fileID: 400000062}` 配線を再確認（Awake の LogError ガードが本番シーンで誤発火しないことの根拠）。
