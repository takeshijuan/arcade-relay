# S-01 レビュー履歴 — GameFlow・シーンルータと Boot 起動配線

## CR-CODE iteration 1 — APPROVE
- 日時: 2026-07-21T09:14:12Z
- 対象コミット: 551ebc284df43f9424a85a1d814620b8d8653a07
- 対象: game/Assets/Scripts/Components/GameBootstrap.cs, GameFlow.cs（新規）, game/Assets/Tests/PlayMode/GameFlowPlayModeTests.cs（新規）
- 指摘要約: なし（findings 0件）
  - unity-code.md 規約準拠を確認: マジックナンバー無し（シーン名は GameConfig.Scenes 定数経由）、delta-time 依存の per-frame ロジック無し（GameFlow は静的ルータ・Update を持たない）、Systems/Components 分離OK（SceneManager 依存のため Systems ではなく Components 配置＝正しい選択）、永続化 I/O は GameBootstrap から ISaveStore 経由でのみ触り File/PlayerPrefs 直呼び無し。
  - silent-failure パターン無し: LogError→Warning 降格無し、batchmode エラー握り潰し無し。破損復旧の実プロトコル（.bak/[SaveCorruption]）は S-07 FileSaveStore の責務で、本 diff は recovered フラグ伝播配線のみ（設計どおり）。
  - acceptance 充足を確認: (a) Boot 起動で GameBootstrap.Start が saveStore.Load()→GameFlow.SetRecovered→GameFlow.GoToTitle()、(b) GoToBoot/GoToTitle/GoToMenu/GoToGame/GoToResult の全遷移 API が GameConfig.Scenes 経由で存在、(c) GoToResult(result) が RunResult をキャリーし TryGetLastRunResult で取得（PlayMode テストで検証）、(d) Boot→Title 自動遷移を PlayMode テスト＋LogAssert.NoUnexpectedReceived で検証、(e) Grep で Components/Ui にシーン名文字列直書きが無いことを確認（SceneManager.LoadScene 呼び出しは GameFlow に集約）。
  - 参考（指摘ではない・並走レーン合流時に解決）: Boot/Title/Result シーン実体と .meta 生成は integrate/他レーンの範囲。コンパイル整合はレーン合流バッチが保証（レビュー方針どおり REJECT 事由としない）。
- 対応: —（APPROVE のため revise 不要）
- 追記（silent-failure-hunter 追加指摘への対応・iteration 1 として記録）:
  - [MEDIUM] GameBootstrap.cs:19 SetSaveStoreForTest の Start() 後注入が黙殺される件 → 対応済み。`started` フラグを追加し、Start() 実行後に SetSaveStoreForTest が呼ばれた場合は `InvalidOperationException` を throw して即時失敗させるよう修正（黙示無効化の禁止）。
  - [LOW] GameFlowPlayModeTests.cs:89-92 RunResult キャリー検証が CoreHpRemaining / UsedBuildSpots 未 assert の件 → 対応済み。両フィールドの `Assert.AreEqual` を追加し、6フィールド全数を検証するよう修正。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-21T09:32:00Z
- 対象コミット: f79fb5bb0eec0d5c8a0b3e5fe68fbeda5a99da7c（"S-01: fix CR-CODE iter 1"）
- 対象: game/Assets/Scripts/Components/GameBootstrap.cs, game/Assets/Tests/PlayMode/GameFlowPlayModeTests.cs
- 指摘要約: なし（findings 0件）
  - iteration 1 の 2 指摘（[MEDIUM] Start() 後注入の黙殺 / [LOW] RunResult キャリー未 assert）への修正を検証し、いずれも適切かつ完結。
  - GameBootstrap.cs: `started` フラグ導入。Start() 冒頭で `started=true`、SetSaveStoreForTest は `started` 時に `InvalidOperationException` を throw（黙示無効化の禁止＝silent-failure ガードとして正しい fail-fast）。Start は1回のみ実行・リセット不要でライフサイクル整合、テスト経路（inactive→inject→SetActive）は started=false のまま通る。`using System;` 追加も適正。
  - GameFlowPlayModeTests.cs: CoreHpRemaining / UsedBuildSpots の `Assert.AreEqual` を追加し RunResult 全6フィールドを検証。参照フィールドは Types.cs の RunResult 定義に実在。
  - unity-code.md 準拠を再確認: マジックナンバー無し（bool フラグのみ）、per-frame/delta-time ロジック無し、Systems/Components 分離維持、永続化 I/O は ISaveStore 経由のみ。silent-failure 降格・batchmode 握り潰し無し。
  - acceptance 再確認: Boot→Title 自動遷移＋LogAssert.NoUnexpectedReceived、GameConfig.Scenes 経由の全遷移 API、GoToResult の RunResult キャリー（TryGetLastRunResult）を PlayMode で検証。Grep で Components/Ui にシーン名直書き・GameFlow 外 LoadScene 無しを確認（合致）。並走レーン前提によりシーン実体未生成は REJECT 事由としない。
- 対応: —（APPROVE のため revise 不要）
- 追記（silent-failure-hunter 追加指摘への対応・iteration 2 として記録）:
  - [LOW] GameBootstrap.cs:31 SetSaveStoreForTest(null) の黙示フォールバック残存の件 → 対応済み。`saveStore = store ?? throw new ArgumentNullException(nameof(store), ...)` に変更し、null 注入時は Start() の `saveStore ??= new InMemorySaveStore()` への無警告フォールバックに委ねず即時失敗させるよう修正（黙示無効化の禁止。started 後注入と同じ fail-fast 方針に統一）。呼び出し側（GameFlowPlayModeTests.cs）に null を渡す既存呼び出しは無いことを Grep で確認済み（影響なし）。
  - （参考）前回 findings 2件（[MEDIUM] Start() 後注入の黙殺／[LOW] RunResult キャリー未 assert）は iteration 1→2 で解消済みであることを再確認。diff 内に LogError 降格・空 catch・batchmode エラー握り潰しは無し。

## CR-CODE iteration 3 — CONCERNS
- 日時: 2026-07-22T02:02:20Z
- 対象コミット: 551ebc284df43f9424a85a1d814620b8d8653a07（再レビュー・固定スコープ）
- 対象: game/Assets/Scripts/Components/GameBootstrap.cs, GameFlow.cs（新規）, game/Assets/Tests/PlayMode/GameFlowPlayModeTests.cs（新規）
- unity-code.md 規約準拠は確認済み（本コミット時点）: マジックナンバー無し（シーン名は GameConfig.Scenes 定数経由のみ）、per-frame/delta-time 依存ロジック無し（GameFlow は Update を持たない静的ルータ）、Systems/Components 分離OK（SceneManager 依存のため Components 配置＝正）、永続化 I/O は GameBootstrap から ISaveStore 経由のみ（File/PlayerPrefs 直呼び無し）。silent-failure の LogError→Warning 降格・batchmode 握り潰しは無し。acceptance の中核（Boot→Title 自動遷移を PlayMode+LogAssert.NoUnexpectedReceived で検証／GameConfig.Scenes 経由の全遷移 API／GoToResult の RunResult キャリー／Components・Ui にシーン名直書き無し）は満たす。
- 指摘要約（優先度順・いずれも修正可能な指摘＝CONCERNS。設計欠陥ではない）:
  1. [MEDIUM] 新規 .cs の .meta 未コミット — game/Assets/Scripts/Components/GameFlow.cs.meta と game/Assets/Tests/PlayMode/GameFlowPlayModeTests.cs.meta が git 未追跡（本コミットにも branch HEAD にも存在せず、どのコミットも触れていない。作業ツリーに Unity 生成の untracked ファイルとしてのみ存在）。同階層の他 .cs は全て .cs.meta をコミット済みで、リポジトリ規約から逸脱。GUID が機械ごとに再生成され参照破壊・マージ衝突・恒常的な dirty working tree を招くため、生成された .meta を `git add` してコミットに含めること。
  2. [LOW] RunResult キャリー検証が不完全 — GameFlowPlayModeTests.cs `GameFlow_GoToResult_CarriesRunResult_AndLoadsResultScene` は fixture で RunResult 全6フィールド（CoreHpRemaining=42・UsedBuildSpots=4 を含む）を設定するが、Assert.AreEqual は IsWin/KillCount/AoeKillCount/ClearTimeSec の4件のみ。CoreHpRemaining・UsedBuildSpots がキャリーされることを検証していない。両フィールドの Assert.AreEqual を追加すること（構造体は値コピーのため実バグ可能性は低いが、テスト意図の完結性として。※後続コミット f79fb5b で解消済みだが本固定スコープ時点では未解消）。
  3. [LOW/advisory] GameBootstrap.SetSaveStoreForTest の遅延注入が黙殺され得る — 本コミット時点の実装 `SetSaveStoreForTest(ISaveStore store) => saveStore = store;` は無条件代入。Start() が既に `saveStore ??= new InMemorySaveStore()` + Load() を実行済みの後に呼ばれた場合、注入は Load/遷移に反映されず黙って無効化される（started ガード/throw 無し）。「Start() 前に呼ぶ」旨の doc コメントはあるがコードで強制されておらず、ゲートが警戒する silent-failure シームに該当。started フラグ + InvalidOperationException で fail-fast 化を推奨（※後続コミット f79fb5b で解消済み）。
- 対応:
  1. [MEDIUM] .meta 未追跡 → 対応済み。`game/Assets/Scripts/Components/GameFlow.cs.meta`（guid: c14a4f03c585647199ecba9a813f0097）と `game/Assets/Tests/PlayMode/GameFlowPlayModeTests.cs.meta`（guid: 6027e4248cc524e0b96c1ecdd931ec1f）は作業ツリーに既に Unity 生成済みで存在していたため、他 `.cs.meta` と同一フォーマット（`fileFormatVersion`+`guid`のみ）であること、GUID がリポジトリ内で他ファイルと衝突しないことを確認した上で `git add` してコミットに含めた（"S-01: fix CR-CODE iter 1"）。以後 GameFlow.cs / GameFlowPlayModeTests.cs の GUID は固定される。
  2. [LOW] RunResult キャリー検証の不完全性 → 現状のコード（`game/Assets/Tests/PlayMode/GameFlowPlayModeTests.cs` の `GameFlow_GoToResult_CarriesRunResult_AndLoadsResultScene`）を確認したところ、既に `CoreHpRemaining`・`UsedBuildSpots` を含む RunResult 全6フィールドの `Assert.AreEqual` が実装済み（本指摘はコミット551ebc2固定スコープでの再レビューにより検出されたもので、実体は後続コミット f79fb5b／iteration 1〜2 の修正で既に解消済み）。追加対応不要。
  3. [LOW/advisory] SetSaveStoreForTest の黙示無効化 → 現状のコード（`game/Assets/Scripts/Components/GameBootstrap.cs`）を確認したところ、既に `started` フラグ＋`InvalidOperationException`（Start() 後注入）／`ArgumentNullException`（null 注入）による fail-fast 実装済み（本指摘も同様に f79fb5b／iteration 1〜2 の修正で既に解消済み）。追加対応不要。
  検証: Unity は起動せず（並走レーン規律）、Read/Grep で GameFlow.cs・GameBootstrap.cs・GameFlowPlayModeTests.cs・両 .meta の実在/内容/GUID 衝突なしを静的確認した。エンジン検証（EditMode/PlayMode 実行）はレーン合流後のバッチ検証区間に委ねる。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-22T02:40:00Z
- 対象コミット: 39702cbabf9dceac7da4473a4dab73d53af65928（"S-01: fix CR-CODE iter 1"・固定スコープ。前 iteration の [MEDIUM] .meta 未追跡指摘への修正コミットを検証）
- 対象 diff: game/Assets/Scripts/Components/GameFlow.cs.meta（新規）, game/Assets/Tests/PlayMode/GameFlowPlayModeTests.cs.meta（新規）, state/reviews/s-01.md（レビュー履歴追記）
- 指摘要約: なし（findings 0件）
  - [MEDIUM] .meta 未追跡（前 iteration 指摘1）→ 解消を確認。GameFlow.cs.meta（guid: c14a4f03c585647199ecba9a813f0097）・GameFlowPlayModeTests.cs.meta（guid: 6027e4248cc524e0b96c1ecdd931ec1f）が本コミットで git 追跡下に入った。両 .meta は同階層の他 .cs.meta（GameBootstrap.cs.meta 等）と同一の最小フォーマット（`fileFormatVersion: 2` + `guid` の2行のみ）でリポジトリ規約に合致。コミット時点の全 .meta（75件）を走査し両 GUID の衝突が無いことを確認した（GUID 安定化＝参照破壊・マージ衝突・恒常 dirty tree の回避が達成）。
  - [LOW] RunResult キャリー検証 / [LOW/advisory] SetSaveStoreForTest 黙示無効化（前 iteration 指摘2・3）→ 本固定コミット時点のコードで既に解消済みを再確認。GameFlowPlayModeTests.cs は RunResult 全6フィールド（IsWin/CoreHpRemaining/KillCount/AoeKillCount/UsedBuildSpots/ClearTimeSec）を Assert.AreEqual で検証。GameBootstrap.SetSaveStoreForTest は `started` フラグ＋`InvalidOperationException`（Start() 後注入）／`ArgumentNullException`（null 注入）で fail-fast 化済み（黙示無効化なし）。
  - unity-code.md 準拠（本コミット時点コード）: マジックナンバー無し（シーン名は GameConfig.Scenes 定数経由のみ・テスト fixture の数値リテラルはテストデータで対象外）、per-frame/delta-time 依存ロジック無し（GameFlow は Update を持たない静的ルータ）、Systems/Components 分離OK（SceneManager 依存のため Components 配置＝正）、永続化 I/O は GameBootstrap から ISaveStore 経由のみ（File/PlayerPrefs 直呼び無し）。silent-failure の LogError→Warning 降格・空 catch・batchmode 握り潰しは無し。
  - acceptance 充足を再確認: Boot→Title 自動遷移を PlayMode（Boot_LoadsSave_AndAutoTransitionsToTitle_WithNoErrors）＋LogAssert.NoUnexpectedReceived で検証／GoToBoot/Title/Menu/Game/Result の全遷移 API が GameConfig.Scenes 経由で存在／GoToResult が RunResult をキャリーし TryGetLastRunResult で取得／Grep で Components・Ui にシーン名文字列直書き無し・GameFlow 外の SceneManager.LoadScene 無し（CoreView.cs の1件はコメントのみ）を確認。
  - （advisory・非指摘）Boot_LoadsSave... は本番既定 SaveStores.CreateDefault()（=FileSaveStore）で実 persistentDataPath/save.json を Load するため、環境に破損セーブが存在すると S-07 の [SaveCorruption] LogError で LogAssert が落ち得る。破損検知は S-07 の責務でありこの diff の欠陥ではない。レーン合流後バッチ／QA-PLAY 環境がクリーンであることを前提とする。
- 対応: —（APPROVE のため revise 不要。前 iteration の全指摘が解消済み）
  - 検証手段: Unity 非起動（並走レーン規律・単一インスタンスロック）。git show/Read/Grep による静的確認のみ。EditMode/PlayMode 実行はレーン合流後のバッチ検証区間に委ねる。

