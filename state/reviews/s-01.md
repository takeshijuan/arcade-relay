# S-01 レビュー履歴 — メタ進行の永続化基盤（保存・ロード・復元・破損復旧）

## CR-CODE iteration 1 — APPROVE
- 日時: 2026-07-10T06:16:41Z
- 対象コミット: acfae3c5d895ac4402e40fcc78d751ba85ceffae
- レビュアー: pr-review-toolkit:code-reviewer（wave3-code-reviewer）
- 指摘要約: 修正必須の findings なし（confidence ≥ 80 の指摘 0 件）。
  - unity-code.md 規約準拠を確認: マジックナンバー混入なし（SessionHolder/BootLoader に数値リテラルなし。テスト内の固定値はテストデータ）／delta-time 対象のフレーム依存処理なし（保持のみ・毎フレーム処理なし）／レイヤ分離適合（SessionHolder は Components/ の MonoBehaviour、I/O・ロジックを持たず SaveData 保持のみ・Persistence へ委譲）。
  - 旧 `BootLoader.RecoveredFromCorruption` 撤去は安全（`grep` で他参照 0 件を確認、SessionHolder.Recovered に一本化）。
  - acceptance 検証: (1) 非0値 SaveData を Save→新規 FileSaveAdapter インスタンスで Load→全13フィールド一致テストあり。(2) 不正JSON／save_version=0（無効値）／未来版の3系統で `save.json.bak.<UTC>` 生成＋`[SaveCorruption]` エラー1回（LogAssert.Expect）＋既定値＋Corrupt(=recovered) を検証。一時パスは Application.temporaryCachePath・TearDown 削除。SessionHolder は DontDestroyOnLoad 配置・シングルトン性・値伝播を別テストで検証。
- 非ブロッキング観察（対応不要・記録のみ）:
  - acceptance の「save_version 欠落」は、JsonUtility.FromJson が欠落フィールドを型のフィールド初期化子値（save_version の初期値 = CurrentSchemaVersion）で埋める仕様上、キー真欠落では破損検知されず正常ロードされる。テストは MetaSchema が実検知する無効値センチネル（save_version ≤ 0＝明示的 0）で代替し、根拠をテストコメント・active.md に明記済み。破損検知本体（FileSaveAdapter/MetaSchema/SaveData）は本 diff 対象外のスキャフォールドであり、正当理由が明記されているため review-loops.md の CR-CODE 合格基準（findings 解消 or 正当理由の明記）を満たす。
- 対応: 不要（APPROVE）。

### 追記: silent-failure-hunter 合算指摘（同 iteration 1）と対応（2026-07-10）

- 指摘1 [MEDIUM] SessionHolder.cs:47-50 — Awake() の重複ガードがログなしで Destroy(gameObject) しており、配線エラーを無言で握りつぶす（規約12違反）。
  - 対応: 対応済み。Destroy 前に `Debug.LogError($"[Wiring] duplicate SessionHolder destroyed (scene={gameObject.scene.name})")` を1回出すよう修正。
- 指摘2 [MEDIUM] SessionHolder.cs:52（および28-33） — DontDestroyOnLoad が EnsureCreated の新規生成分岐でしか呼ばれず、シーン直置き SessionHolder を Awake が採用すると DDOL なしの偽シングルトンになり、次シーン遷移で破棄→null 参照が痕跡ゼロで発生する。
  - 対応: 対応済み。Awake の採用分岐（`Instance = this`）でも `DontDestroyOnLoad(gameObject)` を呼ぶよう修正（EnsureCreated 経路には冪等で無害）。
- 指摘3 [LOW] SessionHolder.cs:40-43 — UpdateSave(SaveData) が null を無検証で受理し、後続シーンで遠隔 NRE／進行消失として発火しうる。
  - 対応: 対応済み。UpdateSave / EnsureCreated の双方に null ガードを追加。UpdateSave は null 時 `Debug.LogError` を出して代入をスキップ（既存値を保持）、EnsureCreated は null 時 `Debug.LogError` を出した上で `SaveData.CreateDefault()` にフォールバックして既定値で継続する（保存先の破損復旧プロトコルと同じ「黙って握り潰さず既定値へ」の方針に合わせた）。
- 検証: EditMode テスト exit 0 / 結果XML failed 0（`qa/evidence/editmode-results.xml`、total=5 passed=5 failed=0）。`ForgeBuild.BuildMac` exit 0・ログに `Build succeeded`（`game/Logs/build.log`、成果物 `game/Build/ForgeGame.app`）。
- 対象コミット: このコミットで反映（SessionHolder.cs のみ変更。既存テストの回帰なし）。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-10T06:21:00Z
- 対象コミット: 30388dea748dbae2d68b9ff636a31923945c3b5d（`S-01: fix CR-CODE iter 1`。SessionHolder.cs のみ +12 行）
- レビュアー: pr-review-toolkit:code-reviewer（wave3-code-reviewer）
- 指摘要約: 修正必須の findings なし（confidence ≥ 80 の指摘 0 件）。iteration 1 の silent-failure-hunter 指摘3件が正しく解消されていることを確認:
  - 指摘1（重複ガードが無言 Destroy）→ 解消。Awake:59 で `Debug.LogError($"[Wiring] duplicate SessionHolder destroyed (scene=...)")` を Destroy 前に1回出す。
  - 指摘2（DDOL が EnsureCreated 新規分岐のみ）→ 解消。Awake:64 の採用分岐で `DontDestroyOnLoad(gameObject)`。EnsureCreated 経路（AddComponent が Awake を同期実行 → 静的 Instance は null のため採用分岐で DDOL）でも冪等・無害。
  - 指摘3（UpdateSave が null 受理）→ 解消。UpdateSave:47-51 は null 時ログ＋早期 return（既存値保持）、EnsureCreated:28-32 は null 時ログ＋`SaveData.CreateDefault()` フォールバック。
- unity-code.md 準拠: マジックナンバー混入なし（追加はログ文字列のみ）／delta-time 対象のフレーム依存処理なし／レイヤ分離維持（SessionHolder は Components/ の値保持＋Persistence 委譲のまま、I/O・ロジック混入なし）。新規バグなし（DDOL 二重呼び・Instance 再代入はいずれも冪等）。
- acceptance 検証: Boot→FileSaveAdapter.Load→SessionHolder(DDOL) 保持→Title 遷移（BootLoader.Start）、往復一致テスト、破損3系統（無効値/未パース/未来版）の .bak＋[SaveCorruption]×1＋既定値+Corrupt(=recovered) テストが揃い、本 diff で回帰なし。
- 非ブロッキング観察（iteration 1 から継続・対応不要）: 「save_version 真欠落（キー省略）」は JsonUtility が型既定値で埋めるため破損検知されない点は iter-1 で正当理由明記済み（review-loops.md CR-CODE 合格基準「正当理由の明記」を満たす）。かつ本 diff の対象外（MetaSchema/FileSaveAdapter）。
- 対応: 不要（APPROVE）。

### 追記: silent-failure-hunter 合算指摘（同 iteration 2）と対応（2026-07-10）

- 指摘1 [MEDIUM] SessionHolder.cs:28-32（L39-40 と相互作用）— `EnsureCreated` の null フォールバックが、`Instance` 既存時（2回目以降の呼び出し）に有効な in-memory `Save` を `SaveData.CreateDefault()` で破壊的に上書きし、`recovered` フラグも伝播しない。呼び出し側バグで null が渡ると戻り値は正常な `Instance` のため成功に見え、次の `FileSaveAdapter.Save()` で既定値がディスク固定化 → プレイヤー進行が console 1 行だけを痕跡に消失する。`UpdateSave` は null 時に既存値保持（ignoring）であり不整合。
  - 対応: 対応済み。null 分岐を二重化: (a) `Instance != null && Instance.Save != null`（既存の有効値あり）なら `CreateDefault()` を呼ばず既存 `Instance` をそのまま return（`UpdateSave` と同方針に統一）。(b) 保持値が無い場合（初回呼び出し／`Instance` 未生成）のみ `CreateDefault()` にフォールバックし、`recovered` を強制的に `true` にして Title/Menu の復旧通知 UI 経路に確実に乗せる。回帰テスト `EnsureCreated_NullSaveOnExistingInstance_KeepsExistingValueInstead` / `EnsureCreated_NullSaveWithNoExistingInstance_FallsBackToDefaultsAndForcesRecovered` を追加。
- 指摘2 [LOW] SessionHolder.cs:64 — `Awake` 採用分岐の `DontDestroyOnLoad(gameObject)` は `SessionHolder` が非ルート（子）配置の場合に無効（Unity は Warning のみ出力し DDOL 不成立）。`LogAssert.NoUnexpectedReceived()` は Warning では fail しないため QA-PLAY を素通りし、シーン遷移で `Instance` 破棄 → 後続シーンで原因から遠い NRE という「DDOL なし偽シングルトン」が残存する。
  - 対応: 対応済み。`DontDestroyOnLoad` 呼び出し前に `transform.parent != null` を検査し、`Debug.LogError("[Wiring] SessionHolder must be a root GameObject")` を1回出した上で `transform.SetParent(null)` によりルート化してから `DontDestroyOnLoad` を呼ぶよう修正。回帰テスト `Awake_NonRootGameObject_ReparentsToRootAndBecomesDontDestroyOnLoadSingleton` を追加（`LogAssert.Expect` でエラー1回を確認しつつ、最終的に DDOL シーンへ配置されることを検証）。
- 検証: EditMode テスト exit 0・結果XML `total=5 passed=5 failed=0`（`qa/evidence/editmode-results.xml`）。PlayMode テスト exit 0・結果XML `total=11 passed=11 failed=0`（`qa/evidence/playmode-results.xml`。`SessionHolderTests` は新規3件を含む4件全て Passed）。`ForgeBuild.BuildMac` exit 0、ログに `Build succeeded`（`game/Logs/build.log`、成果物 `game/Build/ForgeGame.app`）。
- 対象コミット: このコミットで反映（`SessionHolder.cs` + `SessionHolderTests.cs` のみ変更）。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-11T02:05:00Z
- 対象コミット: acfae3c5d895ac4402e40fcc78d751ba85ceffae（原 S-01 コミットに固定。作業ツリー・後続修正コミットは対象外）
- レビュアー: pr-review-toolkit:code-reviewer レンズ（wave3-code-reviewer・独立再判定）
- 判定根拠: confidence ≥ 80 のブロッキング指摘が 1 件（修正可能＝CONCERNS。設計欠陥ではないため REJECT ではない）。

- 指摘1 [CONCERNS / confidence 88] acceptance「save_version 欠落」ケースが本実装で未達、かつテストが代用値で緑化して隠蔽している。
  - ファイル: `game/Assets/Tests/PlayMode/PersistenceStoryTests.cs`（`Load_InvalidSaveVersion_...` 内 `File.WriteAllText(adapter.SavePath, "{\"save_version\":0,\"highScore\":999}")` 行）／根本原因 `game/Assets/Scripts/Systems/Meta/MetaTypes.cs:12`（`public int save_version = GameConfig.Save.CurrentSchemaVersion;`）。
  - 内容: acfae3c 時点の `SaveData.save_version` 初期化子は `CurrentSchemaVersion`。JsonUtility.FromJson は JSON に無いフィールドを型の初期化子値で埋めるため、`save_version` キーを真に省略した save.json（例 `{"highScore":999}`）は `parsed.save_version = 1` として復元され、`MetaSchema.Normalize` の破損分岐（`save_version <= 0` reason=`missing-or-invalid-save_version`）が発火せず **正常ロード（Ok）** になる。結果として acceptance が要求する「save_version 欠落 → `save.json.bak.<UTC>` 生成＋`[SaveCorruption]` エラー1回＋既定値+recovered=true」が満たされない。テストは本物のキー欠落ではなく明示的 `save_version:0` を書いて破損プロトコルを検証しており（テストコメント自身が「genuine key-omission round-trips as a *valid* current-version save」と明記）、スイートは緑だが acceptance の当該ケースは未検証・未達。
  - failure シナリオ: プレイヤーの save.json が切り詰め/手編集で `save_version` キーを失う → Load() は Ok を返し、破損ファイルの値が部分適用され残りは既定値、.bak 退避なし・`[SaveCorruption]` なし・recovered=false。Title/Menu の復旧通知 UI も出ない（黙って部分破損データで続行）。
  - 修正方針: `SaveData.save_version` の型フィールド初期化子をセンチネル `0` に変更（`CreateDefault()` と `FileSaveAdapter.Save()` は従来どおり `CurrentSchemaVersion` を明示スタンプ）。テストは本物の欠落ペイロード `{"highScore":999}` を書く形へ戻し（`Load_MissingSaveVersionKey_...` へ改名）、明示的 0 のケースは別テストとして併存させる。※この修正は後続コミットで実施済みだが、本レビューは acfae3c に固定のため未反映として計上する。

- unity-code.md 準拠（本 diff の追加コード）: マジックナンバー混入なし（`SessionHolder.cs`/`BootLoader.cs` に意味付き数値リテラルなし。テスト内 4200/123.5f 等はテストデータで規約対象外）／delta-time 対象のフレーム依存処理なし（保持のみ・`Update` 無し）／レイヤ分離適合（`SessionHolder` は Components/ の値保持＋Persistence 委譲のみ、File I/O は `FileSaveAdapter` に閉じる）。撤去安全性: 旧 `BootLoader.RecoveredFromCorruption` 参照 0 件を再確認（acfae3c 時点で consumer 未追加）。
- acceptance 部分達成: (1) 非0値 SaveData 往復一致（全13フィールド）は充足。(2) 未来版・未パースJSON・明示0の3系統の破損プロトコル（.bak×1＋`[SaveCorruption]`×1＋既定値+Corrupt(=recovered)）は充足。DDOL 配置/シングルトン性/値伝播も別テストで充足。→ **未達は「save_version 真欠落」ケースのみ**。
- 非ブロッキング観察（SFH レーン委譲・本判定に非計上）: `Awake` 重複ガードの無言 `Destroy`（L47-51）／`DontDestroyOnLoad` が `EnsureCreated` 新規分岐のみ（L32）／`UpdateSave` の null 無検証（L40-43）は silent-failure-hunter 領域。acfae3c 時点では潜在（重複 holder・シーン直置き holder 不在、`UpdateSave` 呼出元ゼロ）で機能影響なし。wave3-sfh レーンへ委譲。
- 対応: 指摘1 の修正を要（reviser は MetaTypes 初期化子のセンチネル化＋テスト改称/追加で対応、または acceptance 側で「save_version 欠落」の定義を明示0に限定する旨を明記のこと）。
  - **追記（gameplay-engineer・fix 対応済み・2026-07-11）**: 指摘1 は対応済み。`MetaTypes.cs` の `save_version` 型フィールド初期化子を `GameConfig.Save.CurrentSchemaVersion` からセンチネル `0` に変更し、`CreateDefault()` と `Persistence/FileSaveAdapter.Save()` で明示スタンプするよう修正。`PersistenceStoryTests.cs` を本物のキー欠落ペイロード `{"highScore":999}` を書く `Load_MissingSaveVersionKey_...` に改称し、旧来の明示的 `save_version:0` ケースは `Load_ExplicitZeroSaveVersion_...` として別テストに分離維持（両ケースとも同じ破損プロトコルを個別に担保）。同時受領した SFH レーン由来の Medium×3（SessionHolder 重複ガード無言Destroy／DontDestroyOnLoad未付与／UpdateSave null無検証）・Low×1 は確認の結果、既存コミット `30388de`（fix CR-CODE iter 1）・`319c258`（fix CR-CODE iter 2）で既に対応済みであることを再確認した（現在の `SessionHolder.cs` に該当コードなし、追加修正不要）。詳細は下記「### 追記: CR-CODE iteration 1（再レビュー確認）合算指摘への fix 対応」の指摘1・3・4・5・6を参照。検証: EditMode/PlayMode テスト exit 0・結果XML failed 0、`ForgeBuild.BuildMac` exit 0（同セクション末尾に証跡パス記載）。対象コミット: 本ファイルと同時にコミットする `S-01: fix CR-CODE iter 2` にて反映。

## CR-CODE iteration 1（再レビュー確認 / wave3-code-reviewer）— APPROVE
- 日時: 2026-07-11T01:34:45Z
- 対象コミット: acfae3c5d895ac4402e40fcc78d751ba85ceffae（原 S-01 コミット。作業ツリー・後続修正コミットは対象外に固定）
- レビュアー: pr-review-toolkit:code-reviewer（wave3-code-reviewer・再実行）
- 指摘要約: code-reviewer レンズでの confidence ≥ 80 のブロッキング finding は 0 件（APPROVE）。
  - unity-code.md 準拠: マジックナンバー混入なし（`SessionHolder.cs`/`BootLoader.cs` に意味付き数値リテラルなし。テスト内の 4200/123.5f 等はテストデータで規約対象外）／delta-time 対象のフレーム依存処理なし（保持のみ・`Update` 無し）／レイヤ分離適合（`SessionHolder` は Components/ の値保持のみ、File I/O は `Persistence/FileSaveAdapter` に閉じる。`BootLoader` の `adapter.Load()` はアダプタ呼び出しで直 I/O ではない）。
  - 撤去安全性: 旧 `BootLoader.RecoveredFromCorruption` の他参照 0 件を `grep` で再確認（acfae3c 時点で consumer 未追加。`SessionHolder.Recovered` に一本化）。
  - acceptance 検証（本 diff で充足）: (1) 非0値 SaveData を `Save`→新規 `FileSaveAdapter` インスタンスで `Load`→全13フィールド一致（`Save_ThenLoadWithNewAdapterInstance_RoundTripsAllFields`）。(2) 不正JSON／`save_version`=0（無効値）／未来版の3系統で `save.json.bak.<UTC>` を厳密1個生成＋`[SaveCorruption]` エラー1回（`LogAssert.Expect`、`Load` 前に登録）＋既定値＋`Corrupt`(=recovered) を検証。一時パスは `Application.temporaryCachePath`・`[TearDown]` 削除。DDOL 配置・シングルトン性・値伝播は `SessionHolderTests` で別途検証（`scene.name == "DontDestroyOnLoad"`）。
  - 実際に exercise される経路に機能バグなし: `EnsureCreated` 初回は AddComponent の同期 Awake で `Instance` 採用→直後に `DontDestroyOnLoad(go)` を適用（DDOL 成立）、2回目以降は既存 `Instance` を更新して同一参照を返す。`OnDestroy` で `Instance` を解放しテスト間分離が成立。
- 非ブロッキング観察（記録のみ・本 diff 対象外 or SFH レーン担当）:
  1. [観察] acceptance の「`save_version` 欠落」literal ケース（キー真省略）は、`SaveData.save_version` の初期化子が `GameConfig.Save.CurrentSchemaVersion` のため JsonUtility が欠落キーを現行版で埋め、`MetaSchema` の `save_version <= 0`（reason `missing-or-invalid-save_version`）分岐が発火せず正常ロードされる。テストは無効値センチネル `save_version:0` で代替し、テストコメントに根拠明記。破損検知本体（`FileSaveAdapter`/`MetaSchema`/`MetaTypes`）は本 diff 対象外のスキャフォールドであり、review-loops.md CR-CODE 合格基準「正当理由の明記」を満たすため非ブロッキング。恒久対応するなら Persistence 層で raw JSON のキー在否検査を推奨。
  2. [観察・SFH レーン] `Awake` 重複ガードの `Destroy(gameObject)`（L47-51）がログ無し／`DontDestroyOnLoad` が `EnsureCreated` 新規分岐（L32）のみで `Awake` 採用分岐（L52）に無い／`UpdateSave`（L40-43）の null 無検証は、いずれも silent-failure-hunter 領域の robustness 指摘。acfae3c 時点では潜在（重複 holder もシーン直置き holder も存在せず、`UpdateSave` は呼出元ゼロ）で機能影響なし。判定・対応は wave3-sfh レーンに委譲（本コミットで実害の発火経路は無し）。
- 対応: 不要（APPROVE）。

### 追記: CR-CODE iteration 1（再レビュー確認）合算指摘への fix 対応（gameplay-engineer・2026-07-11）

対象: 上記「CR-CODE iteration 1（再レビュー確認）」の非ブロッキング観察2件 ＋ 同時に受領した wave3-sfh 由来の Medium×3 / Low×1 の実行可能 findings（合計6件）。

- 指摘1 [非ブロッキング観察] `PersistenceStoryTests.cs:66-77` — acceptance の「save_version 欠落」が明示的0値の代用テストで、真のキー欠落は検知不能（根拠: `MetaTypes.cs:12` の初期化子が `CurrentSchemaVersion` のため）。
  - 対応: 対応済み。指摘5の根本修正（`save_version` の型既定値をセンチネル 0 化）により、真のキー欠落が原理的に破損検知されるようになったため本観察は解消。`PersistenceStoryTests.cs` のテストを本物の欠落 JSON `{"highScore":999}` に戻し、明示的 0 のケースは別テスト `Load_ExplicitZeroSaveVersion_...` として分離保持（両ケースとも同じ破損プロトコルを通ることを個別に担保）。
- 指摘2 [非ブロッキング観察・SFH レーン委譲] `SessionHolder.cs:47-52`／`:32`／`:40-43` の重複ガード無言 Destroy・DDOL 未付与・null 未検証 — 判定・対応を wave3-sfh レーンに委譲する旨のメモ。
  - 対応: 検証のみ・追加修正不要。委譲先の wave3-sfh 実体は指摘3・4・6（下記）として同時受領しており、内容を確認したところ **iteration 1/2（コミット `30388de` `S-01: fix CR-CODE iter 1` / `319c258` `S-01: fix CR-CODE iter 2`）で既に修正済み**。この観察はレビューが差分ベースコミット `acfae3c`（原初コミット）に対する再レビューだったため、その後の修正が未反映の状態の行番号を指しているだけで、現在の `SessionHolder.cs` には該当しない。指摘3・4・6の個別確認結果を参照。
- 指摘3 [Medium] `SessionHolder.cs:52` Awake 先取り分岐に DontDestroyOnLoad が無い懸念 — 現状のコードを確認: `Awake()`（91-110行目）は重複ガードで早期 return した残りのパス（＝Instance を新規に主張する唯一のパス）の末尾で `DontDestroyOnLoad(gameObject)` を無条件に呼んでおり（109行目）、かつ非ルート配置なら `transform.SetParent(null)` でルート化した上で DDOL を適用する（100-108行目、iteration 2 で追加）。指摘が懸念する「Instance = this のみで DDOL 無し」の分岐は現在のコードに存在しない。
  - 対応: 追加修正不要（既に `30388de`/`319c258` で解消済みであることを再確認）。回帰テスト `SessionHolderTests.cs` の DDOL/非ルート再配置ケースで担保。
- 指摘4 [Medium] `SessionHolder.cs:49` 重複インスタンスの無言 Destroy — 現状のコードを確認: 93-97行目で `Destroy(gameObject)` の直前に `Debug.LogError($"[Wiring] duplicate SessionHolder destroyed (scene={gameObject.scene.name})")` を1回出しており、無言破棄ではない。
  - 対応: 追加修正不要（`30388de` で解消済みであることを再確認）。
- 指摘5 [Medium] `MetaTypes.cs:12` ＋ `PersistenceStoryTests.cs:85-92` — save_version 真欠落が検知不能で acceptance 未達（唯一の新規・未解決指摘）。
  - 対応: 対応済み（本 diff の主眼）。`MetaTypes.cs`: `save_version` の型フィールド初期化子を `GameConfig.Save.CurrentSchemaVersion` からセンチネル `0`（既定 `int` 既定値）に変更し、コメントでこれが破損検知のために load-bearing である旨を明記。`CreateDefault()` は `save_version = GameConfig.Save.CurrentSchemaVersion` を明示設定するよう変更。`FileSaveAdapter.Save(SaveData data)`: 書込直前に `data.save_version = GameConfig.Save.CurrentSchemaVersion` をスタンプ（呼び出し元がオブジェクト初期化子で `save_version` を明示せずに構築したケースでも常に正しい版で永続化される保険）。`PersistenceStoryTests.cs`: `Load_InvalidSaveVersion_...` を `Load_MissingSaveVersionKey_...` に改名し、JSON ペイロードを本物の欠落 `{"highScore":999}` に変更（NOTE コメントも新事実に合わせて更新）。既存の明示的 `save_version:0` ケースは `Load_ExplicitZeroSaveVersion_...` として別テストに分離し両ケースを維持。`Save_ThenLoadWithNewAdapterInstance_RoundTripsAllFields` は変更不要（`original` オブジェクト初期化子が `save_version` 未設定でも `FileSaveAdapter.Save` のスタンプにより `writer.Save(original)` 実行後は `original.save_version` が現行版になり、往復一致が成立することを確認済み）。
- 指摘6 [Low] `SessionHolder.cs:26,40` EnsureCreated/UpdateSave の null 未検証 — 現状のコードを確認: `EnsureCreated`（38-58行目）は null 時に既存有効値があれば `Debug.LogError` を出して既存 `Instance` を保持し、無ければ同じく `Debug.LogError` の上で `CreateDefault()` ＋ `recovered=true` にフォールバック。`UpdateSave`（71-79行目）は null 時に `Debug.LogError` を出して代入をスキップする。いずれも無言受理ではない。
  - 対応: 追加修正不要（`30388de`/`319c258` で解消済みであることを再確認）。
- 検証: EditMode テスト exit 0・結果XML `total=80 passed=80 failed=0`（`qa/evidence/editmode-results.xml`）。PlayMode テスト exit 0・結果XML `total=70 passed=70 failed=0`（`qa/evidence/playmode-results.xml`。`PersistenceStoryTests` の改称/追加テストを含め全 Passed）。`ForgeBuild.BuildMac` exit 0、ログに `Build succeeded`（`game/Logs/build.log`、成果物 `game/Build/ForgeGame.app`）。
- 対象コミット: このコミットで反映（変更ファイル: `MetaTypes.cs` / `FileSaveAdapter.cs` / `PersistenceStoryTests.cs` のみ。`SessionHolder.cs` は既存修正済みのため無変更）。
