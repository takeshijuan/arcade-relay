# S-13 レビュー履歴

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-13T01:30:00Z
- 指摘要約（findings、優先度順）:
  1. **[major] `MenuController.cs` `ApplyMixerVolumes`** — `AudioMixer.SetFloat` の戻り値（bool）を無視している。SetFloat は exposed parameter 名が認識されない/ネイティブミキサーが live でない場合に **例外なく false を返して黙って失敗する**（同コミットの `AudioMixerSetup.cs` ヘッダに明記）。exposed parameter 名がドリフトした場合や音声システム未初期化時、スライダーは動き SaveData には保存されるのにミキサーバスには反映されず、story の受け入れ条件「即時反映（AudioMixer バス）」が診断ゼロで無音の no-op になる。null-`_mixer` の縮退パスはログされるのに false-return の縮退パスはログされておらず非対称。
  2. **[minor] `MenuController.cs` `_mixer` フィールドコメント（37行目）** — 「ApplyMixerVolumes logs once and no-ops」と書かれているが、実装（292-301行目）には once-guard が無く、`_mixer` が null の間は Start() 及び A/D 押下のたびに `Debug.LogError` を毎回発火する。ドキュメントと実装の不一致（本番では Menu.unity にミキサーがコミット済み・シーン配線済みのため null 分岐自体は発火しないので機能影響なし）。
  3. **[minor] `AudioMixerSetup.cs` `CreateChildGroup` 109行目** — `addChild?.Invoke(...)` が `GetMethod("AddChildToParent")` の null 戻りに対して黙って no-op する。同ファイルの他の全リフレクション参照（createAtPath/masterGroupProp/createGroup/getGuidForVolume）は null に対して例外を投げる規約なのに一貫していない。将来の Unity バージョンでメソッド名が変わった場合、`EnsureMixer` は成功を報告しつつ Bgm/Sfx グループが Master に接続されない構造的に壊れたアセットを、エラー無しで author してしまう（EditMode テストも GetFloat のみ検証のため通ってしまう）。131行目の `addExposed?.Invoke` にも同じパターンが存在し、今日は `RenameExposedParameter` の分かりにくい例外経由でのみ間接的に表面化している。
  4. **[minor] `AudioMixerSetup.cs` `EnsureMixer`（33-45行目）** — `CreateMixerControllerAtPath` がアセットをディスクに書き込んだ**後**（グループ生成やパラメータ公開の段階）で例外が投げられる部分失敗が、assetPath に半端に authoring されたアセットを残す。以降の `EnsureMixer` 呼び出しは `existing != null` の高速パスを取り、壊れたアセットを成功として返してしまい、1回ログされた失敗が恒久的にサイレントな失敗に変わる（`SceneWiring` が壊れたミキサーをシーンへ焼き込み、`MenuController` の未チェック SetFloat 戻り値と合わさると音量制御がどこにもログを残さず死ぬ）。ロードパスも読み込んだアセットが期待する exposed parameter を持つか検証していない。
  5. **[minor] `MenuScreen.cs` `BuildStatsPanel`（153-154行目）** — クリスタルアイコン行インデックス（`rowIndex: 5`, `rowIndex: 6`）が、同メソッド内約20行上で宣言される `labels` 配列の並びをコメントのみで結合したハードコードリテラル。gdd の統計行が将来追加・並び替えされた場合、IMG-03 アイコンが誤った行にサイレントに付いてしまう（PlayMode テスト `StatsTab_CrystalRows_HaveNonNullIconImages` はスプライトの非null判定のみで行整合は検証していないため、ドリフトが検知されず出荷され得る）。

- 対応（全件対応。見送りなし）:
  1. **[major]（対応）** `ApplyMixerVolumes` で `SetFloat` の戻り値を bgmOk/sfxOk として両方チェックし、false の場合は `[Wiring] ... SetFloat returned false ...` を `Debug.LogError` するよう修正。null-`_mixer` 分岐と統一的に `LogMixerErrorOnce` 経由（下記2の once-guard 実装）でログするよう共通化。
  2. **[minor]（対応）** `_mixerErrorLogged` bool フィールドを追加し、`LogMixerErrorOnce` ヘルパーで null-`_mixer` 分岐・SetFloat-false 分岐の両方を「セッション中1回のみログ」に統一。フィールドコメントも実装と一致するよう修正（once-guard の実在を明記）。
  3. **[minor]（対応）** `CreateChildGroup` の `addChild` と `ExposeGroupVolume` の `addExposed` を、ファイル内の他のリフレクション参照と同じ規約（null なら `InvalidOperationException` を throw、メソッド名を明示）に統一。
  4. **[minor]（対応）** `EnsureMixer`: (a) 既存アセットロード時に `GetFloat(bgmParam, out _) && GetFloat(sfxParam, out _)` で expected parameter の存在を検証し、失敗時はログの上 `AssetDatabase.DeleteAsset` してから再 author する経路に変更。(b) `CreateMixer` が例外を投げた catch 節で、部分的に書き込まれたアセットが存在すれば `AssetDatabase.DeleteAsset` してから null を返すよう変更（次回呼び出しがクリーンな `CreateMixer` からやり直せることを保証）。
  5. **[minor]（対応）** `CreateStatCrystalIcon` のシグネチャを `labels` 配列と対象ラベル文字列を受け取る形に変更し、`System.Array.IndexOf(labels, targetLabel)` で行インデックスを導出。見つからない場合は `Debug.LogError` の上アイコン生成をスキップ（null 返却）するよう変更。

- 検証: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results.xml -logFile -` → exit 0、結果 XML `total="90" passed="90" failed="0"`。`"$UNITY" -batchmode -projectPath game -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit -logFile game/Logs/build.log` → exit 0、ログに `Build succeeded: .../game/Build/ForgeGame.app`。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-13T10:40:00Z
- 指摘要約（findings、優先度順）:
  1. **[minor] `AudioMixerSetup.cs` `EnsureMixer` の新規クリーンアップ2箇所（50行目: 半端アセット検出時の削除 / 70行目: `CreateMixer` 例外時 catch 内の削除）** — `AssetDatabase.DeleteAsset` の bool 戻り値を無視している。`DeleteAsset` は削除失敗時（読み取り専用/ロック中ファイル、VCS 管理下等）に例外を投げず false を返して黙って失敗する — iteration 1 が `SetFloat` で潰したのと同型のサイレント失敗 API。catch 内のコメントは削除により「guarantees the next call starts from a clean CreateMixer attempt」と主張するが、`DeleteAsset` が false を返した場合その保証は成立せず、しかも失敗自体が一切ログされない。50行目側では削除失敗のまま `CreateMixerControllerAtPath` へ進み、そこで失敗すると「reflection authoring 失敗」としてログされ真因（削除できない壊れアセット）が誤帰属される。
  2. **[minor] `MenuScreen.cs`** — iteration 1 finding 5（クリスタルアイコン行インデックスのハードコード）の修正が片側のみで不完全。`BuildStatsPanel` 側のアイコン配置は `labels` 配列から `IndexOf` で導出するよう修正されたが、同じ `labels` 順序への「コメントのみ結合」が `RenderStats`（406-413行目）の `StatsTexts[0]..[7]` ハードコードインデックスにそのまま残存している。`labels` の並び替え/挿入が起きると、アイコンは新しい `labels` 順に追従する一方、`RenderStats` は固定インデックスで旧順序のラベル:値文字列を上書きするため、アイコンと値行のサイレントな不整合（エラー0）が1メソッド下で依然再現可能。

- 対応（全件対応。見送りなし）:
  1. **[minor]（対応）** `EnsureMixer` の2箇所とも `if (!AssetDatabase.DeleteAsset(assetPath)) Debug.LogError(...)` の形に変更し、削除失敗を検査・ログするよう修正（`game/Assets/Scripts/Editor/AudioMixerSetup.cs`）。
  2. **[minor]（対応）** `BuildStatsPanel` が構築する `labels` 配列を `_statsLabels` フィールドとして保持する単一情報源に昇格し、`RenderStats` を `SetStatRow(label, valueText)` ヘルパー経由（`System.Array.IndexOf(_statsLabels, label)` で行インデックスを導出）に書き換え。ハードコードインデックス `StatsTexts[0]..[7]` を全廃（`game/Assets/Scripts/Ui/MenuScreen.cs`）。

- 検証: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results.xml -logFile -` → exit 0、結果 XML `total="90" passed="90" failed="0"`（コンパイルエラー無し = typecheck 相当合格）。`"$UNITY" -batchmode -projectPath game -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit -logFile game/Logs/build.log` → exit 0、ログに `Build succeeded: .../game/Build/ForgeGame.app`。
- MAX_ITER（2）到達につき、`state/stories.yaml` の S-13 は status: done に更新（review-loops.md「合格基準: findings解消 or 正当理由の明記」— 本 iteration の findings は両方とも対応済みのため未解決事項なし）。
