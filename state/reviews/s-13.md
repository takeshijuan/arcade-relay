## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-22T11:03:59Z
- 指摘要約（優先度順）:
  1. [minor] `game/Assets/Scripts/Components/AudioCuePlayer.cs` — `PlayOneShot` のカスタムワンショット経路で pitch<=0 が渡ると無音再生かつ破棄タイマーが `clip.length / Mathf.Max(0.01f, pitch)` で最大100倍に伸びる潜在バグ。現行呼び出し元は全て正値だが「黙殺しない」方針に反する。
  2. [minor] 同ファイル — `Mathf.Max(0.01f, pitch)` の `0.01f`（安全クランプ閾値）と `source.spatialBlend = 1f`（3D空間音源指定）が Components 層本文に直書き。rules/unity-code.md の数値リテラル直書き禁止に抵触し得る。
  3. [minor] `game/Assets/Tests/EditMode/FeedbackCueSystemTests.cs` — `Tick_WaveTransition_EmitsWaveStartEvent_ForNextWave` の所要時間見積もりが `GameConfig.WaveComposition.Waves[0].SpawnIntervalMultiplier` を掛けておらず、WAVE1 の倍率チューニングでテストが壊れ得る。
- 対応:
  1. 対応済み。`pitch <= 0f` を Resources.Load 前にガードし `Debug.LogWarning` 1回で再生をスキップするよう変更（無音・破棄タイマー異常延伸を根本回避）。実呼び出し元（FeedbackCueSystem 経由の BastionCannonFirePitch=0.9f / ArcEmitterFirePitch=1.15f / 既定1f）は全て正値のため本番挙動に影響なし。
  2. 対応済み。`0.01f` は `GameConfig.Presentation.MinAudiblePitchForDestroyTimer` として `GameConfig.cs` の S-13 追記ブロックに名前付き定数化。`spatialBlend = 1f` は `AudioCuePlayer` 内 `private const float SpatialBlend3D = 1f` として名前付き定数化。
  3. 対応済み。見積もり式に `GameConfig.WaveComposition.Waves[0].SpawnIntervalMultiplier` を乗算するよう修正。
- 検証: Unity 単一インスタンスロックのためここでは起動せず、Read/Grep で参照先の実在を静的確認（`GameConfig.Presentation.MinAudiblePitchForDestroyTimer` 追加を確認、`ForgeGame.Components` から `ForgeGame` 名前空間の `GameConfig` への無出所修飾アクセスは既存 `TowerView.cs` と同パターンで前例あり、`WaveComposition.Waves[0].SpawnIntervalMultiplier` は public readonly フィールドとして実在確認済み）。エンジン起動によるコンパイル・EditMode 実行はレーン合流後のバッチ検証区間に委ねる。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-22T11:10:29Z
- 指摘要約（優先度順）:
  1. [minor] `game/Assets/Tests/EditMode/FeedbackCueSystemTests.cs` — `Tick_WaveTransition_EmitsWaveStartEvent_ForNextWave` の所要時間見積もり（iter1 #3 の修正は SpawnIntervalMultiplier は乗算したが、乗数を `MarauderCount` のみとしており `WarbeastCount` を含んでいない）。`WaveSpawnSystem.AdvanceSpawning` は Marauder+Warbeast を同一 intervalSec で順次消化するため、WAVE1 構成で `WarbeastCount>=2` にチューニングされると見積もり不足で WAVE2 開始通知の assert が偽陽性で落ちる（現行 `WaveDef(6,0,1.0f)` では実害なし）。
  2. [minor] `game/Assets/Scripts/Components/AudioCuePlayer.cs` — L36 の `pitch<=0f` ガードと L63 の破棄タイマークランプ閾値 `GameConfig.Presentation.MinAudiblePitchForDestroyTimer`(0.01f) が不整合。`0 < pitch < 0.01` はガードを素通りし（`FeedbackCue` コンストラクタも正値なら許容するため到達可能）、iter1 #1 の症状（ほぼ聴取不能な再生＋破棄タイマー最大100倍延伸）が部分残存する。
- 対応:
  1. 対応済み。見積もり乗数を `GameConfig.WaveComposition.Waves[0].MarauderCount + GameConfig.WaveComposition.Waves[0].WarbeastCount`（総スポーン数）に変更し、`AdvanceSpawning` が Marauder/Warbeast を同一間隔で順次消化する実装と一致させた。
  2. 対応済み。ガード条件を `pitch < GameConfig.Presentation.MinAudiblePitchForDestroyTimer` に引き上げ、破棄タイマークランプと同一閾値に統一。これによりクランプは理論上到達不能なバックストップとなり、コメントと挙動が一致する。現行呼び出し元（0.9f/1.15f/既定1f）は全て閾値超のため本番挙動に影響なし。
- 検証: Unity 単一インスタンスロックのためここでは起動せず、Read/Grep で静的確認（`GameConfig.WaveComposition.Waves[0].WarbeastCount` は public readonly フィールドとして実在確認済み、`GameConfig.Presentation.MinAudiblePitchForDestroyTimer` は iter1 で追加済みの定数を参照）。コンパイル・EditMode 実行はレーン合流後のバッチ検証区間に委ねる。MAX_ITER(2) 到達のため state/stories.yaml の S-13 を status: done に更新し、未対応findingは無し（両指摘とも修正済み）である旨を注記する。
