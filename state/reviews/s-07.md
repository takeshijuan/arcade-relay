# S-07 レビュー履歴 — メタ進行の永続化（FileSaveStore・保存/復元/破損プロトコル）

対象コミット: `7bbd12e99f0ed6ed127a4787df6d47c519863c05`

## CR-CODE iteration 1 — APPROVE
- 日時: 2026-07-21T12:08:55Z
- レビュー範囲: 上記コミット固定（作業ツリー未コミット変更・他コミットは対象外・読み取り専用）
- 指摘要約: findings 0 件（修正必須の指摘なし）。
  - 層分離（rules/unity-code.md 規約15 / contract §11）順守: File I/O・persistentDataPath は Persistence 層（FileSaveStore）に閉じ、検証/マイグレーションは Systems/Meta（MetaSchema）へ委譲。Systems/ から I/O 直呼び出し無し。
  - 破損時プロトコル（contract §6 / tech-stack-unity.md §セーブ）完全実装: `.bak.<UTC>` 退避 → `[SaveCorruption] reason=... backup=...` を1回 → 既定値再生成 → recovered=true。JsonUtility の寛容パーサ取りこぼしを LooksLikeJsonObject の構造チェックで多層防御。
  - アトミック書込: `.tmp` へ書き切り → 本体あり=File.Replace / 無し=File.Move。置換失敗時は `.tmp` を掃除して例外を再送出（silent-failure 回避）。
  - silent-failure 抜け道なし: Save 失敗は握り潰さず throw、破損は明示 LogError、バックアップ失敗も別ログで surface。LogError→Warning 降格や新規 Warning 級記録なし。
  - マジックナンバー/delta-time: FileSaveStore はゲームパラメータ数値リテラル無し（FileName/CorruptionLogPrefix/CurrentVersion は GameConfig.Save に集約済み。パーサ内 depth/index は「意味を持たない値」除外に該当）。新規の時間駆動ロジック無し。違反なし。
  - acceptance 充足: 保存→新規インスタンス再ロードで全 SaveData フィールド一致（SaveThenLoad_WithNewStoreInstance_RestoresAllFields）、破損3種（ParseFailed / VersionMissing=必須欠落 / SchemaInvalid=値域不正）で .bak 生成・[SaveCorruption]1回・recovered=true・既定値復旧を PlayMode（temporaryCachePath・TearDown 削除）で検証。GameBootstrap/RunOutcomeController は SaveStores.CreateDefault() の単一ファクトリ参照で差し替え漏れ経路を構造的に解消。
- 補足観察（非ブロッキング・仕様内）: 破損復旧時に既定値を save.json へ即時再永続化しないため、run 未完了のまま再起動を繰り返すと毎 Boot で再検知（.bak 累積・[SaveCorruption] 再ログ）が起きうる。ただし tech-stack-unity.md §保存タイミング（persist は Result 到達時に1回）と整合し、各 Load() 単位では [SaveCorruption] 1回を満たすため仕様内。将来 UX 改善余地としてのみ記録。
- 対応: 不要（APPROVE）。

### iteration 1 追加指摘対応（CR-CODE 合算レビュー: code-reviewer + silent-failure-hunter、同日再レビュー分）

上記 iteration 1 の APPROVE 後、code-reviewer + silent-failure-hunter の合算レビューで追加指摘8件を受領。
指摘ごとの対応記録（review-loops.md の「対応: 対応済み/見送り＋理由」形式）:

1. [MEDIUM] GameFlow.cs:24-27 / RunOutcomeController.cs:178-181 の孤児化 TODO（★TODO(S-07)）
   - 対応: 部分対応。両ファイルの TODO 宛先を `★TODO(S-20)`（破損復旧トースト・未記録表示など UI 仕上げ）へ
     付け替え、FileSaveStore 自体は S-07 で導入済みで未実装なのは S-20 側の UI 購読/表示のみである旨を明記した。
   - 見送り: state/stories.yaml の S-20 acceptance への SaveFailed 通知の追記は実施しなかった。理由:
     S-20 は `assignee: ui-engineer` のブロックであり、本 fix タスクの並走レーン規律
     （「state/stories.yaml は自 story のブロック内のみをピンポイント Edit」「担当外ストーリーのファイルを
     触らない」）により gameplay-engineer が他ストーリーの acceptance を書き換えることは越権。
     game-designer/ui-engineer への引き継ぎ事項としてこの記録に残す。
2. [MEDIUM] GameFlowPlayModeTests.cs:39-49 が注入なしで実 persistentDataPath に触れる
   - 対応: 対応済み。`Boot_LoadsSave_AndAutoTransitionsToTitle_WithNoErrors` を実 Boot シーン起動から
     「注入済み GameBootstrap を単体駆動 + InMemorySaveStore 注入」方式（既存の
     `GameBootstrap_PropagatesRecoveredFlag_ToGameFlow_AndStillReachesTitle` と同型パターン）に変更し、
     実ユーザーの persistentDataPath には一切触れないようにした。
3. [LOW-MEDIUM] FileSaveStore.cs:29-30 コンストラクタの空文字フォールバック
   - 対応: 対応済み。`directory`/`fileName` が非 null かつ空白/空文字の場合は `ArgumentException` で
     即時失敗するよう変更。`null` のみ本番既定（Application.persistentDataPath / GameConfig.Save.FileName）
     へのフォールバックを許容する。
4. [LOW] FileSaveStore.cs:70-73 JsonUtility.FromJson の `catch (Exception)` が例外情報を破棄
   - 対応: 対応済み。`catch (Exception ex)` で例外型名を `parseExceptionDetail` として保持し、
     `RecoverFromCorruption` の新規 `detail` パラメータ経由でログの `backup=` の後ろに `detail=<型名>` として
     追加出力する（既存テストの正規表現 `^\[SaveCorruption\] reason=ParseFailed backup=` は先頭一致のため非破壊）。
5. [LOW] FileSaveStore.cs:170-183 .bak 退避失敗時に主ログが `backup=<存在しないパス>` を主張
   - 対応: 対応済み。`RecoverFromCorruption` に `backupWritten` フラグを追加し、実際に `.bak` が書けた場合のみ
     `backup=<path>` を、書けなかった場合は `backup=FAILED` を出力するよう変更。
6. [LOW] FileSaveStore.cs:47-55 ReadAllText の I/O 失敗を無条件で破損（ReadFailed）扱い
   - 対応: 対応済み（提案の「1回リトライ」案を採用）。`File.ReadAllText` 失敗時に即時1回だけリトライし、
     リトライも失敗した場合のみ `ReadFailed` として破損復旧プロトコルへ入るよう変更。一過性のロック/権限エラー
     での誤検知（健全なセーブの既定値上書き）を低減する。
7. [LOW] FileSaveStore.cs:117 .tmp 掃除の空 catch
   - 対応: 対応済み。`catch (Exception cleanupEx)` で受け、`"[SaveTempCleanupFailed] path=... reason=..."` を
     1行ログするよう変更（元の Save 失敗例外は引き続き `throw` で伝播し隠されない）。
8. [観点外の付記] 新規3ファイル（FileSaveStore.cs / SaveStores.cs / FileSaveStorePlayModeTests.cs）の .meta 未コミット
   - 対応: 見送り。指摘自体が「silent failure ではないため wave3-code-reviewer 領分として記録のみ」と明記されて
     おり対応不要と判断。加えて同種の .meta 欠落は S-01（GameFlow.cs）・S-06（RunOutcomePlayModeTests.cs）由来の
     ファイルにも既存しており S-07 単独の問題ではないため、本 story の CR-CODE fix 範囲では対応しない。

検証: 並走レーン規律により本修正パスでも Unity は起動していない（Read/Grep による型・メンバ・参照・正規表現互換性
の静的確認のみ）。EditMode/PlayMode 実行はレーン合流後のバッチ検証区間（tech-stack-unity.md「検証コマンド」）で行う。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-21T13:20:00Z
- レビュー範囲: コミット `7bbd12e99f0ed6ed127a4787df6d47c519863c05` に固定（作業ツリー未コミット変更・他コミットは対象外・読み取り専用。Unity 未起動、Read/Grep のみ）
- 総評: FileSaveStore の中核（アトミック `.tmp` 書込 / 層分離 / 破損3種検知 / `.bak`＋`[SaveCorruption]`1回＋recovered=true / acceptance 充足）は iteration 1 の APPROVE 評価どおり健全。マジックナンバー・delta-time 違反なし。ただし本コミットが GameBootstrap 既定ストアを InMemorySaveStore→FileSaveStore（`SaveStores.CreateDefault()`）へ差し替えた副作用として、既存 PlayMode テスト1件が実ユーザー領域に触れる回帰が本コミット状態に残っている（修正可能な指摘＝CONCERNS）。
- 指摘要約（優先度順）:
  1. [MEDIUM] `game/Assets/Tests/PlayMode/GameFlowPlayModeTests.cs` の `Boot_LoadsSave_AndAutoTransitionsToTitle_WithNoErrors`（本コミットでは実 Boot シーンを `LoadSceneAsync` する版のまま）が、本コミットで FileSaveStore 既定化された GameBootstrap（`GameBootstrap.cs` `Start()`: `saveStore ??= SaveStores.CreateDefault()`）経由で実行環境の `Application.persistentDataPath/save.json` を Load する。実環境セーブが破損/未来版の場合、実ユーザー領域へ `.bak` を書き込み＋`[SaveCorruption]` LogError を発火し、`LogAssert.NoUnexpectedReceived()` が環境依存で false-fail する。tech-stack-unity.md「セーブ / 永続化」テスト規約（persistentDataPath を直接使うテスト禁止・一時パス限定）違反。committed コメントの「副作用は無い」は破損経路（.bak 書込・エラーログ）で成立しない。修正方針: 隣接テスト `GameBootstrap_PropagatesRecoveredFlag_...` と同型で、実 Boot シーンを起動せず注入済み GameBootstrap を単体駆動し `SetSaveStoreForTest(new InMemorySaveStore())` を注入する（＝作業ツリーで既に適用済みの形。ただし本コミットには未含のため指摘として記録）。
- 非ブロッキング観察（confidence < 報告閾値・記録のみ）:
  - `FileSaveStore.Load()` の `File.ReadAllText` 失敗を無条件で `ReadFailed` 破損として扱うため、一過性の I/O ロック/権限ヒカップで健全セーブが `.bak` 退避＋既定値復旧され得る（原本は削除されず次回正常ロードで復帰するため影響限定）。contract §6 の「破損」定義に I/O 読取失敗は含まれず、リトライ/別分類が望ましい。作業ツリーでは1回リトライを追加済み。
  - コンストラクタの空白文字ディレクトリ・`catch(Exception)` の例外情報破棄・`.bak` 書込失敗時に主ログが `backup=<未生成パス>` を主張、は LOW の診断品質 nit（作業ツリーで対応済み）。
- 対応: iteration 2 の指摘1は本コミット時点で未修正（作業ツリー側で対応済みだがコミット未反映）。レーン合流後のバッチ検証で working tree 版（InMemorySaveStore 注入）が取り込まれていることを確認すること。

### iteration 2 fix コミット時の対応記録（code-reviewer + silent-failure-hunter 合算レビュー指摘への対応）

呼び出し元から受領した指摘9件（iteration 2 本体の指摘1と重複するものを含む）への対応:

1. [MEDIUM] `GameFlowPlayModeTests.cs` の `Boot_LoadsSave_AndAutoTransitionsToTitle_WithNoErrors` が実 Boot シーン起動で実 persistentDataPath に触れる
   - 対応: 対応済み。上記指摘1・および指摘5と同一問題。作業ツリーで既に「注入済み GameBootstrap を単体駆動 + `SetSaveStoreForTest(new InMemorySaveStore())`」方式へ書き換え済みで、実 Boot シーンの `SceneManager.LoadSceneAsync` は使わない。本 fix コミットでこの working tree 版を確定コミットする。
2. [NON-BLOCKING] `FileSaveStore.Load()` の読取失敗を無条件で破損（ReadFailed）分類
   - 対応: 対応済み（作業ツリーに1回リトライを追加済み・上記非ブロッキング観察と同一）。指摘元も confidence 閾値未満・non-blocking と明記しており、根治（別エラー経路の新設等）までは行わない。リトライで一過性 I/O 障害の誤検知は大幅に低減される。
3. [NON-BLOCKING nits] (a) 空白のみのディレクトリ/ファイル名を有効パス扱い (b) JsonUtility catch(Exception) が例外型を破棄 (c) .bak 書込失敗時も backup=<path> を主張
   - 対応: 全て対応済み。(a) コンストラクタで `ArgumentException` 即時失敗（null のみ本番既定へフォールバック許可）。(b) `parseExceptionDetail` を保持しログの `detail=` に付加。(c) `backupWritten` フラグで実際に書けた場合のみ `backup=<path>`、失敗時は `backup=FAILED` を出力。
4. [NOTE] 対象コミット特定の注記
   - 対応: 了解のみ（アクション不要）。本 fix はレビュー対象コミット `7bbd12e` に対する追加コミットとして作業ツリーの変更を確定する。
5. [MEDIUM] 指摘1と同一（Boot_LoadsSave テストの persistentDataPath 直接使用）
   - 対応: 指摘1と同時に対応済み（同一修正）。
6. [MEDIUM] バックアップ確保に失敗した場合でも読取失敗を破損復旧として進行し、無傷かもしれない元 save.json を既定値で上書きしうる
   - 対応: 見送り（現状維持）。理由: 提案されたリトライ追加は既に作業ツリーで実施済みで誤検知の窓は大幅に縮小した。「バックアップ完全確保できない場合は上書きに進まない」設計変更（読取不能ファイルの rename 退避等）は Save() 呼び出し側（RunOutcomeController）との協調が必要な追加の状態遷移を要し、本 fix task のスコープ（指摘1中心の CONCERNS 解消）を超える設計変更のため見送る。残存するデータ喪失窓は「読取と .bak 双方が同一原因で失敗する」極めて狭い複合条件に限られ、`RecoverFromCorruption` は失敗時に `backup_failed` を別ログで明示するため silent ではない。後続ストーリー/バックログ項目として記録する。
7. [MEDIUM] `RunOutcomeController.cs:180` の `★TODO(S-07)` が孤児化・stale
   - 対応: 対応済み（iteration 1 追加指摘対応の1番と同一修正）。`GameFlow.cs`・`RunOutcomeController.cs` 双方の TODO 宛先を `★TODO(S-20)` へ付け替え済み。state/stories.yaml の S-20 acceptance への SaveFailed 通知追記は、S-20 が `assignee: ui-engineer` のため並走レーン規律により見送り、引き継ぎ事項として記録済み（iteration 1 追加指摘対応 1番参照）。
8. [LOW] JsonUtility.FromJson の catch(Exception) が例外型を破棄
   - 対応: 対応済み（指摘3(b)と同一修正）。
9. [LOW/任意] .tmp 掃除の空 catch
   - 対応: 対応済み（任意対応も実施）。`Debug.LogError($"[SaveTempCleanupFailed] path={tempPath} reason={cleanupEx.GetType().Name}")` を追加。降格禁止規則には抵触しない新規診断ログ。

検証: 並走レーン規律により本 fix でも Unity は起動していない。参照する型・メンバ（`ISaveStore`, `SaveStores.CreateDefault()`, `InMemorySaveStore`, `SaveData`, `LoadResult`, `MetaSchema.Validate/Migrate`, `GameConfig.Save.*`）と正規表現互換性（`^\[SaveCorruption\] reason=...`）を Read/Grep で静的確認し、コンパイルを通らない参照が無いことを確認した。EditMode/PlayMode 実行・ビルドはレーン合流後のバッチ検証区間で行う。

## CR-CODE iteration 3 — APPROVE
- 日時: 2026-07-22T00:00:00Z
- レビュー範囲: 修正コミット `7cbc447ee388e56fe8ccdc8f75a8b7c3e341b55a` に固定（作業ツリー未コミット変更・他コミットは対象外・読み取り専用。Unity 未起動、Read/Grep のみ）
- 総評: iteration 2 の CONCERNS（指摘1: `Boot_LoadsSave_...` テストが実 persistentDataPath に触れる回帰）が本コミットで解消され、iteration 1/2 の追加指摘（コンストラクタ空文字フォールバック・JsonUtility 例外情報破棄・.bak 書込失敗時の虚偽 backup= 主張・読取失敗の無条件破損分類・.tmp 掃除の空 catch）も全て反映済み。findings 0 件（修正必須の指摘なし）＝ APPROVE。
- 差分内容の確認:
  - `GameFlowPlayModeTests.cs`: `Boot_LoadsSave_AndAutoTransitionsToTitle_WithNoErrors` を実 Boot シーン `LoadSceneAsync` から「注入済み GameBootstrap 単体駆動 + `SetSaveStoreForTest(new InMemorySaveStore())`」方式へ変更。実ユーザーの persistentDataPath には一切触れず、tech-stack-unity.md テスト規約（一時パス限定）に整合。`InMemorySaveStore` / `GameBootstrap.SetSaveStoreForTest` は S-06 由来で実在（compile 整合 OK）。
  - `FileSaveStore.cs` コンストラクタ: 非 null かつ空白/空文字の `directory`/`fileName` は `ArgumentException` 即時失敗。null のみ本番既定へフォールバック。黙示無効化禁止を貫徹（silent-failure 抜け道を閉じる方向の変更）。
  - `Load()`: `File.ReadAllText` 失敗時に即時1回リトライ→リトライも失敗した場合のみ `ReadFailed` 破損分類。一過性 I/O 誤検知を低減。
  - `JsonUtility.FromJson` catch: 例外型名を `parseExceptionDetail` に保持し `RecoverFromCorruption(detail)` 経由でログ末尾に `detail=<型名>` を付加。既存テスト正規表現 `^\[SaveCorruption\] reason=ParseFailed backup=` は先頭一致のため非破壊（実テストの破損入力は `LooksLikeJsonObject` で先に弾かれ detail=null 経路のため影響なし）。
  - `RecoverFromCorruption`: `backupWritten` フラグで実際に `.bak` を書けた場合のみ `backup=<path>`、失敗時は `backup=FAILED` を出力（虚偽の成功主張を排除）。
  - `Save()` .tmp 掃除: 空 catch を `Debug.LogError("[SaveTempCleanupFailed] ...")` に置換。元の Save 失敗例外は引き続き `throw` で伝播し隠されない（降格ではなく新規診断ログ）。
  - `GameFlow.cs`: 孤児化 TODO の宛先を `★TODO(S-07)`→`★TODO(S-20)` へ付け替え（コメントのみ・機能変更なし）。
  - .meta: 新規3ファイル（FileSaveStore.cs / SaveStores.cs / FileSaveStorePlayModeTests.cs）の .meta を本コミットで追加（従前の欠落を解消）。
- 規約適合: 層分離（規約15・File I/O は Persistence 層内、検証は Systems/Meta へ委譲）順守。マジックナンバー混入なし（差分はメッセージ文字列・診断ロジックのみでゲームパラメータ数値リテラルの直書き無し。パーサ内 depth/index は「意味を持たない値」除外に該当）。delta-time 依存の新規時間駆動ロジック無し。silent-failure 抜け道なし（本コミットは全体としてエラー可視化を強化する方向）。
- acceptance 充足: `FileSaveStorePlayModeTests.cs`（temporaryCachePath・TearDown 削除）が (a) `.tmp` 経由アトミック書込＋残留なし、(b) 新規インスタンス再ロードで全 SaveData フィールド一致（保存→再起動相当→復元一致）、(c) 破損3種 = パース不能 / VersionMissing（save_version 欠落）/ SchemaInvalid（値域不正）で `.bak` 生成・`[SaveCorruption]` 1回・recovered=true・既定値復旧、を検証。破損ケースに「パース不能」と「ロード可能だがスキーマ不正（欠落・値域）」を各1件以上含み、acceptance の要求を満たす。
- 非ブロッキング観察（confidence < 報告閾値・記録のみ）:
  - `Load()` の読取リトライは遅延（backoff）無しの即時再試行のため、ミリ秒級のロック競合以外の一過性障害には効果が限定的。ただし原本は削除されず次回正常ロードで復帰するため影響は限定的（iteration 1/2 で既出の non-blocking 観察と同一。仕様内）。
  - 破損復旧時に既定値を save.json へ即時再永続化しないため、run 未完了のまま再起動を繰り返すと毎 Boot で再検知（.bak 累積）が起きうる。tech-stack-unity.md §保存タイミング（persist は Result 到達時に1回）と整合し各 Load() 単位では `[SaveCorruption]` 1回を満たすため仕様内（iteration 1 で既出）。
- 対応: 不要（APPROVE）。iteration 2 CONCERNS はクローズ。上記非ブロッキング観察は後続ストーリー/UX 改善のバックログ項目としてのみ残す。

### CR-CODE fix iter 1（code-reviewer + silent-failure-hunter 合算レビュー、新規指摘4件への対応）

iteration 3 APPROVE 後、別回のコードレビューで新規に指摘4件を受領。指摘ごとの対応記録（review-loops.md の
「対応: 対応済み/見送り＋理由」形式）:

1. [LOW][silent-failure] `FileSaveStore.cs:72` の `Load()` 初回 `File.ReadAllText` 失敗を変数なし `catch (Exception)`
   で握り潰し、リトライ成功時に一過性 I/O 失敗の痕跡がゼロになる
   - 対応: 対応済み。`catch (Exception firstEx)` に変更し、リトライ前に
     `Debug.LogWarning($"[SaveReadRetry] first read failed ({firstEx.GetType().Name}); retrying once path={path}")`
     を1行追加した。回復可能条件（直後のリトライで復帰し得る）のため LogWarning が適切で、gates.md の
     「回復不能条件を Warning 記録する」禁止には非該当（回復不能側＝リトライも失敗した場合は従来どおり
     `RecoverFromCorruption` 経由で `[SaveCorruption]` LogError を発火し続ける・変更なし）。既存テスト
     （`FileSaveStorePlayModeTests.cs` の `LogAssert.Expect`/`NoUnexpectedReceived()`）はいずれも通常の
     読取成功パスまたは破損3種（ParseFailed/VersionMissing/SchemaInvalid）を対象とし、初回 `ReadAllText` が
     例外を投げるシナリオを注入するテストは無いため、新規 Warning ログは既存 PlayMode テストの
     `LogAssert.NoUnexpectedReceived()` を破らない（当該コードパスは実行されない）。
2. [非ブロッキング・記録のみ] `FileSaveStore.cs:231` 付近 `RecoverFromCorruption` — `rawForBackup==null` かつ
   復旧時点で save.json が消滅している極小レースで `backup=FAILED` が過大報告される
   - 対応: 見送り。指摘自体が「失敗の過大報告であり silent ではないため修正不要」と明記されており、
     safe-side の挙動として現状維持する。
3. [非ブロッキング・記録のみ] `GameFlowPlayModeTests.cs` — 実 Boot シーンを `LoadSceneAsync` するテストが
   0本になった（iteration 2/3 の注入方式化の副作用）
   - 対応: 見送り（既知・記録済み）。iteration 2/3 で persistentDataPath 汚染防止のために意図的に行った変更で、
     実シーン内 `GameBootstrap` 配線の回帰検知は QA-PLAY 観点1/2（起動・コアループ実操作）が担保する分担。
     本 fix task の並走レーン規律（担当外ファイルの新規変更禁止）からも、正当化済みの既存状態を追加変更する
     理由がないため現状維持。
4. [確認済み・再指摘なし] iteration 2 の指摘6（`.bak` 確保失敗時も既定値復旧を続行しうる）の見送りが
   state/reviews/s-07.md に正当理由付きで明記済みであることの確認
   - 対応: 対応不要（確認事項）。iteration 2 本体の指摘6・および「iteration 2 fix コミット時の対応記録」6番に
     見送り理由（backup_failed 別ログでの surface・スコープ超過の設計変更）を明記済みで、review-loops.md の
     「対応: 対応済み/見送り＋理由」要件を充足している。追加対応なし。

検証: 並走レーン規律により本 fix でも Unity は起動していない。変更は `FileSaveStore.cs` の
`catch (Exception firstEx)` ブロックのみで、参照する `Debug.LogWarning`（UnityEngine 標準 API・既存
`Debug.LogError` 呼び出しと同一 using 内）に未解決参照は無い。既存の `LogAssert.Expect`/`NoUnexpectedReceived()`
テストとの整合も上記1番のとおり静的確認済み。EditMode/PlayMode 実行・ビルドはレーン合流後のバッチ検証区間で行う。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-22T03:12:00Z
- レビュー対象コミット: `5b98c106f0cd442adaeb89068bfd9040aa5b89bc`（"S-07: fix CR-CODE iter 1"）に固定。作業ツリー未コミット変更・他コミットは対象外・読み取り専用（Unity 未起動、Read/Grep のみ）
- 差分の範囲: 本コミットの機能変更は `FileSaveStore.cs` の1点のみ — `Load()` 初回 `File.ReadAllText` 失敗時の `catch (Exception)` を `catch (Exception firstEx)` に改め、即時リトライ前に `Debug.LogWarning("[SaveReadRetry] first read failed (<型名>); retrying once path=...")` を1行追加（併せて s-07.md へ対応記録を追記）。アトミック `.tmp` 書込・層分離・破損3種検知・`.bak`＋`[SaveCorruption]`1回＋recovered=true・acceptance 充足は既レビュー（旧 iteration 1/3）どおり不変。
- 判定根拠（findings 0 件＝APPROVE）:
  1. silent-failure 非該当（むしろ可視化強化）: 従来は変数なし `catch (Exception)` で初回読取失敗の痕跡が完全消失していたところに診断ログを新設。回復可能条件（直後リトライで復帰し得る）に対する Warning であり、gates.md の禁止パターン「LogError→Warning 降格」「新規コードが回復不能条件を Warning 以下で記録」いずれにも非該当。回復不能側（リトライも失敗）は従来どおり `RecoverFromCorruption("ReadFailed:...")` 経由で `[SaveCorruption]` LogError を発火し続けるため、QA のエラー0検査で確実に表面化する（Warning が抜け道化しない）。
  2. 既存テスト非破壊: 追加 Warning は「初回 `ReadAllText` が例外を投げる」パス上にのみ出力されるが、`FileSaveStorePlayModeTests.cs` の全テスト（正常往復＋破損3種 ParseFailed/VersionMissing/SchemaInvalid）はこのパスを注入せず未実行。加えて `LogAssert.NoUnexpectedReceived()` は既定で Error/Assert/Exception のみを失敗扱いとし Warning は対象外のため、仮に当該パスが実行されても偽陰性・偽陽性を生まない。
  3. 規約適合: ゲームパラメータ数値リテラルの新規混入なし（追加は診断メッセージ文字列のみ）＝マジックナンバー違反なし。時間駆動ロジックの追加なし＝delta-time 観点非該当。File I/O は Persistence 層内に閉じたまま（規約15 順守）。`Debug.LogWarning` は `using UnityEngine;` 配下の標準 API で未解決参照なし・コンパイル整合 OK。
- 非ブロッキング観察（confidence < 報告閾値・記録のみ・本コミット由来ではない既出項目）: 読取リトライは backoff 無しの即時再試行のためミリ秒級ロック以外の一過性障害には効果限定的（原本は削除されず次回正常ロードで復帰）。破損復旧時に既定値を save.json へ即時再永続化しないため run 未完了の再起動反復で毎 Boot 再検知しうる（tech-stack-unity.md §保存タイミングと整合・仕様内）。いずれも旧 iteration で既出でありバックログ扱い。
- 対応: 不要（APPROVE）。CR-CODE ループ（MAX_ITER=2）は本 iteration の APPROVE で終了。
