# S-07 レビュー履歴 — ダッシュ回避（無敵窓・クールダウン・方向優先順位）

## CR-CODE iteration 1 — APPROVE
- 日時: 2026-07-10T08:56:49Z
- 対象コミット: 156dd0b07a8406227b468ef6dcc6b8275375de3c
- 指摘要約: ブロッキング指摘なし（findings 0件）。
  - マジックナンバー禁止（unity-code #12/#13）: 準拠。`DashSystem`・`PlayerController` のダッシュ数値は全て `GameConfig.Player.DashSpeed/DashDuration/DashCooldown/DashInvulnDuration`（gdd 数値表と一致・scaffold 済み定数）経由。残る数値リテラルは `sqrMagnitude > 0f` / `<= 0f` の閾値比較・`new Vector3(x, 0f, z)` の Y 平坦化（conventions §3 の構造値）・ループ index の `0`/`i++` のみで、いずれもチューニング値ではなく規約の除外対象。`float.PositiveInfinity`（`TryGetNearestEnemyDirection` の maxRange センチネル）は「範囲無制限」を表す名前付き無限大であり、コメントで意図を明記済み（二乗しても +Infinity で全候補が範囲内＝優先順位(3)の「最寄り敵」を距離ゲートせず選ぶ、の意図と一致）。
  - delta-time（unity-code #14/#15）: 準拠。`DashSystem.TickTimer = Mathf.Max(0f, remaining - deltaTime)` でクールダウン/ダッシュ持続/無敵窓の3タイマーを全て deltaTime 減算＋0床。`DashSystem.ComputeDisplacement = dashDirection * (dashSpeed * deltaTime)` で移動も deltaTime スケール。フレーム固定加算（`x += k`）・60fps 前提なし。
  - エンジン非依存コア（unity-code #16 / conventions §1）: 準拠。`Systems/DashSystem` は static クラス・MonoBehaviour 非継承・シーンAPI（Find/Instantiate/GetComponent）不使用。`Vector2`/`Vector3`/`Mathf` の値型・数学型のみ（規約の許容範囲）。入力読取（`_input.Dash.WasPressedThisFrame()`）・`EnemyAgent.ActiveEnemies` 参照・`transform.position` 反映といったシーン依存は全て `Components/PlayerController`（配線層）に隔離。方向優先順位・タイマー算術・変位はすべて純粋関数へ委譲され、Component 側は状態保持と関数呼び出しのみ（"Components はライフサイクルと配線のみ"）。
  - 式の再実装禁止（conventions §5 の一般化）: 準拠。優先順位(3)の最寄り敵探索は `AutoAttackSystem.FindNearestIndex` を再利用し、探索ロジックを重複実装していない。`_enemyPositionsScratch` 再利用リストで per-call のリスト新規確保を回避（ダッシュ発動時のみ実行＝約1.2s に1回、負荷は無視可能）。
  - facing 非連動（conventions §4 / acceptance）: 準拠。`DashSystem.ResolveDashDirection` は move 入力・直前移動向き・最寄り敵の3値のみを引数に取り、hero の facing/rotation を一切読まない。`PlayerController.TryActivateDash` も facing 状態を読み書きしない（そもそも現行 PlayerController に回転概念なし）。
  - 方向優先順位の正当性（acceptance (1)(2)(3)）: 準拠。(1) 同フレームの move 入力（`sqrMagnitude > 0f`）→ XZ 正規化。(2) 入力ゼロ時は `_lastMoveDirection`（直前の非ダッシュフレームで更新）を XZ 平坦化＋正規化。(3) (1)(2) 不成立時のみ `-最寄り敵方向` を正規化（"最寄り敵の反対"）。全ティアが不成立（一度も移動せず場に敵もいない）なら `Vector3.zero` を返し、`TryActivateDash` は「行き先の無いダッシュでクールダウンを消費しない」ため early-return（発動もクールダウン開始もしない）。lastMove/enemy を二重に XZ 平坦化するのは非XZ world offset からの Y 漏れ防止の defense-in-depth で、`PlayerMovement` の明示 Y=0 構築と同型。
  - Update 内の順序整合: `TickDashTimers → TryActivateDash → 変位適用` の順。発動フレームは Tick（0→0）後に `_dashTimeRemaining = DashDuration` を設定し、直後の `IsDashing`（`_dashTimeRemaining > 0`）が true になりその場でダッシュ変位を1フレーム適用。ダッシュ中は move 入力を無視し `_dashDirection` 固定（"ダッシュ中は移動入力を無視し軌道を固定"）。ダッシュ中は else 分岐に入らないため `_lastMoveDirection` は更新されず、ダッシュ終了後に move 継続していれば通常移動と `_lastMoveDirection` 更新が再開する。
  - 再発動不可・CD中無反応（acceptance / gdd「入力バッファなし」）: 準拠。発動時に `_cooldownRemaining = DashCooldown(1.2s)` を設定。`CanActivate(cooldownRemaining) = cooldownRemaining <= 0f` が false の間は `WasPressedThisFrame()` を単純に drop（キュー/バッファなし）。`DashCooldown(1.2s) > DashDuration(0.2s)` のためダッシュ中は常にクールダウンも作動中で、`|| IsDashing` は防御的冗長（実害なし）。無敵窓 `DashInvulnDuration(0.25s) > DashDuration(0.2s)` でダッシュ全域を包含し、発動と同時に `IsDashInvulnerable` が立つ。
  - 入力配線（conventions §4 / unity-code #18/#19）: 準拠。`GameInput` に `Dash = _gameplay.AddAction("Dash", Button, "<Keyboard>/space")` が既存で存在し、`EnableGameplay()` で有効化される map に属する。旧 `Input.GetKey` 不使用・入力読取は InputLayer 集約を経由。
  - acceptance: 満たす。Space 押下→pure `DashSystem` が優先順位(1)(2)(3)で方向解決→`DashDuration` 間 `DashSpeed` 移動＋`DashInvulnDuration` 無敵→`DashCooldown` 経過まで再発動不可（CD中 drop）、facing 非連動、いずれも実装済み。EditMode `DashSystemTests`（3ティア優先順位・対角正規化・Y漏れ防止・全滅時ゼロ・`CanActivate`/`TickTimer`/`ComputeDisplacement` 算術）と PlayMode `DashSceneTests`（`MoveInputHeld_...` で入力方向へ ≈DashSpeed×DashDuration=4m 移動・無敵フラグ立ち上げ/クリア・Zドリフト無し / `DashPressDuringCooldown_IsIgnored_...` で CD中の再入力が無反応・無敵再武装なし・DashSpeed 規模のバースト無し）が受け入れ条件を網羅（conventions §9 の「新規 pure System は EditMode 必須」も充足）。`.meta`（DashSystem.cs.meta / 両テスト .meta）も同梱で Unity 取込に不足なし。
- 参考（非ブロッキング・対応不要）:
  1. `TryActivateDash` は優先順位(1)(2)が成立する場合でも `TryGetNearestEnemyDirection` を先行評価する（`ResolveDashDirection` の enemy 引数を eager 構築）。ダッシュ発動フレームのみの実行で `_enemyPositionsScratch` も再利用のため負荷は無視可能、かつ純粋関数のシグネチャを保つ設計判断であり correctness 影響なし（信頼度<50・非報告）。
  2. 発動フレームは Tick 後に `_dashTimeRemaining` を設定するため、そのフレーム分の変位（≈DashSpeed×1フレーム dt）が `DashDuration` 相当の変位に上乗せされ、総移動距離は 4m を最大約1フレーム分だけ超えうる。acceptance が "≈4m"、PlayMode 側の許容が 0.6m のため設計上許容範囲内（信頼度<50・非報告）。
- 対応: 追加対応不要（findings 0件・APPROVE）。
