# S-02 レビュー履歴 — Title シーン（→Menu 遷移・復旧通知）

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-21T18:40:00+09:00
- 対象: commit 3192aff（S-02 コード実体は a5a7f8a/80a5ac3/8af26ed/c0c595c）。差分 = game/Assets/Scripts/{GameConfig.cs, Input/InputReader.cs, ForgeGame.asmdef, Ui/TitleScreen.cs}, Scenes/Title.unity, Tests/PlayMode/TitleScreenPlayModeTests.cs
- 指摘要約（優先度順）:
  1. [マジックナンバー] TitleScreen.cs:72 `Mathf.Lerp(0.35f, 1f, phase)` — 明滅の最小アルファ 0.35f が本文直書き。明滅「周期」は GameConfig.Presentation.TitlePromptBlinkPeriodSec に集約済みなのにフェード深度だけ取り残されており、rules/unity-code.md 規約1/13 違反。`GameConfig.Presentation.TitlePromptMinAlpha` 等へ集約する。
  2. [マジックナンバー（レイアウト定数）] TitleScreen.cs:86/93/97/102/126 — アンカーY 0.68/0.86/0.18、matchWidthOrHeight 0.5、幅係数 0.8、行高係数 1.6 が本文直書き。フォントサイズ・基準解像度は GameConfig.Ui に集約済みで、これらの配置係数のみ規約の「チューニングは GameConfig 編集だけで完結」に反する。GameConfig.Ui へ寄せる。（pivot(0.5,0.5)=中央は慣例値のため除外）
- 良好点（合格判断の根拠）:
  - acceptance 充足: ScreenSpaceCamera Canvas 表示（UiCanvasHelper.ConfigureScreenSpaceCamera、Title.unity に MainCamera タグ有り→Camera.main 解決）/ 左クリック・任意キー→Menu（GameFlow.GoToMenu 経由・シーン名文字列の直書き無し）/ recovered=true で復旧通知（Start が GameFlow.Recovered 参照）。PlayMode(InputTestFixture) が renderMode==ScreenSpaceCamera・worldCamera!=null・Title→Menu（click/anyKey）・recovered 表示/非表示を検証。
  - delta-time 準拠: AnimatePrompt は Time.deltaTime + Mathf.PingPong でフレームレート非依存。固定加算なし。
  - 入力抽象化: InputReader に anyKey アクション追加（Input System・旧 Input API 不使用・読み取り集約）。
  - レイヤリング: TitleScreen は薄い Ui Component。I/O 無し、GameFlow 共有状態を複製せず参照のみ。silent-failure パターン無し（save/load・LogError 降格・batchmode 握り潰しいずれも該当なし）。
  - 並走レーン: GameFlow(S-01)・GameConfig.Scenes.Menu・UiCanvasHelper 実在。Menu シーン実体は S-03 レーン（遷移先テストの合流時整合はバッチ検証が保証。実体未実装は blocker としない）。
- 対応:
  1. 対応済み。`GameConfig.Presentation.TitlePromptMinAlpha`(0.35f)/`TitlePromptMaxAlpha`(1f) を追加し、`TitleScreen.AnimatePrompt` の `Mathf.Lerp` 引数をそこから参照するよう変更（TitleScreen.cs:72）。
  2. 対応済み。`GameConfig.Ui` に `CanvasScalerMatchWidthOrHeight`(0.5f)・`TextBoxWidthFraction`(0.8f)・`TextLineHeightFactor`(1.6f)（Title/Menu/HUD/Result 共通で使い回す配置係数）と、Title 専用の `TitleTextAnchorY`(0.68f)・`TitleRecoveryNoticeAnchorY`(0.86f)・`TitlePromptAnchorY`(0.18f) を追加し、TitleScreen.cs の該当箇所（scaler.matchWidthOrHeight / CreateText 呼び出し3箇所のアンカーY / CreateFullScreenPanel 以外の sizeDelta 算出）を全て参照へ置換。pivot(0.5,0.5) は指摘通り慣例値のため据え置き。
  3. 対応不要（指摘自体が non-blocker と明記済み）。Menu シーン（S-03 レーン）の Build Settings 登録状況は自レーンの担当外のため触れていない。合流時のバッチ検証で確認されたい。
  4. 対応記録のみ（コード修正ではなく CR-CODE 運用上の指摘）。今回のコミットが S-02 実装本体（TitleScreen.cs / GameConfig.cs 追記分）への実差分修正であるため、次回 CR-CODE 起動時は本コミットを含む実装レンジ（a5a7f8a 以降〜今回コミットまで）を対象に再固定して再検査することを推奨する。ui-engineer 側での対象コミット選定はできないため、再起動判断は workflow/orchestration 側に委ねる。
  - 静的確認（Unity 起動なし）: `GameConfig.Presentation.TitlePromptMinAlpha/MaxAlpha`・`GameConfig.Ui.CanvasScalerMatchWidthOrHeight/TextBoxWidthFraction/TextLineHeightFactor/TitleTextAnchorY/TitleRecoveryNoticeAnchorY/TitlePromptAnchorY` は全て `GameConfig.cs` に実在し、TitleScreen.cs からの参照名と完全一致することを Read/Grep で確認済み。TitleScreen.cs に数値リテラルの残存が無いことを grep で確認済み（`0.35f`/`1.6f`/`0.8f`/`0.68f`/`0.86f`/`0.18f`/`matchWidthOrHeight = 0` の一致なし）。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-21T18:36:16+09:00
- 対象: commit 0225d71096eea509c820360102f930fa46e9f2ea「S-02: fix CR-CODE iter 1」。差分 = game/Assets/Scripts/Ui/TitleScreen.cs（+ 本レビュー履歴）。engine=unity。読み取り専用レビュー（Unity/ビルド未起動）。
- 指摘要約: なし（findings 0件）。
- 検査結果:
  - iter1 指摘#1（明滅アルファのマジックナンバー）解消確認: `Mathf.Lerp(0.35f, 1f, phase)` → `Mathf.Lerp(GameConfig.Presentation.TitlePromptMinAlpha, GameConfig.Presentation.TitlePromptMaxAlpha, phase)`。定数は GameConfig.cs:183/184 に実在（0.35f / 1f）。
  - iter1 指摘#2（レイアウト定数のマジックナンバー）解消確認: matchWidthOrHeight 0.5f→`CanvasScalerMatchWidthOrHeight`(GameConfig.cs:217)、アンカーY 0.68/0.86/0.18→`TitleTextAnchorY`/`TitleRecoveryNoticeAnchorY`/`TitlePromptAnchorY`(222-224)、幅係数 0.8f→`TextBoxWidthFraction`(218)、行高係数 1.6f→`TextLineHeightFactor`(219)。全定数が GameConfig.cs に実在し参照名・値が完全一致（Read/Grep 静的確認）。
  - 値保存の検証: 抽出前後で全数値が同値（0.35/1/0.5/0.68/0.86/0.18/0.8/1.6）。挙動変化なし＝純粋なリファクタリングで acceptance を破壊しない。
  - delta-time 準拠（rules/unity-code.md 規約）: AnimatePrompt は `Time.deltaTime` + `Mathf.PingPong` 継続、固定加算なし。
  - ScreenSpaceCamera 規約: `UiCanvasHelper.ConfigureScreenSpaceCamera` 使用（ScreenSpaceOverlay 不使用）。
  - レイヤリング/silent-failure: TitleScreen は薄い Ui Component（I/O 無し・GameFlow 共有状態を参照のみ）。save/load・LogError 降格・batchmode 握り潰しいずれも該当なし。
  - 残存マジックナンバー: anchor X=0.5f・pivot(0.5,0.5) は中央配置の慣例値（rules 除外・iter1 で判断済み）。他に数値リテラルの残存なし。
  - acceptance: ScreenSpaceCamera Canvas 表示 / 左クリック・任意キー→Menu（GameFlow.GoToMenu 経由）/ recovered=true で復旧通知（Start が GameFlow.Recovered 参照）を満たす実装を維持。PlayMode(InputTestFixture) の検証観点（Title→Menu・renderMode==ScreenSpaceCamera）は本 diff で不変。
- 対応: 不要（APPROVE・findings 0）。

## CR-CODE iteration 1 — CONCERNS（再レビュー・commit 3192aff 固定）
- 日時: 2026-07-22T00:00:00+09:00
- 対象: commit 3192affc038f04194e9b9ae85939d51979aa2006 に固定（S-02 コード実体は a5a7f8a/80a5ac3/8af26ed/c0c595c）。差分 = game/Assets/Scripts/{GameConfig.cs, Input/InputReader.cs, Ui/TitleScreen.cs}, Tests/PlayMode/TitleScreenPlayModeTests.cs, ForgeGame.asmdef, Scenes/Title.unity。engine=unity。読み取り専用（Unity/ビルド未起動）。
- 指摘要約（優先度順・rules/unity-code.md 規約1「マジックナンバー禁止」）:
  1. [マジックナンバー / 透明度] TitleScreen.cs:72 `c.a = Mathf.Lerp(0.35f, 1f, phase);` — 明滅のフェード深度（最小/最大アルファ）が本文直書き。明滅「周期」は GameConfig.Presentation.TitlePromptBlinkPeriodSec に集約済みなのに深度だけ取り残されており、チューニングが GameConfig 編集で完結しない。`GameConfig.Presentation.TitlePromptMinAlpha`/`TitlePromptMaxAlpha` へ集約する。
  2. [マジックナンバー / レイアウト係数] TitleScreen.cs:86,93,97,102,126 — matchWidthOrHeight `0.5f`、アンカーY `0.68f`/`0.86f`/`0.18f`、幅係数 `0.8f`、行高係数 `1.6f` が本文直書き。フォントサイズ・基準解像度は GameConfig.Ui に集約済みで配置係数のみ規約に反する。GameConfig.Ui へ寄せる（anchor X=0.5f・pivot(0.5,0.5) は中央配置の慣例値のため除外）。
- 良好点（合格判断の根拠）:
  - acceptance 充足: Title.unity に MainCamera タグ付きカメラ実在（Camera.main 解決）＋ TitleScreen コンポーネント配線済み。UiCanvasHelper.ConfigureScreenSpaceCamera で ScreenSpaceCamera+worldCamera 設定。左クリック/任意キー→GameFlow.GoToMenu（シーン名直書き無し）。recovered=true で Start が GameFlow.Recovered を読み通知表示。PlayMode(InputTestFixture) が renderMode==ScreenSpaceCamera・worldCamera!=null・Title→Menu（click/anyKey）・recovered 表示/非表示を検証。
  - delta-time 準拠: AnimatePrompt は Time.deltaTime + Mathf.PingPong でフレームレート非依存。固定加算なし。
  - 入力抽象化: InputReader に AnyKey アクション追加（Input System・旧 Input API 不使用・読み取り集約）。ClickPressedThisFrame/AnyKeyPressedThisFrame のみ購読。
  - レイヤリング/silent-failure: TitleScreen は薄い Ui Component（I/O 無し・GameFlow 共有状態を複製せず参照のみ・transitioning ガードで二重遷移防止）。save/load・LogError 降格・batchmode 握り潰しいずれも該当なし。
  - 並走レーン: GameFlow(S-01)・GameConfig.Scenes.Menu・UiCanvasHelper 実在。Menu シーン実体は S-03 レーン担当のため実体未実装は blocker としない（合流時バッチ検証で保証）。
  - 観察（非 blocker・要実行確認）: PlayMode テストが [UnitySetUp] でシーンロードし各 [UnityTest] 側でデバイス追加/入力擬似発行する構成。batchmode で setup/test コルーチン境界を跨ぐと WasPressedThisFrame が反映されない可能性があり、シーンロードとデバイス追加を同一コルーチンへインライン化する方が堅い（静的レビューでは断定不可・QA-PLAY 実行で確認）。
- 対応（code-reviewer + silent-failure-hunter 合算・iter1 fix コミットで対応）:
  1. 対応済み（既存修正の再確認）。TitleScreen.cs:82 `Mathf.Lerp(GameConfig.Presentation.TitlePromptMinAlpha, GameConfig.Presentation.TitlePromptMaxAlpha, phase)`。定数は GameConfig.cs に既存（`TitlePromptMinAlpha`=0.35f/`TitlePromptMaxAlpha`=1f）。今回のコミット時点で既に HEAD に反映済みであることを Read で確認。追加変更なし。
  2. 対応済み（既存修正の再確認）。TitleScreen.cs の matchWidthOrHeight・アンカーY 3箇所・幅係数/行高係数は全て `GameConfig.Ui.CanvasScalerMatchWidthOrHeight`/`TitleTextAnchorY`/`TitleRecoveryNoticeAnchorY`/`TitlePromptAnchorY`/`TextBoxWidthFraction`/`TextLineHeightFactor` 参照であることを Read で確認済み（HEAD 時点で既に反映済み）。追加変更なし。
  3. 対応済み（既存修正の再確認）。TitleScreenPlayModeTests.cs は既に `LoadTitle()` ヘルパー呼び出し＋デバイス追加/入力擬似発行を各 `[UnityTest]` 内へインライン化する構成に修正済み（ファイル冒頭コメント「QA-PLAY 再現バグ修正メモ」参照。共有 `[UnitySetUp]` は使用していない）。追加変更なし。
  4. 対応記録のみ（コード修正ではなく CR-CODE 運用上の指摘）。今回は指定範囲（S-02 一式）で確認済み。
  5. 対応不要（正当理由あり・見送り）。Camera.main==null の明示エラーは `Ui/UiCanvasHelper.ConfigureScreenSpaceCamera`（S-03 レーンの CR-CODE 修正で追加済み・コメント「CR-CODE iter1 #5 / iter2 #1」）に一元化されており、Title を含む全 UI シーンの Canvas 呼び出しを一括カバーする。TitleScreen.cs 側に同条件のガードを重ねると同一 null 時に LogError が二重発生するため、重複ガードは追加せず一元化された既存実装に委ねる（コメントで経緯を明記）。
  6. 対応済み。`inputReader?.Enable()` / `inputReader?.Disable()` の `?.` を外し、素の `inputReader.Enable()` / `inputReader.Disable()` に変更（TitleScreen.cs OnEnable/OnDisable）。Awake→OnEnable の順序保証が破れた場合は NRE で失敗するようにした。
  7. 対応済み。`TitleScreen.RecoveryNoticeTextExists`（`recoveryNoticeText != null`）を追加し、`NotRecovered_HidesRecoveryNotice` テストに `Assert.IsTrue(titleScreen.RecoveryNoticeTextExists, ...)` を追加。UI 未構築でも `RecoveryNoticeVisible` が false を返して緑になる問題を解消。
  - 静的確認（Unity 起動なし）: `RecoveryNoticeTextExists` の参照名がテストと実装で完全一致することを Read で確認。`inputReader.Enable()/Disable()` の呼び出し形が InputReader の公開シグネチャ（`public void Enable()`/`public void Disable()`）と一致することを Grep で確認。TitleScreen.cs に残存する数値リテラルは `new Vector2(0.5f, ...)` の中央アンカー X と `pivot(0.5f, 0.5f)` のみ（iter1 で慣例値として除外判断済み）。

## CR-CODE iteration 2 — APPROVE（commit 0eaece1 固定・iter1 CONCERNS 修正の確認）
- 日時: 2026-07-22T11:20:00+09:00
- 対象: commit 0eaece17e49beaeabc625b61691681fb69788256「S-02: fix CR-CODE iter 1」。差分 = game/Assets/Scripts/Ui/TitleScreen.cs / game/Assets/Tests/PlayMode/TitleScreenPlayModeTests.cs（+ 本レビュー履歴）。engine=unity。読み取り専用レビュー（Unity/ビルド未起動）。
- 指摘要約: なし（findings 0件）。
- 検査結果:
  - 本 diff の変更点は4点のみ: (a) `RecoveryNoticeTextExists`(`recoveryNoticeText != null`) プロパティ追加、(b) OnEnable/OnDisable の `inputReader?.Enable()/Disable()` から `?.` を除去、(c) BuildUi に Camera.main==null 一元化の説明コメント追加、(d) NotRecovered_HidesRecoveryNotice テストへ `Assert.IsTrue(RecoveryNoticeTextExists, ...)` 追加。
  - iter1 指摘#6（`?.` 除去）解消確認: Unity のライフサイクル順序保証（Awake→OnEnable、OnDisable は OnEnable 後のみ発火）により `inputReader` は非 null が保証される。`InputReader.Enable()/Disable()` は `public void`（InputReader.cs:35/36）で呼び出し形一致。silent-failure（入力無反応の Title をログゼロで成立）を潰し、不変条件破綻時は NRE で大きく失敗する正しい方向の修正。回帰なし。
  - iter1 指摘#7（負テストの UI 未構築見逃し）解消確認: `RecoveryNoticeTextExists` を追加し `NotRecovered_HidesRecoveryNotice` で先にアサート。`recoveryNoticeText==null` でも `RecoveryNoticeVisible` が false を返して緑になる穴を塞いだ。プロパティ名がテスト/実装で完全一致。
  - マジックナンバー（rules/unity-code.md 規約1）: 本 diff は数値リテラルを一切導入していない。新規プロパティ・コメント・テストアサートのみ。違反なし。
  - delta-time 準拠: AnimatePrompt 等の時間依存コードは本 diff で不変（`Time.deltaTime` + `Mathf.PingPong` 継続）。
  - silent-failure パターン: LogError 降格・batchmode 握り潰し・セーブ破損の黙示初期化いずれも該当なし。むしろ silent-failure 経路を1つ除去。
  - acceptance: ScreenSpaceCamera Canvas 表示 / 左クリック・任意キー→Menu / recovered=true で復旧通知 / PlayMode(InputTestFixture) の Title→Menu・renderMode==ScreenSpaceCamera 検証は本 diff で不変かつ維持。追加アサートで負テストのカバレッジが向上。
- 対応: 不要（APPROVE・findings 0）。
