# S-05 レビュー履歴 — ビルドスポット設置・タワー自動攻撃・経済

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-21T11:16:19Z
- 対象コミット: af774b38203c78830080006ff8bd0ff76045502f
- レビュアー: pr-review-toolkit:code-reviewer（CR-CODE 読み替え）
- 指摘要約（優先度順）:
  1. [scene / lane-scope] `game/Assets/Scenes/Game.unity`（+454 付近）— S-05（gameplay-engineer）が
     S-08（ui-engineer）所有の `ForgeGame.Ui.HudPanel`（script guid `aeb65586fa1641f9ad026a17b027c225`）を
     共有 Game シーンへ新規追加している。当コミット時点で HudPanel.cs はツリーに実在せず、guid は
     Game.unity 内のみに出現。S-08 は既に status: done（自身の Game.unity 編集で HUD を追加している見込み）。
     リスク: (a) S-08 レーン合流時に HudPanel が二重化しシーンマージ衝突、(b) guid が S-08 の
     HudPanel.cs.meta と不一致だと恒久的 missing-script → QA-PLAY のログエラー0検査を落とす。
     S-05 の acceptance（ビルドスポット/戦闘/経済）外のスキャフォールドであり、S-05 の PlayMode/テストも
     Game.unity をロードしないため本オブジェクトは S-05 に不要。方針: S-05 diff から HudPanel の GameObject/
     MonoBehaviour ブロックを削除し、HUD の配置・配線は S-08 に委ねる。
  2. [MEDIUM] `Systems/TowerCombatSystem.cs:131-138` — `DamageForLevel` の `default: return lv3` が不正
     Level（0・負値・4以上）を黙って Lv3 ダメージとして成功させる（失敗時に成功を装う戻り値）。
  3. [LOW] `Components/BuildSpotController.cs:90` — 撃破経路で `TryRemoveEnemyView` の戻り値 false を無言破棄。
     View/状態 desync の兆候をログゼロで握り潰す。
  4. [LOW] `Components/PlaceholderFactory.cs:38-41` — sharedMesh 欠落/bounds 高さ非正のフォールバックが無ログ。
     本コミットが修正した「Cube/Sphere で意図の半分の高さ」バグを異常系限定で無言再現しうる。
- 対応:
  1. **見送り（正当理由あり・削除しない）**。指摘時点（コミット af774b3）では HudPanel.cs がツリーに
     未実在だったため「S-05 が先行埋め込みした未検証の依存」と評価されたが、その後 S-08 が着地済み
     （`3221cf0` `HudPanel.cs`/`HudPanel.cs.meta` 追加、guid `aeb65586fa1641f9ad026a17b027c225` は
     Game.unity 側の既存参照と完全一致・二重化なし）、かつ `fb03bc7` で S-08 は status: done・CR-CODE
     APPROVE 済み。S-08 の `Assets/Tests/PlayMode/GameHudPlayModeTests.cs` は `Game.unity` を
     `SceneManager.LoadSceneAsync` でロードした上で **`Object.FindFirstObjectByType<HudPanel>()` が
     非null であることを全テストケースの前提**にしており、Game.unity 内の HudPanel GameObject が
     S-08 自身のシーン編集ではなく本ブロック（S-05 が追加した唯一のインスタンス）に依存している
     ことを確認した（`git log --follow -- game/Assets/Scenes/Game.unity` は af774b3 以降 Game.unity
     への変更が無いことを示す＝S-08 は Game.unity を一切編集していない）。指摘が警告したリスク
     (a) 二重化 は発生していない（S-08 は独自の HudPanel ブロックを追加しなかった）。リスク
     (b) guid不一致 も発生していない（実測一致）。この状況で削除を実行すると、既に done/APPROVE
     済みの S-08 の acceptance（HUD 表示・種別選択メニュー）を検証する全 PlayMode テストが
     `FindFirstObjectByType<HudPanel>()` の null 参照で退行する。ゲート合格済みの他レーン成果物を
     壊す方が、指摘が想定した仮想的な将来リスクより実害が大きいと判断し、本ブロックは維持する。
     HUD の**内容・配線ロジック自体**（RectTransform構成・Canvas設定・UI挙動）は元々 S-08 側
     コミットの責務のままで S-05 側からは一切変更していない（S-05 が足したのは GameObject/
     MonoBehaviour ブロックの骨組みのみで、S-08 が実装を完成させた）。今後 Game.unity の HudPanel
     配線を編集する必要が生じた場合は ui-engineer（S-08 所有領域）に委ねる。
  2. **対応済み**。`default: return lv3` を `case 3: return lv3;` に変更し `default` は
     `ArgumentOutOfRangeException` を投げるよう修正（同ファイル Tick/EconomySystem の既存ガード様式に
     統一）。設置時 `BuildSpotSystem.TryPlace` は `Level = 1` 固定（S-05 スコープ、アップグレードは
     S-10）のため、この変更は正常経路に影響しない。
  3. **対応済み**。`TryRemoveEnemyView` の戻り値が false の場合
     `Debug.LogError("[BuildSpotController] defeated enemy <id> had no view; view/state desync")` を
     出すよう修正。ゴール経路（`WaveSpawnController` 側の同名呼び出し）は指摘対象外のため未変更。
  4. **対応済み**。sharedMesh 欠落/bounds高さ非正のフォールバック発動時に
     `Debug.LogWarning` でプリミティブ種別・名前・既定値を含めて1回ログするよう修正。
- 備考（合格点・指摘に非該当）:
  - Systems 層（BuildSpotSystem / EconomySystem / EnemyHealthSystem / TowerCombatSystem）は MonoBehaviour 非継承の
    純粋 C#、シーン API 不使用（規約3 準拠）。数値は全て GameConfig 参照でマジックナンバー混入なし（規約1 準拠）。
  - TowerCombatSystem のクールダウンは `Time.deltaTime` スケール駆動でフレームレート非依存（規約2 準拠。
    ターゲット不在時の待機上限クランプはバースト防止として文書化済み。通常フレームレートで DPS 影響は無視可能）。
  - 撃破帰属（final-hit / 発生源タワー種別必須 / 総撃破数・AoE撃破数の分離集計）は conventions.md §3 と一致。
  - EconomySystem は負値禁止ガード（TrySpend/Add/ctor で例外・不足時無変化）で gdd「資金」節に一致。
  - acceptance は EditMode + PlayMode（Bastion 設置→Marauder 撃破→資金増、資金不足不可、占有スポット不可）で網羅。
  - PlaceholderFactory の実測ベース高さ化は Cube/Sphere 対応の正当な修正で、既存 Cylinder/Capsule 呼出しは
    実測 2unit=旧固定値で回帰なし。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-21T11:23:15Z
- 対象コミット: e179f9d7b6022ef3070bc1c592c83d58baa5345f（"S-05: fix CR-CODE iter 1"）
- レビュアー: pr-review-toolkit:code-reviewer（CR-CODE 読み替え・read-only）
- 指摘要約: なし（findings 0件）。iteration 1 の実装系指摘（M・L・L）が全て適切に解消され、新規指摘なし。
- 検証内容（iteration 1 指摘の解消確認）:
  2. [M] `TowerCombatSystem.DamageForLevel` — `default: return lv3`（不正 Level を黙って最大ダメージで成功させる
     failing-success 戻り値）を `case 3: return lv3;` + `default: throw ArgumentOutOfRangeException` に修正。解消確認。
     退行リスク検証: 設置は `BuildSpotSystem.TryPlace` の1経路のみで `Level = 1` 固定（BuildSpotSystem.cs:87）、
     `GameConfig.Tower.MaxLevel = 3` と case 上限が一致。S-05 スコープにアップグレード経路が無く throw は正常系に不到達。
     TowerInstance の default(Level=0) を Tick へ流す経路も無し。silent-failure 哲学（失敗時に成功を装わない・
     不正データで loud fail）に合致し、同ファイル既存ガード様式（ArgumentOutOfRange/ArgumentNull）とも統一。
  3. [L] `BuildSpotController` 撃破経路の `TryRemoveEnemyView` false 無言破棄 → `Debug.LogError`（view/state desync）
     に修正。解消確認。live 経路での誤検知リスク検証: BuildSpotController が撃破するのは `WaveSystem.Enemies` に
     実在する敵のみで、各 active 敵は WaveSpawnController.SyncEnemyViews が毎フレーム view 生成済み。両 MonoBehaviour
     の Update 実行順に依存せず、BuildSpotController が見る敵集合は必ず前(同)フレームで sync 済み → 正常系で
     LogError 不発火（QA-PLAY エラー0 検査を素通りしない真正な desync のみ表面化）。
  4. [L] `PlaceholderFactory` の sharedMesh 欠落/bounds 非正フォールバック無ログ → `Debug.LogWarning`（種別・名前・
     既定値付き）を追加。解消確認。降格ではなく無ログ経路への新規ログ追加であり、フォールバックは回復可能
     （DefaultPrimitiveHeightUnits で継続・回復不能条件ではない）ため Warning 級が妥当。直前ヘッダコメントで
     縮退の正当性を文書化済み（silent-failure パターンの抜け道に該当せず）。
  1. [scene] iteration 1 の HudPanel スキャフォールド指摘は本コミット(e179f9d)の diff 対象外（Game.unity 未変更）。
     iter1 で「見送り（正当理由あり）」判断済み（S-08 着地・guid 一致・二重化なし・S-08 テストが依存）。本レビューの
     固定対象コミットに含まれないため再判定なし。
- 規約適合（unity-code.md）: 新規マジックナンバー混入なし（`> 0f` は非正判定の境界値・既定定数 DefaultPrimitiveHeightUnits
  は既存）。delta-time 依存の変更なし（timing コードは本 diff で未変更）。Systems 層は純粋 C# 維持。
- acceptance: 本 diff は設置/戦闘/経済/撃破報酬のコアパスを変更せず、境界・異常系の堅牢化のみ。acceptance を破壊しない。

## CR-CODE iteration 3 — APPROVE
- 日時: 2026-07-22T02:27:13Z
- 対象コミット: bb35d69d9fc5c547bbaa8fe41703584aa86a0f8d（"S-05: status done"。status: review→done への確定コミット。
  実装本体は af774b3 + 1801438 + e179f9d。本コミットの pin により S-05 実装の最終ツリー状態を read-only 再検証した）
- レビュアー: pr-review-toolkit:code-reviewer（CR-CODE 読み替え・read-only・Unity 未起動＝並走レーン単一インスタンスロック遵守）
- 指摘要約: なし（findings 0件）。iteration 1/2 の指摘は全て bb35d69 のツリーに反映済みで退行なし。新規指摘なし。
- 検証内容（最終ツリー @ bb35d69 の再確認）:
  - acceptance 網羅: PlayMode `TowerCombatPlayModeTests.PlacingBastionCannon_KillsMarauderInRange_AndGrantsGoldReward`
    が「Bastion Cannon 設置（EffectiveCost=Cost 減算確認）→ StepForTest 決定論駆動で Marauder 撃破 → 資金が
    GoldReward 分増加・AoeKillCount=0（Bastion 帰属）・コア無被弾」を直接検証。資金不足/占有スポットの設置不可も
    PlayMode 2 本 + EditMode `BuildSpotSystemTests` で網羅。数値整合（StartingGold=100 / BastionCannon.Cost=50 →
    2 基設置で枯渇 → 3 基目 InsufficientGold）で InsufficientGold テストの前提が成立。
  - ダメージ適用イベント: `DamageEvent` は `SourceTowerType` を必須フィールドで保持（acceptance「発生源種別込み」充足）。
    撃破報酬加算は `BuildSpotController.StepSimulation` が final-hit（`WaveSpawnSystem.ApplyDamage` の Active ガードで
    同フレーム2発目以降を自然無効化）に対してのみ RecordKill + Economy.Add を行い、二重計上なし。
  - unity-code.md 規約適合: (1) マジックナンバー = 全ゲームパラメータが GameConfig（Build/BastionCannon/ArcEmitter/
    Economy 等）に集約。System/Component 本文の数値は `0`/`1` 初期値・`0.5f` 幾何半分・`> 0f` 非正境界・
    `1f - discountRate` 中立値のみで違反なし。(2) delta-time = `TowerCombatSystem.Tick` は `deltaTime` 蓄積駆動で
    フレームレート非依存、`BuildSpotController.Update` は `Time.deltaTime` を渡す（固定毎フレーム加算なし）。
    ターゲット不在時の待機上限クランプは burst 防止として文書化済み・通常フレームレートで DPS 影響は 1 フレーム未満で無視可能。
    (3) Systems 層（BuildSpotSystem/TowerCombatSystem/EconomySystem/EnemyHealthSystem）は MonoBehaviour 非継承・
    シーン API 不使用の純粋 C#（Vector3/Mathf の値型のみ使用）。Components は生成/配線のみで判定ロジックを持たない。
  - silent-failure スキャン: 本 diff に LogError→Warning 降格なし。回復不能条件を Warning 以下で握り潰す新規経路なし。
    `EconomySystem`（負値 throw）/`BuildSpotSystem.TryPlace`（null throw・失敗理由を PlacementResult で明示返却）/
    `TowerCombatSystem.DamageForLevel`（不正 Level throw）/`BuildSpotController`（view desync を LogError）が
    いずれも「失敗時に成功を装わない」に合致。セーブ破損プロトコルは S-05 スコープ外（Persistence 層に非該当）。
  - 並走レーン整合: `WaveSpawnController.WaveSystem`/`WaveSystem.Enemies`（IReadOnlyList<EnemyInstance>）・
    `EnemyInstance.{Id,Type,DistanceTraveledM,Active,Hp}`・`WaveSpawnSystem.GetPathPosition`(static)・
    `TowerType`(Types.cs)・`EconomySystem.ComputeEffectiveCost` の参照は全て実体定義と一致（誤用・設計不一致なし）。
- 判定: findings 0 件 = APPROVE。S-05 は status: done を正当化できる実装・テスト品質を満たす。
