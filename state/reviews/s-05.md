# S-05 レビュー履歴 — 敵接近AI + ウェーブスポーン + 難度カーブ

## CR-CODE iteration 1 — APPROVE
- 日時: 2026-07-10T00:00:00Z
- 対象コミット: e1d974a8b2eae1b1fa477d2868d2dc1cbfe306cc
- 指摘要約: ブロッキング指摘なし（findings 0件）。
  - マジックナンバー禁止（unity-code #12/#13）: 準拠。全パラメータは `GameConfig.Wave` / `GameConfig.Enemy` から参照。残る数値リテラルは `Mathf.PI * 2f`（全周＝2π・構造定数）、`CollisionRadius * 2f`（半径→直径変換）、`1e-8f`（ゼロ長判定の数値εps）のみで、いずれもチューニング値ではなく規約の除外対象。
  - delta-time（unity-code #14/#15）: 準拠。`EnemyAgent.Update` / `WaveSpawner.Update` は `Time.deltaTime` でスケール、`EnemyApproachSystem.ComputeStep` は deltaTime 引数を受け取る。フレーム固定加算なし。
  - エンジン非依存コア（unity-code #16）: 準拠。`Systems/WaveSpawnSystem` `Systems/EnemyApproachSystem` は MonoBehaviour 非継承・シーンAPI(Find/Instantiate/GetComponent)不使用・RNG不使用。`Vector3`/`Mathf`/`System.Math` の値・数学型のみ。RNG(spawn角)・Instantiate・タイマー副作用は Components/WaveSpawner に隔離。
  - 数式 vs gdd「ウェーブ・難度カーブ」表: 全行一致を検算で確認（Wave1=1.5s/1/2.5/40, Wave3=1.34s/1/2.70/40, Wave5=1.18s/2/2.92/45, Wave8=0.94s/3/3.29/54, Wave12=0.62s/4/3.85/68）。HP成長開始 `growthSteps = max(0, wave-4+1)` は表の「HP成長はWave4から」および Wave5≈45 を正しく満たす。
  - MAX_CONCURRENT_ENEMIES 上限: `room` をループ前に一度算出し `toSpawn ≤ room` で反復するため、OnEnable が spawn ごとに Count を増やしても上限超過なし（off-by-one なし）。
  - acceptance: 満たす。導出（WaveSpawnSystem.Compute）、スポーンリング配置、接近（EnemyApproachSystem）、上限、全て実装＋テストで検証済み。ゼロ長正規化のNaN回避・オーバーシュートclampも適切。
- 参考（非ブロッキング・対応不要）:
  1. acceptance は「PlayMode テストで導出値が Wave1/Wave5 表と一致」を要求するが、導出値一致は EditMode テスト（WaveSpawnSystemTests, Wave1/3/5/8/12＋min-clamp）で検証している。純粋 Systems の数値は EditMode が適切（conventions.md §9・テストヘッダも同旨）で、PlayMode 化は遅くなるだけで検証価値の増分なし。検証目的（導出値一致・接近・上限）は自動テスト全体で満たされており、配置差は改善であり欠陥ではない。
  2. `WaveSpawner.ApplyPlaceholderColor` は spawn ごとに `new Material(shader)` を生成し、敵破棄時に Destroy されないため将来（敵撃破実装後）にマテリアルリークの芽になる。ただし本 story のスコープでは敵は破棄されず、既存 `SceneWiring.ApplyPlaceholderColor` と同一パターンのプレースホルダで MDL-02 差替時に置換予定。現時点で対応不要。
- 追加指摘（silent-failure-hunter・code-reviewer 合算分）:
  - M-1 [silent-failure/rule12違反] `EnemyAgent.cs:66-69` — `Update()` の `if (PlayerController.Instance == null) return;` がログも文書化も無い毎フレーム無言 return。Player 配線破損時に全敵が原因不明のまま永久静止し `LogAssert.NoUnexpectedReceived()` も通過してしまう。
  - L-1 [silent-failure/構成ミス隠蔽] `WaveSpawner.cs:56-60` — `SpawnOne()` の `agent == null → AddComponent<EnemyAgent>()` が、prefab 割当後でも prefab 側の EnemyAgent 欠落を毎スポーン無言で補ってしまい、misconfiguration が永久に表面化しない。
- 対応:
  - M-1: 対応済み。`EnemyAgent` に `_playerMissingErrorLogged` の once フラグを追加し、`PlayerController.Instance == null` 分岐で `Debug.LogError("[Wiring] EnemyAgent: PlayerController.Instance is null — Game scene wiring broken?")` を1回だけ出すよう修正（既存の `_wiringErrorLogged` ガードと同型）。将来プレイヤー死亡が実装され null が正当状態になった時点で LogWarning に緩和する旨をヘッダコメントに明記した。
  - L-1: 対応済み。`WaveSpawner.SpawnOne()` に `_prefabMissingAgentLogged` の once フラグを追加し、`enemyPrefab != null && agent == null`（＝プレハブ割当済みなのに EnemyAgent が欠けている構成ミス経路）のときのみ `Debug.LogWarning("[Wiring] enemyPrefab lacks EnemyAgent; auto-adding at runtime — fix the prefab.")` を1回出すよう修正。placeholder-cube 経路（`enemyPrefab == null`）は設計どおりの AddComponent なので警告を出さない。
  - 検証: EditMode テスト（`Unity -batchmode -runTests -testPlatform EditMode`）と `ForgeBuild.BuildMac` を再実行し、両方 exit 0・結果 XML で failed 0 を確認（詳細は本コミットの検証ログ）。

## CR-CODE iteration 2 — APPROVE
- 日時: 2026-07-10T08:22:38Z
- 対象コミット: 98165efcbb293a9d1f79fdf268fc5dae2fece283（iteration 1 の M-1/L-1 修正コミット）
- 指摘要約: ブロッキング指摘なし（findings 0件）。iteration 1 の silent-failure 指摘 M-1/L-1 の修正を検証し、いずれも適切に解消されていることを確認した。
  - M-1 解消確認: `EnemyAgent.cs` に per-instance の `_playerMissingErrorLogged` フラグを追加し、`PlayerController.Instance == null` 分岐で `Debug.LogError("[Wiring] EnemyAgent: ...")` を1回だけ出力。既存の `_wiringErrorLogged` と同型の once-flag パターンで毎フレーム無言 return を解消。`PlayerController.Instance` は Awake で set・重複ガード有・OnDisable/OnDestroy で null 化される実在の singleton（PlayerController.cs:19/26/32/71）のため null 判定は有意で、ヘッダコメント（Player root 欠落 / 重複破棄 / SceneWiring 退行）は正確。将来のプレイヤー死亡実装時に LogWarning へ緩和する旨も明記済み。正常配線シーンでは Instance 非 null のため QA-PLAY の `LogAssert.NoUnexpectedReceived()` を誤って落とさない。
  - L-1 解消確認: `WaveSpawner.SpawnOne()` に per-instance の `_prefabMissingAgentLogged` フラグを追加し、`enemyPrefab != null && agent == null`（＝プレハブ割当済みなのに EnemyAgent 欠落＝構成ミス経路）でのみ `Debug.LogWarning(...)` を1回出力。placeholder-cube 経路（`enemyPrefab == null`）は設計どおり無言 AddComponent を維持しており、gate 条件が正しく分岐している。LogWarning のため QA の error アサートには影響しない（意図どおり — auto-add で復旧するため warning が適切）。
  - unity-code 規約: 追加分はすべて純粋なログ出力で、マジックナンバー無し（unity-code #12/#13 準拠）・delta-time 非関連（#14/#15 該当なし）・変更は Components 層のみでエンジン非依存コア（#16）に無影響。
  - acceptance: iteration 1 で確認済みの実装（WaveSpawnSystem 導出 / SPAWN_RADIUS 配置 / MAX_CONCURRENT_ENEMIES 上限 / EnemyApproachSystem 直線接近）は本 diff で無変更・無回帰。EditMode（WaveSpawnSystemTests: Wave1/3/5/8/12＋min-clamp）+ PlayMode（EnemySpawnSceneTests: リング配置・接近・上限）で引き続き充足。
- 対応: 追加対応不要（findings 0件・APPROVE）。

## CR-CODE iteration 2 fix — 追加指摘（code-reviewer + silent-failure-hunter 合算・iteration 2 とは別バッチ）
- 日時: 2026-07-10T17:30:00Z
- 対象コミット: 本コミット（"S-05: fix CR-CODE iter 2"）
- 指摘一覧:
  1. [LOW-MEDIUM / silent-failure] `WaveSpawner.cs:64-68` — prefab-misconfiguration 経路（`enemyPrefab != null` なのに `EnemyAgent` 欠落）の通知が `Debug.LogWarning` のため、QA-PLAY の `LogAssert.NoUnexpectedReceived()`（Error/Exception/Assert のみ失敗扱い）を素通りする。コメント自身が非正当状態（misconfiguration）と明言しており rule 12 の LogWarning 但し書き（縮退が正当なケース）は不適用。
  2. [合格確認] `EnemyAgent.cs:73-81` — M-1 対応は適切。対応不要。
  3. [合格確認] `WaveSpawner.cs` placeholder-cube 経路の無言 AddComponent は正当な設計。対応不要。
- 対応:
  1. 対応済み。`WaveSpawner.SpawnOne()` の `Debug.LogWarning("[Wiring] enemyPrefab lacks EnemyAgent; ...")` を `Debug.LogError(...)` に変更。ヘッダコメントに「これは回復不能/正当縮退ケースではなく配線破損ケースであり rule 12 の LogWarning 許容は適用されない」旨を明記。`enemyPrefab != null` 条件・once フラグ（`_prefabMissingAgentLogged`）・placeholder 経路（`enemyPrefab == null`）の無言 AddComponent は変更なし（当該分岐は S-18 の prefab 割当まで到達不能のため既存動作への回帰リスクなし）。
  2. 対応不要（指摘どおり既存実装で充足）。
  3. 対応不要（指摘どおり既存実装で充足）。
- 検証: EditMode テスト（`Unity -batchmode -runTests -testPlatform EditMode`）を再実行し exit 0・結果XML (`qa/evidence/editmode-results.xml`) で `total=41 passed=41 failed=0` を確認。`ForgeBuild.BuildMac`（`-batchmode -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit`）を再実行し exit 0・ログに `Build succeeded: .../game/Build/ForgeGame.app` を確認。

## CR-CODE iteration 2 fix 検証（re-review）— APPROVE
- 日時: 2026-07-11T03:23:43Z
- 対象コミット: 53b688f97c6fd8d93c81b52c83e2a0977f438e90（"S-05: fix CR-CODE iter 2"）に固定
- 指摘要約: ブロッキング指摘なし（findings 0件）。当コミット diff は `WaveSpawner.cs` の `Debug.LogWarning` → `Debug.LogError` 変更＋説明コメント拡充＋レビュー履歴追記のみ。前バッチの silent-failure 指摘（prefab-misconfiguration 経路が LogWarning で QA-PLAY を素通り）が適切に解消されていることを確認。
  - silent-failure 解消確認: `WaveSpawner.SpawnOne()` の prefab-misconfiguration ガード（`enemyPrefab != null && !_prefabMissingAgentLogged`）が `Debug.LogError(...)` を1回出力するよう変更。これにより QA-PLAY の `LogAssert.NoUnexpectedReceived()`（Error/Exception/Assert のみ失敗扱い）で misconfiguration が確実に露呈する。once フラグ・`enemyPrefab != null` 条件・placeholder 経路（`enemyPrefab == null`）の無言 AddComponent は不変。当該分岐は enemyPrefab 未割当（S-18 まで）で到達不能のため、現行 QA-PLAY で誤って LogError を発火させて落とすリスクは無い。AddComponent による実行時リカバリは維持されるためゲーム進行は止まらず、rule 12（回復可能な縮退は once-warning／配線破損は once-error）の使い分けと整合。
  - unity-code 規約: 変更はログレベルとコメントのみ。マジックナンバー無し（#12/#13）・delta-time 非関連（#14/#15）・Components 層限定でエンジン非依存コア（#16）に無影響。
  - acceptance: 導出ロジック・スポーン・接近・上限は本 diff で無変更。数式を gdd「ウェーブ・難度カーブ」表と再検算し全行一致（SpawnInterval 1.5/1.34/1.18/0.94/0.62・min 0.3 クランプ、Speed 2.5/2.70/2.92/3.29/3.85、HP 40/40/45/54/68・成長 Wave4 起点、Count 1/1/2/3/4）。EditMode（WaveSpawnSystemTests: Wave1/3/5/8/12＋min-clamp＋spawn-ring）＋ PlayMode（EnemySpawnSceneTests: リング配置・直線接近・MAX_CONCURRENT_ENEMIES 上限）で充足。
- 対応: 追加対応不要（findings 0件・APPROVE）。
