# S-28 レビュー履歴（URP Outline 輪郭線 + hero/swarmer マテリアル増強）

## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-14T08:38:43Z
- 指摘要約（優先度順）:
  1. **[major]** `ApplyOutlineToRenderers` が hero の SkinnedMeshRenderer に追加したインバースハル Outline マテリアル（Opaque・ZWrite On・Cull Front）が、既存の死亡フェード（S-16 `Components/HeroFxController`）と衝突。`HeroFxController.ResolveVisual` は先頭スロット（`.material`）のみをフェード対象にしており、Outline スロットは alpha 無関係に常時不透明描画されるため、死亡フェード中に本体が透明化してもシルエット全域を覆う紺色の Outline ハルが実体として残存する。
  2. **[major]** `GameConfig.cs` の S-28 コミットに、無関係な S-22（Camera.PitchDeg/Height/Fov）・S-23（Fx.DashNearMissShake*）のハンクが混入。`state/active.md` の「S-23 系の未コミット変更は S-28 の担当外で一切含めていない」という記載が、`GameConfig.cs` に関しては実際のコミット内容と矛盾していた。
  3. **[minor]** `ApplyRoleColorBlockingAndRimHighlight` の [DEGRADED]（tint parse 失敗・material finish 無効）が `WriteReport` に構造化 key=value 行として出力されておらず、コメント行スクレイピングでしか判別できない。さらに `patched++` が実際にプロパティを設定していないマテリアルでもカウントされていた。
  4. **[minor]** `GameConfig.Outline.WidthMeters` のコメントと `Outline.shader` の Properties 表示名が「object-space」のまま残存（実装はワールド空間押し出しに修正済みだが記述が stale）。
  5. **[minor]** `OutlineSceneTests.Capture_DenseSwarmerCluster_...` の `darkLuminanceThreshold=0.30` が、キーライト影下の床輝度（概算 ≈0.29）と近接しており、輪郭線が壊れていても影パターンで「isolated dark run」シグネチャが偽陽性成立し得る（テストの偽陽性リスク）。

- 対応:
  1. **対応した**（major）。`Components/HeroFxController.cs` の `ResolveVisual` で各 Renderer の `sharedMaterials` 末尾を `GameConfig.Outline.ShaderName`（新設定数、`AssetIntegration.cs` の private const と重複していたシェーダ名文字列を集約）で検出して記録し、`SetOutlineVisible(bool)` を新設。`BeginDeathFx` でフェード開始と同時に Outline スロットを除去（`renderer.materials = [baseInstance]`）、防御的リバートパス（`IsDeathSequenceActive` が false に戻る未到達分岐）で `DisableTransparency()` と対で復元する。回帰テスト
     `HeroFxSceneTests.Death_RemovesOutlineMaterialSlot_SoFadedHeroDoesNotLeaveASolidNavyHullBehind` を追加（死亡フェード中に Outline シェーダのマテリアルが一切残っていないことを機械検証。テスト前提として「Outline スロットが死亡前に実在すること」も検証し空振り合格を防止）。
  2. **一部対応・一部見送り（理由明記）**（major）。`state/active.md` の S-28 エントリに訂正セクションを追記し、`GameConfig.cs` の Camera/Fx.DashNearMissShake* ハンクが S-22/S-23 スコープであり、共有正本 `GameConfig.cs` への並走エージェントの同時編集（git はファイル単位 add のためハンク選択不可）により無自覚に S-28 コミットへ相乗りしたことを明記。**revert は見送った**: 現在の worktree には S-22/S-23 の実装（`Components/ArenaCameraRig.cs`・`Systems/ArenaCameraMath.cs`・`Systems/CameraShakeSystem.cs` 等）がこれらの定数の存在を前提に未コミットで進行中であり、revert すると他ストーリーの並走作業のコンパイルを破壊するため。S-22/S-23 側のコミット時にこれらのハンクの内容自体（値レンジ・命名）を改めて CR-CODE でレビューすることを申し送り事項として記録した。
  3. **対応した**（minor）。`ApplyRoleColorBlockingAndRimHighlight` を `AssignClipResult` を返す形に変更し、`_materialFinishHeroResult`/`_materialFinishSwarmerResult` を `WriteReport` に `material_finish_hero=`/`material_finish_swarmer=` として構造化出力。`patched` は `_BaseColor`/`_Smoothness`/`_SpecColor` のいずれかを実際に設定した場合のみカウントし、0件なら `[DEGRADED]` ログ＋`Failed` を返す。
  4. **対応した**（minor）。`GameConfig.cs` の `Outline.WidthMeters` コメントと `Outline.shader` の `_OutlineWidth` Properties 表示名を「world-space meters」に修正。
  5. **対応した**（minor）。`OutlineSceneTests.CountIsolatedDarkRuns` に blue-dominance 判定（`c.b > c.r && c.b > c.g`）を追加し、暗輝度判定と併用。`GameConfig.Outline.Color`（#164583、青優位）と `GameConfig.Environment.FloorColor`（#7FE850、緑優位）はグレーディング後も channel dominance が保たれる可能性が高く、輝度単独より頑健な判別になる。

- 検証: EditMode 160/160 pass（`qa/evidence/editmode-results-s28-crfix.xml`）、PlayMode 119/119 pass（118 既存 + 新規回帰テスト1件、`qa/evidence/playmode-results-s28-crfix.xml`）、`ForgeGame.EditorTools.ForgeBuild.BuildMac` exit 0（`Build succeeded: .../game/Build/ForgeGame.app`、`game/Logs/build-s28-crfix.log`）。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-14T17:53:00Z
- 指摘要約（優先度順、全て minor）:
  1. **[minor]** `Components/HeroFxController.cs`: `SetOutlineVisible` が復元時に固定2要素配列 `[_materials[i], outline]`（非表示時は `[_materials[i]]`）を書き戻しており、`_materials[i]` は `ResolveVisual` の `renderer.material` 取得（スロット0のみ）に由来するため、ベースマテリアルが複数サブメッシュスロットを持つ Renderer では `SetOutlineVisible` の復元でスロット1..N-1 が黙って脱落する（現行の hero/swarmer 資産は単一ベースマテリアルのため未発火・実害なし）。
  2. **[minor]** `Assets/Tests/PlayMode/OutlineSceneTests.cs`: `GameConfig.Outline.ShaderName` へ集約したはずの `"ForgeGame/Outline"` 文字列が、本ファイルにローカル `const string OutlineShaderName` として3つ目の重複コピーのまま残存（Editor/AssetIntegration・Components/HeroFxController は既に集約済み）。
  3. **[minor]** `Components/HeroFxController.cs`: 上記1と表裏一体の設計非対称。検出側 `ResolveVisual` は `sharedMaterials.Length > 1` なら任意スロット数を受理する一方、復元側 `SetOutlineVisible` は固定2スロット構成しか書き戻せない。将来 hero FBX がマルチサブメッシュ化した場合の潜在的サイレント破損（ただし新設回帰テスト `Death_RemovesOutlineMaterialSlot_...` が全スロット走査のため検出漏れそのものはパイプライン上黙殺されない）。

- 対応:
  1. **対応した**（minor）。`Components/HeroFxController.cs` の `ResolveVisual` を変更し、各 Renderer の outline スロット検出直後に `renderer.materials`（全スロットを個別インスタンス化）を取得して `outlineIndex` 手前の全スロットを `_baseMaterialSlots[i]`（新設 `Material[][]` フィールド）へ保持するようにした。`SetOutlineVisible` は `_baseMaterialSlots[i]` + outline の再構成（表示時）/ `_baseMaterialSlots[i]` そのもの（非表示時）を書き戻す形へ変更し、スロット0のみの固定2要素配列を廃止。既存のフェード/フラッシュ演算（`ApplyAlpha`/`ApplyFlash`/`ApplyBaseColors`/`EnableTransparency`/`DisableTransparency`）は従来どおり `_materials[i]`（= `_baseMaterialSlots[i][0]`）のみを対象にする既存仕様を維持（この finding の対象外）。
  2. **対応した**（minor）。`OutlineSceneTests.cs` のローカル `const string OutlineShaderName = "ForgeGame/Outline";` を `private static readonly string OutlineShaderName = GameConfig.Outline.ShaderName;` に変更し、3つ目の重複コピーを解消。
  3. **対応した**（minor、1と同一修正で解消）。`SetOutlineVisible` が `_baseMaterialSlots[i].Length` に応じた可変長スロット構成を書き戻すようになったため、`ResolveVisual` の任意スロット数受理と `SetOutlineVisible` の復元が対称になった。指摘に付随した「`ResolveVisual` で想定外構成を LogError で顕在化」という代替案は、想定外構成そのものが解消されたため実装不要と判断（見送り・理由: 一般化した N スロット処理には「想定外の構成」という分岐がもはや存在しない）。

- 検証: EditMode 160/160 pass（`qa/evidence/editmode-results-s28-iter2.xml`）、PlayMode 119/119 pass（`qa/evidence/playmode-results-s28-iter2.xml`）、`ForgeGame.EditorTools.ForgeBuild.BuildMac` exit 0（`Build succeeded: .../game/Build/ForgeGame.app`、`game/Logs/build-s28-iter2.log`）。MAX_ITER(2) 到達のため review-loops.md に従い status: done へ更新。未対応 finding は無し（全3件 対応済み）。
