## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-13T15:00:00Z
- 指摘要約（優先度順）:
  1. [major] `game/Assets/Scripts/Components/HeroFxController.cs` `EnableTransparency()` の「`_Surface` も `_Mode` も無い未知シェーダー」分岐（コメントのみのパス）が完全に無音で握り潰されており、この分岐に入ると死亡フェードが alpha 値だけ変わり見た目は不透明のまま描画される。同一欠陥クラス（`ArenaEnvironment`/`EnemyAgent` の「両シェーダー欠落」パス）は S-14 iter1 / S-20 iter1 の CR-CODE で `Debug.LogError` へ昇格済みであり、確立済みパターンとの不整合。
  2. [major] `BeginDeathFx()` は「`SetFloat(Speed,0)` で既存 AnimatorController を Idle に落ち着かせてフェードする」設計だが、`AutoAttackDriver.Update()` は死亡シーケンス中も無条件に動き続け、射程内に敵がいれば `SetTrigger(Attack)` を発火してしまう（AnyState→Attack 遷移で Idle から引き戻される）。死亡時は接触してきた敵が必ず射程内にいるため、フェード中に攻撃モーション/SFX/撃破が再生され演出が破綻する。
  3. [minor] `ApplyFlash()` がフラッシュ中の毎フレーム `ColorUtility.TryParseHtmlString` で `GameConfig.Ui.ColorHitFlash` を再パースしており、不変値の無駄な再計算に加え、パース失敗時は毎フレーム `Debug.LogError` がスパムする構造だった（`Ui/GameHud.ParseColor` の構築時1回パースと不整合）。
  4. [minor] `GameHudController.UpdateDeathDissolve` は「`IsDeathSequenceActive` がシーン再利用で false に戻るケース」への防御リセットを実装しているが、対で設計された `HeroFxController` 側には同等の防御が無く、非対称だった。
  5. [minor] `HeroFxSceneTests` の非致死ヒットテストが `Assert.AreNotEqual(baseColor, ...)` のみで「どの色へ変化したか」を検証しておらず、誤った色定数への回帰でも通ってしまう。
  6. [minor] 死亡テストの中間サンプル（`WaitForSecondsRealtime(DeathSequenceDuration*0.4f)` 後の厳密開区間 assert）が batchmode のフレームヒッチに脆弱（合計 ~0.3s 超のヒッチで演出 0.5s がサンプル前に完了し fail しうる）。
- 対応:
  1. 対応した。`EnableTransparency()` の else 分岐に `Debug.LogError("[Wiring] HeroFxController: hero material '...' has neither URP/Lit (_Surface) nor legacy Standard (_Mode) shader properties — death fade alpha will not be visually transparent.")` を追加し、`ArenaEnvironment.CreateUrpSafeMaterial` / `EnemyAgent.CreateFallbackMaterial` と同型の「両シェーダー欠落＝配線エラー」パターンに揃えた。現行 hero マテリアルは URP/Lit（`_Surface` あり）のためこの分岐には入らず、PlayMode テストへの影響なし（`playmode-results.xml` で確認済み）。
  2. 対応した。`AutoAttackDriver.Update()` の冒頭に `if (PlayerController.Instance != null && PlayerController.Instance.IsLocked) return;` を追加し、`HealthComponent.BeginDeathSequence` が `PlayerController.LockInput()` で立てる既存の入力ロックフラグ（`PlayerController.Update()` 自身が同じ条件で早期 return している）を自動攻撃側にも適用した。新しい状態やSystems層の変更は無く、既存の公開プロパティを参照する1行ゲートのみ。
  3. 対応した。`HeroFxController` に `_flashColor` フィールドを追加し `ResolveVisual()` で1回だけ `ParseColorOrFallback` を呼んでキャッシュ、`ApplyFlash()` は都度パースせず `_flashColor` を参照するよう変更（`Ui/GameHud.ParseColor` と同じ構築時1回パース方針に統一）。
  4. 対応した。`HeroFxController.Update()` に `GameHudController.UpdateDeathDissolve` と対称な防御リセット（`_deathActive` が true のまま `IsDeathSequenceActive` が false に戻った場合に `_deathActive`/`_deathElapsed`/`DeathFadeAlpha`/`DeathTiltDeg` をリセットしベース色・チルト0へ戻す）を追加した。
  5. 対応した。`HeroFxSceneTests.NonLethalHit_...` に、`Systems/HeroFxSystem.ComputeHitFlashColor(baseColor, ColorHitFlash, heroFx.HitFlashIntensity)` で期待色を再計算し `heroFx.CurrentMaterialColor` と（新設ヘルパー `AssertColorApproximatelyEqual`、成分毎許容誤差0.01）で突き合わせる assert を追加した。
  6. 対応した。死亡テストの中間サンプルブロックを、`SceneManager.GetActiveScene().name == GameConfig.Scenes.Game` でシーケンス継続中かどうかをまずガードし（ヒッチで既に Result 遷移済み・オブジェクト破棄済みの場合は丸ごとスキップ＝末尾の完了系 assert が代わりに検証する）、継続中の場合も厳密開区間ではなく「範囲外に出ていない」＋「少なくとも1チャンネルが進行している」という緩い条件に変更した。
- 検証結果:
  - EditMode（typecheck相当）: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results.xml -logFile qa/evidence/editmode.log` → exit 0 / 結果XML `total="135" passed="135" failed="0"`
  - build相当: `"$UNITY" -batchmode -projectPath game -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit -logFile game/Logs/build.log` → exit 0 / ログに `Build succeeded: .../game/Build/ForgeGame.app` を確認
  - PlayMode（今回の主変更対象。QA相当だが影響範囲確認のため追加実行）: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform PlayMode -testResults qa/evidence/playmode-results.xml -logFile qa/evidence/playmode.log` → exit 0 / 結果XML `total="93" passed="93" failed="0"`（`HeroFxSceneTests` 2件・`AutoAttackSceneTests` 2件を含め全件Passed）
- state/stories.yaml の S-16 は status: review のまま据え置き（次iterationで再レビュー）。未対応finding: 無し（6件全て対応済み）。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-13T06:00:00Z
- 指摘要約（優先度順、全て minor）:
  1. `game/Assets/Tests/PlayMode/HeroFxSceneTests.cs`（死亡テスト中間サンプル、L194-206）: iter1 minor #6 の de-flake が過剰修正で、閉区間 bound のみになり「即座に終端値へスナップする回帰」を検出できなくなっていた（コメントが主張する gradualness 検証が実質失われていた）。
  2. `game/Assets/Scripts/Components/AutoAttackDriver.cs`（L45 の `IsLocked` ゲート）: iter1 major #2 の修正にカバーするテストが無く、将来の回帰を検知できない。
  3. `game/Assets/Tests/PlayMode/HeroFxSceneTests.cs`（L118）: `ColorUtility.TryParseHtmlString` の戻り値 bool を黙殺しており、パース失敗時に真因が assert 失敗メッセージに現れない。
  4. `game/Assets/Tests/PlayMode/HeroFxSceneTests.cs`（L181-207）: 指摘1と同一問題の詳細版（コメント文言と実装の不一致含む）。
  5. `game/Assets/Scripts/Components/HeroFxController.cs`（L130-145 の防御リセット）: `EnableTransparency` の片道副作用（サーフェスモード切替）を戻しておらず、`_previousHp`/`_hpTracked` も据え置きで偽フラッシュのリスクがあり、到達不能パスへの到達を無言で正規化していた。
  6. `game/Assets/Scripts/Components/AutoAttackDriver.cs`（L45 ゲートの残余窓）: 致死フレームの実行順次第で `Attack` トリガーが未消費のままラッチされ、フェード開始直後に攻撃モーションが被る1フレームの残余窓がある。
- 対応:
  1. 対応した。`HeroFxSceneTests.Death_...` の中間サンプルブロックに、`deathStartRealtime` からの実測経過時間（`elapsedSinceDeathStart`）による判定を追加し、`elapsedSinceDeathStart < DeathSequenceDuration - 0.05f`（＝ヒッチ無しでこのサンプルが確実にシーケンス中間に着地したと証明できる場合）でのみ厳密開区間 assert（`Assert.Less(DeathFadeAlpha, 1f)` / `Assert.Greater(DeathTiltDeg, 0f)`）を復活させた。それ以外（ヒッチで既に完了寸前/完了）は iter1 の緩い「いずれかのチャンネルが進行済み」チェックにフォールバックする。コメントも実態（測定経過時間ベースの分岐）に合わせて書き直した。`HeroFxSystem.ComputeDeathFadeAlpha`/`ComputeDeathTiltDeg` は `Clamp01` ベースで progress=0.4 時に alpha=0.6・tilt=0.4*max と決定的なため、この閾値判定は健全。
  2. 対応した。`AutoAttackSceneTests.cs` に新規テスト `DyingHero_DoesNotFireAutoAttack_DuringDeathFadeSequence` を追加。`AutoAttackDriver` に test-only hook（`AttackIntervalOverrideForTests` static / `AttackCallCountForTests` インスタンスプロパティ。既存の `HealthComponent.SaveDirectoryOverrideForTests` / `WaveSpawner.SpawnCallCountForTests` パターンを踏襲）を追加し、`AUTO_ATTACK_INTERVAL`(0.6s) が `DeathSequenceDuration`(0.5s) より長いため素の死亡ウィンドウでは1tickも自然発生しない問題を、インターバルを意図的に短縮することで「ゲートが無ければ複数tick発生するはず」という状況を作って検証する形にした。テストは死亡シーケンス開始確認後のフレームからカウンタが変化しないことを、シーン滞在中の全フレームで assert し、サンプル数0（vacuous pass）にならないことも別途 assert する。
  3. 対応した。`HeroFxSceneTests.cs` L118 を `Assert.IsTrue(ColorUtility.TryParseHtmlString(...), "GameConfig.Ui.ColorHitFlash must be a parseable hex color")` でラップし、パース失敗を明示的に表面化させた。
  4. 対応した（1と同一修正）。
  5. 対応した。`HeroFxController.cs` に `DisableTransparency()`（`EnableTransparency` の対称メソッド。`_Surface`/`_Mode` 双方のブランチでサーフェスモード・ブレンド設定・render queue を Opaque へ戻す）を追加し、防御リセットブロックから呼び出した。あわせて `_hpTracked = false` を追加して次フレームで HP を再ベースライン化し（復帰後の偽フラッシュ防止）、到達時に一度だけ `Debug.LogWarning`（`_deathReverseWarningLogged` フラグでワンショット化）を出すようにした。
  6. 対応した。`HeroFxController.BeginDeathFx()` の `SetFloat(Speed,0)` 直後に `_animator.ResetTrigger(GameConfig.Animation.AttackTrigger)` を追加し、同一フレームの実行順レースで未消費のままラッチされた `Attack` トリガーを無効化するようにした（`AutoAttackDriver.Update` が `HeroFxController.Update` より先に走った場合の残余窓を縮小。指摘コメント通り、`HeroFxController` の観測がさらに1フレーム遅れる実行順では救えない点は許容し、コードコメントに明記した）。
- 検証結果:
  - EditMode（typecheck相当）: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform EditMode -testResults qa/evidence/editmode-results.xml -logFile qa/evidence/editmode.log` → exit 0 / 結果XML `total="135" passed="135" failed="0"`
  - build相当: `"$UNITY" -batchmode -projectPath game -executeMethod ForgeGame.EditorTools.ForgeBuild.BuildMac -quit -logFile game/Logs/build.log` → exit 0 / ログに `Build succeeded: .../game/Build/ForgeGame.app` を確認
  - PlayMode（今回の主変更対象。QA相当だが影響範囲確認のため追加実行）: `"$UNITY" -batchmode -projectPath game -runTests -testPlatform PlayMode -testResults qa/evidence/playmode-results.xml -logFile qa/evidence/playmode.log` → exit 0 / 結果XML `total="96" passed="96" failed="0"`（新規 `AutoAttackSceneTests.DyingHero_DoesNotFireAutoAttack_DuringDeathFadeSequence` および `HeroFxSceneTests` 2件を含め全件Passed）
- MAX_ITER(2) 到達のため state/stories.yaml の S-16 を `status: done` に更新。未対応finding: 無し（6件全て対応済み）。
