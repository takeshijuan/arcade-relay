## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-22T08:24:39Z
- 指摘要約（優先度順）:
  1. [major] `FileAudioSettingsStore.Load()` が「パース可能だが不正」（`{}`・必須フィールド欠落・切詰め）を検知できず、`JsonUtility` の寛容パース既知挙動により Warning すら出さず BgmVolume=SfxVolume=0（毎起動ミュート）を黙って採用してしまう。
  2. [minor] `MenuScreen.cs` のスライダー `onValueChanged` 毎に同期ファイル書込（デバウンス無し）でドラッグ中にヒッチが発生し得る。
  3. [minor] 本番 File I/O 経路（クランプ・アトミック書込・破損フォールバック）の専用テストが無く、`FileSaveStore`（S-07）と非対称。
  4. [minor] `FileSaveStore` が持つ「一過性 I/O エラー1回リトライ」が `FileAudioSettingsStore.Load()` に無く、同型のデータ消失経路が成立する。
  5. [minor] `InMemoryAudioSettingsStore.Save(null)` が `ArgumentNullException` ではなく `NullReferenceException` になり、本番実装（`FileAudioSettingsStore`）と契約が不一致。
  6. [minor] 新規4スクリプトの `.meta` ファイルが未コミット（同ディレクトリの既存ファイルは全て `.meta` を持つ）。
- 対応:
  1. 対応済み。`FileAudioSettingsStore.cs` に `LooksLikeJsonObject`（`FileSaveStore.cs` と同型の軽量構造チェック。意図的な複製 — 他ストーリー担当ファイルへの非追記編集を避けるため）+ `HasRequiredFields`（`"BgmVolume"`/`"SfxVolume"` キー実在チェック）を追加し、`{}` やフィールド欠落を Warning 1回 + 既定値へフォールバックするよう修正。再帰防止の PlayMode テスト（`Load_EmptyJsonObject_...` / `Load_MissingSfxVolumeField_...`）を追加。
  2. 見送り。本プロジェクトには `EventSystem` が未導入（`HudPanel`/`TowerSelectPanel`/`MenuScreen` 冒頭コメントの共通方針: クリック判定は `InputReader` + `RectTransformUtility` の矩形ヒットテストで行い uGUI `EventSystem` を使わない）ため、実際のプレイヤー操作でスライダーがドラッグイベントを発生させる経路が現状存在しない（`Slider.onValueChanged` は現状プログラム的な値設定 — テスト・将来の他入力経路 — でのみ発火する）。デバウンスを安全に導入するには「ドラッグ終了/ポインタ解放」を検知する仕組み（EventSystem 導入や Update() での時間ベースフラッシュ）が必要で、これは本 story のレーン境界（ui-engineer は Unity を起動できない並走レーン中）で Unity 実行検証なしに実装すると、既存の S-16 acceptance PlayMode テスト（`ChangeBgmVolumeSlider_UpdatesAudioSourceVolume_AndPersistsAcrossReload` / `ChangeSfxVolumeSlider_...`。1フレーム後に同期的な永続化を assert）を壊す高いリグレッションリスクがある。技術的負債として明記し、EventSystem 導入・実ドラッグ操作対応のフォローアップ story で対応することを推奨する。
  3. 対応済み。`FileAudioSettingsStorePlayModeTests.cs` を新規追加（`FileSaveStorePlayModeTests.cs` と同型構成）: 初回起動時既定値、アトミック書込（.tmp残留無し）、保存→新規インスタンス再ロードの復元一致、範囲外値のクランプ、パース不能/`{}`/フィールド欠落の Warning+既定値フォールバック、`Save(null)` の `ArgumentNullException` を検証。
  4. 対応済み。`FileSaveStore.cs` と同型の「読込1回リトライ + `[AudioSettingsReadRetry]` Warning」を `Load()` に追加。
  5. 対応済み。`InMemoryAudioSettingsStore.Save()` に `data == null` の明示 `ArgumentNullException` ガードを追加し、`FileAudioSettingsStore.Save()` と契約を揃えた。
  6. 対応済み。`AudioSettingsStores.cs.meta` / `FileAudioSettingsStore.cs.meta` / `IAudioSettingsStore.cs.meta` / `InMemoryAudioSettingsStore.cs.meta` を既存ファイルと同じ最小形式（`fileFormatVersion: 2` + 一意 `guid`）で追加。新規テストファイルの `.meta` も同様に追加。
- 検証: Unity 未起動（並走レーン規律 — レーン合流後のバッチ検証区間へ委譲）。代わりに Read/Grep で以下を静的確認: `AudioSettingsData`（`Types.cs`）のフィールド名（`BgmVolume`/`SfxVolume`）と `HasRequiredFields` のキー文字列一致、`GameConfig.Ui.MenuDefaultVolume`/`GameConfig.Save.AudioSettingsFileName` の実在、`JsonUtility.ToJson` が public フィールド名をそのままキーにする既存 `FileSaveStore` 実装との整合、新規テストファイルの `using` 対象型（`ForgeGame.Persistence.FileAudioSettingsStore` 等）の実在。コンパイル整合はレーン合流後のバッチ検証（`tech-stack-unity.md`）が最終確認する。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-22T08:40:18Z
- 指摘要約（優先度順）:
  1. [major] `MenuScreen.cs` の音量スライダーが実プレイヤーの入力経路を持たないデッドUIだった。本プロジェクトは `EventSystem` 未導入で、`MenuScreen.Update()` の矩形ヒットテストは `playButton`/`backToTitleButton`/UPG購入ボタンのみを対象とし、`bgmVolumeSlider`/`sfxVolumeSlider` へのクリック・ドラッグ処理が無い。uGUI `Slider` は `EventSystem` 無しではポインタイベントを一切受け取れないため、実機セッションで音量を変更する手段がゼロだった。
  2. [minor] `FileAudioSettingsStore.HasRequiredFields` はキー実在のみを見て値の型を検証しないため、手動改変で数値をクォートで括った入力（`{"BgmVolume":"1.0","SfxVolume":"0.5"}`）が両プリチェックを通過し、`JsonUtility.FromJson` が型不一致フィールドを例外無く既定値0（ミュート）のまま採用してしまう残存経路があった。
  3. [minor] `Load()` の `JsonUtility.FromJson` 例外 catch 経路（構文エラーで `FromJson` 自体が例外を投げるケース）が新規テストでも一度も実行されていなかった（既存の不正入力フィクスチャは全てプリチェック段で弾かれる）。
  4. [minor] 同一 diff 内で iteration 1 指摘番号とコードコメントの相互参照が矛盾していた（`FileAudioSettingsStore.cs`/`InMemoryAudioSettingsStore.cs`/`FileAudioSettingsStorePlayModeTests.cs` のコメント番号が `state/reviews/s-16.md` の番号付けと不一致）。
- 対応:
  1. 対応済み。`EventSystem` を導入せず既存の「`InputReader` + `RectTransformUtility` 矩形ヒットテスト」パターンをスライダーへ拡張した: `InputReader` に `ClickHeld`（押下継続。既存 `WasPressedThisFrame` エッジ検知とは別の継続検知。既存行は変更せず追記のみ）を新規追加し、`MenuScreen` に `HandleVolumeSliderPointerInput`/`ApplyPointerToSlider`（押下開始フレームでスライダー矩形上ならそのスライダーを握り、押下継続中はポインタのローカルX位置から `slider.value` を直接設定。押下解除で解放）を追加。`MenuScreenPlayModeTests.ClickNearRightEdgeOfBgmVolumeSlider_ViaPointerInput_RaisesVolumeAndPersists` を新規追加し、実マウス入力擬似発行（Move+Press+Release）でスライダーを操作→`AudioSource.volume`反映→Persistence経由保存までを検証する（従来の2テストは `slider.value = x` の直接代入のみで実入力経路を検証していなかった）。
  2. 対応済み。`FileAudioSettingsStore.HasNumericFieldTypes`（`"BgmVolume"`/`"SfxVolume"` の直後がクォート開始でないことを確認する軽量Regexチェック。既存チェックを緩めない・厳格化のみの追加）を追加し `Load()` の不正判定に組み込んだ。再帰防止テスト `Load_QuotedNumericFieldValue_TreatedAsInvalid_LogsWarningAndReturnsDefault` を追加。
  3. 対応済み。全プリチェックを通過するが構文的に不正な末尾カンマ入力（`{"BgmVolume":0.5,"SfxVolume":0.5,}`）を与える `Load_TrailingCommaSyntaxError_ThrowsDuringParse_LogsWarningAndReturnsDefault` を追加し、`FromJson` の catch 経路を実行させる。**留意**: Unity 未起動の並走レーン中の追加のため、`JsonUtility.FromJson` が末尾カンマで実際に例外を投げるという前提は静的確認（Read/Grep）では検証不能。バッチ検証区間で本テストが赤くなった場合は追加修正が必要（既知の未確定事項として記録）。
  4. 対応済み。`FileAudioSettingsStore.cs`（iter1 #1=JSON pretend-valid major / iter1 #4=リトライminor）、`InMemoryAudioSettingsStore.cs`（iter1 #5=null契約minor）、`FileAudioSettingsStorePlayModeTests.cs`（iter1 #3=テスト非対称minor、および `{}`テストの `#3`→`#1` 誤記）のコメント番号を `state/reviews/s-16.md` の iteration 1 記載順（1=パース可能だが不正/major, 2=デバウンス/minor, 3=テスト非対称/minor, 4=リトライ/minor, 5=null契約/minor, 6=.meta/minor）に揃えた。なお `MenuScreen.cs` 内の「CR-CODE iter1 #1/#3」等は S-15 側の別レビュー（`state/reviews/s-15.md`）参照であり本指摘の対象外のため変更していない。
- 検証: Unity 未起動（並走レーン規律。エンジン検証はレーン合流後のバッチ検証区間へ委譲）。代わりに Read/Grep で以下を静的確認: `InputAction.IsPressed()` が Input System 1.19.0 パッケージ（`Assets/../Library/PackageCache/com.unity.inputsystem@21a28c3a6c83/InputSystem/Actions/InputAction.cs:1178`）の public instance メソッドとして実在すること、`MenuScreen.cs` 新規メソッドが参照する `bgmVolumeSlider`/`sfxVolumeSlider`/`uiCamera`/`inputReader` フィールドの実在、`AudioSettingsData`（`Types.cs`）のフィールド名一致、新規テストの `using` 対象型（`ForgeGame.Persistence.InMemoryAudioSettingsStore` 等）の実在。新規追加した `Load_TrailingCommaSyntaxError_...` テストの前提（`JsonUtility.FromJson` が末尾カンマで例外を投げること）は Unity 未起動のため実行検証できておらず、バッチ検証での確認が必要（上記対応3の留意点）。コンパイル整合はレーン合流後のバッチ検証（`tech-stack-unity.md`）が最終確認する。
- MAX_ITER(2) 到達のため本 story は status: done へ更新する（未対応finding無し。残る不確定事項は対応3の留意点のみ — バッチ検証・Checkpoint で人間へ提示）。
