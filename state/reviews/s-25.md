## CR-CODE iteration 1 — CONCERNS
- 日時: 2026-07-22T18:22:42Z
- 指摘要約（優先度順・いずれも minor）:
  1. `game/Assets/Tests/PlayMode/TowerCombatPlayModeTests.cs`: `DefeatedEnemyView_PlaysScaleDownMotion_BeforeBeingDestroyed` の破棄待ち予算 `maxWaitFramesForDestroy=60` が、同ファイル内 S-24 テストの同種アニメーション待ち予算（`maxWaitFrames=300`、対象時間 0.18s）に対して薄く、batchmode の実フレーム `Time.deltaTime` 次第で意図せぬ間欠的失敗を起こし得る。
  2. `game/Assets/Scripts/Components/EnemyView.cs`: `PlayDefeatMotion()`（public API）に再入ガードが無く、演出中に再度呼ばれると `defeatElapsedSec` がリセットされ演出が延長される（想定外状態の黙示受容）。
  3. `game/Assets/Tests/PlayMode/TowerCombatPlayModeTests.cs`: 同テストのスケール変化観測ループが、`EnemyView.Update()` の「スケール書き換え→Destroy」を同一フレーム内で行う実装特性により、ヒッチで中間状態を観測できないまま破棄されると誤誘導的なメッセージで false-fail する。
- 対応:
  1. 対応済み。`maxWaitFramesForDestroy` を S-24 と同じ `300` に引き上げ、根拠をコメントに明記（`TowerCombatPlayModeTests.cs` L165-169）。
  2. 対応済み。`EnemyView.PlayDefeatMotion()` に `if (IsPlayingDefeatMotion) return;` の再入ガードを追加（`EnemyView.cs` L78-84）。
  3. 対応済み。スケール変化観測ループを「観測前に破棄された場合はスケール変化済みとみなす」ロジックに変更し、`scaleChanged || destroyedBeforeScaleObserved` で判定するよう修正（`TowerCombatPlayModeTests.cs` L140-163）。
- 検証: Unity は起動していない（単一インスタンスロック・並走レーン規律のため、レーン合流後のバッチ検証区間に委ねる）。代わりに Read/Grep で以下を静的確認: `EnemyView.IsPlayingDefeatMotion`（public bool プロパティ、既存宣言のまま参照のみ）・`GameConfig.Presentation.EnemyDefeatShrinkDurationSec`・`GameConfig.Presentation.TowerFireRecoilDurationSec` の実在、シンボル参照に構文・型の不整合が無いこと。コンパイルを通らない参照は残していない。

## CR-CODE iteration 2 — CONCERNS
- 日時: 2026-07-22T19:08:42Z
- レビュー対象の特定: workflow から渡された対象が literal `COMMIT_FAILED`（コミット SHA 解決失敗。S-24 iter1 と同型）。
  git 履歴に S-25 コミットは存在せず、S-25 実装は未コミットの作業ツリー変更としてのみ存在
  （EnemyView.cs 全変更 / WaveSpawnController.cs の TryStartEnemyDefeatMotion 追加＋TryRemoveEnemyView doc 更新 /
  BuildSpotController.cs の撃破解決ブロック差し替え hunk のみ / GameConfig.cs の Presentation
  EnemyDefeatShrinkDurationSec 追加 hunk のみ / TowerCombatPlayModeTests.cs の
  DefeatedEnemyView_PlaysScaleDownMotion_BeforeBeingDestroyed 追加）。同一作業ツリーに S-14 iter2・S-22・
  S-23・S-24 の hunk が混在しており、これらは S-25 のレビュー対象から除外して判定した。
- iteration 1 指摘の対応検証（3件とも実装上で確認済み）:
  1. 破棄待ち予算 60→300 フレーム: TowerCombatPlayModeTests.cs の `maxWaitFramesForDestroy = 300`＋根拠コメント実在 — 対応確認
  2. `PlayDefeatMotion()` 再入ガード: `if (IsPlayingDefeatMotion) return;`＋XML doc で正規経路 one-shot と再入時挙動を文書化 — 対応確認
  3. スケール変化観測の false-fail: `scaleChanged || destroyedBeforeScaleObserved` フォールバック実装 — 対応確認
- 指摘要約（優先度順）:
  1. **[major] レビュー対象コミット不在（プロセス完全性・S-22 iter2 / S-24 iter1 と同型の再発）** — stories.yaml
     status=review だが S-25 実装が未コミットで、対象 diff に安定した同一性が無い。並走レーンのコミット操作で
     S-25 hunk が混入・消失するリスク（同一ファイル BuildSpotController.cs / GameConfig.cs に他 story hunk の
     混在を実観測）。外部要因（1Password 署名エージェント障害・stories.yaml note に約15回試行失敗と記録済み）
     によるものでコード内容の欠陥ではないが、署名エージェント復旧後のレーン合流バッチで S-25 単独（または
     混在 story を明示した合流）コミットとして確定させること。
- コード内容の新規 findings: 0件。静的確認（Read/Grep。Unity 起動はレーン規律で禁止）で以下を検証済み:
  - acceptance 充足: 撃破時の同フレーム Destroy 廃止（TryStartEnemyDefeatMotion → EnemyView が
    EnemyDefeatShrinkDurationSec=0.2s・調整レンジ 0.1〜0.3 明記の新設定数でスケールダウン後に自壊）/
    ゴール到達は gdd どおり即時破棄 TryRemoveEnemyView を維持 / SFX-03 は従来位置・タイミング
    （hitPosition は辞書除去前の L120 で取得済み）/ Systems 層（ApplyDamage・EnemyHealthSystem・
    EconomySystem）無変更 / PlayMode テストが「同フレーム非消滅・視覚変化・その後破棄」の3点を検証
  - ゴースト View 非再生成: ApplyDamage が撃破と同期で Active=false → SyncEnemyViews は Active のみ生成対象
  - desync 検知契約維持: TryStartEnemyDefeatMotion false 時の Debug.LogError（S-05 iter1 契約と同一）
  - ポーズ整合: PausePanel は Time.timeScale=0 方式 → EnemyView.Update の Time.deltaTime 駆動演出は正しく停止
  - 勝利遷移との競合無し: WinResultDelaySec(1s) > 演出 0.2s のため最終撃破の演出はクリップされない
  - rules/unity-code.md: マジックナンバー無し（-1f はセンチネル・コメント付き）/ delta-time 駆動 /
    Components 薄い・Systems 非汚染 / 黙殺 catch・LogError 降格・空 catch 無し
- 対応: 対応済み。指摘は「S-25 コード内容の欠陥」ではなく「レビュー対象の同一性（未コミット）」のプロセス指摘のため、
  修正 = S-25 のみを安定コミットとして確定させることで対応した。作業ツリーには GameConfig.cs / BuildSpotController.cs /
  TowerCombatPlayModeTests.cs の3ファイルに他 story（S-14 iter2 / S-22 iter2 / S-24）の hunk が混在していたため、
  ファイル単位ではなく **hunk 単位**で S-25 分のみを index に取り込んだ:
  - `EnemyView.cs`（既に index が S-25 のみで単独 stage 済みだったためそのまま採用）
  - `WaveSpawnController.cs`（unstaged 差分が S-25 のみだったためファイル単位で stage）
  - `GameConfig.cs`: unstaged 差分中の `EnemyDefeatShrinkDurationSec` ブロックのみを `git apply --check --cached` で
    検証済みの部分パッチとして stage。`RadiusM`（S-22 iter2 是正）と `TowerFireRecoilDurationSec`/`TowerFireRecoilScalePunch`
    （S-24）は unstaged のまま残し、当該 story のレーンに委ねた
  - `BuildSpotController.cs`: unstaged 差分中の `TryRemoveEnemyView`→`TryStartEnemyDefeatMotion` 置換 hunk のみを
    同様に部分パッチで stage。`UsedDefaultSaveFallback`（S-14 iter2）と `PlayFireMotion` トリガー追加（S-24）の
    2 hunk は unstaged のまま残した
  - `TowerCombatPlayModeTests.cs`: 一旦 `git reset HEAD --` で index を HEAD に戻し、
    `DefeatedEnemyView_PlaysScaleDownMotion_BeforeBeingDestroyed`（S-25）の hunk のみを部分パッチで再 stage。
    `TowerFires_RotatesTowardTargetAndPlaysRecoilScalePunch_ThenReturnsToDefault`（S-24）の hunk は unstaged のまま残した
  各部分パッチは `git apply --check --cached` で事前検証してから適用し、適用後に `git diff --staged` で
  S-25 以外の内容が混入していないこと、`git diff`（unstaged）で目的の hunk のみが正しく除外されたことを
  逐一確認した。この7ファイル（上記5ファイル＋自ブロックのみの `state/stories.yaml`、新規 `state/reviews/s-25.md`）
  のみを個別パス指定で `git commit -- <paths>` により確定させることを試みた。
- infra-blocker（2026-07-22 実測・S-06/S-22/S-23/S-24 と同型の既知の外部要因の継続）: `git commit` を2回試行し
  いずれも `error: 1Password: failed to fill whole buffer` / `fatal: failed to write commit object` で失敗。
  `op whoami` は `[ERROR] account is not signed in` を返し、1Password アカウント未サインインが原因と確認
  （`git config` は `commit.gpgsign=true`・`gpg.format=ssh`・signingkey 設定済みで、ロック競合ではなく署名不能状態）。
  絶対規約により `--no-gpg-sign` 等の署名バイパスは行っていない。上記7ファイルの hunk-isolated 変更は
  index に stage 済みのまま未コミットで残る（コード内容・分離作業自体は完了）。1Password 側のサインイン回復
  （人間による解錠）後、`git commit -- <上記7ファイル>` を再試行すれば確定できる状態。
